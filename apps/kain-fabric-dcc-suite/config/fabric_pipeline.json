{
  "steps": [
    {
      "id": "python_suite_bootstrap",
      "runtime": "python",
      "summary": "Seeds project defaults, workspace selection, preview sizing, runtime pack counts, and tensor readiness hints."
    },
    {
      "id": "dcc_suite_seed",
      "runtime": "kain",
      "summary": "Authors the scene graph bootstrap, asset catalog bootstrap, PBR material contract, SVG mask contract, preview buffers, tensor features, GPU sculpt seed buffers, and session report."
    },
    {
      "id": "gpu_sculpt_displacement",
      "runtime": "gpu_compute",
      "summary": "Runs a GPU sculpt proof over seeded heightfield and brush parameter buffers, emitting the canonical sculpt delta payload."
    },
    {
      "id": "material_authoring_projection",
      "runtime": "kain",
      "summary": "Projects a substance-painter-style texture set, layer stack, and export preset report from Kain-owned session truth."
    },
    {
      "id": "svg_material_mask_projection",
      "runtime": "kain",
      "summary": "Projects SVG mask documents, anchor-driven vector motifs, and reusable decal shapes for the material lane."
    },
    {
      "id": "native_sculpt_kernel",
      "runtime": "c_abi",
      "summary": "Uses the native helper to summarize GPU sculpt output into native-friendly sculpt signatures and reports."
    },
    {
      "id": "rig_graph_analysis",
      "runtime": "rust_crate",
      "summary": "Builds graph and topology health reporting from authored scene and sculpt outputs."
    },
    {
      "id": "tensor_train_stage",
      "runtime": "python",
      "summary": "Emits PyTorch-oriented training readiness and plan summaries without pretending the runtime is fully wired."
    },
    {
      "id": "tensor_infer_stage",
      "runtime": "python",
      "summary": "Emits tensor inference readiness and model-serving summaries."
    },
    {
      "id": "gpu_material_preview",
      "runtime": "gpu_compute",
      "summary": "Runs a compute preview and material bake pass across the shared preview buffers after PBR and SVG authoring receipts are materialized."
    },
    {
      "id": "material_texture_export_projection",
      "runtime": "kain",
      "summary": "Builds channel-pack export receipts for metal-rough, ORM, height, emissive, and custom packed delivery."
    },
    {
      "id": "publish_suite_report",
      "runtime": "kain",
      "summary": "Builds the publish and package report through a Kain-authored bridge that includes the material export lane."
    }
  ]
}

# Simple test to verify stdlib types are loaded

fn test_types():
    # Use a type from patterns.kn
    let slot = InventorySlot {
        item_id: 1,
        quantity: 5,
        max_stack: 99
    }
    println("Created inventory slot")

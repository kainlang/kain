// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "FilterPosterize.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FFilterPosterizeShader, "/Plugin/AlphaGen/FilterPosterize.usf", "FilterPosterizeCS", SF_Compute);

// Helper function to add pass to render graph
void AddPass_FilterPosterize(
    FRDGBuilder& GraphBuilder,
    FUintVector2 texture_size,
    float steps,
    FRDGTextureRef input_texture,
    FRDGTextureRef OutputTexture,
    FIntVector GroupCount
)
{
    FFilterPosterizeShader::FParameters* Params = GraphBuilder.AllocParameters<FFilterPosterizeShader::FParameters>();
    // Bind Scalars
    Params->texture_size = texture_size;
    Params->steps = steps;
    // Bind Textures
    Params->input_texture = input_texture;
    Params->input_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    // Bind UAVs
    Params->OutputTexture = GraphBuilder.CreateUAV(OutputTexture);

    FFilterPosterizeShader::Exec(GraphBuilder, *Params, GroupCount);
}

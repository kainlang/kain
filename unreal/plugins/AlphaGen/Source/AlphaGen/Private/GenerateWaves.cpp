// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "GenerateWaves.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FGenerateWavesShader, "/Plugin/AlphaGen/GenerateWaves.usf", "GenerateWavesCS", SF_Compute);

// Helper function to add pass to render graph
void AddPass_GenerateWaves(
    FRDGBuilder& GraphBuilder,
    FUintVector2 texture_size,
    float frequency,
    float amplitude,
    float wave_type,
    FRDGTextureRef OutputTexture,
    FIntVector GroupCount
)
{
    FGenerateWavesShader::FParameters* Params = GraphBuilder.AllocParameters<FGenerateWavesShader::FParameters>();
    // Bind Scalars
    Params->texture_size = texture_size;
    Params->frequency = frequency;
    Params->amplitude = amplitude;
    Params->wave_type = wave_type;
    // Bind UAVs
    Params->OutputTexture = GraphBuilder.CreateUAV(OutputTexture);

    FGenerateWavesShader::Exec(GraphBuilder, *Params, GroupCount);
}

// Generated by KAIN - UE5 Editor Tools
#pragma once
#include "CoreMinimal.h"
#include "AlphaGenEditorTypes.h"
#include "Widgets/SCompoundWidget.h"
#include "Widgets/DeclarativeSyntaxSupport.h"

class SAlphaPreviewWidget : public SCompoundWidget
{
public:
	// Layout Optimization Report
	// Static Properties: 5 (no Tick overhead)
	// Reactive Properties: 0 (requires Tick)
	// Event Handlers: 0
	// Estimated Tick Reduction: 100.0%
	//
	// SLATE_ARGUMENT texture - Explicit @argument attribute
	// SLATE_ARGUMENT zoom_level - Explicit @argument attribute
	// SLATE_ARGUMENT pan_offset - Explicit @argument attribute
	// SLATE_ARGUMENT on_zoom_changed - Default (no clear reactive usage)
	// SLATE_ARGUMENT on_pan_changed - Default (no clear reactive usage)
	SLATE_BEGIN_ARGS(SAlphaPreviewWidget)
		: _Content()
		, _texture(FTexture2D())
		, _zoom_level(0.0f)
		, _pan_offset(FVector2D::ZeroVector)
		, _on_zoom_changed(FOnSliderChanged())
		, _on_pan_changed(FOnSliderChanged())
		{}
		
		SLATE_DEFAULT_SLOT(FArguments, Content)
		SLATE_ARGUMENT(FTexture2D, texture)
		SLATE_ARGUMENT(float, zoom_level)
		SLATE_ARGUMENT(FVector2D, pan_offset)
		SLATE_ARGUMENT(FOnSliderChanged, on_zoom_changed)
		SLATE_ARGUMENT(FOnSliderChanged, on_pan_changed)
	SLATE_END_ARGS()
	
	void Construct(const FArguments& InArgs);
	
	FReply Handleon_zoom_changed();
	
	FReply Handleon_pan_changed();
};


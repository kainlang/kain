// Generated by KAIN Compiler - UE5 C++ Codegen
// Do not edit - regenerate from .kn source

#include "SketchfabImporterTypes.h"

	USketchfabImporterComponent::USketchfabImporterComponent()
	{
		PrimaryComponentTick.bCanEverTick = false;
	}

	ASketchfabImportManager::ASketchfabImportManager()
	{
		PrimaryActorTick.bCanEverTick = false;
	}

	void ASketchfabImportManager::Server_StartDownload_Implementation(const FString& model_uid, const FString& api_token)
	{
		UE_LOG(LogTemp, Warning, TEXT("Starting download: %s"), *model_uid);
		Multicast_NotifyDownloadStarted(model_uid);
	}

	void ASketchfabImportManager::Server_CancelDownload_Implementation(const FString& model_uid)
	{
		UE_LOG(LogTemp, Warning, TEXT("Cancelling download: %s"), *model_uid);
		Multicast_NotifyDownloadCancelled(model_uid);
	}

	void ASketchfabImportManager::Client_UpdateProgress_Implementation(const FString& model_uid, const float progress)
	{
		UE_LOG(LogTemp, Warning, TEXT("Download progress: %s - %f%%"), *model_uid, progress);
	}

	void ASketchfabImportManager::Multicast_NotifyDownloadStarted_Implementation(const FString& model_uid)
	{
		UE_LOG(LogTemp, Warning, TEXT("Download started: %s"), *model_uid);
	}

	void ASketchfabImportManager::Multicast_NotifyDownloadComplete_Implementation(const FString& model_uid, const bool success)
	{
		UE_LOG(LogTemp, Warning, TEXT("Download complete: %s - Success: %s"), *model_uid, success ? TEXT("true") : TEXT("false"));
	}

	void ASketchfabImportManager::Multicast_NotifyDownloadCancelled_Implementation(const FString& model_uid)
	{
		UE_LOG(LogTemp, Warning, TEXT("Download cancelled: %s"), *model_uid);
	}

			TArray<FSketchfabModel> UKainFunctionLibrary::search_sketchfab(const FString query, const int64 count, const FString api_token)
			{
				const TArray<FSketchfabModel> results = TArray<FSketchfabModel>{};
				return results;
			}

			FSketchfabDownloadResult UKainFunctionLibrary::get_download_url(const FString model_uid, const FString api_token)
			{
				const auto result = FSketchfabDownloadResult{false, TEXT(""), TEXT("glb"), TEXT("")};
				return result;
			}

			bool UKainFunctionLibrary::download_and_import_model(const FString model_uid, const FString api_token, const FString import_path)
			{
				return false;
			}

			FString UKainFunctionLibrary::get_sort_mode_string(const ESketchfabSortMode mode)
			{
				return ((mode == ESketchfabSortMode::Relevance) ? TEXT("-relevance") : (mode == ESketchfabSortMode::LikeCount) ? TEXT("-likeCount") : (mode == ESketchfabSortMode::ViewCount) ? TEXT("-viewCount") : (mode == ESketchfabSortMode::PublishedDate) ? TEXT("-publishedAt") : (mode == ESketchfabSortMode::CreatedDate) ? TEXT("-createdAt") : TEXT("-relevance"));
			}

			bool UKainFunctionLibrary::validate_api_token(const FString token)
			{
				return (token.Len() > 0);
			}

			FString UKainFunctionLibrary::build_search_url(const FString query, const int64 count, const FString sort_mode)
			{
				const FString base_url = TEXT("https://api.sketchfab.com/v3/search");
				// Note: URL encoding should be handled here for production
				const FString url = FString::Printf(TEXT("%s?type=models&q=%s&downloadable=true&sort_by=%s&count=%lld"), *base_url, *query, *sort_mode, count);
				return url;
			}

			FString UKainFunctionLibrary::parse_thumbnail_url(const FSketchfabModel model)
			{
				return model.thumbnail_url;
			}

			FString UKainFunctionLibrary::format_model_info(const FSketchfabModel model)
			{
				return FString::Printf(TEXT("%s by %s (%lld likes, %lld views)"), *model.name, *model.author_name, model.like_count, model.view_count);
			}

			FString UKainFunctionLibrary::create_auth_header(const FString api_token)
			{
				return FString::Printf(TEXT("Token %s"), *api_token);
			}

			bool UKainFunctionLibrary::is_valid_model_uid(const FString uid)
			{
				return (uid.Len() == 32);
			}

			FString UKainFunctionLibrary::get_model_viewer_url(const FString model_uid)
			{
				return FString::Printf(TEXT("https://sketchfab.com/3d-models/%s"), *model_uid);
			}

			FString UKainFunctionLibrary::get_api_base_url()
			{
				return TEXT("https://api.sketchfab.com/v3");
			}

			bool UKainFunctionLibrary::is_cache_valid(const FSketchfabCache cache, const float current_time)
			{
				const auto age = (current_time - cache.cache_timestamp);
				return (age < cache.max_cache_age);
			}

			FSketchfabCache UKainFunctionLibrary::clear_cache(const FSketchfabCache cache)
			{
				const auto new_cache = FSketchfabCache{TArray<FString>{}, TArray<FSketchfabModel>{}, 0.000000f, 300.000000f};
				return new_cache;
			}


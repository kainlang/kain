// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "K11CityLights.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"
#include "Interfaces/IPluginManager.h"
#include "Misc/Paths.h"
#include "ShaderCore.h"

namespace {
    struct FShaderDirectoryRegistration {
        FShaderDirectoryRegistration() {
            static bool bRegistered = false;
            if (!bRegistered) {
                TSharedPtr<IPlugin> Plugin = IPluginManager::Get().FindPlugin(TEXT("KainCosmos"));
                if (Plugin.IsValid()) {
                    FString PluginShaderDir = FPaths::Combine(Plugin->GetBaseDir(), TEXT("Shaders"));
                    AddShaderSourceDirectoryMapping(TEXT("/Plugin/KainCosmos"), PluginShaderDir);
                    bRegistered = true;
                } else {
                    UE_LOG(LogShaders, Error, TEXT("Failed to find plugin 'KainCosmos' for shader directory mapping"));
                }
            }
        }
    };
    static FShaderDirectoryRegistration GShaderDirReg_K11CityLights;
}

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FK11CityLightsShader, "/Plugin/KainCosmos/Shaders/K11CityLights.usf", "K11CityLightsPS", SF_Pixel);

// Helper function to add pass to render graph
void AddPass_K11CityLights(
    FRDGBuilder& GraphBuilder,
    float claim_status
)
{
    FK11CityLightsShader::FParameters* Params = GraphBuilder.AllocParameters<FK11CityLightsShader::FParameters>();
    // Bind Scalars
    Params->claim_status = claim_status;

    FK11CityLightsShader::Exec(GraphBuilder, *Params);
}

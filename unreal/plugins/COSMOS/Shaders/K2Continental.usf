// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
float seed;
float time;
float3 view;
float3 sun;
float claim_status;

// Texture Inputs
Texture2D<float4> tectonic_map : register(t0);
SamplerState tectonic_mapSampler : register(s0);
Texture2D<float4> height_map : register(t0);
SamplerState height_mapSampler : register(s0);
Texture2D<float4> thermal : register(t0);
SamplerState thermalSampler : register(s0);
Texture2D<float4> h_map : register(t0);
SamplerState h_mapSampler : register(s0);
Texture2D<float4> moisture : register(t1);
SamplerState moistureSampler : register(s1);
Texture2D<float4> biome_map : register(t0);
SamplerState biome_mapSampler : register(s0);
Texture2D<float4> albedo : register(t0);
SamplerState albedoSampler : register(s0);
Texture2D<float4> lights : register(t1);
SamplerState lightsSampler : register(s1);

// UAV Outputs
RWTexture2D<float4> OutputTexture : register(u0);

[numthreads(32, 32, 1)]
void K1_TectonicsCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float2 uv = float2(id.x, id.y);
    float n = frac((sin(dot(uv, float2(12.989800, 78.233000))) * 43758.545300));
    OutputTexture[ThreadId.xy] = float4(n, n, n, 1.000000);
    return;
}
[numthreads(32, 32, 1)]
void K2_ContinentalCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float h = (tectonic_map.Sample(tectonic_mapSampler, float2(id.x, id.y)).x * 0.500000);
    OutputTexture[ThreadId.xy] = float4(h, h, h, 1.000000);
    return;
}
[numthreads(32, 32, 1)]
void K3_ThermalCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float h = height_map.Sample(height_mapSampler, float2(id.x, id.y)).x;
    float temp = (1.000000 - h);
    OutputTexture[ThreadId.xy] = float4(temp, 0.000000, 0.000000, 1.000000);
    return;
}
[numthreads(32, 32, 1)]
void K4_MoistureCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float m = (thermal.Sample(thermalSampler, float2(id.x, id.y)).x * 0.800000);
    OutputTexture[ThreadId.xy] = float4(0.000000, 0.000000, m, 1.000000);
    return;
}
[numthreads(32, 32, 1)]
void K5_ResourcesCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float res = frac((sin((id.x * seed)) * cos((id.y * seed))));
    OutputTexture[ThreadId.xy] = float4(res, 0.000000, 0.000000, 1.000000);
    return;
}
[numthreads(32, 32, 1)]
void K6_NormalsCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float2 uv = float2(id.x, id.y);
    float hL = h_map.Sample(h_mapSampler, float2((uv.x - 0.010000), uv.y)).x;
    float hR = h_map.Sample(h_mapSampler, float2((uv.x + 0.010000), uv.y)).x;
    OutputTexture[ThreadId.xy] = float4(normalize(float3((hL - hR), 1.000000, 0.000000)), 1.000000);
    return;
}
struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput K7_OceanPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;

    float w = (sin(((uv.x * 10.000000) + time)) * 0.100000);
    FPSOutput _Result;
    _Result.Color = float4(0.000000, 0.200000, 0.500000, (0.800000 + w));
    return _Result;
}
struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput K8_AtmospherePS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;

    float scatter = ((dot(normalize(view), normalize(sun)) * 0.500000) + 0.500000);
    FPSOutput _Result;
    _Result.Color = float4(0.400000, 0.600000, 1.000000, scatter);
    return _Result;
}
struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput K9_BiomesPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;

    float t = thermal.Sample(thermalSampler, uv).x;
    float m = moisture.Sample(moistureSampler, uv).x;
    FPSOutput _Result;
    _Result.Color = float4(t, m, 0.000000, 1.000000);
    return _Result;
}
[numthreads(32, 32, 1)]
void K10_FloraCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float4 data = biome_map.Sample(biome_mapSampler, float2(id.x, id.y));
    OutputTexture[ThreadId.xy] = float4(data.x, data.y, 0.000000, 1.000000);
    return;
}
struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput K11_CityLightsPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;

    float lights = (sin((uv.x * 100.000000)) * cos((uv.y * 100.000000)));
    FPSOutput _Result;
    _Result.Color = float4((lights * claim_status), (lights * 0.500000), 0.000000, 1.000000);
    return _Result;
}
struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput K12_SovereignPBRPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;

    float3 c = (albedo.Sample(albedoSampler, uv).rgb + lights.Sample(lightsSampler, uv).rgb);
    FPSOutput _Result;
    _Result.Color = float4(c, 1.000000);
    return _Result;
}

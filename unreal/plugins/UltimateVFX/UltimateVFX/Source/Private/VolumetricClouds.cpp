// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "VolumetricClouds.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FVolumetricCloudsShader, "/Plugin/UltimateVFX/Shaders/VolumetricClouds.usf", "VolumetricCloudsPS", SF_Pixel);

// Helper function to dispatch VolumetricClouds from game thread
void DispatchVolumetricCloudsShader(
    float cloud_coverage,
    float cloud_density,
    float cloud_height_min,
    float cloud_height_max,
    float detail_scale,
    float detail_strength,
    float erosion_scale,
    float erosion_strength,
    FVector2f wind_direction,
    float wind_speed,
    float turbulence,
    FVector3f ambient_color,
    float sun_scatter,
    float powder_effect,
    float march_steps,
    float light_steps)
{
    ENQUEUE_RENDER_COMMAND(VolumetricCloudsRender)([=](FRHICommandListImmediate& RHICmdList) {
        FRDGBuilder GraphBuilder(RHICmdList);

        FVolumetricCloudsShader::FParameters* Params = GraphBuilder.AllocParameters<FVolumetricCloudsShader::FParameters>();
        Params->cloud_coverage = cloud_coverage;
        Params->cloud_density = cloud_density;
        Params->cloud_height_min = cloud_height_min;
        Params->cloud_height_max = cloud_height_max;
        Params->detail_scale = detail_scale;
        Params->detail_strength = detail_strength;
        Params->erosion_scale = erosion_scale;
        Params->erosion_strength = erosion_strength;
        Params->wind_direction = wind_direction;
        Params->wind_speed = wind_speed;
        Params->turbulence = turbulence;
        Params->ambient_color = ambient_color;
        Params->sun_scatter = sun_scatter;
        Params->powder_effect = powder_effect;
        Params->march_steps = march_steps;
        Params->light_steps = light_steps;

        // Dispatch shader (adjust group count as needed)
        FVolumetricCloudsShader::Exec(GraphBuilder, *Params);

        GraphBuilder.Execute();
    });
}

// Generated by KAIN Compiler - UE5 Shader Parameter Reflection
// This file provides C++ bindings for shader parameters

#pragma once

#include "CoreMinimal.h"
#include "GlobalShader.h"
#include "ShaderParameters.h"
#include "ShaderParameterStruct.h"
#include "ShaderParameterUtils.h"
#include "RenderGraphResources.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderCompilerCore.h"
#include "RHIStaticStates.h"

class FVolumetricCloudsShader : public FGlobalShader
{
    DECLARE_GLOBAL_SHADER(FVolumetricCloudsShader);
    SHADER_USE_PARAMETER_STRUCT(FVolumetricCloudsShader, FGlobalShader);

    BEGIN_SHADER_PARAMETER_STRUCT(FParameters, )
        SHADER_PARAMETER(float, cloud_coverage)
        SHADER_PARAMETER(float, cloud_density)
        SHADER_PARAMETER(float, cloud_height_min)
        SHADER_PARAMETER(float, cloud_height_max)
        SHADER_PARAMETER(float, detail_scale)
        SHADER_PARAMETER(float, detail_strength)
        SHADER_PARAMETER(float, erosion_scale)
        SHADER_PARAMETER(float, erosion_strength)
        SHADER_PARAMETER(FVector2f, wind_direction)
        SHADER_PARAMETER(float, wind_speed)
        SHADER_PARAMETER(float, turbulence)
        SHADER_PARAMETER(FVector3f, ambient_color)
        SHADER_PARAMETER(float, sun_scatter)
        SHADER_PARAMETER(float, powder_effect)
        SHADER_PARAMETER(float, march_steps)
        SHADER_PARAMETER(float, light_steps)
    END_SHADER_PARAMETER_STRUCT()

    // Automatically generates RDG pixel shader pass
    static void Exec(FRDGBuilder& GraphBuilder, const FParameters& Parameters)
    {
        TShaderMapRef<FVolumetricCloudsShader> PixelShader(GetGlobalShaderMap(GMaxRHIFeatureLevel));

        FParameters* PassParameters = GraphBuilder.AllocParameters<FParameters>();
        *PassParameters = Parameters;

        // For pixel shaders, you'll need to set up a full-screen quad or custom geometry
        // This is a simplified example - adapt to your rendering pipeline
        // Use AddDrawPass or integrate with your post-process pipeline
    }
};

// In your .cpp file, add:
// IMPLEMENT_GLOBAL_SHADER(FVolumetricCloudsShader, "/Plugin/YourPlugin/VolumetricClouds.usf", "VolumetricCloudsPS", SF_Pixel);

// Helper function to add pass to render graph
void AddPass_VolumetricClouds(
    FRDGBuilder& GraphBuilder,
    float cloud_coverage,
    float cloud_density,
    float cloud_height_min,
    float cloud_height_max,
    float detail_scale,
    float detail_strength,
    float erosion_scale,
    float erosion_strength,
    FVector2f wind_direction,
    float wind_speed,
    float turbulence,
    FVector3f ambient_color,
    float sun_scatter,
    float powder_effect,
    float march_steps,
    float light_steps
);

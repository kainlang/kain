// Generated by KAIN Compiler - UE5 Shader Parameter Reflection
// This file provides C++ bindings for shader parameters

#pragma once

#include "CoreMinimal.h"
#include "GlobalShader.h"
#include "ShaderParameters.h"
#include "ShaderParameterStruct.h"
#include "ShaderParameterUtils.h"
#include "RenderGraphResources.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderCompilerCore.h"
#include "RHIStaticStates.h"

class FVolumetricFogShader : public FGlobalShader
{
    DECLARE_GLOBAL_SHADER(FVolumetricFogShader);
    SHADER_USE_PARAMETER_STRUCT(FVolumetricFogShader, FGlobalShader);

    BEGIN_SHADER_PARAMETER_STRUCT(FParameters, )
        SHADER_PARAMETER(float, fog_density)
        SHADER_PARAMETER(float, fog_height)
        SHADER_PARAMETER(float, fog_falloff)
        SHADER_PARAMETER(FVector3f, fog_color)
        SHADER_PARAMETER(float, absorption)
        SHADER_PARAMETER(float, scattering)
        SHADER_PARAMETER(float, anisotropy)
        SHADER_PARAMETER(float, noise_scale)
        SHADER_PARAMETER(float, animation_speed)
        SHADER_PARAMETER(float, ray_march_steps)
        SHADER_PARAMETER(float, light_march_steps)
    END_SHADER_PARAMETER_STRUCT()

    // Automatically generates RDG pixel shader pass
    static void Exec(FRDGBuilder& GraphBuilder, const FParameters& Parameters)
    {
        TShaderMapRef<FVolumetricFogShader> PixelShader(GetGlobalShaderMap(GMaxRHIFeatureLevel));

        FParameters* PassParameters = GraphBuilder.AllocParameters<FParameters>();
        *PassParameters = Parameters;

        // For pixel shaders, you'll need to set up a full-screen quad or custom geometry
        // This is a simplified example - adapt to your rendering pipeline
        // Use AddDrawPass or integrate with your post-process pipeline
    }
};

// In your .cpp file, add:
// IMPLEMENT_GLOBAL_SHADER(FVolumetricFogShader, "/Plugin/YourPlugin/VolumetricFog.usf", "VolumetricFogPS", SF_Pixel);

// Helper function to add pass to render graph
void AddPass_VolumetricFog(
    FRDGBuilder& GraphBuilder,
    float fog_density,
    float fog_height,
    float fog_falloff,
    FVector3f fog_color,
    float absorption,
    float scattering,
    float anisotropy,
    float noise_scale,
    float animation_speed,
    float ray_march_steps,
    float light_march_steps
);

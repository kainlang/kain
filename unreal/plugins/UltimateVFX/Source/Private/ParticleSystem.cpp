// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "ParticleSystem.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FParticleSystemShader, "/Plugin/YourPlugin/Shaders/ParticleSystem.usf", "ParticleSystemPS", SF_Pixel);

// Helper function to dispatch ParticleSystem from game thread
void DispatchParticleSystemShader(
    float wave_amplitude,
    float wave_frequency,
    float wave_speed,
    FVector3f water_color,
    FVector3f foam_color,
    float time_scale,
    float fire_intensity,
    FVector3f fire_color,
    float noise_scale,
    float turbulence,
    float time,
    FVector2f uv_offset,
    FVector3f hologram_color,
    FVector4f color_modulation,
    float glitch_intensity,
    float scan_line_count,
    float flicker_speed,
    float particle_size,
    float particle_velocity,
    float life_time,
    float fade_distance,
    FVector3f color_tint,
    float orphan_param1,
    FVector3f orphan_param2,
    FVector4f orphan_param3)
{
    ENQUEUE_RENDER_COMMAND(ParticleSystemRender)([=](FRHICommandListImmediate& RHICmdList) {
        FRDGBuilder GraphBuilder(RHICmdList);

        FParticleSystemShader::FParameters* Params = GraphBuilder.AllocParameters<FParticleSystemShader::FParameters>();
        Params->wave_amplitude = wave_amplitude;
        Params->wave_frequency = wave_frequency;
        Params->wave_speed = wave_speed;
        Params->water_color = water_color;
        Params->foam_color = foam_color;
        Params->time_scale = time_scale;
        Params->fire_intensity = fire_intensity;
        Params->fire_color = fire_color;
        Params->noise_scale = noise_scale;
        Params->turbulence = turbulence;
        Params->time = time;
        Params->uv_offset = uv_offset;
        Params->hologram_color = hologram_color;
        Params->color_modulation = color_modulation;
        Params->glitch_intensity = glitch_intensity;
        Params->scan_line_count = scan_line_count;
        Params->flicker_speed = flicker_speed;
        Params->particle_size = particle_size;
        Params->particle_velocity = particle_velocity;
        Params->life_time = life_time;
        Params->fade_distance = fade_distance;
        Params->color_tint = color_tint;
        Params->orphan_param1 = orphan_param1;
        Params->orphan_param2 = orphan_param2;
        Params->orphan_param3 = orphan_param3;

        // Dispatch shader (adjust group count as needed)
        FParticleSystemShader::Exec(GraphBuilder, *Params);

        GraphBuilder.Execute();
    });
}

// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "VolumetricFog.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FVolumetricFogShader, "/Plugin/UltimateVFX/VolumetricFog.usf", "VolumetricFogPS", SF_Pixel);

// Helper function to add pass to render graph
void AddPass_VolumetricFog(
    FRDGBuilder& GraphBuilder,
    float fog_density,
    float fog_height,
    float fog_falloff,
    FVector3f fog_color,
    float absorption,
    float scattering,
    float anisotropy,
    float noise_scale,
    float animation_speed,
    float ray_march_steps,
    float light_march_steps
)
{
    FVolumetricFogShader::FParameters* Params = GraphBuilder.AllocParameters<FVolumetricFogShader::FParameters>();
    // Bind Scalars
    Params->fog_density = fog_density;
    Params->fog_height = fog_height;
    Params->fog_falloff = fog_falloff;
    Params->fog_color = fog_color;
    Params->absorption = absorption;
    Params->scattering = scattering;
    Params->anisotropy = anisotropy;
    Params->noise_scale = noise_scale;
    Params->animation_speed = animation_speed;
    Params->ray_march_steps = ray_march_steps;
    Params->light_march_steps = light_march_steps;

    FVolumetricFogShader::Exec(GraphBuilder, *Params);
}

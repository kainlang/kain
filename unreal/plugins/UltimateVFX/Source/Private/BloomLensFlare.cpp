// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "BloomLensFlare.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FBloomLensFlareShader, "/Plugin/UltimateVFX/BloomLensFlare.usf", "BloomLensFlarePS", SF_Pixel);

// Helper function to add pass to render graph
void AddPass_BloomLensFlare(
    FRDGBuilder& GraphBuilder,
    float bloom_threshold,
    float bloom_intensity,
    float bloom_radius,
    float bloom_softness,
    float lens_flare_intensity,
    float lens_flare_ghosts,
    float lens_flare_halo_radius,
    float lens_flare_halo_thickness,
    float chromatic_aberration,
    float lens_dirt_intensity
)
{
    FBloomLensFlareShader::FParameters* Params = GraphBuilder.AllocParameters<FBloomLensFlareShader::FParameters>();
    // Bind Scalars
    Params->bloom_threshold = bloom_threshold;
    Params->bloom_intensity = bloom_intensity;
    Params->bloom_radius = bloom_radius;
    Params->bloom_softness = bloom_softness;
    Params->lens_flare_intensity = lens_flare_intensity;
    Params->lens_flare_ghosts = lens_flare_ghosts;
    Params->lens_flare_halo_radius = lens_flare_halo_radius;
    Params->lens_flare_halo_thickness = lens_flare_halo_thickness;
    Params->chromatic_aberration = chromatic_aberration;
    Params->lens_dirt_intensity = lens_dirt_intensity;

    FBloomLensFlareShader::Exec(GraphBuilder, *Params);
}

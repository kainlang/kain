// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
float3 light_position;
float3 light_color;
float light_intensity;
float ray_density;
float ray_weight;
float ray_decay;
float ray_exposure;
float num_samples;
float dither_strength;

struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput GodRaysPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;
    float2 screen_pos = Input.TexCoord0.zw;

    float2 light_screen_pos = float2(0.500000, 0.500000);
    float2 delta = (((light_screen_pos - screen_pos)) / num_samples);
    float illumination = 0.000000;
    float2 sample_pos = screen_pos;
    float i = 0.000000;
    while ((i < num_samples))
    {
        illumination = (illumination + (ray_density * ray_weight));
        sample_pos = (sample_pos + delta);
        ray_weight = (ray_weight * ray_decay);
        i = (i + 1.000000);
    }
    float4 god_rays = (((light_color * illumination) * ray_exposure) * light_intensity);
    FPSOutput _Result;
    _Result.Color = float4(god_rays, 1.000000);
    return _Result;
}

// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
float fog_density;
float fog_height;
float fog_falloff;
float3 fog_color;
float absorption;
float scattering;
float anisotropy;
float noise_scale;
float animation_speed;
float ray_march_steps;
float light_march_steps;

struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
    float4 TexCoord1 : TEXCOORD1;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput VolumetricFogPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;
    float3 world_pos = Input.TexCoord1.xyz;

    float time = 0.000000;
    float height_factor = exp(((-max((world_pos.y - fog_height), 0.000000)) * fog_falloff));
    float3 noise_coord = ((world_pos * noise_scale) + float3((time * animation_speed), 0.000000, 0.000000));
    float fog_noise = ((((sin(noise_coord.x) * cos(noise_coord.y)) * sin(noise_coord.z)) * 0.500000) + 0.500000);
    float4 density = ((fog_density * height_factor) * fog_noise);
    float fog_alpha = (1.000000 - exp(((-density) * absorption)));
    FPSOutput _Result;
    _Result.Color = float4(fog_color, fog_alpha);
    return _Result;
}

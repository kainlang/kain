// Generated by KAIN Compiler - UE5 Shader Parameter Reflection
// This file provides C++ bindings for shader parameters

#pragma once

#include "CoreMinimal.h"
#include "GlobalShader.h"
#include "ShaderParameters.h"
#include "ShaderParameterStruct.h"
#include "ShaderParameterUtils.h"
#include "RenderGraphResources.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderCompilerCore.h"
#include "RHIStaticStates.h"

class FParticlePositionShader : public FGlobalShader
{
    DECLARE_GLOBAL_SHADER(FParticlePositionShader);
    SHADER_USE_PARAMETER_STRUCT(FParticlePositionShader, FGlobalShader);

    using FPermutationDomain = TShaderPermutationDomain<>;

    BEGIN_SHADER_PARAMETER_STRUCT(FParameters, )
        SHADER_PARAMETER(float, time)
        SHADER_PARAMETER(float, delta_time)
        SHADER_PARAMETER(int32, mode)
        SHADER_PARAMETER(float, life_enabled)
        SHADER_PARAMETER(float, life_decay_rate)
        SHADER_PARAMETER(float, respawn_bounds)
        SHADER_PARAMETER_RDG_TEXTURE(Texture2D, position_texture)
        SHADER_PARAMETER_SAMPLER(SamplerState, position_textureSampler)
        SHADER_PARAMETER_RDG_TEXTURE(Texture2D, velocity_texture)
        SHADER_PARAMETER_SAMPLER(SamplerState, velocity_textureSampler)
        SHADER_PARAMETER_RDG_TEXTURE(Texture2D, origin_texture)
        SHADER_PARAMETER_SAMPLER(SamplerState, origin_textureSampler)
        SHADER_PARAMETER_RDG_TEXTURE_UAV(RWTexture2D<float4>, OutputTexture)
    END_SHADER_PARAMETER_STRUCT()

    // Automatically generates RDG compute pass
    static void Exec(FRDGBuilder& GraphBuilder, const FParameters& Parameters, FIntVector GroupCount)
    {
        TShaderMapRef<FParticlePositionShader> ComputeShader(GetGlobalShaderMap(GMaxRHIFeatureLevel));

        FPermutationDomain PermutationVector;

        FParameters* PassParameters = GraphBuilder.AllocParameters<FParameters>();
        *PassParameters = Parameters;

        FComputeShaderUtils::AddPass(
            GraphBuilder,
            RDG_EVENT_NAME("ParticlePosition Dispatch"),
            ComputeShader,
            PassParameters,
            GroupCount
        );
    }
};

// In your .cpp file, add:
// IMPLEMENT_GLOBAL_SHADER(FParticlePositionShader, "/Plugin/YourPlugin/Shaders/ParticlePosition.usf", "ParticlePositionCS", SF_Compute);

// Helper function to add pass to render graph
void AddPass_ParticlePosition(
    FRDGBuilder& GraphBuilder,
    float time,
    float delta_time,
    int32 mode,
    float life_enabled,
    float life_decay_rate,
    float respawn_bounds,
    FRDGTextureRef position_texture,
    FRDGTextureRef velocity_texture,
    FRDGTextureRef origin_texture,
    FRDGTextureRef OutputTexture,
    FIntVector GroupCount = FIntVector(32, 32, 1));

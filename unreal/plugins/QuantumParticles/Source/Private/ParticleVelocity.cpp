// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "ParticleVelocity.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FParticleVelocityShader, "/Plugin/QuantumParticles/ParticleVelocity.usf", "ParticleVelocityCS", SF_Compute);

// Helper function to add pass to render graph
void AddPass_ParticleVelocity(
    FRDGBuilder& GraphBuilder,
    float time,
    float speed,
    float chaos,
    float damping,
    int32 mode,
    float force_multiplier,
    float curl_strength,
    float center_pull,
    float max_velocity,
    float heartbeat_active,
    float vortex_active,
    float gravity_active,
    float explosion_active,
    float heartbeat_bpm,
    float heartbeat_intensity,
    float vortex_strength,
    float vortex_lift,
    float gravity_force,
    float gravity_radius,
    float explosion_force,
    float audio_bass,
    float audio_high,
    float audio_level,
    FRDGTextureRef position_texture,
    FRDGTextureRef velocity_texture,
    FRDGTextureRef origin_texture,
    FRDGTextureRef OutputTexture,
    FIntVector GroupCount
)
{
    FParticleVelocityShader::FParameters* Params = GraphBuilder.AllocParameters<FParticleVelocityShader::FParameters>();
    // Bind Scalars
    Params->time = time;
    Params->speed = speed;
    Params->chaos = chaos;
    Params->damping = damping;
    Params->mode = mode;
    Params->force_multiplier = force_multiplier;
    Params->curl_strength = curl_strength;
    Params->center_pull = center_pull;
    Params->max_velocity = max_velocity;
    Params->heartbeat_active = heartbeat_active;
    Params->vortex_active = vortex_active;
    Params->gravity_active = gravity_active;
    Params->explosion_active = explosion_active;
    Params->heartbeat_bpm = heartbeat_bpm;
    Params->heartbeat_intensity = heartbeat_intensity;
    Params->vortex_strength = vortex_strength;
    Params->vortex_lift = vortex_lift;
    Params->gravity_force = gravity_force;
    Params->gravity_radius = gravity_radius;
    Params->explosion_force = explosion_force;
    Params->audio_bass = audio_bass;
    Params->audio_high = audio_high;
    Params->audio_level = audio_level;
    // Bind Textures
    Params->position_texture = position_texture;
    Params->position_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    Params->velocity_texture = velocity_texture;
    Params->velocity_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    Params->origin_texture = origin_texture;
    Params->origin_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    // Bind UAVs
    Params->OutputTexture = GraphBuilder.CreateUAV(OutputTexture);

    FParticleVelocityShader::Exec(GraphBuilder, *Params, GroupCount);
}

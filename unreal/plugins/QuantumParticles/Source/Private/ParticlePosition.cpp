// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "ParticlePosition.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FParticlePositionShader, "/Plugin/QuantumParticles/ParticlePosition.usf", "ParticlePositionCS", SF_Compute);

// Helper function to add pass to render graph
void AddPass_ParticlePosition(
    FRDGBuilder& GraphBuilder,
    float time,
    float delta_time,
    int32 mode,
    float life_enabled,
    float life_decay_rate,
    float respawn_bounds,
    FRDGTextureRef position_texture,
    FRDGTextureRef velocity_texture,
    FRDGTextureRef origin_texture,
    FRDGTextureRef OutputTexture,
    FIntVector GroupCount
)
{
    FParticlePositionShader::FParameters* Params = GraphBuilder.AllocParameters<FParticlePositionShader::FParameters>();
    // Bind Scalars
    Params->time = time;
    Params->delta_time = delta_time;
    Params->mode = mode;
    Params->life_enabled = life_enabled;
    Params->life_decay_rate = life_decay_rate;
    Params->respawn_bounds = respawn_bounds;
    // Bind Textures
    Params->position_texture = position_texture;
    Params->position_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    Params->velocity_texture = velocity_texture;
    Params->velocity_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    Params->origin_texture = origin_texture;
    Params->origin_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    // Bind UAVs
    Params->OutputTexture = GraphBuilder.CreateUAV(OutputTexture);

    FParticlePositionShader::Exec(GraphBuilder, *Params, GroupCount);
}

[package]
name = "KainFlow"
version = "1.0.0"
authors = ["UE5 Plugin Factory"]
description = "God-Tier Single File Fluid Simulation"

[build]
targets = ["ue5"]

[ue5]
plugin_name = "KainFlow"
plugin_dir = "."
# THE GOD FILE - Single source of truth
sources = ["KainFlowGod.kn"]

# These shader kernels will be extracted and compiled to .usf
shaders = [

]

# Ensure we generate clean, modular C++ headers
modular_output = true
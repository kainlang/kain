// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "PressureCompute.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FPressureComputeShader, "/Plugin/KainFlow/PressureCompute.usf", "PressureComputeCS", SF_Compute);

// Helper function to add pass to render graph
void AddPass_PressureCompute(
    FRDGBuilder& GraphBuilder,
    float res,
    FRDGTextureRef pressure_tex,
    FRDGTextureRef divergence_tex,
    FRDGTextureRef OutputTexture,
    FIntVector GroupCount
)
{
    FPressureComputeShader::FParameters* Params = GraphBuilder.AllocParameters<FPressureComputeShader::FParameters>();
    // Bind Scalars
    Params->res = res;
    // Bind Textures
    Params->pressure_tex = pressure_tex;
    Params->pressure_texSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    Params->divergence_tex = divergence_tex;
    Params->divergence_texSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    // Bind UAVs
    Params->OutputTexture = GraphBuilder.CreateUAV(OutputTexture);

    FPressureComputeShader::Exec(GraphBuilder, *Params, GroupCount);
}

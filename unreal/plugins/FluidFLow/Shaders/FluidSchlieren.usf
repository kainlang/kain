// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// POD mirror for VisualizationComponent
struct FVisualizationComponentData {
    int visualization;
    float exposure;
    float contrast;
    float saturation;
    float line_thickness;
    int sample_count;
    float step_size;
    float3 color_a;
    float3 color_b;
    float3 color_c;
};

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
FVisualizationComponentData viz;

// UAV Outputs
RWTexture2D<float4> OutputTexture : register(u0);

[numthreads(32, 32, 1)]
void FluidSchlierenCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float s = (viz.contrast * 0.500000);
    OutputTexture[ThreadId.xy] = float4(s, s, s, 1.000000);
    return;
}

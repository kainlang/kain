// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "JacobiPressure.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FJacobiPressureShader, "/Plugin/FluidFlow/JacobiPressure.usf", "JacobiPressureCS", SF_Compute);

// Helper function to add pass to render graph
void AddPass_JacobiPressure(
    FRDGBuilder& GraphBuilder,
    FPhysicalPropertiesComponentData physics,
    FRDGTextureRef pressure_texture,
    FRDGTextureRef divergence_texture,
    FRDGTextureRef OutputTexture,
    FIntVector GroupCount
)
{
    FJacobiPressureShader::FParameters* Params = GraphBuilder.AllocParameters<FJacobiPressureShader::FParameters>();
    // Bind Scalars
    Params->physics = physics;
    // Bind Textures
    Params->pressure_texture = pressure_texture;
    Params->pressure_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    Params->divergence_texture = divergence_texture;
    Params->divergence_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    // Bind UAVs
    Params->OutputTexture = GraphBuilder.CreateUAV(OutputTexture);

    FJacobiPressureShader::Exec(GraphBuilder, *Params, GroupCount);
}

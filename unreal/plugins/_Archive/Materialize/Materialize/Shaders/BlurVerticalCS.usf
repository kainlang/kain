// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
// @N is ordering index, not register binding
uint2 TextureSize;  // @1
float4 FilterParams;  // @2

// Texture Inputs
Texture2D<float4> InputTexture : register(t0);  // @0 -> t0 register
SamplerState InputTextureSampler : register(s0);

// UAV Outputs
RWTexture2D OutputTexture : register(u3);  // @3 -> u3 register

[numthreads(8, 8, 1)]
void BlurVerticalCSCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    if (((ThreadId.x >= TextureSize.x) || (ThreadId.y >= TextureSize.y)))
    {
        return;
    }
    float2 texelSize = (float2(1.000000, 1.000000) / float2(TextureSize.x, TextureSize.y));
    float2 uv = (((float2(ThreadId.x, ThreadId.y) + float2(0.500000, 0.500000))) * texelSize);
    float radius = FilterParams.y;
    float4 result = float4(0.000000, 0.000000, 0.000000, 0.000000);
    int i = (-4);
    while ((i <= 4))
    {
        float gwv = (((i == (-4)) || (i == 4)) ? 0.016200 : (((i == (-3)) || (i == 3)) ? 0.054000 : (((i == (-2)) || (i == 2)) ? 0.121600 : (((i == (-1)) || (i == 1)) ? 0.193300 : 0.225800))));
        float2 sampleUV = (uv + float2(0.000000, ((i * texelSize.y) * radius)));
        float4 samplePos = uvec2(clamp((sampleUV.x * TextureSize.x), 0.000000, (TextureSize.x - 1.000000)), clamp((sampleUV.y * TextureSize.y), 0.000000, (TextureSize.y - 1.000000)));
        result = (result + (InputTexture.Sample(InputTextureSampler, samplePos) * gwv));
        i = (i + 1);
    }
    write(OutputTexture, uvec2(ThreadId.x, ThreadId.y), result);
}

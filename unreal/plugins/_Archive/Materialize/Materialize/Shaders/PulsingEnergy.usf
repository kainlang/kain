// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
// @N is ordering index, not register binding
float time;  // @0
float pulse_speed;  // @1
float pulse_intensity;  // @2
float3 energy_color;  // @3
float core_size;  // @4
float noise_scale;  // @5

struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput PulsingEnergyPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;

    float2 center = float2(0.500000, 0.500000);
    float dist = length((uv - center));
    float pulse = ((sin((time * pulse_speed)) * 0.500000) + 0.500000);
    float pulse_ring = ((sin(((dist * 20.000000) - ((time * pulse_speed) * 2.000000))) * 0.500000) + 0.500000);
    float4 noise = perlin_noise(((uv * noise_scale) + float2((time * 0.200000), (time * 0.300000))));
    float core = exp((((-dist) * 10.000000) / core_size));
    float energy = (((pulse_ring * ((1.000000 - dist))) * pulse) * pulse_intensity);
    float final_value = (((core + energy)) * ((0.800000 + (noise * 0.200000))));
    final_value = clamp(final_value, 0.000000, 1.000000);
    FPSOutput _Result;
    _Result.Color = float4((energy_color.r * final_value), (energy_color.g * final_value), (energy_color.b * final_value), final_value);
    return _Result;
}

// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
// @N is ordering index, not register binding
float time;  // @0
float ripple_speed;  // @1
float ripple_frequency;  // @2
float ripple_amplitude;  // @3
float3 water_color;  // @4

// Texture Inputs
Texture2D normal_map : register(t5);  // @5 -> t5 register
SamplerState normal_mapSampler : register(s5);

struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput WaterRipplesPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;

    float2 center1 = float2(0.300000, 0.300000);
    float2 center2 = float2(0.700000, 0.600000);
    float2 center3 = float2(0.500000, 0.800000);
    float dist1 = length((uv - center1));
    float dist2 = length((uv - center2));
    float dist3 = length((uv - center3));
    float ripple1 = (sin(((dist1 * ripple_frequency) - (time * ripple_speed))) * ripple_amplitude);
    float ripple2 = (sin((((dist2 * ripple_frequency) * 1.300000) - ((time * ripple_speed) * 0.800000))) * ripple_amplitude);
    float ripple3 = (sin((((dist3 * ripple_frequency) * 0.700000) - ((time * ripple_speed) * 1.200000))) * ripple_amplitude);
    float combined_ripples = ((((ripple1 + ripple2) + ripple3)) / 3.000000);
    float2 distorted_uv = (uv + float2((combined_ripples * 0.020000), (combined_ripples * 0.020000)));
    float3 normal = normal_map.Sample(normal_mapSampler, distorted_uv).rgb;
    float height = ((combined_ripples * 0.500000) + 0.500000);
    float reflection = pow(height, 2.000000);
    float3 final_color = ((water_color * ((1.000000 - (reflection * 0.300000)))) + ((float3(1.000000, 1.000000, 1.000000) * reflection) * 0.300000));
    FPSOutput _Result;
    _Result.Color = float4(final_color.r, final_color.g, final_color.b, 0.800000);
    return _Result;
}

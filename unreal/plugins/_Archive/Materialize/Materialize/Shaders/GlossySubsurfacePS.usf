// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
// @N is ordering index, not register binding
float3 subsurface_color;  // @0
float subsurface_radius;  // @1
float subsurface_intensity;  // @2
float3 normal;  // @3
float3 view_dir;  // @4
float3 light_dir;  // @5

struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput GlossySubsurfacePSPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;

    float3 n = normalize(normal);
    float3 v = normalize(view_dir);
    float3 l = normalize(light_dir);
    float ndl = dot(n, l);
    float back_scatter = max(0.000000, (-ndl));
    float wrap_ndl = max(0.000000, (((ndl + subsurface_radius)) / ((1.000000 + subsurface_radius))));
    float subsurface = (((back_scatter + wrap_ndl)) * subsurface_intensity);
    float3 color = (subsurface_color * subsurface);
    FPSOutput _Result;
    _Result.Color = float4(color, 1.000000);
    return _Result;
}

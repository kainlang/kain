// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
// @N is ordering index, not register binding
uint2 TextureSize;  // @2
uint Operation;  // @3
float Scalar;  // @4

// Texture Inputs
Texture2D<float4> InputA : register(t0);  // @0 -> t0 register
SamplerState InputASampler : register(s0);
Texture2D<float4> InputB : register(t1);  // @1 -> t1 register
SamplerState InputBSampler : register(s1);

// UAV Outputs
RWTexture2D OutputTexture : register(u5);  // @5 -> u5 register

[numthreads(8, 8, 1)]
void MathOperationsCSCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    if (((ThreadId.x >= TextureSize.x) || (ThreadId.y >= TextureSize.y)))
    {
        return;
    }
    float4 pos = uvec2(ThreadId.x, ThreadId.y);
    float4 a = InputA.Sample(InputASampler, pos);
    float4 b = InputB.Sample(InputBSampler, pos);
    float4 result = ((Operation == 0) ? (a + b) : ((Operation == 1) ? (a - b) : ((Operation == 2) ? (a * b) : ((Operation == 3) ? (a / max(b, float4(0.001000, 0.001000, 0.001000, 0.001000))) : ((Operation == 4) ? float4(min(a.rgb, b.rgb), max(a.a, b.a)) : ((Operation == 5) ? float4(max(a.rgb, b.rgb), max(a.a, b.a)) : ((Operation == 6) ? float4(pow(a.rgb, float3(Scalar, Scalar, Scalar)), a.a) : ((Operation == 7) ? float4((a.rgb * Scalar), a.a) : ((Operation == 8) ? float4((a.rgb + float3(Scalar, Scalar, Scalar)), a.a) : ((Operation == 9) ? float4(abs((a.rgb - b.rgb)), max(a.a, b.a)) : ((Operation == 10) ? float4(lerp(a.rgb, b.rgb, Scalar), lerp(a.a, b.a, Scalar)) : ((Operation == 11) ? float4(clamp(a.rgb, float3(0.000000, 0.000000, 0.000000), float3(1.000000, 1.000000, 1.000000)), a.a) : ((Operation == 12) ? float4((float3(1.000000, 1.000000, 1.000000) - a.rgb), a.a) : )))))))))))));
    write(OutputTexture, pos, result);
}

// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
// @N is ordering index, not register binding
float grain_intensity;  // @0
float grain_size;  // @1
float grain_luminance_contribution;  // @2
float grain_color_contribution;  // @3

struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput FilmGrainPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;

    float time = 0.000000;
    float grain = ((sin(((uv.x * 1000.000000) + time)) * cos(((uv.y * 1000.000000) + time))) * grain_intensity);
    FPSOutput _Result;
    _Result.Color = float4(float3(grain, grain, grain), 1.000000);
    return _Result;
}

# Generated from C source by kain import-c

fn smoketest_bridge_bias(value: Int) -> Int:
    return ((value * 3) + 17)

fn smoketest_bridge_mix(seed: Int, salt: Int) -> Int:
    let mut lane: Int = (((seed * 31) + (salt * 17)) + 13)
    let mut folded: Int = ((lane ^ (seed << 2)) ^ (salt << 1))
    if (folded < 0):
        (folded = (-folded))
    return (folded % 1000000007)

fn smoketest_bridge_fold(seed: Int, salt: Int, turns: Int) -> Int:
    let mut acc: Int = (seed + 97)
    let mut step: Int = 0
    while (step < turns):
        (acc = (((((acc * 17) + salt) + (step * 7)) + 19) % 1000003))
        (step = (step + 1))
    return acc


use std::runtime

// NOTE: std::collections is temporarily skipped due to pre-existing
// StringIntMap shadowing bug in stdlib/collections.kn (also affects
// benchmark/cases/stdlib_foundations). Re-enable when stdlib is fixed.

pub fn smoke_collections_lane() -> Int:
    return 0

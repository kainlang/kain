use std::runtime

// NOTE: std::alloc transitively loads std::collections which has a
// pre-existing StringIntMap shadowing bug. Re-enable when fixed.

pub fn smoke_alloc_lane() -> Int:
    return 0

# Generated by kain-c-ffi for library cgltf_scene_probe
# Header: M:\Code\Kain\smoketest\3D\zen_workflow_city\native/cgltf_scene_probe.h

mod c:
    mod cgltf_scene_probe:
        fn cgltf_probe_close(probe: Any):
            return ()

        fn c_cgltf_scene_probe_cgltf_probe_close(probe: Any):
            return ()

        fn cgltf_probe_material_count(probe: Any) -> Int:
            return 0

        fn c_cgltf_scene_probe_cgltf_probe_material_count(probe: Any) -> Int:
            return 0

        fn cgltf_probe_mesh_count(probe: Any) -> Int:
            return 0

        fn c_cgltf_scene_probe_cgltf_probe_mesh_count(probe: Any) -> Int:
            return 0

        fn cgltf_probe_node_count(probe: Any) -> Int:
            return 0

        fn c_cgltf_scene_probe_cgltf_probe_node_count(probe: Any) -> Int:
            return 0

        fn cgltf_probe_open(path: String) -> Any:
            return ()

        fn c_cgltf_scene_probe_cgltf_probe_open(path: String) -> Any:
            return ()

        fn cgltf_probe_primitive_count(probe: Any) -> Int:
            return 0

        fn c_cgltf_scene_probe_cgltf_probe_primitive_count(probe: Any) -> Int:
            return 0

        fn cgltf_probe_scene_name(probe: Any) -> String:
            return ""

        fn c_cgltf_scene_probe_cgltf_probe_scene_name(probe: Any) -> String:
            return ""

        fn cgltf_probe_signature(probe: Any) -> String:
            return ""

        fn c_cgltf_scene_probe_cgltf_probe_signature(probe: Any) -> String:
            return ""

        fn cgltf_probe_vertex_count(probe: Any) -> Int:
            return 0

        fn c_cgltf_scene_probe_cgltf_probe_vertex_count(probe: Any) -> Int:
            return 0


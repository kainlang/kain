// Generated by KAIN Compiler (Project Ouroboros)
// Do not edit - regenerate from .kn source

#![allow(unused_variables)]
#![allow(unused_mut)]
#![allow(dead_code)]
#![allow(unused_parens)]

use std::collections::{HashMap, HashSet};
use std::rc::Rc;
use std::cell::{Cell, RefCell};
use std::sync::{Arc, Mutex, RwLock};
use std::borrow::Cow;
use std::mem::{size_of, ManuallyDrop, MaybeUninit};
use std::pin::Pin;
use std::cmp::min;
use std::ffi::c_void;
use std::fmt::Formatter;

unsafe extern "C" {
    fn malloc(size: usize) -> *mut c_void;
    fn calloc(count: usize, size: usize) -> *mut c_void;
    fn realloc(ptr: *mut c_void, size: usize) -> *mut c_void;
}
fn __kain_pow(left: f64, right: f64) -> f64 {
    left.powf(right)
}
fn __kain_alloc_bytes(size: usize, zeroed: bool) -> *mut u8 {
    unsafe {
        if zeroed { calloc(1, size) as *mut u8 } else { malloc(size) as *mut u8 }
    }
}
fn __kain_realloc_bytes(ptr: *mut u8, size: usize, _zeroed_new: bool) -> *mut u8 {
    unsafe { realloc(ptr.cast::<c_void>(), size) as *mut u8 }
}
fn __kain_union_wrap<TObject: Copy, TValue: Copy>(mut value: TObject, _active: &str, _type_key: &str, byte_size: i64, union_size: i64, active_value: TValue) -> TObject {
    let zero_span = min(union_size.max(0) as usize, size_of::<TObject>());
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe {
        std::ptr::write_bytes((&mut value as *mut TObject).cast::<u8>(), 0, zero_span);
        if copy_span > 0 { std::ptr::copy_nonoverlapping((&active_value as *const TValue).cast::<u8>(), (&mut value as *mut TObject).cast::<u8>(), copy_span); }
    }
    value
}
fn __kain_union_get<TObject: Copy, TValue: Copy>(value: TObject, _field: &str, _type_key: &str, byte_size: i64, union_size: i64, fallback: TValue) -> TValue {
    let mut result = fallback;
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe { if copy_span > 0 { std::ptr::copy_nonoverlapping((&value as *const TObject).cast::<u8>(), (&mut result as *mut TValue).cast::<u8>(), copy_span); } }
    result
}
fn __kain_union_set<TObject: Copy, TValue: Copy>(mut value: TObject, _field: &str, _type_key: &str, byte_size: i64, union_size: i64, next: TValue) -> TValue {
    let zero_span = min(union_size.max(0) as usize, size_of::<TObject>());
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe {
        std::ptr::write_bytes((&mut value as *mut TObject).cast::<u8>(), 0, zero_span);
        if copy_span > 0 { std::ptr::copy_nonoverlapping((&next as *const TValue).cast::<u8>(), (&mut value as *mut TObject).cast::<u8>(), copy_span); }
    }
    next
}
fn __kain_load_bitfield_unit<T: Copy>(value: &T, unit_offset: i64) -> u64 {
    if unit_offset < 0 || unit_offset as usize >= size_of::<T>() { return 0; }
    let mut unit = 0u64;
    let available = min(8usize, size_of::<T>() - unit_offset as usize);
    unsafe { std::ptr::copy_nonoverlapping((value as *const T).cast::<u8>().add(unit_offset as usize), (&mut unit as *mut u64).cast::<u8>(), available); }
    unit
}
fn __kain_store_bitfield_unit<T: Copy>(value: &mut T, unit_offset: i64, unit: u64) {
    if unit_offset < 0 || unit_offset as usize >= size_of::<T>() { return; }
    let available = min(8usize, size_of::<T>() - unit_offset as usize);
    unsafe { std::ptr::copy_nonoverlapping((&unit as *const u64).cast::<u8>(), (value as *mut T).cast::<u8>().add(unit_offset as usize), available); }
}
fn __kain_bitfield_mask(width: i64) -> u64 { if width <= 0 { 0 } else if width >= 64 { u64::MAX } else { (1u64 << width) - 1 } }
fn __kain_sign_extend(value: u64, width: i64) -> i64 { if width <= 0 { 0 } else if width >= 64 { value as i64 } else { let sign_bit = 1u64 << (width - 1); if (value & sign_bit) == 0 { value as i64 } else { (value | !__kain_bitfield_mask(width)) as i64 } } }
fn __kain_bitfield_get<T: Copy>(value: T, _field: &str, unit_offset: i64, bit_offset: i64, width: i64, is_signed: bool, _promoted_bits: i64) -> i64 {
    let mask = __kain_bitfield_mask(width);
    let unit = __kain_load_bitfield_unit(&value, unit_offset);
    let shifted = if bit_offset <= 0 { unit } else { unit >> bit_offset };
    let encoded = shifted & mask;
    if is_signed { __kain_sign_extend(encoded, width) } else { encoded as i64 }
}
fn __kain_bitfield_set<T: Copy, TValue: Copy + Into<i64>>(mut value: T, _field: &str, unit_offset: i64, bit_offset: i64, width: i64, is_signed: bool, promoted_bits: i64, next: TValue) -> TValue {
    let mask = __kain_bitfield_mask(width);
    let mut unit = __kain_load_bitfield_unit(&value, unit_offset);
    let encoded = (next.into() as u64) & mask;
    let shifted_mask = if bit_offset <= 0 { mask } else { mask << bit_offset };
    unit = (unit & !shifted_mask) | if bit_offset <= 0 { encoded } else { encoded << bit_offset };
    __kain_store_bitfield_unit(&mut value, unit_offset, unit);
    let _ = __kain_bitfield_get(value, "", unit_offset, bit_offset, width, is_signed, promoted_bits);
    next
}

#[derive(Debug, Clone, PartialEq)]
struct AppProps {
    pub children: String,
}

fn App(props: AppProps) -> String {
    let AppProps { children } = props;
    format!("<slot{}{}</slot>", vec![format!(" layout=\"{}\"", "column"), format!(" gap=\"{}\"", 22), format!(" padding=\"{}\"", 22), format!(" overflow_y=\"{}\"", "scroll")].join(""), vec![format!("<theme{}{}</theme>", vec![format!(" name=\"{}\"", "atrium_showcase")].join(""), vec![format!("<scope{}{}</scope>", vec![format!(" name=\"{}\"", "atrium"), format!(" selector=\"{}\"", "material-atrium")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.chrome.topbar.visible"), format!(" category=\"{}\"", "chrome"), format!(" value=\"{}\"", false)].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.spacing.scale"), format!(" category=\"{}\"", "space"), format!(" value=\"{}\"", 1.1)].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.radius.scale"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", 1.2)].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.typography.scale"), format!(" category=\"{}\"", "type"), format!(" value=\"{}\"", 1.1)].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.background.top"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#060b11")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.background.bottom"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#151017")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.surface.base"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#121922")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.surface.alt"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#1a202b")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.surface.raised"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#212734")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.surface.overlay"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#10151ddd")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.accent.primary"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#71d9ff")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.accent.soft"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#b4efff")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.highlight"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#ffba72")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.text.default"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#f7f4ee")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.text.muted"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#aab4c2")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "widget.panel.surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "glass")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "widget.viewport3d.surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "canvas")].join(""), String::new()), format!("<variant{}{}</variant>", vec![format!(" scope=\"{}\"", "atrium"), format!(" name=\"{}\"", "nav")].join(""), vec![format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "ghost")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.padding"), format!(" category=\"{}\"", "space"), format!(" value=\"{}\"", 10)].join(""), String::new())].join("")), format!("<variant{}{}</variant>", vec![format!(" scope=\"{}\"", "atrium"), format!(" name=\"{}\"", "hero")].join(""), vec![format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "glass")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.padding"), format!(" category=\"{}\"", "space"), format!(" value=\"{}\"", 18)].join(""), String::new())].join("")), format!("<variant{}{}</variant>", vec![format!(" scope=\"{}\"", "atrium"), format!(" name=\"{}\"", "section")].join(""), vec![format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "layered")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.padding"), format!(" category=\"{}\"", "space"), format!(" value=\"{}\"", 16)].join(""), String::new())].join("")), format!("<variant{}{}</variant>", vec![format!(" scope=\"{}\"", "atrium"), format!(" name=\"{}\"", "card")].join(""), vec![format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "flat")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.padding"), format!(" category=\"{}\"", "space"), format!(" value=\"{}\"", 14)].join(""), String::new())].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "nav"), format!(" layout=\"{}\"", "row"), format!(" gap=\"{}\"", 14)].join(""), vec![format!("<slot{}{}</slot>", vec![format!(" layout=\"{}\"", "column"), format!(" gap=\"{}\"", 3)].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["KAIN NATIVE RUNTIME".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["Qt shell, primitive-backed 3D, and renderer identity in one authored surface".to_string()].join(""))].join("")), format!("<slot{}{}</slot>", vec![format!(" layout=\"{}\"", "row"), format!(" gap=\"{}\"", 10)].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["Qt native shell".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["material_atrium scene".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["native smoke app".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["backend matrix".to_string()].join(""))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" title=\"{}\"", "Atrium Hero"), format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "hero"), format!(" layout=\"{}\"", "grid"), format!(" columns=\"{}\"", 2), format!(" gap=\"{}\"", 18)].join(""), vec![format!("<slot{}{}</slot>", vec![format!(" layout=\"{}\"", "column"), format!(" gap=\"{}\"", 14)].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["PRIMITIVE-POWERED MATERIAL ATRIUM".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "hero")].join(""), vec!["The new Qt-native shell is carrying the showcase now, and the 3D content is built from the authored Kain primitive library instead of hand-wired one-offs.".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "muted")].join(""), vec!["This smoke stays inside the native runtime path, surfaces the shared scene contract, and keeps the visual proof focused on a polished product shell rather than a bare harness.".to_string()].join("")), format!("<slot{}{}</slot>", vec![format!(" layout=\"{}\"", "row"), format!(" gap=\"{}\"", 12)].join(""), vec![format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "metric")].join(""), vec!["4".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["renderer identities".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "metric")].join(""), vec!["8".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["authored primitive shapes".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "metric")].join(""), vec!["1".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["shared scene contract".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "metric")].join(""), vec!["1".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["Qt native shell".to_string()].join(""))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card"), format!(" title=\"{}\"", "Showcase Brief")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Material-first, shell-first, runtime-first.".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The viewport stays central, but the app reads like product software instead of a test harness.".to_string()].join(""))].join(""))].join("")), format!("<viewport3d{}{}</viewport3d>", vec![format!(" title=\"{}\"", "Material Atrium"), format!(" scene=\"{}\"", "material_atrium")].join(""), String::new())].join("")), format!("<panel{}{}</panel>", vec![format!(" title=\"{}\"", "Primitive Stack"), format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "section"), format!(" layout=\"{}\"", "grid"), format!(" columns=\"{}\"", 4), format!(" gap=\"{}\"", 14)].join(""), vec![format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["STARTUP CUBE".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Structural anchor".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The clean block primitive used for monoliths, plinths, and stable massing.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["STUDIO CYLINDER".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Gallery columns".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The repeating vertical primitive driving the atrium perimeter and depth cues.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["HERO TORUS".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Halo accent".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["Used as a large ring around the central orb to add motion and polish.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["HERO CONE".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Crown spire".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The tapered primitive that keeps the scene readable and sculptural.".to_string()].join(""))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" title=\"{}\"", "Renderer Matrix"), format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "section"), format!(" layout=\"{}\"", "grid"), format!(" columns=\"{}\"", 4), format!(" gap=\"{}\"", 14)].join(""), vec![format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["BGFX".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Baseline backend".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["Cross-platform renderer lane for viewport, swapchain, and debug-draw work.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["FILAMENT".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Premium presentation".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The higher-level material and lighting lane for polished scene output.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["DILIGENT".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Graph and compute".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The explicit-engine lane for render-graph control and pipeline visibility.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["THE FORGE".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Low-level substrate".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["Staged as a bridge-first backend identity for future device and queue depth.".to_string()].join(""))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" title=\"{}\"", "Runtime Notes"), format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "section"), format!(" layout=\"{}\"", "grid"), format!(" columns=\"{}\"", 2), format!(" gap=\"{}\"", 16)].join(""), vec![format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Native runtime lane".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The smoke app builds through kain-ui-native, launches the Qt shell, and keeps the showcase inside the runtime path.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Primitive-backed scene".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The atrium scene now composes its massing from Kain primitives so the 3D system reads like an authored product surface.".to_string()].join(""))].join(""))].join(""))].join(""))
}

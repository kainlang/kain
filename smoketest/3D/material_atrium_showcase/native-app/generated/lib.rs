// Generated by KAIN Compiler (Project Ouroboros)
// Do not edit - regenerate from .kn source

#![allow(unused_variables)]
#![allow(unused_mut)]
#![allow(dead_code)]
#![allow(unused_parens)]

use std::collections::{HashMap, HashSet};
use std::rc::Rc;
use std::cell::{Cell, RefCell};
use std::sync::{Arc, Mutex, RwLock};
use std::borrow::Cow;
use std::mem::{size_of, ManuallyDrop, MaybeUninit};
use std::pin::Pin;
use std::cmp::min;
use std::ffi::c_void;
use std::fmt::Formatter;

unsafe extern "C" {
    fn malloc(size: usize) -> *mut c_void;
    fn calloc(count: usize, size: usize) -> *mut c_void;
    fn realloc(ptr: *mut c_void, size: usize) -> *mut c_void;
}
fn __kain_pow(left: f64, right: f64) -> f64 {
    left.powf(right)
}
fn __kain_alloc_bytes(size: usize, zeroed: bool) -> *mut u8 {
    unsafe {
        if zeroed { calloc(1, size) as *mut u8 } else { malloc(size) as *mut u8 }
    }
}
fn __kain_realloc_bytes(ptr: *mut u8, size: usize, _zeroed_new: bool) -> *mut u8 {
    unsafe { realloc(ptr.cast::<c_void>(), size) as *mut u8 }
}

fn __kain_union_wrap<TObject: Copy, TValue: Copy>(mut value: TObject, _active: &str, _type_key: &str, byte_size: i64, union_size: i64, active_value: TValue) -> TObject {
    let zero_span = min(union_size.max(0) as usize, size_of::<TObject>());
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe {
        std::ptr::write_bytes((&mut value as *mut TObject).cast::<u8>(), 0, zero_span);
        if copy_span > 0 { std::ptr::copy_nonoverlapping((&active_value as *const TValue).cast::<u8>(), (&mut value as *mut TObject).cast::<u8>(), copy_span); }
    }
    value
}
fn __kain_union_get<TObject: Copy, TValue: Copy>(value: TObject, _field: &str, _type_key: &str, byte_size: i64, union_size: i64, fallback: TValue) -> TValue {
    let mut result = fallback;
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe { if copy_span > 0 { std::ptr::copy_nonoverlapping((&value as *const TObject).cast::<u8>(), (&mut result as *mut TValue).cast::<u8>(), copy_span); } }
    result
}
fn __kain_union_set<TObject: Copy, TValue: Copy>(mut value: TObject, _field: &str, _type_key: &str, byte_size: i64, union_size: i64, next: TValue) -> TValue {
    let zero_span = min(union_size.max(0) as usize, size_of::<TObject>());
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe {
        std::ptr::write_bytes((&mut value as *mut TObject).cast::<u8>(), 0, zero_span);
        if copy_span > 0 { std::ptr::copy_nonoverlapping((&next as *const TValue).cast::<u8>(), (&mut value as *mut TObject).cast::<u8>(), copy_span); }
    }
    next
}
fn __kain_load_bitfield_unit<T: Copy>(value: &T, unit_offset: i64) -> u64 {
    if unit_offset < 0 || unit_offset as usize >= size_of::<T>() { return 0; }
    let mut unit = 0u64;
    let available = min(8usize, size_of::<T>() - unit_offset as usize);
    unsafe { std::ptr::copy_nonoverlapping((value as *const T).cast::<u8>().add(unit_offset as usize), (&mut unit as *mut u64).cast::<u8>(), available); }
    unit
}
fn __kain_store_bitfield_unit<T: Copy>(value: &mut T, unit_offset: i64, unit: u64) {
    if unit_offset < 0 || unit_offset as usize >= size_of::<T>() { return; }
    let available = min(8usize, size_of::<T>() - unit_offset as usize);
    unsafe { std::ptr::copy_nonoverlapping((&unit as *const u64).cast::<u8>(), (value as *mut T).cast::<u8>().add(unit_offset as usize), available); }
}
fn __kain_bitfield_mask(width: i64) -> u64 { if width <= 0 { 0 } else if width >= 64 { u64::MAX } else { (1u64 << width) - 1 } }
fn __kain_sign_extend(value: u64, width: i64) -> i64 { if width <= 0 { 0 } else if width >= 64 { value as i64 } else { let sign_bit = 1u64 << (width - 1); if (value & sign_bit) == 0 { value as i64 } else { (value | !__kain_bitfield_mask(width)) as i64 } } }
fn __kain_bitfield_get<T: Copy>(value: T, _field: &str, unit_offset: i64, bit_offset: i64, width: i64, is_signed: bool, _promoted_bits: i64) -> i64 {
    let mask = __kain_bitfield_mask(width);
    let unit = __kain_load_bitfield_unit(&value, unit_offset);
    let shifted = if bit_offset <= 0 { unit } else { unit >> bit_offset };
    let encoded = shifted & mask;
    if is_signed { __kain_sign_extend(encoded, width) } else { encoded as i64 }
}
fn __kain_bitfield_set<T: Copy, TValue: Copy + Into<i64>>(mut value: T, _field: &str, unit_offset: i64, bit_offset: i64, width: i64, is_signed: bool, promoted_bits: i64, next: TValue) -> TValue {
    let mask = __kain_bitfield_mask(width);
    let mut unit = __kain_load_bitfield_unit(&value, unit_offset);
    let encoded = (next.into() as u64) & mask;
    let shifted_mask = if bit_offset <= 0 { mask } else { mask << bit_offset };
    unit = (unit & !shifted_mask) | if bit_offset <= 0 { encoded } else { encoded << bit_offset };
    __kain_store_bitfield_unit(&mut value, unit_offset, unit);
    let _ = __kain_bitfield_get(value, "", unit_offset, bit_offset, width, is_signed, promoted_bits);
    next
}

fn backend_mood(backend: String) -> String {
    if (backend == "bgfx".to_string()) { return "bgfx feels immediate: cross-platform, direct, and stable enough to anchor the smoke.".to_string(); } else if (backend == "filament".to_string()) { return "filament feels premium: materials, lighting, and presentation should read expensive.".to_string(); } else if (backend == "diligent".to_string()) { return "diligent feels surgical: explicit passes, graph control, and compute depth.".to_string(); } else if (backend == "the-forge".to_string()) { return "the-forge feels raw: low-level substrate, bridge-first, and deliberately close to the metal.".to_string(); } else { return "Kain feels owned: the runtime contract is authored here, not borrowed from a host example.".to_string(); }
}

fn backend_focus(backend: String) -> String {
    if (backend == "bgfx".to_string()) { return "baseline lane for the first real runtime-backed viewport.".to_string(); } else if (backend == "filament".to_string()) { return "premium lane for a polished material and lighting feel.".to_string(); } else if (backend == "diligent".to_string()) { return "graph lane for explicit renderer control and GPU visibility.".to_string(); } else if (backend == "the-forge".to_string()) { return "substrate lane for bridge-first device work and future low-level render depth.".to_string(); } else { return "Kain-owned lane with Qt as the present fallback shell.".to_string(); }
}

fn backend_state_label(active_backend: String, candidate_backend: String) -> String {
    if (active_backend == candidate_backend) { return "active lane".to_string(); };
    return "ready lane".to_string();
}



fn handle_bgfx() {
    choose_backend(MaterialAtriumShowcase, "bgfx".to_string());
}

fn handle_filament() {
    choose_backend(MaterialAtriumShowcase, "filament".to_string());
}

fn handle_diligent() {
    choose_backend(MaterialAtriumShowcase, "diligent".to_string());
}

fn handle_forge() {
    choose_backend(MaterialAtriumShowcase, "the-forge".to_string());
}

#[derive(Debug, Clone, PartialEq)]
struct AppProps {
    pub children: String,
}

fn App(props: AppProps) -> String {
    let AppProps { children } = props;
    format!("<slot{}{}</slot>", vec![format!(" layout=\"{}\"", "column"), format!(" gap=\"{}\"", 18), format!(" padding=\"{}\"", 18)].join(""), vec![format!("<theme{}{}</theme>", vec![format!(" name=\"{}\"", "atrium_showcase")].join(""), vec![format!("<scope{}{}</scope>", vec![format!(" name=\"{}\"", "atrium"), format!(" selector=\"{}\"", "material-atrium")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.chrome.topbar.visible"), format!(" category=\"{}\"", "chrome"), format!(" value=\"{}\"", false)].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.spacing.scale"), format!(" category=\"{}\"", "space"), format!(" value=\"{}\"", 1.1)].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.radius.scale"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", 1.2)].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.typography.scale"), format!(" category=\"{}\"", "type"), format!(" value=\"{}\"", 1.1)].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.background.top"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#07111c")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.background.bottom"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#130f17")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.surface.base"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#101827")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.surface.alt"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#192230")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.surface.raised"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#21293a")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.surface.overlay"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#0c1421dd")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.accent.primary"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#70ddff")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.accent.soft"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#b9f2ff")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.highlight"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#ffbf74")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.text.default"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#f7fbff")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "theme.text.muted"), format!(" category=\"{}\"", "color"), format!(" value=\"{}\"", "#96a4b5")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "widget.panel.surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "glass")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "widget.viewport3d.surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "canvas")].join(""), String::new()), format!("<variant{}{}</variant>", vec![format!(" scope=\"{}\"", "atrium"), format!(" name=\"{}\"", "nav")].join(""), vec![format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "ghost")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.padding"), format!(" category=\"{}\"", "space"), format!(" value=\"{}\"", 10)].join(""), String::new())].join("")), format!("<variant{}{}</variant>", vec![format!(" scope=\"{}\"", "atrium"), format!(" name=\"{}\"", "hero")].join(""), vec![format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "glass")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.padding"), format!(" category=\"{}\"", "space"), format!(" value=\"{}\"", 18)].join(""), String::new())].join("")), format!("<variant{}{}</variant>", vec![format!(" scope=\"{}\"", "atrium"), format!(" name=\"{}\"", "section")].join(""), vec![format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "layered")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.padding"), format!(" category=\"{}\"", "space"), format!(" value=\"{}\"", 16)].join(""), String::new())].join("")), format!("<variant{}{}</variant>", vec![format!(" scope=\"{}\"", "atrium"), format!(" name=\"{}\"", "card")].join(""), vec![format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.mode"), format!(" category=\"{}\"", "surface"), format!(" value=\"{}\"", "flat")].join(""), String::new()), format!("<token{}{}</token>", vec![format!(" name=\"{}\"", "surface.padding"), format!(" category=\"{}\"", "space"), format!(" value=\"{}\"", 14)].join(""), String::new())].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "nav"), format!(" layout=\"{}\"", "row"), format!(" gap=\"{}\"", 16)].join(""), vec![format!("<slot{}{}</slot>", vec![format!(" layout=\"{}\"", "column"), format!(" gap=\"{}\"", 4)].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["KAIN MATERIAL ATRIUM SHOWCASE".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["Source-first smoke. Native-owner metadata. Kain primitives. Qt is the current presentation fallback.".to_string()].join(""))].join("")), format!("<slot{}{}</slot>", vec![format!(" layout=\"{}\"", "row"), format!(" gap=\"{}\"", 10)].join(""), vec![format!("<button{}{}</button>", vec![format!(" onclick=\"{}\"", handle_bgfx)].join(""), vec!["bgfx".to_string()].join("")), format!("<button{}{}</button>", vec![format!(" onclick=\"{}\"", handle_filament)].join(""), vec!["filament".to_string()].join("")), format!("<button{}{}</button>", vec![format!(" onclick=\"{}\"", handle_diligent)].join(""), vec!["diligent".to_string()].join("")), format!("<button{}{}</button>", vec![format!(" onclick=\"{}\"", handle_forge)].join(""), vec!["the-forge".to_string()].join(""))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" title=\"{}\"", "Atrium Hero"), format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "hero"), format!(" layout=\"{}\"", "grid"), format!(" columns=\"{}\"", 2), format!(" gap=\"{}\"", 18)].join(""), vec![format!("<slot{}{}</slot>", vec![format!(" layout=\"{}\"", "column"), format!(" gap=\"{}\"", 14)].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["NATIVE OWNER PATH".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "hero")].join(""), vec!["Kain owns the runtime contract here. The smoke is source-first, the buttons change the shell mood, and the scene stays Kain-authored instead of living off a bundle example.".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "muted")].join(""), vec![format!("{}", MaterialAtriumShowcase.active_mood)].join("")), format!("<slot{}{}</slot>", vec![format!(" layout=\"{}\"", "row"), format!(" gap=\"{}\"", 12)].join(""), vec![format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "metric")].join(""), vec![format!("{}", MaterialAtriumShowcase.active_backend)].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["active backend".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "metric")].join(""), vec!["1".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["Kain-owned scene".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "metric")].join(""), vec!["4".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["backend moods".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "metric")].join(""), vec!["qt".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["compatibility host".to_string()].join(""))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card"), format!(" title=\"{}\"", "Runtime Owner Note")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec![format!("{}", MaterialAtriumShowcase.status_line)].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec![format!("{}", MaterialAtriumShowcase.active_focus)].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The new 3D runtime should feel like authored product software, not a thin renderer demo.".to_string()].join(""))].join(""))].join("")), format!("<viewport3d{}{}</viewport3d>", vec![format!(" title=\"{}\"", "Material Atrium"), format!(" scene=\"{}\"", "material_atrium")].join(""), String::new())].join("")), format!("<panel{}{}</panel>", vec![format!(" title=\"{}\"", "Backend Switchboard"), format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "section"), format!(" layout=\"{}\"", "grid"), format!(" columns=\"{}\"", 4), format!(" gap=\"{}\"", 14)].join(""), vec![format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["BGFX".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Baseline lane".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec![format!("{}", backend_state_label(MaterialAtriumShowcase.active_backend, "bgfx".to_string()))].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec![format!("{}", backend_mood("bgfx".to_string()))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["FILAMENT".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Premium lighting".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec![format!("{}", backend_state_label(MaterialAtriumShowcase.active_backend, "filament".to_string()))].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec![format!("{}", backend_mood("filament".to_string()))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["DILIGENT".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Graph and compute".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec![format!("{}", backend_state_label(MaterialAtriumShowcase.active_backend, "diligent".to_string()))].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec![format!("{}", backend_mood("diligent".to_string()))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["THE FORGE".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Low-level substrate".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec![format!("{}", backend_state_label(MaterialAtriumShowcase.active_backend, "the-forge".to_string()))].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec![format!("{}", backend_mood("the-forge".to_string()))].join(""))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" title=\"{}\"", "Atrium Geometry"), format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "section"), format!(" layout=\"{}\"", "grid"), format!(" columns=\"{}\"", 4), format!(" gap=\"{}\"", 14)].join(""), vec![format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["STARTUP CUBE".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Structural anchor".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The clean block primitive used for the central plinth and stable massing.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["STUDIO COLUMNS".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Perimeter ring".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["A repeating vertical system that gives the atrium depth and rhythm.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["HERO HALO".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Ambient accent".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["A luminous ring around the center mass that keeps the scene feeling premium.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "eyebrow")].join(""), vec!["CROWN SPAN".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Roofline grid".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["A lifted frame that keeps the scene readable as an atrium instead of a cube room.".to_string()].join(""))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" title=\"{}\"", "Runtime Notes"), format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "section"), format!(" layout=\"{}\"", "grid"), format!(" columns=\"{}\"", 2), format!(" gap=\"{}\"", 16)].join(""), vec![format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Source-first smoke".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["The launcher now includes smoke.kn directly, so the authored surface is the thing being booted rather than a generated runtime bundle example.".to_string()].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "card")].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "title")].join(""), vec!["Native owner profile".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["material_atrium is the runtime scene profile, and the Windows native viewport branch now knows about it as a first-class scene name.".to_string()].join(""))].join(""))].join("")), format!("<panel{}{}</panel>", vec![format!(" title=\"{}\"", "Footer"), format!(" scope=\"{}\"", "atrium"), format!(" variant=\"{}\"", "nav"), format!(" layout=\"{}\"", "row"), format!(" gap=\"{}\"", 16)].join(""), vec![format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["Kain native runtime smoke".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["material_atrium".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["native-owner metadata".to_string()].join("")), format!("<text{}{}</text>", vec![format!(" role=\"{}\"", "caption")].join(""), vec!["Qt compatibility host".to_string()].join(""))].join(""))].join(""))
}


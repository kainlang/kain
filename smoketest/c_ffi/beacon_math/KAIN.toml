[package]
name = "c_ffi_beacon_math"
version = "0.1.0"

[build]
entry = "smoke.kn"
output = "outputs"
targets = ["run", "test"]

[c_ffi]

[[c_ffi.libraries]]
name = "beacon_math"
header = "native/beacon_math.h"
shared_lib = "native/beacon_math.dll"

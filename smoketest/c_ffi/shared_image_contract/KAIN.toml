[package]
name = "c_ffi_shared_image_contract"
version = "0.1.0"

[build]
entry = "smoke.kn"
output = "outputs"
targets = ["run", "test"]

[c_ffi]

[[c_ffi.libraries]]
name = "image_fx"
header = "native/image_fx.h"
shared_lib = "native/${kain_dynlib:image_fx}"

# Generated by kain-c-ffi for library image_fx
# Header: M:\Code\Kain\smoketest\py_cargo_node_c\quad_prism_halo\native/image_fx.h

mod c:
    mod image_fx:
        fn imagefx_checksum(pixels: Any, len: Int) -> Int:
            return 0

        fn c_image_fx_imagefx_checksum(pixels: Any, len: Int) -> Int:
            return 0

        fn imagefx_halo_rgba(pixels: Any, len: Int, accent: Int):
            return ()

        fn c_image_fx_imagefx_halo_rgba(pixels: Any, len: Int, accent: Int):
            return ()

        fn imagefx_signature(width: Int, height: Int, checksum: Int, accent: Int) -> String:
            return ""

        fn c_image_fx_imagefx_signature(width: Int, height: Int, checksum: Int, accent: Int) -> String:
            return ""

        fn imagefx_workspace_area(workspace: Any) -> Int:
            return 0

        fn c_image_fx_imagefx_workspace_area(workspace: Any) -> Int:
            return 0

        fn imagefx_workspace_create(width: Int, height: Int) -> Any:
            return ()

        fn c_image_fx_imagefx_workspace_create(width: Int, height: Int) -> Any:
            return ()

        fn imagefx_workspace_destroy(workspace: Any):
            return ()

        fn c_image_fx_imagefx_workspace_destroy(workspace: Any):
            return ()


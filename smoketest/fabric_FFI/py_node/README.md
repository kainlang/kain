# Python + Node Mixed Smokes

These smokes prove Kain can drive Python FFI and the JavaScript/Node bridge from the same `.kn` file.

Current examples:

- `nebula_canvas`: Python generates orbital signal data, Kain composes the SVG scene, and Node packages document/image payloads into visible web artifacts

Each smoke folder includes local launchers so it can be used as a standalone reference example.

# Generated by kain-crate-ffi for crate py_cargo_canvas
# Resolution: PathConfig

mod rust:
    mod py_cargo_canvas:
        const HYBRID_REVISION: Int = 11

        struct HybridStamp:
            pulses: Int
            label: String

        enum HybridMode:
            Ember
            Tidal

        fn hybrid_async_probe() -> Unit:
            panic("Rust crate FFI stub: crate 'py_cargo_canvas' symbol 'hybrid_async_probe' is unsupported in v1: generic and async functions are stubbed in crate FFI v1")

        fn generic_passthrough() -> Unit:
            panic("Rust crate FFI stub: crate 'py_cargo_canvas' symbol 'generic_passthrough' is unsupported in v1: generic and async functions are stubbed in crate FFI v1")

        fn cargo_signature(label: String, phase: Int) -> String:
            return ""

        fn rust_py_cargo_canvas_cargo_signature(label: String, phase: Int) -> String:
            return ""

        fn cargo_energy(values: Array<Int>) -> Int:
            return 0

        fn rust_py_cargo_canvas_cargo_energy(values: Array<Int>) -> Int:
            return 0

        fn cargo_mask_row(width: Int, row: Int, phase: Int) -> Array<Int>:
            return []

        fn rust_py_cargo_canvas_cargo_mask_row(width: Int, row: Int, phase: Int) -> Array<Int>:
            return []

        fn cargo_beacon_points(width: Int, height: Int, phase: Int) -> Array<Int>:
            return []

        fn rust_py_cargo_canvas_cargo_beacon_points(width: Int, height: Int, phase: Int) -> Array<Int>:
            return []

        fn hybrid_stamp_orbit(seed: Int, lanes: Int) -> Int:
            return 0

        fn rust_py_cargo_canvas_hybrid_stamp_orbit(seed: Int, lanes: Int) -> Int:
            return 0


"""Built-in optional pykain adapters.

The core package keeps these tiny and dependency-soft: importing pykain should
never force a GUI stack, but `window_backend="auto"` should still find one when
the Python environment already has it.
"""


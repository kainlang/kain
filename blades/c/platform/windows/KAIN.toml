[package]
name = "win32-raw-windows-h"
version = "0.1.0"
description = "Proof that libclang parses the real Windows SDK <windows.h> header. Zero shims."

[build]
entry = "src/main.kn"
artifact_root = ".kain/out/llvm"
cache_root = ".kain/cache/build"
profile = "debug"

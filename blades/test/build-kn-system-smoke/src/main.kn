use std::runtime

fn main() -> Int:
    let boot = runtime_init()
    if boot != 0:
        return 100 + boot
    let shutdown = runtime_shutdown()
    if shutdown != 0:
        return 200 + shutdown
    return 0

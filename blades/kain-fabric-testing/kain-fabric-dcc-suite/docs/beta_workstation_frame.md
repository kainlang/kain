# Beta Workstation Frame

Date: 2026-03-27

- The Fabric DCC suite now has explicit authored chrome for the workstation frame: top bar, workspace navigator, status strip, command spotlight, and property grid.
- Surface registry entries now carry real property rows so the projector can render selection and workstation metadata as dense editor UI instead of generic cards.
- The workbench registry mirrors the same page rails and quick commands so generated output stays disposable and the app-owned contract stays canonical.
- Next step: keep polishing shell feel in app-local config while the runtime and packaging lanes finish the deeper truth behind the chrome.

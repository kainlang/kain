use std::json
use std::kain
use std::fs
use std::mcp
use transport
use lsp
use mcp_server as ms

pub fn main() -> Int with IO:
    let tools = ms.mcp_tool_defs()
    let result = ms.mcp_dispatch_tool
    return 0

use std::runtime
use std::ui

fn main() -> Int:
    let _init = runtime_init()
    let s = ui_host_session_create("sc", "SC", 400, 300, "winit")
    if s <= 0:
        return 1

    let root = ui_reconcile_node(s, 0, "root", "sc.root", 0.0, 0.0, 400.0, 300.0)

    let _b = ui_frame_begin(s, 16.0)
    let _f = native_ui_node_set_style_string(s, root, "fill_color", "#f00")
    let _x = ui_frame_submit(s)

    let _d = ui_session_destroy(s)
    return runtime_shutdown()

// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
// @N is ordering index, not register binding
float heat_seed;  // @0
float alert_level;  // @1
float threat_bias;  // @2
float pulse_speed;  // @3

// UAV Outputs
RWTexture2D<float4> OutputTexture : register(u0);  // @0 -> u0 register

[numthreads(8, 8, 1)]
void ThreatHeatmapCSCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float2 uv = float2((ThreadId.x / 1024.000000), (ThreadId.y / 1024.000000));
    float base_noise = frac((sin((dot(uv, float2(12.989800, 78.233000)) + heat_seed)) * 43758.545300));
    float pulse = ((sin(((((uv.x + uv.y)) * 20.000000) + pulse_speed)) * 0.500000) + 0.500000);
    float danger = (base_noise * ((0.350000 + (alert_level * 0.650000))));
    float weighted = max(danger, (pulse * threat_bias));
    OutputTexture[ThreadId.xy] = float4(weighted, (weighted * alert_level), pulse, 1.000000);
    return;
}

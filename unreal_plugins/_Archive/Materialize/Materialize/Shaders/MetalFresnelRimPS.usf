// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
// @N is ordering index, not register binding
float3 metal_f0;  // @0
float rim_intensity;  // @1
float rim_power;  // @2
float3 normal;  // @3
float3 view_dir;  // @4

struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
};

struct FPSOutput
{
    float4 Color : SV_Target0;
};

FPSOutput MetalFresnelRimPSPS(FPSInput Input)
{
    // Unpack interpolators
    float2 uv = Input.TexCoord0.xy;

    float3 n = normalize(normal);
    float3 v = normalize(view_dir);
    float ndv = max(0.000000, dot(n, v));
    float3 fresnel = (metal_f0 + (((float3(1.000000, 1.000000, 1.000000) - metal_f0)) * pow((1.000000 - ndv), 5.000000)));
    float rim = (pow((1.000000 - ndv), rim_power) * rim_intensity);
    float3 color = (fresnel * rim);
    FPSOutput _Result;
    _Result.Color = float4(color, 1.000000);
    return _Result;
}

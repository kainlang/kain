// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
    float4 TexCoord1 : TEXCOORD1;
};

struct FPSOutput
{
    float Color : SV_Target0;
};

FPSOutput MaterializeGGXDistributionPS(FPSInput Input)
{
    // Unpack interpolators
    float3 normal = Input.TexCoord0.xyz;
    float3 half_dir = Input.TexCoord1.xyz;
    float roughness = Input.TexCoord0.w;

    float3 n = normalize(normal);
    float3 h = normalize(half_dir);
    float ndh = max(0.000000, dot(n, h));
    float alpha = max(0.001000, (roughness * roughness));
    float alpha2 = (alpha * alpha);
    float denom = (((ndh * ndh) * ((alpha2 - 1.000000))) + 1.000000);
    float d = (alpha2 / (((3.141590 * denom) * denom)));
    FPSOutput _Result;
    _Result.Color = d;
    return _Result;
}

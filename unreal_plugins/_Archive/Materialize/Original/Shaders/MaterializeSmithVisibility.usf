// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

struct FPSInput
{
    float4 TexCoord0 : TEXCOORD0;
    float4 TexCoord1 : TEXCOORD1;
    float4 TexCoord2 : TEXCOORD2;
};

struct FPSOutput
{
    float Color : SV_Target0;
};

FPSOutput MaterializeSmithVisibilityPS(FPSInput Input)
{
    // Unpack interpolators
    float3 normal = Input.TexCoord0.xyz;
    float3 view_dir = Input.TexCoord1.xyz;
    float3 light_dir = Input.TexCoord2.xyz;
    float roughness = Input.TexCoord0.w;

    float3 n = normalize(normal);
    float3 v = normalize(view_dir);
    float3 l = normalize(light_dir);
    float ndv = max(0.000000, dot(n, v));
    float ndl = max(0.000000, dot(n, l));
    float alpha = max(0.001000, (roughness * roughness));
    float alpha2 = (alpha * alpha);
    float lambda_v = (ndl * sqrt((alpha2 + ((((1.000000 - alpha2)) * ndv) * ndv))));
    float lambda_l = (ndv * sqrt((alpha2 + ((((1.000000 - alpha2)) * ndl) * ndl))));
    float vis = (0.500000 / max(0.001000, (lambda_v + lambda_l)));
    FPSOutput _Result;
    _Result.Color = vis;
    return _Result;
}

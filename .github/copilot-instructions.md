

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v5.4.0 -->
<!-- Updated: 2026-04-18T04:02:37.277Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures

<!-- Generated by SigMap gen-context.js v5.4.0 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## deps
```
apps/3D/tools/validation/validate_scene_spine.py ← __future__
apps/3D/tools/validation/run_template_checks.py ← __future__
```

## todos
```
labs/threejs_node_ffi_space_lab/outputs/client/three-space.bundle.js:4084  # TODO: lengthSquared?
labs/threejs_node_ffi_space_lab/outputs/client/three-space.bundle.js:15611  # TODO: (abelnation): RectAreaLight BRDF data needs to be moved from example t
crates/kain-sys-codegen/src/codegen_llvm/mod.rs:2232  # TODO: Handle Structs, Enums, Consts
crates/kain-core/src/runtime.rs:4367  # TODO: proper format
```

## changes (last 10 commits — 4 hours ago)
```
src/core/lexer.kn                             +helpers
apps/3D/tools/validation/validate_scene_spine.py ~main
apps/3D/tools/validation/run_template_checks.py +load_toml_like_text  +require_token  +run_check  +main
crates/kain-sys-codegen/src/codegen_c.rs      +struct  +enum
```

## MCP

### MCP/README.md
```
h1 KAIN MCP
```

### MCP/server.kn
```
fn parse_truthy_env(name: String, default_value: Bool) -> Bool:
let raw = env(name).trim().to_ascii_lowercase()
fn parse_csv_env(name: String, fallback_csv: String) -> Array<String>:
let raw = env(name).trim()
let ch = char_at(source, index)
let trimmed = current.trim()
let final_piece = current.trim()
fn resolve_repo_root() -> String:
let primary = env("KAIN_REPO_ROOT").trim()
let fallback = env("KAIN_MCP_REPO_ROOT").trim()
fn resolve_kain_bin(repo_root: String) -> String:
let override_bin = env("KAIN_MCP_KAIN_BIN").trim()
let local_binary = path_join(repo_root, "target/debug/kain")
fn resolve_settings() -> McpRuntimeSettings:
let repo_root = resolve_repo_root()
```

## apps

### apps/3D/ARCHITECTURE.md
```
h1 3D Template Architecture
h2 Current Shape
h2 Scene Composition Spine
h2 Design Goals
h2 Maintenance Rules
h2 Common Errors
```

### apps/3D/MEMORY.md
```
h1 Memory
h2 2026-03-29 - DCC Fuel Mapping for kain-fabric-dcc-suite
h2 2026-04-16 - Scene composition spine documented
h2 2026-04-16 - Scene spine validator added
h2 2026-04-16 - 3D lane wiring guarded end-to-end
h2 2026-04-17 - Scene spine validator promoted to an explicit steering gate
h2 2026-04-17 - Template 3D check runner added
h2 2026-04-16 - Scene composition staging surfaced in renderer diagnostics
```

### apps/3D/src-kain/stdlib/three_d_runtime/viewport_runtime.kn
```
fn default_viewport_runtime_profile() -> ViewportRuntimeProfile with Pure:
let profile = ViewportRuntimeProfile()
```

### apps/3D/tools/validation/validate_scene_spine.py
```
def load_json(path: Path)
def main() → int
```

### apps/3D/tools/validation/run_template_checks.py
```
def load_toml_like_text(path: Path) → str
def require_token(text: str, token: str, label: str, problems: list[str]) → None
def run_check(command: list[str]) → None
def main() → int
```

## crates

### crates/kain-ui-native/src/no_egui_qt_host.rs
```
pub enum QtQuickHostLaunchError
impl QtQuickHostLaunchError
impl QtQuickHostLaunchError
pub fn launch_qt_quick_host(bundle: &UiRuntimeBundle, backend_plan: &KainUiNativeBackendPlan,) → Result<(), QtQuickHostLaunc...
```

### crates/cli/src/packager/build.rs
```
pub fn build_project(target_overrides: Option<Vec<String>>) → KainResult<()>
pub fn build_rust_project() → KainResult<()>
```

### crates/cli/src/lib.rs
```
pub enum LauncherKind
impl LauncherKind
  pub fn display_name(self) → &'static str
  pub fn prefers_interpret_default(self) → bool
pub fn detect_launcher_from_path(path: Option<&std::path::Path>) → LauncherKind
pub fn should_show_launcher_menu(launcher: LauncherKind, has_command: bool, has_input: bool,) → bool
pub fn resolve_legacy_target_alias(launcher: LauncherKind, requested_target: &str, has_output: bool,) → String
pub fn render_launcher_menu(launcher: LauncherKind) → Option<String>
pub fn parse_compile_target(alias: &str) → Option<CompileTarget>
pub fn target_extension(target: CompileTarget) → &'static str
pub fn supported_targets_csv() → String
pub fn compile(source: &str, target: CompileTarget) → Result<String, KainError>
pub fn format_source(source: &str) → Result<String, KainError>
pub fn compile_runtime_contract_bundle(source: &str, target: CompileTarget,) → Result<RuntimeContractBundl...
pub fn compile_realtime_app_bundle(source: &str, target: CompileTarget, root_component: Option<&str>,) → Result<kain_driver::Realtim...
pub fn compile_spirv_binary(source: &str) → Result<Vec<u8>, KainError>
pub fn compile_wasm_binary(source: &str) → Result<Vec<u8>, KainError>
pub fn compile_hybrid_artifacts(source: &str,) → Result<kain_driver::HybridA...
pub fn compile_shader_artifact_bundle(source: &str,) → Result<kain_driver::ShaderA...
pub fn compile_ue5(source: &str, output_name: Option<&str>, copyright: Option<&str>,) → Result<ue5::Ue5Output, Kain...
pub fn compile_ue5_with_context(source: &str, output_name: Option<&str>, copyright: Option<&str>, metadata_dir: Option<std::path::PathBuf>,) → Result<ue5::Ue5Output, Kain...
pub fn generate_usf_header(source: &str, shader_name: &str) → Result<String, KainError>
pub fn generate_usf_implementation(source: &str, shader_name: &str, plugin_name: &str,) → Result<String, KainError>
pub fn compile_ue5editor(source: &str, plugin_name: &str, copyright: Option<&str>,) → Result<ue5_editor::Ue5Edito...
```

### crates/kain-sys-codegen/src/codegen_llvm/mod.rs
```
impl LlvmGenerator
pub fn generate(program: &TypedProgram) → KainResult<Vec<u8>>
```

### crates/cli/src/llvm_native_stage.rs
```
pub struct LlvmNativeArtifactStage
pub fn stage_llvm_native_artifacts(source: &str, output_path: &Path, root_component: Option<&str>,) → Result<LlvmNativeArtifactSt...
pub fn stage_native_backend_artifacts(source: &str, target: CompileTarget, output_path: &Path, root_component: Option<&str>,) → Result<LlvmNativeArtifactSt...
pub fn stage_gpu_runtime_dll(executable_path: &Path) → Result<Option<PathBuf>, Str...
pub fn runtime_contract_artifact_path(output_path: &Path) → PathBuf
pub fn realtime_app_artifact_path(output_path: &Path) → PathBuf
pub fn shader_bundle_artifact_path(output_path: &Path) → PathBuf
```

### crates/cli/src/selfhost.rs
```
pub struct Root
pub enum SelfHostCommand
impl SelfHostRustSourceKind
pub fn run(command: SelfHostCommand) → KainResult<()>
```

### crates/cli/src/selfhost_bootstrap.rs
```
impl BootstrapMode
impl BootstrapBackend
pub fn run_bootstrap(manifest_path: Option<PathBuf>, backend: &str, combine_only: bool, emit_llvm_only: bool, link_native: bool, verify_ouroboros: bool,) → KainResult<()>
```

### crates/kain-3D/src/authoring.rs
```
pub struct GeometryAttribute
pub struct Geometry
pub struct Spline
pub struct Instancer
pub struct Brush
pub struct NodeId
pub struct ToolContext
pub struct MeshNode
pub struct Node
pub struct Scene
pub enum AttributeDomain
pub enum AttributeValues
pub enum GeometryTopology
pub enum GeometryError
pub enum Field
pub enum Modifier
pub enum Effector
pub enum SplineType
pub enum Volume
pub enum InstancePattern
pub enum BrushFalloff
pub enum Light
pub enum NodeKind
pub enum SceneBuildError
impl AttributeValues
```

### crates/cli/src/selfhost_report.rs
```
pub struct MacroFinding
pub struct TraitDynSummary
pub struct CratePhase1Result
pub struct InventoryInputEvidence
pub struct Stage2WorkspaceCrateEvidence
pub struct SelfHostPhase1Report
pub struct SelfHostBootstrapArtifacts
pub struct SelfHostBootstrapStepReport
pub struct SelfHostBootstrapReport
pub enum SelfHostPhaseStatus
pub fn render_phase_markdown(title: &str, report: &SelfHostPhase1Report) → String
pub fn render_phase1_markdown(report: &SelfHostPhase1Report) → String
pub fn render_bootstrap_markdown(report: &SelfHostBootstrapReport) → String
```

### crates/kain-3D/src/primitive.rs
```
pub struct PrimitiveDefinition
pub struct PrimitiveLibrary
pub enum PrimitiveShape
impl PrimitiveShape
  pub fn build_geometry(&self) → Geometry
  pub fn build_mesh(&self) → Result<Mesh, GeometryError>
impl PrimitiveDefinition
  pub fn authored(id: impl Into<String>, display_name: impl Into<String>, shape: PrimitiveShape, subdivision_ready: bool, authored_intent: impl Into<String>,) → Self
  pub fn build_geometry(&self) → Geometry
impl PrimitiveLibrary
  pub fn authored_defaults() → Self
  pub fn definition(&self, id: &str) → Option<&PrimitiveDefinition>
  pub fn summary(&self) → String
```

### crates/kain-c-ffi/src/lib.rs
```
pub fn register()
pub fn import_libraries_for_source(source: &str, options: &ImportCOptions, prepare: &PrepareContext,) → Result<Vec<ImportCOutput>, ...
pub fn load_prebuilt_bridge(dylib_path: &Path) → Result<(), KainError>
pub fn shared_library_env_var(import_name: &str) → String
pub fn load_packaged_bridges_from_manifest(manifest_path: &Path) → Result<usize, KainError>
pub fn import_library(import_name: &str, options: &ImportCOptions, prepare: &PrepareContext,) → Result<ImportCOutput, KainE...
pub fn augment_source_for_runtime(source: &str, target: CompileTarget, prepare: &PrepareContext,) → Result<String, KainError>
pub fn detect_c_library_imports(source: &str) → Vec<String>
```

### crates/kain-core/src/lib.rs
```
pub enum CompileTarget
impl CompileTarget
  pub fn from_str(s: &str) → Option<Self>
pub fn compile(source: &str, target: CompileTarget) → Result<String, KainError>
```

### crates/kain-core/src/low_level_memory.rs
```
pub struct BackendMemoryCapabilities
impl LayoutRegistry
impl LayoutRegistry
pub fn backend_memory_capabilities(target: CompileTarget) → BackendMemoryCapabilities
pub fn validate_typed_program_memory_support(program: &TypedProgram, target: CompileTarget,) → KainResult<()>
pub fn lower_typed_program_memory_for_target(program: &TypedProgram, target: CompileTarget,) → KainResult<TypedProgram>
pub fn lower_monomorphized_program_memory_for_target(program: &MonomorphizedProgram, target: CompileTarget,) → KainResult<MonomorphizedPro...
pub fn format_ptr_type(ty: &Type, mutable: bool) → String
```

### crates/kain-core/src/realtime_app_bundle.rs
```
pub struct RealtimeAppBundle
pub struct RealtimeUiContractsBundle
pub struct RealtimeUiStructureNode
pub struct RenderSceneBundle
pub struct RealtimeSceneBinding
pub struct RealtimeViewportCameraBinding
pub struct RealtimeViewportPresentationBinding
pub struct RealtimeViewportGizmoBinding
pub struct CompiledMaterialDefinition
pub struct CompiledMaterialInstance
pub struct RealtimeShaderCanvasBinding
pub struct RealtimeShaderCanvasFontAtlas
pub struct RealtimeShaderCanvasTextRun
pub struct RealtimeShaderCanvasResourceBinding
pub struct RealtimeShaderBundleRef
pub struct RealtimeResourceBinding
pub struct RealtimeTensorBinding
pub struct RealtimeStreamBinding
pub struct RealtimeNeuralNode
pub struct RealtimeAssetBinding
pub struct RealtimePatchBinding
pub struct RealtimeLawBinding
pub struct RealtimeConvergeLaneBinding
pub struct RealtimeConvergeBinding
pub struct RealtimeWorldStateBinding
```

### crates/kain-core/src/runtime.rs
```
pub struct ActorRef
pub struct Message
pub struct PatchMutationRecord
pub struct PatchCollaborationEvent
pub struct PatchRuntimeRecord
pub struct Env
pub enum Value
impl Value
impl Value
impl Value
  pub fn host_object(label: impl Into<String>, object: Arc<dyn Any + Send + Sync>) → Self
  pub fn downcast_host_object(&self) → Option<Arc<T>>
  pub fn host_object_label(&self) → Option<&str>
impl Env
  pub fn new() → Self
  pub fn set_extension_state(&mut self, key: impl Into<String>, state: Arc<dyn Any + Send + Sync>,)
  pub fn get_extension_state(&self, key: &str) → Option<Arc<T>>
  pub fn register_kos_bridge(&mut self)
impl DeterministicValueSynthesizer
pub fn register_env_extension(name: impl Into<String>, registrar: EnvExtensionRegistrar)
pub fn interpret(program: &TypedProgram) → KainResult<Value>
pub fn interpret_with_env(env: &mut Env, program: &TypedProgram) → KainResult<Value>
pub fn eval_block(env: &mut Env, block: &Block) → KainResult<Value>
pub fn eval_expr(env: &mut Env, expr: &Expr) → KainResult<Value>
pub fn run_tests(program: &TypedProgram) → KainResult<()>
```

### crates/kain-core/src/stdlib.rs
```
pub struct StdLib
pub struct BuiltinFn
impl StdLib
  pub fn new() → Self
impl StdLib
pub fn register_stdlib_extension(name: impl Into<String>, registrar: StdlibExtensionRegistrar)
pub fn find_stdlib_search_roots() → Vec<std::path::PathBuf>
pub fn load_stdlib() → String
pub fn load_stdlib_for_target(target: CompileTarget) → String
```

### crates/kain-core/src/runtime_contract.rs
```
pub struct RuntimeContractBundle
pub struct RuntimeCapability
pub struct RuntimeServiceBinding
pub struct RuntimePlatformAvailabilityMetadata
pub struct RuntimeVersionRecord
pub struct RuntimeLifecyclePolicy
pub struct RuntimeCompatibilityMetadata
pub struct RuntimeContractItem
pub struct RuntimePatchContract
pub struct RuntimeLawContract
pub struct RuntimeConvergeLaneContract
pub struct RuntimeConvergeContract
pub struct RuntimeWorldStateContract
pub struct RuntimeWorldSurfaceContract
pub struct RuntimeWorldContract
pub struct RuntimeOrchestrationStageContract
pub struct RuntimeOrchestrationContract
pub struct RuntimeReflectionSummary
pub struct RuntimeReflectionPayload
pub struct ReflectedType
pub struct ReflectedField
pub struct ReflectedItem
pub struct ReflectedActor
pub struct ReflectedComponent
pub struct ReflectedMessage
```

### crates/kain-core/src/types.rs
```
pub struct TypedProgram
pub struct TypedComptime
pub struct TypedConst
pub struct TypedUse
pub struct TypedMod
pub struct TypedTest
pub struct TypedTypeAlias
pub struct TypedImpl
pub struct TypedMacro
pub struct TypedActor
pub struct TypedFunction
pub struct TypedPatch
pub struct TypedLaw
pub struct TypedConverge
pub struct TypedWorld
pub struct OrchestrateStageDescriptor
pub struct TypedOrchestrate
pub struct TypedComponent
pub struct TypedShader
pub struct TypedStruct
pub struct TypedEnum
pub struct TypedTrait
pub struct TypeEnv
pub enum TypedItem
pub enum PatchUndoMode
```

### crates/kain-driver/src/lib.rs
```
pub struct DriverSession
pub struct ShaderArtifactBundleOutput
pub struct RealtimeAppBundleOutput
pub struct HybridArtifactOutput
pub struct CheckedFrontend
pub struct RustBundleOutput
impl DriverSession
  pub fn new() → Self
  pub fn with_ue5_metadata_dir(mut self, path: impl Into<PathBuf>) → Self
  pub fn set_ue5_metadata_dir(&mut self, path: impl Into<PathBuf>)
  pub fn frontend_to_checked_program(&self, source: &str, target: CompileTarget,) → Result<CheckedFrontend, Kai...
  pub fn frontend_to_checked_program_with_extra_globals(&self, source: &str, target: CompileTarget, extra_globals: I,) → Result<CheckedFrontend, Kai...
  pub fn frontend_to_monomorphized_program(&self, source: &str, target: CompileTarget,) → Result<MonomorphizedProgram...
  pub fn frontend_to_typed_program(&self, source: &str, target: CompileTarget,) → Result<TypedProgram, KainEr...
  pub fn frontend_to_typed_program_with_extra_globals(&self, source: &str, target: CompileTarget, extra_globals: I,) → Result<TypedProgram, KainEr...
impl WorldSelectionInfo
pub fn parse_compile_target(alias: &str) → Option<CompileTarget>
pub fn target_extension(target: CompileTarget) → &'static str
pub fn supported_targets_csv() → String
pub fn frontend_to_monomorphized_program(source: &str, target: CompileTarget,) → Result<MonomorphizedProgram...
pub fn frontend_to_checked_program(source: &str, target: CompileTarget,) → Result<CheckedFrontend, Kai...
pub fn frontend_to_typed_program(source: &str, target: CompileTarget,) → Result<TypedProgram, KainEr...
pub fn compile(source: &str, target: CompileTarget) → Result<String, KainError>
pub fn compile_spirv_binary(source: &str) → Result<Vec<u8>, KainError>
pub fn compile_wasm_binary(source: &str) → Result<Vec<u8>, KainError>
```

### crates/kain-sys-codegen/src/codegen_rust/mod.rs
```
impl StringBuilder
impl RustGen
pub fn generate(program: &TypedProgram) → KainResult<String>
pub fn generate_gpu_host_wrappers(program: &TypedProgram) → KainResult<String>
pub fn gen_cargo_toml(name: &str, deps: &[&str]) → String
```

### crates/kain-3D/src/renderer.rs
```
pub struct RenderResolution
pub struct SoftwareRendererConfig
pub struct RenderViewSettings
pub struct FrameDiagnostics
pub struct RenderStats
pub struct RenderFrame
pub struct SoftwareRenderer
pub enum FrameCameraSource
pub enum RenderError
pub trait RenderBackend
impl RenderResolution
  pub fn new(width: usize, height: usize) → Self
impl SoftwareRendererConfig
impl RenderError
impl RenderError
impl SoftwareRenderer
impl SoftwareRenderer
  pub fn render_catalog_scene(&mut self, catalog: &SceneCatalog, scene_name: &str, time_seconds: f32, resolution: RenderResolution,) → Result<RenderFrame, RenderE...
  pub fn render_catalog_scene_with_view(&mut self, catalog: &SceneCatalog, scene_name: &str, time_seconds: f32, resolution: RenderResolution, view: &RenderViewSettings,) → Result<RenderFrame, RenderE...
  pub fn render_scene(&mut self, scene: &SceneDescription, time_seconds: f32, resolution: RenderResolution,) → Result<RenderFrame, RenderE...
  pub fn render_scene_with_view(&mut self, scene: &SceneDescription, time_seconds: f32, resolution: RenderResolution, view: &RenderViewSettings,) → Result<RenderFrame, RenderE...
impl SoftwareRenderer
```

### crates/kain-3D/src/scene.rs
```
pub struct Camera
pub struct CameraPose
pub struct SceneBounds
pub struct SceneCompositionSummary
pub struct SceneCompositionDiagnostics
pub struct Material
pub struct DirectionalLight
pub struct PointLight
pub struct LightingRig
pub struct BackgroundGradient
pub struct Vertex
pub struct Mesh
pub struct SceneInstance
pub struct ParticleEmitter
pub struct BlackHole
pub struct TerrainSurface
pub struct SceneDescription
pub struct SceneCatalog
pub struct SceneCatalogSummary
pub struct SceneCatalogEntry
pub struct SceneResolution
pub struct ResolvedScene
pub enum SceneAnimation
pub enum SceneResolutionKind
impl SceneBounds
```

### crates/kain-3D/src/wgpu_renderer.rs
```
pub struct WgpuRendererInitError
pub struct WgpuRenderer
pub struct GpuVertex
pub struct GizmoVertex
pub struct ParticleVertex
pub struct SceneUniforms
pub struct PreparedWgpuFrame
impl WgpuRendererInitError
impl WgpuRendererInitError
impl WgpuRendererInitError
impl PickVertex
  pub fn layout() → wgpu::VertexBufferLayout<'s...
impl GizmoVertex
  pub fn layout() → wgpu::VertexBufferLayout<'s...
impl ParticleVertex
  pub fn layout() → wgpu::VertexBufferLayout<'s...
impl GpuVertex
  pub fn layout() → wgpu::VertexBufferLayout<'s...
impl WgpuRenderer
  pub fn new() → Result<Self, WgpuRendererIn...
  pub fn new_with_shader_bundle(shader_bundle: ShaderArtifactBundle,) → Result<Self, WgpuRendererIn...
impl WgpuRenderer
pub fn prepare_wgpu_frame(scene: &SceneDescription, time_seconds: f32, resolution: RenderResolution, view: &RenderViewSettings,) → Result<PreparedWgpuFrame, R...
```

### crates/kain-sys-codegen/src/codegen_c.rs
```
impl StringBuilder
impl CGen
pub fn generate(program: &TypedProgram) → KainResult<String>
```

## docs

### docs/crates/ui-gpu-3d.md
```
h1 UI, GPU, And 3D Crates
h2 UI
h2 3D
h2 GPU And Web
h2 What This Family Proves
```

## element

### element/src/element_kernel.kn
```
pub fn make_diagnostic(severity: DiagnosticSeverity, code: String, message: String) -> ElementDiagnostic:
pub fn push_error(diagnostics: Array<ElementDiagnostic>, code: String, message: String) -> Unit:
pub fn push_warning(diagnostics: Array<ElementDiagnostic>, code: String, message: String) -> Unit:
pub fn push_note(diagnostics: Array<ElementDiagnostic>, code: String, message: String) -> Unit:
pub fn append_diagnostics(target: Array<ElementDiagnostic>, incoming: Array<ElementDiagnostic>) -> Unit:
pub fn count_bonds_with_role(node: ElementNode, role: ElementBondRole) -> Int:
let count = 0
pub fn recommended_memory_width_bits(profile: ElementPhysicalProfile) -> Int:
pub fn has_negative_oxidation_state(profile: ElementPhysicalProfile) -> Bool:
pub fn oxidation_state_span(profile: ElementPhysicalProfile) -> Int:
let has_value = false
let minimum_state = 0
let maximum_state = 0
pub fn validate_projection_matches_physical_profile(contract: ElementContract) -> Array<ElementDiagnostic>:
let diagnostics = []
```

### element/memory.md
```
h1 Element Memory
h2 2026-04-17
```

### element/ARCHITECTURE.md
```
h1 Element Architecture
h2 Current Shape
h2 Design Rule
h2 Current Semantics
h2 Audit Rule
h2 Intended Growth Path
h2 Common Errors
```

### element/src/nitrogen.kn
```
pub fn nitrogen_physical_profile() -> ElementPhysicalProfile:
pub fn nitrogen_semantic_projection() -> ElementSemanticProjection:
pub fn nitrogen_contract() -> ElementContract:
pub fn nitrogen_contract_audit() -> Array<ElementDiagnostic>:
pub fn nitrogen_default_node(debug_name: String) -> ElementNode:
pub fn nitrogen_execution_model() -> String:
pub fn nitrogen_lone_pair_model() -> String:
pub fn nitrogen_projection_justification() -> Array<String>:
pub fn nitrogen_validate(node: ElementNode) -> Array<ElementDiagnostic>:
let contract = nitrogen_contract()
let diagnostics = []
pub fn nitrogen_semantic_rules() -> Array<String>:
```

### element/src/oxygen.kn
```
pub fn oxygen_physical_profile() -> ElementPhysicalProfile:
pub fn oxygen_semantic_projection() -> ElementSemanticProjection:
pub fn oxygen_contract() -> ElementContract:
pub fn oxygen_contract_audit() -> Array<ElementDiagnostic>:
pub fn oxygen_default_node(debug_name: String) -> ElementNode:
pub fn oxygen_execution_model() -> String:
pub fn oxygen_lowering_story() -> String:
pub fn oxygen_projection_justification() -> Array<String>:
pub fn oxygen_validate(node: ElementNode) -> Array<ElementDiagnostic>:
let contract = oxygen_contract()
let diagnostics = []
let input_bond_count = count_bonds_with_role(node, ElementBondRole::Input)
pub fn oxygen_semantic_rules() -> Array<String>:
```

## labs

### labs/kain_native_taste_lab/kain-native-taste-lab-native-ui/Cargo.toml
```
table [package]
table [workspace]
table [dependencies]
key name
key version
key edition
```

### labs/threejs_node_ffi_space_lab/src/hybrid_motion.kn
```
let half_cycle = cycle_units / 2
let falling_frame = local_frame - half_cycle
fn beacon_spin_units(frame_units: Int, speed_units: Int, cycle_units: Int) -> Int:
```

### labs/threejs_node_ffi_space_lab/helpers/client/model.ts
```
export type Vector3Tuple
export type ViewportBehavior
export type SceneConfig
export type SculptToolConfig
export type SculptSuiteConfig
export type ViewportModeConfig
export type ViewportProfilesConfig
export type WasmPipelineConfig
export type HybridPipelineConfig
export type AppModel
export function readAppModel(rawValue) → AppModel
```

### labs/threejs_node_ffi_space_lab/helpers/space_lab_runtime.mjs
```
export async function loadAppConfig(projectRoot = ".", manifestPath = defaultManifestPath)
export async function buildApp(projectRoot = ".", manifestPath = defaultManifestPath)
export async function buildWasmOnly(projectRoot = ".", manifestPath = defaultManifestPath)
export async function buildHybridOnly(projectRoot = ".", manifestPath = defaultManifestPath)
export async function printSummary(projectRoot = ".", manifestPath = defaultManifestPath)
export async function serveApp(projectRoot = ".", manifestPath = defaultManifestPath)
function resolveProjectRoot(projectRoot = ".")
function resolveInsideProject(projectRoot, relativePath)
function toPosixPath(filePath)
async function readJsonFile(filePath)
async function ensureDirectory(directoryPath)
async function copyFileIfPresent(sourcePath, destinationPath)
function buildSummary(model)
async function loadRegistries(projectRoot, appConfig)
function createRuntimeModel(projectRoot, appConfig, registries, manifestPath)
async function bundleClient(model)
async function buildHybridBundle(model)
async function runProcess(command, args, options = {})
async function buildWasmBundle(model)
function createHtmlDocument(model)
async function emitRuntimeOutputs(model, clientArtifacts)
function chooseContentType(filePath)
async function readStaticAsset(outputsRoot, requestPath)
async function runFromCli()
```

### labs/threejs_node_ffi_space_lab/ARCHITECTURE.md
```
h1 Three.js Node FFI Sculpt Lab Architecture
h2 What It Is
h2 Ownership Boundaries
h2 Primary Data Flow
h2 Common Commands
h2 Architectural Guardrails
h2 Common Errors
code-fence bash
code-fence plain
```

### labs/threejs_node_ffi_space_lab/outputs/kain/orb_motion.ts
```
export interface GenServerCallOutcomeShape
  reply: R
  state: S
export class GenServerCallOutcome
  constructor(reply, state)
export function gen_server_call_result(reply, state) → GenServerCallOutcome
export function gen_server_call(server, request) → R
export function gen_server_cast(server, request) → void
export function gen_server_info(server, message) → void
export function beacon_spin_units(frame_units, speed_units, cycle_units) → number
```

### labs/threejs_node_ffi_space_lab/outputs/kain/orb_motion.js
```
class GenServerCallOutcome
  constructor(reply, state)
function __readString(ptr)
function __writeString(str)
function __readArray(ptr, elemSize, readElem)
function __wasmAlloc(size)
async function __initWasm()
async function __ensureWasm()
function orb_hover_units(...args)
async function orb_hover_units_async(...args)
function __kain_new_ptr(meta)
function __kain_clone_value(value)
function __kain_addr_of(value)
function __kain_bind_local(value)
function __kain_field_ptr(ptr, field, _offset)
function __kain_index_ptr(ptr, index, _stride)
function __kain_ptr_offset(ptr, offset, stride)
function __kain_mem_load(ptr)
function __kain_mem_store(ptr, value)
function __kain_union_buffer(value, size)
function __kain_union_encode(view, typeKey, byteSize, value)
function __kain_union_decode(typeKey, byteSize, fallback, view)
function __kain_union_wrap(value, active, typeKey, byteSize, unionSize, activeValue)
function __kain_union_get(value, field, typeKey, byteSize, unionSize, fallback)
function __kain_union_set(value, field, typeKey, byteSize, unionSize, next)
```

### labs/threejs_node_ffi_space_lab/outputs/client/three-space.bundle.js
```
function generateUUID()
function clamp(value, min, max) → Vector3
function euclideanModulo(n, m)
function mapLinear(x, a1, a2, b1, b2)
function inverseLerp(x, y, value)
function lerp(x, y, t) → Color
function damp(x, y, lambda, dt)
function pingpong(x, length = 1)
function smoothstep(x, min, max)
function smootherstep(x, min, max)
function randInt(low, high)
function randFloat(low, high)
function randFloatSpread(range)
function seededRandom(s)
function degToRad(degrees)
function radToDeg(radians)
function isPowerOfTwo(value)
function ceilPowerOfTwo(value)
function floorPowerOfTwo(value)
function setQuaternionFromProperEuler(q, a, b, c, order)
function denormalize(value, array)
function normalize(value, array) → Plane
function arrayNeedsUint32(array)
function createElementNS(name)
function createCanvasElement()
```

### labs/threejs_node_ffi_space_lab/outputs/client/three-space.css
```
var --page-background
var --panel-background
var --panel-border
var --panel-shadow
var --text-main
var --text-muted
var --accent-cyan
var --accent-orange
var --accent-blue
.suite-shell
.suite-header
.suite-header
.suite-header
.card-kicker
.header-chip-row
.header-chip
.suite-grid
```

### labs/threejs_node_ffi_space_lab/outputs/index.html
```
title: Kain Universal Sculpt Space Lab
```

## memory

### memory/2026-04-17.md
```
h2 Light Sleep
h2 REM Sleep
h3 Reflections
h3 Possible Lasting Truths
```

### memory/2026-04-16.md
```
h2 Light Sleep
h2 REM Sleep
h3 Reflections
h3 Possible Lasting Truths
```

## ouroboros

### ouroboros/docs/selfhost/ouroboros-v2-selfhost-pipeline.md
```
h1 Ouroboros V2 Selfhost Pipeline
h2 Purpose
h2 Current pipeline entry points
h2 Current pipeline data flow
h3 Rust mirror lane high-level flow
h3 Owned bootstrap lane high-level flow
h2 Existing pipeline inputs
h2 Existing pipeline artifacts
h2 Current gates
h3 Gate A: import gate
h3 Gate B: bundle emission gate
h3 Gate C: round-trip Rust emission gate
h3 Gate D: stage-2 assembly gate
h3 Gate E: stage-2 build gate
h3 Gate F: owned manifest/runtime gate
h3 Gate G: owned compiler gate
h3 Gate H: native self-build gate
h3 Gate I: ouroboros gate
h2 Current blocker classes
h2 Recommended pipeline shape
h3 Lane 1: rust mirror integrity
h3 Lane 2: parser-safe KAIN emission
h3 Lane 3: Rust round-trip validity
h3 Lane 4: owned bootstrap/native
h3 Lane 5: ouroboros parity
```

## src

### src/core/_LEGACYREADME.md
```
h2 Repository Structure
h2 The Motivation: Project Ouroboros
h2 The Origin
h2 What is KAIN?
h3 Example
h3 Install V1 (Production)
h3 Build V2 (Self-Hosting, Experimental)
h1 Build native compiler
h1 Compile a KAIN file
h1 Link with runtime and execute
h3 Prerequisites (V2 only)
h2 Features
h3 Language Features
h3 Compilation Targets
h3 Unreal Engine 5 Integration
h3 Current Limitations
h2 Architecture
h3 Project Structure
h2 Building
h3 Linux/macOS Build System (`build.sh`)
h4 Quick Reference
h1 Full build (bootstrap + native)
h1 Build specific targets
h4 Environment Variables
h4 Architecture
```

### src/.rustimport/phase2/kain-core/ast.kn
```
pub fn as_str(_self: &WorldSurfaceKind) -> String:
pub fn as_str(_self: &OrchestrateStageRuntime) -> String:
pub fn default_() -> ComputeMetadata:
fn fmt(_self: &ComputeMetadataError, f: &mut std::fmt::Formatter<_>) -> Result<(), Error>:
fn __selfhost_empty_impl__():
let __selfhost_empty = none
pub fn explicit_compute_metadata(_self: &Shader) -> Result<Option<ComputeMetadata>, ComputeMetadataError>:
fn extract_compute_metadata_from_block(block: &Block) -> Result<Option<ComputeMetadata>, ComputeMetadataError>:
fn extract_compute_metadata_from_comptime_block(block: &Block) -> Result<Option<ComputeMetadata>, ComputeMetadataError>:
fn is_compute_plan_binding(name: String) -> Bool:
fn parse_compute_metadata_expr(expr: &Expr) -> Result<ComputeMetadata, ComputeMetadataError>:
let tuple_items = match expr:
let dispatch_size = parse_dispatch_size_expr(dispatch_expr)?
let tensor_plans = parse_tensor_plan_list_expr(tensor_expr)?
let stream_plans = match stream_expr:
```

### src/.rustimport/phase2/kain-core/comptime.kn
```
pub fn eval_program(program: &mut Program) -> Result<(), KainError>:
pub fn eval_program(_program: &mut Program) -> Result<(), KainError>:
fn eval_item(env: &mut Env, item: &mut Item) -> Result<(), KainError>:
fn eval_block(env: &mut Env, block: &mut Block) -> Result<(), KainError>:
fn eval_stmt(env: &mut Env, stmt: &mut Stmt) -> Result<(), KainError>:
fn eval_expr_in_place(env: &mut Env, expr: &mut Expr) -> Result<(), KainError>:
let __selfhost_empty = none
let __selfhost_empty = none
let __selfhost_empty = none
fn eval_jsx(env: &mut Env, node: &mut JSXNode) -> Result<(), KainError>:
fn value_to_expr(val: Value, span: Span) -> Expr:
```

### src/.rustimport/phase2/kain-core/diagnostic_registry.kn
```
pub fn spec_for_code(code: DiagnosticCode) -> &static DiagnosticSpec:
pub fn default_code_for_kind(kind: ErrorKind) -> DiagnosticCode:
```

### src/.rustimport/phase2/kain-core/diagnostics.kn
```
pub fn new_(file: String, line_: usize, col: usize) -> SourceLocation<a>:
pub fn new_(source: String) -> SpanMapper:
pub fn span_to_location<a>(_self: &SpanMapper, span: Span, file: String) -> SourceLocation<a>:
let start = span.start.min((*_self).source.len())
let line_idx = match (*_self).line_starts.binary_search(&start):
let line_start = (*_self).line_starts[line_idx]
let col = start.saturating_sub(line_start)
pub fn source(_self: &SpanMapper) -> String:
pub fn new_(source: String, filename: String) -> Diagnostics<a>:
pub fn format_error(_self: &Diagnostics<a>, error: &KainError) -> String:
fn format_with_context(_self: &Diagnostics<a>, error_type: String, message: String, span: Span) -> String:
let pointer_offset = col.saturating_sub(1)
let content_len = line_content.len()
let remaining_len = content_len.saturating_sub(pointer_offset)
let span_len = span.end.saturating_sub(span.start)
```

### src/.rustimport/phase2/kain-core/effects.kn
```
pub fn from_str(s: String) -> Option<Effect>:
fn effect_name(effect: Effect) -> String:
fn effect_set_to_string(effect_set: &EffectSet) -> String:
pub fn default_() -> EffectSet:
pub fn new_() -> EffectSet:
pub fn pure() -> EffectSet:
pub fn with_(_self: EffectSet, e: Effect) -> EffectSet:
pub fn is_pure(_self: &EffectSet) -> Bool:
pub fn can_call(_self: &EffectSet, callee: &EffectSet) -> Bool:
pub fn check_effect_call(caller: &EffectSet, callee: &EffectSet, caller_name: String, callee_name: String, _span: Span) 
let caller_effect_str = effect_set_to_string(caller)
let callee_effect_str = effect_set_to_string(callee)
```

### src/.rustimport/phase2/kain-core/error.kn
```
fn fmt(_self: &ErrorKind, f: &mut std::fmt::Formatter<_>) -> Result<(), Error>:
fn format_enhanced_error(kind: &ErrorKind, code: &DiagnosticCode, file: &Option<std::path::PathBuf>, location: &Option<(
let spec = diagnostic_registry__spec_for_code((*code))
fn format_multi_errors(errors: &[KainError]) -> String:
pub fn lexer(message: impl Into<String>, span: Span) -> KainError:
pub fn parser(message: impl Into<String>, span: Span) -> KainError:
pub fn type_error(message: impl Into<String>, span: Span) -> KainError:
pub fn effect_error(message: impl Into<String>, span: Span) -> KainError:
pub fn borrow_error(message: impl Into<String>, span: Span) -> KainError:
pub fn codegen(message: impl Into<String>, span: Span) -> KainError:
pub fn codegen_with_location(message: impl Into<String>, file: impl Into<String>, line_: usize, col: usize, span: Span) 
pub fn runtime(message: impl Into<String>) -> KainError:
pub fn multi(errors: Array<KainError>) -> KainError:
pub fn parse_error(message: impl Into<String>) -> KainError:
pub fn type_err(message: impl Into<String>) -> KainError:
```

### src/.rustimport/phase2/kain-core/language_features.kn
```
fn default_() -> LanguageCapabilities:
pub fn is_enabled(_self: &LanguageCapabilities, capability: LanguageCapability) -> Bool:
pub fn with_override(_self: LanguageCapabilities, capability: LanguageCapability, enabled: Bool) -> LanguageCapabilities
pub fn supports_parser_struct_literals(_self: &LanguageCapabilities) -> Bool:
pub fn supports_parser_binary_op(_self: &LanguageCapabilities, op: BinaryOp) -> Bool:
pub fn supports_runtime_binary_op(_self: &LanguageCapabilities, op: BinaryOp) -> Bool:
pub fn default_language_capabilities() -> LanguageCapabilities:
pub fn parser_supports_binary_op(op: BinaryOp) -> Bool:
pub fn runtime_supports_binary_op(op: BinaryOp) -> Bool:
fn parser_capability_for_binary_op(op: BinaryOp) -> Option<LanguageCapability>:
fn runtime_capability_for_binary_op(op: BinaryOp) -> Option<LanguageCapability>:
```

### src/.rustimport/phase2/kain-core/lexer.kn
```
pub fn new_(kind: TokenKind, span: Span) -> Token:
pub fn new_(source: String) -> Lexer<a>:
pub fn tokenize(_self: &Lexer<a>) -> Result<Array<Token>, KainError>:
fn process_indentation(_self: &Lexer<a>, raw: Array<Token>) -> Result<Array<Token>, KainError>:
let current = (*indent_stack.last().unwrap())
let final_span = result.last().map(fn(t): t.span).unwrap_or(Span__new_(0, 0))
fn unescape(s: String) -> String:
```

### src/.rustimport/phase2/kain-core/low_level_abi.kn
```
pub fn as_str(_self: CCompilerFlavor) -> String:
fn policy_entry(key: String, kind: CAbiKind, flavor: CCompilerFlavor, long_bits: usize) -> CAbiPolicyEntry:
pub fn c_compiler_flavor_from_str(value: String) -> Option<CCompilerFlavor>:
pub fn selected_c_compiler_flavor() -> CCompilerFlavor:
pub fn c_abi_kind_for_target(target: CompileTarget) -> CAbiKind:
pub fn default_c_abi_policy() -> &static CAbiPolicy:
pub fn c_abi_policy_for_kind_and_flavor(kind: CAbiKind, flavor: CCompilerFlavor) -> &static CAbiPolicy:
pub fn c_abi_policy_for_target_and_flavor(target: CompileTarget, flavor: CCompilerFlavor) -> &static CAbiPolicy:
pub fn c_abi_policy_for_target(target: CompileTarget) -> &static CAbiPolicy:
pub fn promoted_integer_bits(width: usize, _signed: Bool, abi: &CAbiPolicy) -> usize:
pub fn arithmetic_domain_for_type(ty: &Type, abi: &CAbiPolicy) -> Option<ArithmeticDomain>:
pub fn promoted_type_for_arithmetic(ty: &Type, abi: &CAbiPolicy) -> Option<Type>:
let domain = arithmetic_domain_for_type(ty, abi)?
pub fn usual_arithmetic_conversion_type(left: &Type, right: &Type, abi: &CAbiPolicy) -> Option<Type>:
let lhs = arithmetic_domain_for_type(left, abi)?
```

### src/.rustimport/phase2/kain-core/low_level_memory.kn
```
pub fn default_() -> StructLayoutInfo:
fn build_with_abi(items: &[TypedItem], abi: &static CAbiPolicy) -> LayoutRegistry:
fn collect_struct_layouts(items: &[TypedItem], registry: &mut LayoutRegistry):
let is_union = low_level_memory_metadata__has_attr(&st.ast.attributes, low_level_memory_metadata__C_UNION_ATTR)
let is_packed = low_level_memory_metadata__has_attr(&st.ast.attributes, low_level_memory_metadata__C_PACKED_ATTR)
let pack_align = match low_level_memory_metadata__attr_usize_arg(&st.ast.attributes, low_level_memory_metadata__C_PACK_A
let align = bits.div_ceil(8)
let explicit_type_align = match low_level_memory_metadata__attr_usize_arg(&st.ast.attributes, low_level_memory_metadata_
let align = bits.div_ceil(8)
let size = low_level_memory_metadata__attr_usize_arg(&field.attributes, low_level_memory_metadata__C_STORAGE_BITS_ATTR).
let natural_align = low_level_memory_metadata__attr_usize_arg(&field.attributes, low_level_memory_metadata__C_STORAGE_AL
let align = match pack_align:
let field_name = field.name.clone()
let width = width.max(1)
let unit_size = size.max(1)
```

### src/.rustimport/phase2/kain-core/low_level_memory_metadata.kn
```
pub fn marker_attr(name: String, span: Span) -> Attribute:
pub fn usize_attr(name: String, value: usize, span: Span) -> Attribute:
pub fn usize_bool_attr(name: String, value: usize, flag: Bool, span: Span) -> Attribute:
pub fn has_attr(attrs: &[Attribute], name: String) -> Bool:
pub fn attr_usize_arg(attrs: &[Attribute], name: String) -> Option<usize>:
pub fn attr_usize_bool_args(attrs: &[Attribute], name: String) -> Option<(usize, Bool)>:
```

### src/.rustimport/phase2/kain-core/monomorphize.kn
```
pub fn monomorphize(program: &TypedProgram) -> Result<MonomorphizedProgram, KainError>:
let type_name = match &imp.ast.target_type:
let target_ty = resolve_ast_type(&imp.ast.target_type).unwrap_or(ResolvedType__Unknown)
let type_name_str = type_to_string(&target_ty)
let mangled_name = f"{type_name}_{method.name}"
let ret = method.return_type.map(fn(t): resolve_ast_type(t).unwrap_or(ResolvedType__Unknown)).unwrap_or(ResolvedType__Un
let method_ty = ResolvedType::Function { params: params, ret: Box__new_(ret), effects: EffectSet__new_() }
let typed_method = TypedFunction { ast: standalone_fn, resolved_type: method_ty, effects: EffectSet__new_() }
let item = ctx.concrete_items[i].clone()
let entry_fn = lower_async_fn(&mut ctx, &func)?
let new_func = scan_function(&mut ctx, &func)?
fn new_() -> MonoContext:
fn instantiate(_self: &mut MonoContext, name: String, type_args: &[ResolvedType]) -> Result<String, KainError>:
let mangled_name = f"{name}_{mangle_types(type_args)}"
let generic_func = ((*_self).generic_functions.get(name).ok_or_else(fn(): KainError__type_error(f"Generic function {name
```

### src/.rustimport/phase2/kain-core/parser.kn
```
fn parse_orchestrate_stage_runtime(name: String) -> Option<OrchestrateStageRuntime>:
pub fn new_(tokens: &a [Token], span_mapper: &a SpanMapper, filename: String) -> Parser<a>:
pub fn with_capabilities(tokens: &a [Token], span_mapper: &a SpanMapper, filename: String, capabilities: LanguageCapabil
fn parser_error(_self: &Parser<a>, message: impl Into<String>, span: Span) -> KainError:
let loc = (*_self).span_mapper.span_to_location(span, (*_self).filename)
let formatted_message = f"{loc.file}:{loc.line_}:{loc.col}: {message.into()}"
fn token_to_user_string(_self: &Parser<a>, kind: &TokenKind) -> String:
fn expected_tokens_list(_self: &Parser<a>, expected: &[String]) -> String:
let last = expected.last().unwrap()
let rest = &expected[..(expected.len() - 1)]
fn validate_identifier(_self: &Parser<a>, name: String, span: Span) -> Result<(), KainError>:
pub fn parse(_self: &mut Parser<a>) -> Result<Program, KainError>:
let start = (*_self).current_span()
let before_pos = (*_self).pos
let before_pos = (*_self).pos
```

### src/.rustimport/phase2/kain-core/runtime.kn
```
fn lowered_impl_function_names(type_name: String, method_name: String) -> [String; 2]:
fn module_scoped_name(module_path: &[String], item_name: String) -> String:
fn selfhost_enum_variant_alias_name(enum_name: String, variant_name: String) -> String:
pub fn register_env_extension(name: impl Into<String>, registrar: EnvExtensionRegistrar):
fn fmt(_self: &Value, f: &mut std::fmt::Formatter<_>) -> Result<(), Error>:
fn fmt(_self: &Value, f: &mut std::fmt::Formatter<_>) -> Result<(), Error>:
let arr = arr.read().unwrap()
let fields = fields.read().unwrap()
pub fn host_object(label: impl Into<String>, object: std::sync::Arc<impl Any>) -> Value:
pub fn downcast_host_object<T>(_self: &Value) -> Option<std::sync::Arc<T>>:
pub fn host_object_label(_self: &Value) -> Option<String>:
pub fn new_() -> Env:
fn apply_registered_extensions(_self: &mut Env):
let registrars = ENV_EXTENSION_REGISTRARS.read().unwrap().values().copied().collect()
pub fn set_extension_state(_self: &mut Env, key: impl Into<String>, state_: std::sync::Arc<impl Any>):
```

### src/.rustimport/phase2/kain-core/shader_analysis.kn
```
pub fn generate_report(_self: &ShaderComplexity, shader_name: String) -> String:
pub fn analyze_shader(_program: &TypedProgram) -> Array<(String, ShaderComplexity)>:
```

### src/.rustimport/phase2/kain-core/span.kn
```
pub fn default_() -> Span:
pub fn new_(start: usize, end: usize) -> Span:
pub fn merge(_self: Span, other: Span) -> Span:
pub fn to_range(_self: Span) -> std::ops::Range<usize>:
fn from(range: std::ops::Range<usize>) -> Span:
fn from(span: Span) -> std::ops::Range<usize>:
pub fn new_(value: T, span: Span) -> Spanned<T>:
pub fn map<U, F>(_self: Spanned<T>, f: F) -> Spanned<U>:
```

### src/.rustimport/phase2/kain-core/stdlib.kn
```
pub fn register_stdlib_extension(name: impl Into<String>, registrar: StdlibExtensionRegistrar):
pub fn new_() -> StdLib:
let registrars = STDLIB_EXTENSION_REGISTRARS.read().unwrap().values().copied().collect()
fn add_fn(_self: &mut StdLib, name: String, params: &[(String, String)], ret: String, doc: String):
fn default_() -> StdLib:
pub fn find_stdlib_search_roots() -> Array<path::PathBuf>:
let path = PathBuf__from(env_path)
let stdlib_dir = current.join("stdlib")
let stdlib_dir = current.join("stdlib")
pub fn load_kn_files_from_dir(path: &path::Path) -> Option<String>:
let entries = match std__fs__read_dir(path):
let entry = match entry:
let path = entry.path()
let filename_str = filename.to_string_lossy()
let concatenated = kn_files.into_iter().map(fn(__kain_closure_arg0): __kain_closure_arg0.__kain_tuple_1).collect().join(
```

### src/.rustimport/phase2/kain-core/stdlib_tests.kn
```
fn new_(name: String) -> TempTestDir:
let temp_dir = std__env__temp_dir().join(f"kain_test_{name}")
let _ = std__fs__remove_dir_all(&temp_dir)
fn create_file(_self: &TempTestDir, relative_path: String, content: String):
let file_path = (*_self).path.join(relative_path)
fn create_dir(_self: &TempTestDir, relative_path: String):
let dir_path = (*_self).path.join(relative_path)
fn path(_self: &TempTestDir) -> &std::path::PathBuf:
fn drop(_self: &mut TempTestDir):
let _ = std__fs__remove_dir_all(&(*_self).path)
fn test_find_stdlib_with_env_var_valid():
let test_dir = TempTestDir__new_("env_var_valid")
let roots = stdlib__find_stdlib_search_roots()
fn test_find_stdlib_with_env_var_invalid():
let invalid_path = std__env__temp_dir().join("kain_nonexistent_stdlib_12345")
```

### src/.rustimport/phase2/kain-core/types.kn
```
pub fn new_(span_mapper: &a SpanMapper, filename: String) -> TypeEnv<a>:
pub fn push_scope(_self: &mut TypeEnv<a>):
pub fn pop_scope(_self: &mut TypeEnv<a>):
pub fn define(_self: &mut TypeEnv<a>, name: String, ty: ResolvedType):
pub fn define_global(_self: &mut TypeEnv<a>, name: String, ty: ResolvedType):
pub fn define_method(_self: &mut TypeEnv<a>, type_name: String, method_name: String, ty: ResolvedType):
pub fn lookup(_self: &TypeEnv<a>, name: String) -> Option<&ResolvedType>:
pub fn lookup_type(_self: &TypeEnv<a>, name: String) -> Option<&ResolvedType>:
pub fn lookup_method(_self: &TypeEnv<a>, type_name: String, method_name: String) -> Option<&ResolvedType>:
pub fn lookup_enum_variant_fields(_self: &TypeEnv<a>, enum_name: String, variant_name: String) -> Option<&Array<Resolved
pub fn lookup_enum_variant_named_field(_self: &TypeEnv<a>, enum_name: String, variant_name: String, field_name: String) 
fn type_error(_self: &TypeEnv<a>, message: impl Into<String>, span: Span) -> KainError:
let loc = (*_self).span_mapper.span_to_location(span, (*_self).filename)
let formatted_message = f"{loc.file}:{loc.line_}:{loc.col}: {message.into()}"
fn selfhost_map_type() -> ResolvedType:
```

### src/.rustimport/phase2/kain-core/ui.kn
```
pub fn ui_backend_profile(kind: UIBackendKind) -> &static UIBackendProfile:
fn fmt(_self: &VNode, f: &mut std::fmt::Formatter<_>) -> Result<(), Error>:
pub fn render_to_string(node: &VNode) -> String:
let attrs = attrs.iter().map(render_attr_to_string).filter(fn(part): (!part.is_empty())).collect().join(" ")
let attr_suffix = if attrs.is_empty():
let children = children.iter().map(render_to_string).collect().join("")
pub fn reconcile(current: Option<&VNode>, next: &VNode) -> VNode:
pub fn eval_jsx(env: &mut Env, node: &JSXNode) -> Result<Value, KainError>:
let attrs = eval_attrs(env, attributes)?
let children = eval_children(env, children)?
let key = find_attr_key(&attrs, "key")
let cond = runtime__eval_expr(env, condition)?
let iter_value = runtime__eval_expr(env, iter)?
let items = match iter_value:
let rendered = eval_jsx(env, body)?
```

### src/.rustimport/phase2/kain-import/c/mod.kn
```
pub fn default_() -> CImportOptions:
pub fn import_c_file(path: &std::path::Path) -> Result<kain_core::ast::Program, Error>:
pub fn import_c_file_with_options(path: &std::path::Path, options: &CImportOptions) -> Result<kain_core::ast::Program, E
pub fn parse_c_file_ast_with_options(path: &std::path::Path, options: &CImportOptions) -> Result<lang_c::ast::Translatio
let parsed = parser__parse_c_file_with_metadata(path, options)?
pub fn import_c_file_with_language_capabilities(path: &std::path::Path, language_capabilities: kain_core::language_featu
pub fn import_c_file_with_language_capabilities_and_options(path: &std::path::Path, language_capabilities: kain_core::la
let parsed = parser__parse_c_file_with_metadata(path, options)?
let kain_ast = transformer__transform_with_language_capabilities_and_layout_metadata(parsed.unit, language_capabilities,
pub fn import_c_project(paths: &[&std::path::Path]) -> Result<kain_core::ast::Program, Error>:
pub fn import_c_project_with_language_capabilities(paths: &[&std::path::Path], language_capabilities: kain_core::languag
let program = import_c_file_with_language_capabilities_and_options(path, language_capabilities.clone(), &CImportOptions_
```

### src/.rustimport/phase2/kain-import/c/parser.kn
```
pub fn default_() -> CSourceLayoutMetadata:
pub fn from_source(source: String) -> CSourceLayoutMetadata:
let source = source.to_string()
let line_starts = compute_line_starts(&source)
let pack_align_by_line = collect_pack_align_by_line(&strip_c_comments(source.as_str()))
pub fn pack_align_bits_for_span(_self: &CSourceLayoutMetadata, span: lang_c::span::Span) -> Option<usize>:
let line_ = (*_self).line_for_offset(span.start)?
pub fn has_packed_attr_for_span(_self: &CSourceLayoutMetadata, span: lang_c::span::Span) -> Bool:
pub fn explicit_type_align_bits_for_span(_self: &CSourceLayoutMetadata, span: lang_c::span::Span) -> Option<usize>:
let snippet = normalized_span_snippet(*_self, span)?
fn line_for_offset(_self: &CSourceLayoutMetadata, offset: usize) -> Option<usize>:
let idx = match (*_self).line_starts.binary_search(&offset):
fn snippet_for_span(_self: &CSourceLayoutMetadata, span: lang_c::span::Span) -> Option<String>:
let end = span.end.min((*_self).source.len())
pub fn parse_c_file_with_metadata(path: &std::path::Path, options: &CImportOptions) -> Result<ParsedCTranslationUnit, Er
```

### src/.rustimport/phase2/kain-import/c/transformer.kn
```
pub fn new_() -> CTransformer:
pub fn with_language_capabilities(language_capabilities: kain_core::language_features::LanguageCapabilities) -> CTransfo
pub fn with_language_capabilities_and_layout_metadata(language_capabilities: kain_core::language_features::LanguageCapab
fn rename_identifier(_self: &mut CTransformer, domain: IdentifierDomain, raw: String) -> String:
fn rename_value_identifier(_self: &mut CTransformer, raw: String) -> String:
fn rename_type_identifier(_self: &mut CTransformer, raw: String) -> String:
fn rename_field_identifier(_self: &mut CTransformer, raw: String) -> String:
fn rename_variant_identifier(_self: &mut CTransformer, raw: String) -> String:
fn fresh_temp(_self: &mut CTransformer, prefix: String) -> String:
let name = f"{prefix}_{(*_self).temp_counter}"
fn anonymous_type_name(_self: &mut CTransformer, owner_name: String, field_name: String, kind: String) -> String:
let raw = f"{owner_name}_{field_name}_{kind}"
fn register_pending_synthetic_item(_self: &mut CTransformer, item: Item):
let key = match &item:
fn drain_pending_items(_self: &mut CTransformer, out_: &mut Array<Item>):
```

### src/.rustimport/phase2/kain-import/c/types.kn
```
pub fn new_() -> CTypeTransformer:
pub fn transform_type_specifier(_self: &CTypeTransformer, spec: &lang_c::ast::TypeSpecifier) -> Result<kain_core::ast::T
fn transform_struct_type(_self: &CTypeTransformer, _struct_type: &lang_c::ast::StructType) -> Result<kain_core::ast::Typ
fn transform_enum_type(_self: &CTypeTransformer, _enum_type: &lang_c::ast::EnumType) -> Result<kain_core::ast::Type, Err
pub fn transform_pointer(_self: &CTypeTransformer, inner: kain_core::ast::Type, _qualifiers: &[lang_c::ast::TypeQualifie
pub fn transform_array(_self: &CTypeTransformer, element: kain_core::ast::Type, size: Option<usize>) -> kain_core::ast::
pub fn add_typedef(_self: &mut CTypeTransformer, name: String, ty: kain_core::ast::Type):
fn default_() -> CTypeTransformer:
```

### src/.rustimport/phase2/kain-import/common/c_registry.kn
```
pub fn c_type_name_aliases() -> &static [(String, KainTypeDescriptor)]:
pub fn materialize_type_descriptor(descriptor: KainTypeDescriptor) -> kain_core::ast::Type:
pub fn named_type(name: String) -> kain_core::ast::Type:
pub fn map_c_builtin_type_specifier(spec: &lang_c::ast::TypeSpecifier) -> Option<kain_core::ast::Type>:
let key = match spec:
pub fn resolve_c_binary_operator(op: &lang_c::ast::BinaryOperator) -> CBinaryOperatorResolution:
let Some(key) = c_binary_operator_key(op)
pub fn resolve_c_compound_assignment_binary_operator(op: &lang_c::ast::BinaryOperator) -> Option<kain_core::ast::BinaryO
let key = c_binary_operator_key(op)?
fn c_binary_operator_key(op: &lang_c::ast::BinaryOperator) -> Option<CBinaryOperatorKey>:
let key = match op:
```

### src/.rustimport/phase2/kain-import/common/language_schema.kn
```
pub fn bootstrap_core_schema() -> KainLanguageSchema:
```

### src/.rustimport/phase2/kain-import/common/identifier_registry.kn
```
pub fn is_reserved_identifier(name: String) -> Bool:
pub fn default_() -> StableIdentifierRenamer:
pub fn resolve(_self: &mut StableIdentifierRenamer, domain: IdentifierDomain, raw: String) -> String:
let trimmed = raw.trim()
let base = sanitize_identifier_base(trimmed)
let used = (*_self).used_by_domain.entry(domain).or_default()
pub fn sanitize_identifier_base(raw: String) -> String:
let mapped = if (ch.is_ascii_alphanumeric() || (ch == "_")):
```

### src/.rustimport/phase2/kain-import/common/mod.kn
```
pub fn new_() -> ImportContext:
pub fn with_include_paths(_self: ImportContext, paths: Array<String>) -> ImportContext:
pub fn with_defines(_self: ImportContext, defines: Map<String, String>) -> ImportContext:
fn default_() -> ImportContext:
```

### src/.rustimport/phase2/kain-import/common/preprocessor.kn
```
pub fn new_() -> PreprocessorConfig:
pub fn add_include_path(_self: &mut PreprocessorConfig, path: impl Into<std::path::PathBuf>):
pub fn add_define(_self: &mut PreprocessorConfig, name: impl Into<String>, value: impl Into<String>):
fn default_() -> PreprocessorConfig:
pub fn resolve_include(include_name: String, current_file: &std::path::Path, config: &PreprocessorConfig) -> Result<Opti
let relative = current_file.parent().map(fn(p): p.join(include_name))
let path = include_path.join(include_name)
```

### src/.rustimport/phase2/kain-import/common/type_mapper.kn
```
pub fn new_c() -> TypeMapper:
pub fn new_rust() -> TypeMapper:
pub fn get(_self: &TypeMapper, type_name: String) -> Option<&kain_core::ast::Type>:
pub fn add_mapping(_self: &mut TypeMapper, source_type: String, kain_type: kain_core::ast::Type):
pub fn map_pointer(_self: &TypeMapper, inner: kain_core::ast::Type, mutable_: Bool) -> kain_core::ast::Type:
pub fn map_array(_self: &TypeMapper, element: kain_core::ast::Type, size: Option<usize>) -> kain_core::ast::Type:
fn default_() -> TypeMapper:
fn named_type(name: String) -> kain_core::ast::Type:
```

### src/.rustimport/phase2/kain-import/rust/parser.kn
```
pub fn parse_rust(source: String, path: &std::path::Path) -> Result<syn::File, Error>:
```

### src/.rustimport/phase2/kain-import/rust/mod.kn
```
pub fn import_rust_file(path: &std::path::Path) -> Result<kain_core::ast::Program, Error>:
let source = std__fs__read_to_string(path).map_err(ImportError__IoError)?
pub fn import_rust_file_detailed(path: &std::path::Path) -> Result<(kain_core::ast::Program, Array<String>), Error>:
let source = std__fs__read_to_string(path).map_err(ImportError__IoError)?
pub fn import_rust_source(source: String, path: &std::path::Path) -> Result<kain_core::ast::Program, Error>:
pub fn import_rust_source_detailed(source: String, path: &std::path::Path) -> Result<(kain_core::ast::Program, Array<Str
let file = parser__parse_rust(source, path)?
let program = tx.transform(file)?
pub fn import_rust_project(paths: &[&std::path::Path]) -> Result<kain_core::ast::Program, Error>:
pub fn import_rust_project_detailed(paths: &[&std::path::Path]) -> Result<(kain_core::ast::Program, Array<String>), Erro
let span = Span__default_()
let source = std__fs__read_to_string(path).map_err(ImportError__IoError)?
let file = parser__parse_rust(&source, path)?
let program = tx.transform(file)?
pub fn import_rust_dir(dir: &std::path::Path, flat: Bool) -> Result<kain_core::ast::Program, Error>:
```

### src/.rustimport/phase2/kain-import/rust/selfhost.kn
```
fn default_() -> RustSelfHostOptions:
pub fn from_inventory_dir(inventory_dir: &std::path::Path) -> Result<RustSelfHostOptions, Error>:
let allowlist_path = inventory_dir.join("selfhost_allowlist.json")
let module_map_path = inventory_dir.join("module_map.json")
pub fn discover(crate_root: &std::path::Path, options: &RustSelfHostOptions) -> Result<RustCrateGraph, Error>:
pub fn import_rust_selfhost_dir_detailed(crate_root: &std::path::Path, options: &RustSelfHostOptions) -> Result<RustSelf
let graph = RustCrateGraph__discover(crate_root, options)?
let source = std__fs__read_to_string(&module.file_path).map_err(ImportError__IoError)?
let file = parser__parse_rust(&source, &module.file_path)?
let program = tx.transform(file)?
pub fn import_rust_selfhost_dir(crate_root: &std::path::Path, options: &RustSelfHostOptions) -> Result<kain_core::ast::P
let result = import_rust_selfhost_dir_detailed(crate_root, options)?
fn collect_modules(root: &std::path::Path, dir: &std::path::Path, options: &RustSelfHostOptions, modules: &mut Array<Rus
let entry = entry.map_err(ImportError__IoError)?
let path = entry.path()
```

### src/.rustimport/phase2/kain-import/rust/transformer.kn
```
pub fn default_() -> RustTransformOptions:
pub fn default_() -> RustMacroPolicy:
pub fn phase1_default() -> RustMacroPolicy:
pub fn new_() -> RustTransformer:
pub fn new_selfhost() -> RustTransformer:
pub fn with_options(options: RustTransformOptions) -> RustTransformer:
let type_mapper = if options.strict_selfhost:
fn transform_attributes(_self: &mut RustTransformer, attrs: &[syn::Attribute]) -> Array<Attribute>:
let name = path_to_ident(attr.path())
let args = match &attr.meta:
let parsed = attr.parse_args_with(Punctuated__parse_terminated)
let target = Expr::Ident(path_to_ident(&value.path), S)
let value = (*_self).transform_attr_expr(&value.value).unwrap_or_else(fn(_): Expr::None(S))
fn transform_attr_expr(_self: &mut RustTransformer, expr: &syn::Expr) -> Result<Expr, Error>:
fn rename_value(_self: &mut RustTransformer, raw: String) -> String:
```

### src/.rustimport/phase2/kain-import/rust/types.kn
```
pub fn new_() -> RustTypeMapper:
pub fn new_selfhost() -> RustTypeMapper:
pub fn register_visible_path(_self: &mut RustTypeMapper, visible_name: impl Into<String>, full_path: Array<String>):
pub fn resolve_path_segments(_self: &RustTypeMapper, path: &syn::Path) -> Array<String>:
pub fn resolve_value_path_segments(_self: &RustTypeMapper, path: &syn::Path) -> Array<String>:
fn normalize_path_segments(_self: &RustTypeMapper, path: &syn::Path) -> Array<String>:
let rooted = match segments.first().map(String__as_str):
fn collect_path_segments(_self: &RustTypeMapper, path: &syn::Path) -> Array<String>:
pub fn map_type(_self: &RustTypeMapper, ty: &syn::Type) -> kain_core::ast::Type:
fn map_path(_self: &RustTypeMapper, tp: &syn::TypePath) -> kain_core::ast::Type:
let resolved_segments = (*_self).resolve_path_segments(&(*tp).path)
let seg = match (*tp).path.segments.last():
let name = resolved_segments.last().cloned().unwrap_or_else(fn(): seg.ident.to_string())
let generics = (*_self).generic_args(&seg.arguments)
let ok = g.next().unwrap_or(kain_core__ast__Type::Unit(S))
```

### src/.rustimport/phase2/kain-import/typescript/parser.kn
```
pub fn parse_typescript(source: String, path: &std::path::Path) -> Result<swc_ecma_ast::Module, Error>:
let fm = cm.new_source_file(Lrc__new_(swc_common__FileName::Real(path.to_path_buf())), source.to_string())
let syntax = swc_ecma_parser__Syntax::Typescript(swc_ecma_parser::TsSyntax { tsx: (path.extension().and_then(fn(e): e.to
let lexer = Lexer__new_(syntax, swc_ecma_ast__EsVersion__Es2022, StringInput__from(&(*fm)), none)
```

### src/.rustimport/phase2/kain-import/typescript/mod.kn
```
pub fn import_typescript_file(path: &std::path::Path) -> Result<kain_core::ast::Program, Error>:
pub fn import_ts_file(path: &std::path::Path) -> Result<kain_core::ast::Program, Error>:
pub fn import_typescript_file_detailed(path: &std::path::Path) -> Result<TypeScriptImportDiagnostics, Error>:
let source = std__fs__read_to_string(path).map_err(ImportError__IoError)?
pub fn import_typescript_source(source: String, path: &std::path::Path) -> Result<kain_core::ast::Program, Error>:
pub fn import_typescript_source_detailed(source: String, path: &std::path::Path) -> Result<TypeScriptImportDiagnostics, 
let module = parser__parse_typescript(source, path)?
let program = tx.transform(module)?
pub fn import_typescript_project(paths: &[&std::path::Path]) -> Result<kain_core::ast::Program, Error>:
pub fn import_typescript_project_detailed(paths: &[&std::path::Path]) -> Result<TypeScriptImportDiagnostics, Error>:
let span = Span__default_()
let result = import_typescript_file_detailed(path)?
pub fn import_ts_project(paths: &[&std::path::Path]) -> Result<kain_core::ast::Program, Error>:
pub fn import_typescript_dir(dir: &std::path::Path, flat: Bool) -> Result<kain_core::ast::Program, Error>:
let files = collect_typescript_files(dir)?
```

### src/.rustimport/phase2/kain-import/typescript/transformer.kn
```
fn default_() -> LoweringPolicy:
pub fn new_() -> TypeScriptTransformer:
pub fn transform(_self: &mut TypeScriptTransformer, module: swc_ecma_ast::Module) -> Result<Program, Error>:
fn transform_module_item(_self: &mut TypeScriptTransformer, item: swc_ecma_ast::ModuleItem) -> Result<Array<Item>, Error
fn transform_module_decl(_self: &mut TypeScriptTransformer, decl: swc_ecma_ast::ModuleDecl) -> Result<Array<Item>, Error
fn transform_export_default_decl(_self: &mut TypeScriptTransformer, export_: swc_ecma_ast::ExportDefaultDecl) -> Result<
let name = func.ident.map(fn(ident): (*_self).rename_value(&ident.sym)).unwrap_or_else(fn(): ("default_export").to_strin
let ident = class_.ident.unwrap_or_else(fn(): Ident__new_no_ctxt(("DefaultExport").into(), class_.class_.span))
fn transform_top_level_stmt(_self: &mut TypeScriptTransformer, stmt: swc_ecma_ast::Stmt) -> Result<Array<Item>, Error>:
fn transform_decl(_self: &mut TypeScriptTransformer, decl: swc_ecma_ast::Decl) -> Result<Array<Item>, Error>:
fn transform_fn_decl(_self: &mut TypeScriptTransformer, func: swc_ecma_ast::FnDecl) -> Result<Array<Item>, Error>:
let name = (*_self).rename_value(&func.ident.sym)
fn transform_top_level_function(_self: &mut TypeScriptTransformer, name: String, function: &swc_ecma_ast::Function, visi
fn transform_interface(_self: &mut TypeScriptTransformer, interface_: swc_ecma_ast::TsInterfaceDecl) -> Result<Struct, E
let span = Span__default_()
```

### src/.rustimport/phase2/kain-import/typescript/types.kn
```
pub fn new_() -> TypeMapper:
pub fn map_type(_self: &TypeMapper, ts_type: &swc_ecma_ast::TsType, span: kain_core::span::Span) -> Result<kain_core::as
let mapped = match ts_type:
fn map_keyword_type(_self: &TypeMapper, kind: swc_ecma_ast::TsKeywordTypeKind, span: kain_core::span::Span) -> kain_core
fn map_type_ref(_self: &TypeMapper, type_ref: &swc_ecma_ast::TsTypeRef, span: kain_core::span::Span) -> Result<kain_core
let name = entity_name_to_string(&(*type_ref).type_name)
let generics = ((*type_ref).type_params.map(fn(params): params.params.iter().map(fn(param): (*_self).map_type(param, spa
fn map_union_or_intersection(_self: &TypeMapper, ty: &swc_ecma_ast::TsUnionOrIntersectionType, span: kain_core::span::Sp
fn map_function_type(_self: &TypeMapper, function: &swc_ecma_ast::TsFnOrConstructorType, span: kain_core::span::Span) ->
fn map_ts_fn_param(_self: &TypeMapper, param: &swc_ecma_ast::TsFnParam, span: kain_core::span::Span) -> Result<kain_core
let ty = match param:
fn entity_name_to_string(entity: &swc_ecma_ast::TsEntityName) -> String:
fn is_nullish_type(ty: &swc_ecma_ast::TsType) -> Bool:
```

### src/.rustimport/phase2/cli/import_asm.kn
```
pub fn import_asm(input: &std::path::Path, format: String, out_kn: Option<&std::path::Path>, validate_only: Bool) -> Res
```

### src/.rustimport/phase2/cli/gpu_artifacts.kn
```
pub fn compile_gpu_artifacts(source: String) -> Result<GpuArtifactOutput, kain_core::error::KainError>:
pub fn compile_gpu_artifacts(_source: String) -> Result<(), kain_core::error::KainError>:
pub fn write_gpu_artifacts_bundle(input: &std::path::Path, output: Option<&std::path::PathBuf>, artifacts: &GpuArtifactO
let base_path = output.cloned().unwrap_or_else(fn(): input.with_extension(target_extension(CompileTarget__Spirv)))
let spirv_path = with_file_name_suffix(&base_path, "", "spv")
let rust_path = with_file_name_suffix(&base_path, ".gpu", "rs")
let json_path = with_file_name_suffix(&base_path, ".reflect", "json")
let bundle_path = with_file_name_suffix(&base_path, ".shader_bundle", "json")
let hlsl_path = with_file_name_suffix(&base_path, ".derived", "hlsl")
pub fn run_gpu_artifact_pipeline(input: &std::path::Path, output: Option<&std::path::PathBuf>) -> Result<Array<std::path
let source = std__fs__read_to_string(input).map_err(fn(err): KainError__runtime(f"Failed to read {input.display()}: {err
let artifacts = compile_gpu_artifacts(&source)?
fn with_file_name_suffix(base: &std::path::Path, suffix: String, extension: String) -> std::path::PathBuf:
let parent = base.parent().map(Path__to_path_buf).unwrap_or_default()
let stem = base.file_stem().and_then(fn(stem): stem.to_str()).unwrap_or("shader")
```

### src/.rustimport/phase2/cli/import_c.kn
```
fn default_() -> ImportCBatchOptions:
pub fn default_() -> ImportCSummary:
pub fn import_c(input: &std::path::Path, output: Option<&std::path::Path>, target: Option<String>, include_paths: &[Stri
pub fn import_c_with_batch(input: &std::path::Path, output: Option<&std::path::Path>, target: Option<String>, include_pa
let options = kain_import::c::CImportOptions { include_paths: include_paths.to_vec(), defines: defines.to_vec(), cpp_opt
let kain_source = generate_kain_source(&program)?
let compile_target = parse_compile_target(target_str).ok_or_else(fn(): KainError__runtime(f"Unknown target: {target_str}
let compiled = compile_(&kain_source, compile_target).map_err(fn(e): KainError__runtime(f"Compilation failed: {e}"))?
let compiled_output = match resolved_output:
fn import_path_to_program(input: &std::path::Path, options: &kain_import::c::CImportOptions, batch: &ImportCBatchOptions
let program = kain_import__c__import_c_file_with_options(input, options).map_err(fn(e): KainError__runtime(f"C import fa
let normalized_includes = normalize_filters(&(*batch).include_filters)
let normalized_excludes = normalize_filters(&(*batch).exclude_filters)
let rel = file.strip_prefix(input).unwrap_or(file.as_path())
let base_name = sanitize_module_name(rel)
```

### src/.rustimport/phase2/cli/import_rust.kn
```
fn default_() -> ImportRustBatchOptions:
pub fn default_() -> ImportRustSummary:
pub fn import_rust(input: &std::path::Path, output: Option<&std::path::Path>, target: Option<String>) -> Result<(), Kain
pub fn import_rust_with_batch(input: &std::path::Path, output: Option<&std::path::Path>, target: Option<String>, batch: 
let kain_source = generate_kain_source(&program)?
let compile_target = parse_compile_target(target_str).ok_or_else(fn(): KainError__runtime(f"Unknown target: {target_str}
let compiled = compile_(&kain_source, compile_target).map_err(fn(e): KainError__runtime(f"Compilation failed: {e}"))?
let compiled_output = match resolved_output:
let class_counts = diagnostic_class_counts(&summary.diagnostics)
let external_mod_diags = (*class_counts.get("external_mod_decl").unwrap_or(&0))
let visible_diags = total_diags.saturating_sub(external_mod_diags)
let class_counts = diagnostic_class_counts(&summary.diagnostics)
let external_mod_diags = (*class_counts.get("external_mod_decl").unwrap_or(&0))
let visible_diags = total_diags.saturating_sub(external_mod_diags)
fn import_path_to_program(input: &std::path::Path, batch: &ImportRustBatchOptions) -> Result<(kain_core::ast::Program, I
```

### src/.rustimport/phase2/cli/import_typescript.kn
```
fn default_() -> ImportDiscoveryPolicy:
fn default_() -> ImportTypeScriptBatchOptions:
fn should_descend(_self: &ImportDiscoveryPolicy, path: &std::path::Path) -> Bool:
let Some(name) = path.file_name().and_then(fn(name): name.to_str())
fn should_include_file(_self: &ImportDiscoveryPolicy, path: &std::path::Path) -> Bool:
let Some(file_name) = path.file_name().and_then(fn(name): name.to_str())
let Some(ext) = path.extension().and_then(fn(ext): ext.to_str())
pub fn default_() -> ImportTypeScriptSummary:
fn has_import_degradation(_self: &ImportTypeScriptSummary) -> Bool:
fn is_degraded(_self: &ImportTypeScriptSummary, validation: &GeneratedKainValidation) -> Bool:
fn degradation_reasons(_self: &ImportTypeScriptSummary, validation: &GeneratedKainValidation) -> Array<String>:
fn ok() -> ImportTypeScriptValidationStatus:
fn failed(error: String) -> ImportTypeScriptValidationStatus:
fn skipped(reason: String) -> ImportTypeScriptValidationStatus:
fn is_failed(_self: &ImportTypeScriptValidationStatus) -> Bool:
```

### src/.rustimport/phase2/cli/lsp.kn
```
pub fn default_() -> DocumentStore:
fn upsert(_self: &DocumentStore, uri: Url, text: String, version: i32):
fn remove(_self: &DocumentStore, uri: &Url):
fn get_text(_self: &DocumentStore, uri: &Url) -> Option<String>:
let guard = await (*_self).docs.read()
fn get_version(_self: &DocumentStore, uri: &Url) -> Option<i32>:
let guard = await (*_self).docs.read()
fn get_analysis(_self: &DocumentStore, uri: &Url) -> Option<DocumentAnalysis>:
let guard = await (*_self).docs.read()
fn update_analysis(_self: &DocumentStore, uri: &Url, analysis: Option<DocumentAnalysis>):
fn apply_changes(_self: &DocumentStore, uri: &Url, version: i32, changes: &[TextDocumentContentChangeEvent]) -> Option<S
let doc = entry.get_mut()
let full_text_change = changes.iter().rev().find(fn(change): change.range.is_none())?
let text = full_text_change.text.clone()
fn from_program(text: String, program: &Program, tokens: &[Token]) -> DocumentAnalysis:
```

### src/.rustimport/phase2/cli/main.kn
```
pub fn default_() -> NativeRuntimeManifest:
pub fn default_() -> NativeRuntimeArchiveManifest:
pub fn default_() -> NativeRuntimeLinkManifest:
pub fn default_() -> NativeRuntimeCompiledArtifacts:
fn normalize_script_source(source: String) -> String:
let source = source.trim_start_matches("\u{feff}").to_string()
fn read_source_from_path(input: &std::path::Path) -> Result<String, String>:
fn read_source_text(input: &std::path::Path) -> Result<String, String>:
let source = if (input == Path__new_("-")):
fn read_format_source(input: Option<&std::path::PathBuf>) -> Result<(String, Option<std::path::PathBuf>, String), String
let source = read_source_text(path)?
let source_name = if (path == Path__new_("-")):
let source_path = if (path == Path__new_("-")):
fn run_format_command(input: Option<std::path::PathBuf>, check: Bool, write: Bool) -> Bool:
let Some(path) = match input:
```

### src/.rustimport/phase2/cli/omni.kn
```
pub fn run(command: OmniCommand) -> Result<(), KainError>:
let manifest_path = kain_omni__init_manifest(&path).map_err(fn(err): KainError__runtime(f"Omni init failed: {err}"))?
let result = kain_omni__build_manifest_path(&manifest).map_err(fn(err): KainError__runtime(f"Omni build failed: {err}"))
```

### src/.rustimport/phase2/cli/packager/build.kn
```
fn patch_hybrid_wasm_reference(source: String, wasm_file_name: String) -> String:
let wasm_url_expression = f"new URL('{wasm_file_name}', document.currentScript?.src ?? window.location.href).toString()"
fn write_hybrid_bundle(descriptor_path: &std::path::Path, artifacts: HybridArtifactOutput) -> Result<(), KainError>:
let js_path = descriptor_path.with_extension("js")
let ts_path = descriptor_path.with_extension("ts")
let wasm_path = descriptor_path.with_extension("wasm")
let wasm_file_name = wasm_path.file_name().and_then(fn(value): value.to_str()).ok_or_else(fn(): KainError__runtime(f"Inv
let js_file_name = js_path.file_name().and_then(fn(value): value.to_str()).ok_or_else(fn(): KainError__runtime(f"Invalid
let ts_file_name = ts_path.file_name().and_then(fn(value): value.to_str()).ok_or_else(fn(): KainError__runtime(f"Invalid
let descriptor = HybridBundleDescriptor { schema_version: 1, target: "hybrid", js: js_file_name.to_string(), ts: ts_file
let descriptor_json = serde_json__to_string_pretty(&descriptor).map_err(fn(err): KainError__runtime(f"Failed to serializ
pub fn build_project(target_overrides: Option<Array<String>>) -> Result<(), KainError>:
let cwd = std__env__current_dir().unwrap_or_else(fn(_): PathBuf__from("."))
let manifest = load_manifest(&cwd)?
let targets = match target_overrides:
```

### src/.rustimport/phase2/cli/packager/build_cs_gen.kn
```
pub fn has_datatable_structs(program: &kain_core::types::TypedProgram) -> Bool:
pub fn has_viewport_items(program: &kain_core::types::TypedProgram) -> Bool:
pub fn has_toolbar_items(program: &kain_core::types::TypedProgram) -> Bool:
fn format_dep_list(deps: &Set<String>, indent: String) -> String:
pub fn generate_build_cs_module(module_name: String, public_deps: &[String], private_deps: &[String]) -> String:
let public_list = format_dep_list(&public_, "\t\t\t\t")
let private_list = format_dep_list(&private_, "\t\t\t\t")
pub fn generate_build_cs(plugin_name: String, has_editor_items: Bool, extra_public_deps: &[String], extra_private_deps: 
let public_list = format_dep_list(&public_deps, "\t\t\t\t")
let private_list = format_dep_list(&private_deps, "\t\t\t\t")
let include_paths = if has_editor_items:
pub fn generate_build_cs_runtime(plugin_name: String, extra_public_deps: &[String], has_datatable: Bool) -> String:
let public_list = format_dep_list(&public_deps, "\t\t\t\t")
pub fn generate_build_cs_editor(plugin_name: String, extra_public_deps: &[String], extra_private_deps: &[String], has_vi
let editor_module_name = f"{plugin_name}Editor"
```

### src/.rustimport/phase2/cli/packager/codegen.kn
```
fn enhance_codegen_result<T>(result: Result<T, KainError>, span_mapper: &kain_core::diagnostics::SpanMapper, file_path: 
pub fn default_() -> SymbolRoutingManifest:
fn register_(_self: &mut SymbolRoutingManifest, symbol: String, module: String, include_path: String, output_stem: Strin
fn extend_from(_self: &mut SymbolRoutingManifest, other: &SymbolRoutingManifest):
pub fn default_() -> OutputNameRegistry:
fn resolve(_self: &mut OutputNameRegistry, symbol: String, requested_stem: String, bucket: String, symbol_source_map: &M
let conflicting_symbol = (*_self).owner_for_resolved.get(requested_stem).cloned().unwrap_or_default()
let source_hint = symbol_source_map.get(symbol).and_then(fn(path): path.file_stem()).and_then(fn(stem): stem.to_str()).m
let symbol_hint = sanitize_output_fragment(symbol)
let bucket_hint = sanitize_output_fragment(bucket)
let resolved_stem = candidates.into_iter().find(fn(candidate): (!(*_self).owner_for_resolved.contains_key(candidate))).u
fn sanitize_output_fragment(value: String) -> String:
fn build_runtime_output_name_registry(program: &kain_core::types::TypedProgram, symbol_source_map: &Map<String, std::pat
fn normalize_path_for_match(path: &std::path::Path) -> String:
fn wildcard_match(pattern: String, text: String) -> Bool:
```

### src/.rustimport/phase2/cli/packager/config.kn
```
fn default_rust_build_artifacts() -> Array<RustBuildArtifact>:
fn default_native_ui_window_size() -> [f32; 2]:
fn default_true() -> Bool:
fn default_() -> RustBuildConfig:
pub fn as_uplugin_type(_self: &Ue5ModuleType) -> String:
pub fn is_editorish(_self: &Ue5ModuleType) -> Bool:
fn default_runtime_module_type() -> Ue5ModuleType:
fn default_loading_phase() -> String:
pub fn default_() -> Ue5ModuleOutputConfig:
pub fn has_module_plan(_self: &Ue5Config) -> Bool:
pub fn validate_module_configs(_self: &Ue5Config) -> Result<(), String>:
fn dfs(node: String, by_name: &Map<String, &Ue5ModuleConfig>, visiting: &mut Set<String>, visited: &mut Set<String>) -> 
fn default_plugin_dir() -> std::path::PathBuf:
fn default_entry() -> std::path::PathBuf:
fn default_output() -> std::path::PathBuf:
```

### src/.rustimport/phase2/cli/packager/cpp_validator.kn
```
pub fn find_inspectcode() -> Option<std::path::PathBuf>:
let locations = [PathBuf__from("C:\\Tools\\ReSharper.CLI\\inspectcode.exe"), PathBuf__from("C:\\Tools\\JetBrains.ReSharp
pub fn validate_cpp_with_resharper(plugin_dir: &std::path::Path) -> Result<(), KainError>:
let inspectcode = match find_inspectcode():
let source_dir = plugin_dir.join("Source")
let output_file = plugin_dir.join("resharper_report.xml")
let output = Command__new_(&inspectcode).arg(source_dir.to_str().unwrap()).arg(f"--output={output_file.display()}").arg(
let stderr = String__from_utf8_lossy(&output.stderr)
let _ = std__fs__remove_file(&output_file)
fn parse_resharper_report(report_path: &std::path::Path) -> Result<(), KainError>:
let xml_content = std__fs__read_to_string(report_path).map_err(fn(e): KainError__runtime(f"Failed to read report: {e}"))
pub fn default_() -> CppIssue:
pub fn validate_cpp_basic(plugin_dir: &std::path::Path) -> Result<(), KainError>:
let source_dir = plugin_dir.join("Source")
let headers = find_headers(&source_dir)?
```

### src/.rustimport/phase2/cli/packager/dependencies.kn
```
pub fn new_() -> Dependencies:
pub fn new_() -> DependencyResolver:
pub fn load(metadata_path: Option<std::path::PathBuf>) -> Result<DependencyResolver, KainError>:
let content = std__fs__read_to_string(&path).map_err(fn(e): KainError__io_error(e.to_string()))?
fn build_include_map(_module_map: &Map<String, Array<String>>) -> Map<String, Array<String>>:
pub fn default_module_map() -> Map<String, Array<String>>:
fn default_include_map() -> Map<String, Array<String>>:
pub fn analyze(_self: &DependencyResolver, generated_files: &[(std::path::PathBuf, String)]) -> Result<Dependencies, Kai
fn analyze_file(_self: &DependencyResolver, deps: &mut Dependencies, file_path: &std::path::PathBuf, content: String) ->
let is_header = (file_path.extension().and_then(fn(s): s.to_str()) == Option::Some("h"))
let trimmed = line_.trim()
fn infer_module_from_include(_self: &DependencyResolver, include: String) -> Option<String>:
pub fn extract_include(_self: &DependencyResolver, line_: String) -> Option<String>:
pub fn validate_dependencies(_self: &DependencyResolver, deps: &mut Dependencies) -> Result<(), KainError>:
fn is_optional_module(_self: &DependencyResolver, module: String) -> Bool:
```

### src/.rustimport/phase2/cli/packager/inject.kn
```
pub fn inject_into_plugin(inputs: &[std::path::PathBuf], plugin_dir: Option<&std::path::PathBuf>, plugin_name: Option<St
let uplugin_dir = detect_plugin_dir(plugin_dir)?
let detected_plugin_name = detect_plugin_name(&uplugin_dir, plugin_name)?
let plugin_root = find_source_root(&uplugin_dir)?
let existing_files = scan_existing_files(&plugin_root)?
let source = std__fs__read_to_string(input).map_err(fn(e): KainError__runtime(f"Failed to read {input.display()}: {e}"))
let tokens = Lexer__new_(source).tokenize().map_err(fn(e): KainError__parse_error(f"Lexer error in {path.display()}: {e}
let span_mapper = SpanMapper__new_(source)
let file_name = path.file_name().and_then(fn(n): n.to_str()).unwrap_or("<unknown>")
let ast = Parser__new_(&tokens, &span_mapper, file_name).parse().map_err(fn(e): KainError__parse_error(f"Parse error in 
let merged_ast = kain_core::ast::Program { items: merged_items, span: Span__default_() }
let typed_program = kain_core__types__check(&merged_ast, first_span_mapper, first_filename).map_err(fn(e): KainError__ru
let ue5_config = Ue5Config { plugin_name: detected_plugin_name.clone(), plugin_dir: plugin_root.clone(), sources: inputs
let layout = plugin_layout__detect_existing(&plugin_root, &detected_plugin_name)?
let generated_files = generate_injection_files(&layout, &ue5_config, &typed_program)?
```

### src/.rustimport/phase2/cli/packager/mod.kn
```
pub fn init_project(path: &std::path::PathBuf, name: Option<String>) -> Result<(), KainError>:
let name = name.unwrap_or_else(fn(): path.file_name().and_then(fn(s): s.to_str()).unwrap_or("my_project").to_string())
let manifest = PackageManifest__default_(&name)
let toml = toml__to_string_pretty(&manifest).map_err(fn(e): KainError__runtime(f"Failed to serialize manifest: {e}"))?
let src_dir = path.join("src")
let main_src = f"\n# {name} - Main Entry Point\n\nfn main():\n    println(\"Hello, KAIN World!\")\n"
pub fn load_manifest(path: &std::path::PathBuf) -> Result<PackageManifest, KainError>:
let manifest_path = if path.ends_with("KAIN.toml"):
let content = std__fs__read_to_string(&manifest_path).map_err(fn(e): KainError::Io(e))?
```

### src/.rustimport/phase2/cli/packager/material_gen.kn
```
pub fn generate_material_factories(plugin_name: String, graphs: &[ue5_materials::MaterialGraph], private_dir: &std::path
let generator = MaterialFactoryGenerator__new_(plugin_name.to_string())
let generated_dir = private_dir.join("Generated")
let header = generator.generate_factory_header(graphs)
let cpp = generator.generate_factory_cpp(graphs)
pub fn generate_material_factories(_plugin_name: String, _graphs: &[()], _private_dir: &std::path::Path) -> Result<(), K
```

### src/.rustimport/phase2/cli/packager/plugin_layout.kn
```
pub fn detect_editor_items(program: &kain_core::types::TypedProgram) -> Bool:
pub fn detect_runtime_items(program: &kain_core::types::TypedProgram, has_shaders: Bool) -> Bool:
pub fn setup(config: &Ue5Config, cwd: &std::path::Path, program: &kain_core::types::TypedProgram, has_shaders: Bool, has
let plugin_root = if ((*config).plugin_dir == PathBuf__from(".")):
let source_dir = plugin_root.join("Source")
let shaders_dir = plugin_root.join("Shaders")
let base_dir = source_dir.join(&module.name)
let public_dir = module.output.public_.clone().map(fn(p): if p.is_absolute(): p else: plugin_root.join(p)).unwrap_or_els
let private_dir = module.output.private_.clone().map(fn(p): if p.is_absolute(): p else: plugin_root.join(p)).unwrap_or_e
let has_runtime_items = (*config).modules.iter().any(fn(m): none)
let has_editor_items = (*config).modules.iter().any(fn(m): m.module_type.is_editorish())
let needs_split = (has_runtime_items && has_editor_items)
let runtime_module_name = (*config).modules.iter().find(fn(m): (none && (m.name == (*config).plugin_name))).or_else(fn()
let editor_module_name = (*config).modules.iter().find(fn(m): (m.module_type.is_editorish() && (m.name == f"{(*config).p
let has_editor_items = (detect_editor_items(program) || has_graph_editors)
```

### src/.rustimport/phase2/cli/packager/post_process.kn
```
pub fn run_python_post_processor(plugin_path: &std::path::PathBuf, plugin_name: String) -> Result<(), KainError>:
let cwd_script = std__env__current_dir().unwrap_or_else(fn(_): PathBuf__from(".")).join("kain").join("python").join("pos
let candidate = current.join("kain").join("python").join("post_process.py")
let exe_script = exe_dir.join("python").join("post_process.py")
let script_path = match script_path:
let output = Command__new_("python").arg(&script_path).arg(plugin_path).arg(plugin_name).arg("--verbose").output()
let stdout = String__from_utf8_lossy(&output.stdout)
let stderr = String__from_utf8_lossy(&output.stderr)
pub fn format_error_with_location(source: String, error_msg: String, file_name: String) -> String:
let span_str = &error_msg[start_idx..((start_idx + end_idx) + 2)]
let line_content = get_line_content(source, line_)
fn position_to_line_col(source: String, pos: usize) -> (usize, usize):
fn get_line_content(source: String, line_num: usize) -> String:
pub fn extract_shader_names(source: String) -> Result<Array<String>, KainError>:
let span_mapper = SpanMapper__new_(source)
```

### src/.rustimport/phase2/cli/packager/registry.kn
```
pub fn add_dependency(package_name: String, version: Option<String>) -> Result<(), KainError>:
let meta_url = index.packages.get(package_name).ok_or_else(fn(): KainError__runtime(f"Package '{package_name}' not found
let version_to_install = version.unwrap_or_else(fn(): pkg_meta.versions.keys().max().cloned().unwrap_or_else(fn(): ("0.1
let pkg_ver = pkg_meta.versions.get(&version_to_install).ok_or_else(fn(): KainError__runtime(f"Version {version_to_insta
let cwd = std__env__current_dir().unwrap_or_else(fn(_): PathBuf__from("."))
let toml = toml__to_string_pretty(&manifest).map_err(fn(e): KainError__runtime(f"Failed to serialize: {e}"))?
pub fn install_all() -> Result<(), KainError>:
let cwd = std__env__current_dir().unwrap_or_else(fn(_): PathBuf__from("."))
let manifest = load_manifest(&cwd)?
fn install_package(name: String, version: String, url: String) -> Result<(), KainError>:
let deps_dir = PathBuf__from("deps")
let pkg_dir = deps_dir.join(name)
let response = reqwest__blocking__get(url).map_err(fn(e): KainError__runtime(f"Download failed: {e}"))?
let bytes = response.bytes().map_err(fn(e): KainError__runtime(f"Failed to read response: {e}"))?
let decoder = GzDecoder__new_(&bytes[..])
```

### src/.rustimport/phase2/cli/packager/registry_writer.kn
```
pub fn new_(package_name: String, asset_name: String, asset_class: String) -> AssetEntry:
let package_path = package_name.rsplit_once("/").map(fn(__kain_closure_arg0): __kain_closure_arg0.__kain_tuple_0.to_stri
let object_path = f"{package_name}.{asset_name}"
pub fn blueprint(package_name: String, asset_name: String) -> AssetEntry:
pub fn material(package_name: String, asset_name: String) -> AssetEntry:
pub fn data_asset(package_name: String, asset_name: String) -> AssetEntry:
pub fn custom(package_name: String, asset_name: String, class_path: String) -> AssetEntry:
pub fn register_assets(registry_path: &std::path::Path, entries: &[AssetEntry], engine_version: unreal_asset_base::engin
let name_map = registry.name_map().cloned().unwrap_or_else(NameMap__new_)
let asset_data = build_asset_data(&name_map, entry, REGISTRY_VERSION)
let pkg_data = build_package_data(&name_map, entry, REGISTRY_VERSION)
fn load_existing_registry(path: &std::path::Path, engine_version: unreal_asset_base::engine_version::EngineVersion) -> R
let data = std__fs__read(path).map_err(fn(e): f"Failed to read AssetRegistry.bin: {e}")?
let cursor = Cursor__new_(data)
let name_map = NameMap__new_()
```

### src/.rustimport/phase2/cli/packager/ue5_pipeline.kn
```
fn enhance_codegen_result<T>(result: Result<T, KainError>, span_mapper: &kain_core::diagnostics::SpanMapper, file_path: 
pub fn build_ue5_plugin() -> Result<(), KainError>:
pub fn build_ue5_plugin_with_options(embed_kain: Bool) -> Result<(), KainError>:
let cwd = std__env__current_dir().unwrap_or_else(fn(_): PathBuf__from("."))
let ue5_config = (manifest.ue5.ok_or_else(fn(): KainError__runtime("No [ue5] section in KAIN.toml"))?).clone()
let config = create_default_config(&cwd)?
let layout = plugin_layout__setup(&ue5_config, &cwd, &typed_program, (!all_shader_names.is_empty()), (!graph_editors.is_
let shader_names = if ue5_config.shaders.is_empty():
let mat_content_dir = layout.plugin_root.join("Content").join("Materials")
let path = mat_content_dir.join(f"{graph.name}.uasset")
let func_content_dir = layout.plugin_root.join("Content").join("Materials").join("Functions")
let path = func_content_dir.join(f"MF_{func_def.name}.uasset")
let graph_content_dir = layout.plugin_root.join("Content").join("Graphs")
let uasset_path = graph_content_dir.join(f"{graph_def.name}.uasset")
let header_path = match layout.editor_public_dir:
```

### src/.rustimport/phase2/cli/packager/uplugin_gen.kn
```
pub fn generate_uplugin_file(plugin_name: String, description: &Option<String>, has_editor_items: Bool, needs_split: Boo
let desc = (none).map(fn(s): s.as_str()).unwrap_or("Generated by KAIN-PRO")
let modules_json = if needs_split:
let can_contain_content = if has_shaders:
let plugins_json = format_plugin_dependencies_json(plugin_dependencies)
pub fn generate_uplugin_file_from_modules(plugin_name: String, description: &Option<String>, has_shaders: Bool, modules:
let desc = (none).map(fn(s): s.as_str()).unwrap_or("Generated by KAIN-PRO")
let modules_json = modules.iter().map(fn(m): "    {\n      \"Name\": \"{m.name}\",\n      \"Type\": \"{m.module_type.as_
let can_contain_content = if has_shaders:
let plugins_json = format_plugin_dependencies_json(plugin_dependencies)
fn format_plugin_dependencies_json(plugin_dependencies: &[String]) -> String:
```

### src/.rustimport/phase2/cli/rust_build.kn
```
pub fn compile_rust_build(source: String, config: &RustBuildConfig) -> Result<RustBuildOutput, kain_core::error::KainErr
let typed_program = frontend_to_typed_program(source, CompileTarget__Rust)?
let bundle = kain_sys_codegen__generate_rust_artifact_bundle(&typed_program)?
let spirv = if ((*config).artifacts.contains(&RustBuildArtifact__Spirv) && bundle.shader_metadata.is_some_and(fn(metadat
pub fn compile_rust_build(_source: String, _config: &RustBuildConfig) -> Result<RustBuildOutput, kain_core::error::KainE
pub fn run_rust_build_pipeline(input: &std::path::Path, output: Option<&std::path::PathBuf>, config: Option<&RustBuildCo
let should_auto_configure_native_ui = config.is_none()
let native_ui_was_explicit = config.and_then(fn(config): config.native_ui).is_some()
let source = std__fs__read_to_string(input).map_err(fn(err): KainError__runtime(f"Failed to read {input.display()}: {err
let base_name = input.file_stem().and_then(fn(stem): stem.to_str()).unwrap_or("kain").to_string()
let output_root = resolve_file_mode_output_root(input, output, config.output)
let native_app_bundle = match &config.native_ui:
let bundle_config = native_app_bundle_config_from_cli(input, &base_name, native_ui, config.artifacts.contains(&RustBuild
let compiled = (native_app_bundle.map(rust_build_output_from_native_app_bundle).transpose()?).unwrap_or(compile_rust_bui
let project_dir = resolve_project_dir(&output_root, &bundle.metadata.app_name, native_ui.output)
```

### src/.rustimport/phase2/kain-sys-codegen/codegen_cpp/mod.kn
```
pub fn generate(program: &kain_core::types::TypedProgram) -> Result<String, KainError>:
let lowered = kain_core__lower_typed_program_memory_for_target(program, kain_core__CompileTarget__Cpp)?
fn new_() -> StringBuilder:
fn push(_self: &mut StringBuilder, text: String):
fn push_line(_self: &mut StringBuilder, text: String):
fn build(_self: &StringBuilder) -> String:
fn new_() -> CppGen:
fn push_indent(_self: &mut CppGen):
fn pop_indent(_self: &mut CppGen):
fn indent_str(_self: &CppGen) -> String:
fn write_line(_self: &mut CppGen, line_: String):
let indented = f"{(*_self).indent_str()}{line_}"
fn write_blank(_self: &mut CppGen):
fn gen_program(_self: &mut CppGen, program: &kain_core::types::TypedProgram) -> String:
fn write_low_level_memory_helpers(_self: &mut CppGen):
```

### src/.rustimport/phase2/kain-sys-codegen/codegen_llvm/mod.kn
```
fn runtime_symbol_for_stdlib_function(name: String) -> String:
fn resolve_host_llvm_target_descriptor() -> &static LlvmTargetDescriptor:
let target_id = if cfg!(all(target_os = "windows", target_arch = "x86_64")):
pub fn generate(program: &kain_core::types::TypedProgram) -> Result<Array<u8>, KainError>:
let lowered = kain_core__lower_typed_program_memory_for_target(program, kain_core__CompileTarget__Llvm)?
fn new_() -> LlvmGenerator:
fn emit_(_self: &mut LlvmGenerator, s: String):
fn emit_label(_self: &mut LlvmGenerator, label: String):
fn next_reg(_self: &mut LlvmGenerator) -> String:
let r = f"%{(*_self).reg_count}"
fn next_label(_self: &mut LlvmGenerator) -> String:
let l = f"L{(*_self).label_count}"
fn sanitize_type_fragment(fragment_: String) -> String:
fn tuple_struct_name_from_types(field_tys: &[String]) -> String:
fn tuple_struct_ptr_type_from_types(field_tys: &[String]) -> String:
```

### src/.rustimport/phase2/kain-sys-codegen/codegen_rust/artifact_bundle.kn
```
pub fn generate_rust_artifact_bundle(program: &kain_core::types::TypedProgram) -> Result<RustArtifactBundle, KainError>:
let primary_source = codegen_rust__generate(program)?
let shader_metadata = codegen_rust__collect_gpu_artifacts(program)
let shader_support = if shader_metadata.shaders.is_empty():
let shader_host = codegen_rust__generate_gpu_host_wrappers(program)?
let shader_reflection = codegen_rust__collect_gpu_artifacts_json(program).map_err(fn(err): KainError__runtime(f"Failed t
```

### src/.rustimport/phase2/kain-sys-codegen/codegen_rust/gpu_artifacts.kn
```
pub fn collect_gpu_artifacts(program: &kain_core::types::TypedProgram) -> RustGpuArtifactOutput:
let shaders = (*program).items.iter().filter_map(fn(item): none).collect()
pub fn collect_gpu_artifacts_json(program: &kain_core::types::TypedProgram) -> Result<String, serde_json::Error>:
pub fn format_gpu_type(ty: &kain_core::ast::Type) -> String:
let generics = generics.iter().map(format_gpu_type).collect().join(", ")
let types = types.iter().map(format_gpu_type).collect().join(", ")
let params = params.iter().map(format_gpu_type).collect().join(", ")
let generics = generics.iter().map(format_gpu_type).collect().join(", ")
fn map_shader_stage(stage: kain_core::ast::ShaderStage) -> RustGpuShaderStage:
fn classify_binding(name: String, ty: &kain_core::ast::Type) -> RustGpuBindingKind:
fn is_sampler_2d(ty: &kain_core::ast::Type) -> Bool:
fn is_storage_buffer(ty: &kain_core::ast::Type) -> Bool:
fn is_local_size_param(name: String) -> Bool:
fn is_specialization_constant(name: String) -> Bool:
let uppercase = name.chars().all(fn(ch): (!ch.is_ascii_lowercase()))
```

### src/.rustimport/phase2/kain-sys-codegen/codegen_rust/gpu_host.kn
```
pub fn generate_gpu_host(program: &kain_core::types::TypedProgram) -> String:
let artifacts = codegen_rust__gpu_artifacts__collect_gpu_artifacts(program)
pub fn render_gpu_host(artifacts: &RustGpuArtifactOutput) -> String:
let shader_mod = sanitize_ident(&shader_.name)
fn stage_name(stage: RustGpuShaderStage) -> String:
fn binding_kind_name(kind: RustGpuBindingKind) -> String:
fn binding_param_type(kind: RustGpuBindingKind) -> String:
fn default_binding_initializer(binding: &codegen_rust::gpu_artifacts::RustGpuBindingArtifact) -> String:
let axis = (*binding).name.strip_prefix("LOCAL_SIZE_").unwrap_or(&(*binding).name)
let default_value = if (axis == "Z"):
fn sanitize_ident(name: String) -> String:
```

### src/.rustimport/phase2/kain-sys-codegen/codegen_rust/mod.kn
```
pub fn generate(program: &kain_core::types::TypedProgram) -> Result<String, KainError>:
let lowered = kain_core__lower_typed_program_memory_for_target(program, kain_core__CompileTarget__Rust)?
let has_modules = typed_program_has_modules(&lowered)
let rendered = gen.gen_program(&lowered)
pub fn generate_gpu_host_wrappers(program: &kain_core::types::TypedProgram) -> Result<String, KainError>:
let lowered = kain_core__lower_typed_program_memory_for_target(program, kain_core__CompileTarget__Rust)?
fn new_() -> StringBuilder:
fn push_line(_self: &mut StringBuilder, text: String):
fn build(_self: &StringBuilder) -> String:
fn postprocess_flattened_selfhost_output(source: String) -> String:
fn replace_prefix_at_token_boundaries(source: String, prefix: String, replacement: String) -> String:
let ch = source[cursor..].chars().next().unwrap()
fn is_token_boundary_before(source: String, index: usize) -> Bool:
fn typed_program_has_modules(program: &kain_core::types::TypedProgram) -> Bool:
fn sanitize_rust_identifier(name: String) -> String:
```

### src/README.md
```
h2 Source Tree Ownership
h2 Durable Direction
h2 Active Bootstrap Surface
```

### src/SELFHOST_DIRECTION.md
```
h1 Selfhost Direction
h2 Core Call
h2 What Counts As Real Selfhost
h2 Architectural Direction
h3 1. `KAIN.toml` is the canonical compiler contract
h3 2. `src/core` is the owning compiler lane
h3 3. Keep the structure from `.legacy`, not the runtime model
h3 4. Use a mixed bridge strategy during bootstrap
h3 5. The native C runtime is the runtime target, not an optional side lane
h2 Recommended Stages
h3 Stage 0: Rust bootstrap host
h3 Stage 1: Kain-owned compiler in host-backed mode
h3 Stage 2: Kain compiles Kain
h3 Stage 3: Native selfhost lane
h2 Immediate Compiler Priorities
h2 Keep / Drop Summary
h2 Anti-Goals
h2 Practical Rule For Future Agents
h2 Concrete Bootstrap Contract
```

### src/KAIN.toml
```
table [package]
table [build]
table [selfhost]
table [selfhost.runtime]
table [selfhost.outputs]
table [selfhost.verify]
table [ffi]
table [ffi.c]
table [ffi.rust]
key name
key version
key description
key entry
key entry_module
key source_root
key bootstrap_strategy
key future_frontend_mode
key mode
key lane
key target
key runtime_owner
key bootstrap_owner
key default_cli_command
key default_wrapper
key manifest_path
```

### src/core/types.kn
```
pub fn to_llvm_type(self) -> String:
pub fn is_void(self) -> Bool:
pub fn display_name(self) -> String:
fn resolved_type_from_name(name: String) -> ResolvedType:
fn typeenv_new() -> TypeEnv:
let root_scope = TypeScope { bindings: [] }
let env = TypeEnv {
fn typeenv_register_fn(env: TypeEnv, name: String, ret: ResolvedType) -> Unit:
fn typeenv_push_scope(env: TypeEnv) -> Unit:
fn typeenv_pop_scope(env: TypeEnv) -> Unit:
fn scope_arr_slice(arr: Array<TypeScope>, new_len: Int) -> Array<TypeScope>:
let result = []
let i = 0
fn typeenv_define(env: TypeEnv, name: String, ty: ResolvedType) -> Unit:
let top = env.scopes[len(env.scopes) - 1]
```

### src/.selfhost/bootstrap/combined/kain_core_bootstrap.kn
```
pub fn from_str(name: String) -> Option<CompileTarget>:
pub fn core() -> StdLibCatalog:
pub fn load_stdlib_for_target(target: CompileTarget) -> String:
pub fn core_keywords() -> Array<String>:
pub fn new_(start: Int, end: Int, file: String) -> Span:
pub fn empty() -> Span:
pub fn is_defined(self) -> Bool:
pub fn is_empty(self) -> Bool:
pub fn len(self) -> Int:
pub fn merge(self, other: Span) -> Span:
let start = self.start
let end = self.end
let file = self.file
pub fn extend_to(self, pos: Int) -> Span:
let start = self.start
```

### src/.selfhost/reports/bootstrap_report.md
```
h1 Self-Host Bootstrap Report
h2 Sources
h2 Module roots
h2 Search paths
h2 FFI
h2 Artifacts
h2 Steps
```

### src/core/codegen_llvm.kn
```
pub fn generate_llvm_ir(program: TypedProgram) -> String:
```

### src/core/low_level_abi.kn
```
pub fn pointer_layout() -> String:
```

### src/core/low_level_memory_metadata.kn
```
pub fn layout_for_unit() -> String:
```

### src/core/comptime.kn
```
pub fn eval_program(program: Program) -> Result<Program, KainError>:
pub fn eval_program_with_stats(program: Program) -> Result<ComptimeEvalOutput, KainError>:
pub fn reject_unsupported_host_evaluation(message: String) -> KainError:
```

### src/core/runtime.kn
```
pub fn run_program(program: Program) -> Result<Value, KainError>:
let _ = program
```

### src/core/kainc.kn
```
fn main() -> Unit:
let args_list = args()
let input_path = ""
let output_path = ""
let print_ir = false
let invoke_clang = false
let verbose = false
let i = 1  # skip argv[0] (the binary name)
let arg = args_list[i]
let n = len(input_path)
let last3 = ""
let j = n - 3
let base = ""
let k = 0
let source = read_file(input_path)
```

### src/core/parser.kn
```
pub fn parse_source(source_tokens: Array<Token>, file_name: String) -> Result<Program, KainError>:
let _ = source_tokens
let prog_span = Span { file: file_name, start_row: 1, start_column: 1, end_row: 1, end_column: 1 }
```

### src/core/low_level_memory.kn
```
pub fn lower_program_memory(program: Program) -> String:
let _ = program
```

### src/core/stdlib.kn
```
pub fn from_str(name: String) -> CompileTarget:
pub fn core() -> StdLibCatalog:
pub fn load_stdlib_for_target(target: CompileTarget) -> String:
pub fn core_keywords() -> Array<String>:
fn substring(text: String, start: Int, end: Int) -> String:
let result = ""
let i = start
fn to_float(value: String) -> Float:
let i = 0
let sign = 1.0
let first = char_at(value, 0)
let int_part = 0.0
let frac_part = 0.0
let divisor = 10.0
let seen_dot = false
```

### src/core/lexer.kn
```
pub fn tokenize(self) -> Array<Token>:
fn scan_peek(sc: ScanState) -> String:
fn scan_peek_at(sc: ScanState, offset: Int) -> String:
let p = sc.pos + offset
fn scan_advance(sc: ScanState) -> String:
let ch = scan_peek(sc)
fn is_alpha_char(ch: String) -> Bool:
fn is_digit_char(ch: String) -> Bool:
fn is_alnum_char(ch: String) -> Bool:
fn digit_val(ch: String) -> Int:
let _ = self.source
let raw = []
fn is_keyword(word: String) -> Bool:
fn push_symbol(tok_buf: Array<Token>, sc: ScanState, start_pos: Int, start_row: Int, start_col: Int, sym: String) -> Uni
let span = Span { start: start_pos, end: sc.pos, file: sc.file_name }
```

## stdlib

### stdlib/javascript/site_report.kn
```
pub fn js_site_build_report(app_manifest_path: String, template_name: String, archetypes: String, outputs: String) -> St
let summary = js_site_write_matrix(app_manifest_path)
let catalog = js_site_catalog(app_manifest_path)
let app = js_site_load_app(app_manifest_path)
let client_features = app.site_runtime.client_features
let report = "Kain web template build\n"
let report = report + "template: " + template_name + "\n"
let report = report + "default_experience: " + summary.default_experience + "\n"
let report = report + "experience_count: " + str(summary.experience_count) + "\n"
let report = report + "artifact_count: " + str(summary.artifact_count) + "\n"
let report = report + "output_root: " + summary.output_root + "\n"
let report = report + "server_port: " + str(summary.server_port) + "\n"
let report = report + "modes: " + str(summary.modes) + "\n"
let report = report + "catalog_default: " + catalog.default_experience + "\n"
let report = report + "client_features: " + str(client_features) + "\n"
```

## tidus

### tidus/ARCHITECTURE.md
```
h1 Tidus Architecture
h2 Overview
h2 Top-Level Layout
h2 Frontend Structure
h2 Kain Acceleration Surface
h2 Build And Run Commands
h2 Key Data Flow
h2 Architectural Constraints
h2 Common Errors
```

### tidus/memory.md
```
h1 Durable Memory
h2 Current Repo Shape
h2 Why This Changed
h2 Durable Decisions
h2 Current Risks
h2 Recommended Next Step
```

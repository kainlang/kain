# Kain Architecture

This file is the durable repo overview for the current Kain checkout.
It is the fast way for future agents to understand what Kain is, where the important code lives, and which architectural rules matter enough to preserve.

## Current Blade Addenda

- [blades/kloner](blades/kloner): KCloner.tsx reference recreation and native LLVM mograph proof blade. It now keeps authored app policy in a dedicated Kain session lane (`src/kloner_session.kn`), returns a Kain-owned UI frame from `src/kloner_ui.kn`, and translates that session into a `VulkainKlonerPacket` before presentation so Kloner is no longer a one-shot bridge arg dump. The blade itself now requires `platform::vulkan(system)` through both `build.kn` and `KAIN.toml`, while the same-window Vulkain host remains the substrate for the live 3D viewport. Default launch is interactive-until-close with native viewport controls and a foreground overlay pass; finite automation uses `blades/kloner/run.ps1 -FrameBudget N`. Validate with `blades/kloner/run.ps1 -FrameBudget 2 -SkipShaderCompile`, `.kain/run/kloner_frame.txt`, `.kain/run/kloner_scene.txt`, `.kain/run/kloner_vulkain_report.txt`, and the Kloner instance bounds in `blades/vulkain/native/z3/vulkain_bridge_bounds.smt2`.
- [blades/platform-package-smoke](blades/platform-package-smoke): tiny platform-package proof blade that must stay ahead of Vulkain/Kloner generic bridge work. `run.ps1` stages `fixtures/platform_sdk/tiny_math`, imports it twice, byte-compares lock/report output, checks relocatable paths, stable blocked-symbol reasons, and the no-public-`call_typed` rule, then runs a `std::platform` open/resolve/close smoke. The callable typed function-pointer proof is `//runtime:native_test_platform_library`; the fixture proves deterministic generated package metadata before real Vulkan consumption.

## Documentation System

The repo's documentation is deliberately split by trust level and reader intent:

- `guides/` is the canonical long-form guide tree for the live checkout.
- `guides/reference/legacy-crosswalk.md` translates stale prose and old folder names into the current guide tree.
- `guides/pipelines/` holds the conceptual Omni and Fabric orchestration pages.
- `guides/ue5/` holds the conceptual Unreal-facing authoring and packaging pages.
- `docs/` is legacy support material and may lag behind the code; do not promote it back to the source of truth.
- `docs/examples/` is the deliberate exception inside `docs/`: it is now a runnable, manifest-driven Kain source suite with real `.kn` files plus a validator, and it exists to give agents a locally proven authoring ladder.
- `smoketest/`, `apps/`, `unreal_plugins/`, and `kn_library/` are workflow, proof, and corpus surfaces that support the guide tree rather than replace it.

When the docs and code disagree, treat the mismatch as a signal to update the canonical guide tree from source rather than to copy the stale wording forward.

## What Kain Is

Kain is a compiled multi-target language toolchain, an executable semantic runtime, and an embeddable host stack.

It is not only a `KAIN.toml`/materialization language or an orchestration shell over Rust, C, Python, Node, and GPU targets. `crates/kain-core` already owns real language execution for substantial parts of Kain itself: parsing, `comptime`, executable-body typechecking, direct interpretation of functions and blocks, closures, control flow, `match`, async/await, actor runtime execution, JSX/UI expression evaluation, and runtime execution of compiler-owned declarations such as `patch`, `converge`, `world`, `entangle`, `orchestrate`, `axiom`, `pulse`, `shatter`, and `teleport`. Shared actor vocabulary now lives in `crates/kain-actor` so supervision, mailboxes, scheduler policy, behavior contracts, native ABI descriptors, and typed actor contracts have a dedicated crate instead of hiding only inside `kain-core`. Portable filesystem semantics now live in `crates/kain-fs`, with `kain-core`, the root `stdlib/` modules, LLVM, and direct C consuming the same typed file/path contract instead of each lane inventing its own ad hoc file helpers. Typed input semantics now live in `crates/kain-input`, so human, CLI, UI-runtime, native-platform, synthetic, and agent/LLM intent input reduce through the same frames, actions, axes, text commits, and replay traces instead of target adapters inventing their own event models. Portable networking contracts now live in `crates/kain-net`, with the raw native substrate in `stdlib/net.kn`, public aliases at `std.net` / `std.http` / `std.tls` / `std.http2`, and `runtime/native/include/net_system.h` exposing TCP, protocol-aware HTTP request/response metadata, capability queries, Windows-first HTTPS client flows, and early HTTP/2 client negotiation primitives to LLVM/direct-C Kain builds. Portable child-process and PTY contracts now live in `crates/kain-process`, with `stdlib/process.kn` and `runtime/native/include/process_system.h` exposing the same handle-driven process model to LLVM/direct-C Kain builds. The immediate execution loop now lives in `crates/kain-run`, which resolves Kain source, C files, Cargo crates, Fabric manifests, Node/Bun entries, blades, and workspace `[run]` metadata through one reportable pipeline instead of scattering `run` behavior through CLI branches.

The build, packaging, and adapter crates matter because Kain is meant to ship into multiple targets, not because the language is limited to manifests and glue. The durable architecture rule is: keep authored logic in Kain when it belongs to Kain semantics, and use host bridges when the capability is genuinely platform-, ABI-, or ecosystem-owned.

Stdlib authoring rule: prefer public root-domain imports such as `std.actor`, `std.fs`, `std.http`, `std.process`, `std.graphics`, `std.ui`, `std.text`, `std.collections`, `std.crypto`, and `std.alloc` in new Kain source. The root `stdlib/` folder is now the single canonical authored surface for those domains, LLVM/direct-C loads that same root profile directly, and the older `std.native.*` path is compatibility-only when an old file still mentions it.

Stdlib inspectability rule: `kain stdlib-map` is the canonical generated atlas for agents and tools that need the native LLVM stdlib in one scan. It writes `stdlib/stdlib.map.json` plus compact `stdlib/STDLIB_MAP.llm.md`, indexes only top-level `stdlib/*.kn` root-domain modules, merges Rust-registered builtins and native runtime service manifests, and is guarded by `//stdlib:stdlib_map_check` so native stdlib edits cannot silently drift the atlas. Target/vendor overlays such as `stdlib/ue5`, `stdlib/python`, `stdlib/javascript`, `stdlib/interop`, and `stdlib/c` are intentionally out of this atlas.

Platform package rule: native dynamic libraries enter through a deterministic lock/import lane, not public generic ABI call magic. `runtime/native` owns only `platform.library` open/resolve/close and target capability reporting; `crates/kain-c-ffi` owns `kain import platform`, target-aware lockfiles under `.kain/platform/<package>/<target-triple>/`, generated typed thunks, relocatable package metadata, stable blocked-symbol reasons, and package-specific metadata such as Vulkan loader dispatch. `build.kn` / `platform.kn` may declare graph requirements like `platform_package("vulkan").provider("system")`, equivalent `KAIN.toml` data should produce the same graph, and any override must be explicit in provenance instead of hiding filesystem/env decisions. `crates/kain-run` consumes that provenance in plan/report output, stages package locks before execution, adds generated platform modules and reports to watch inputs, and exposes `KAIN_PLATFORM_LOCKS` / `KAIN_PLATFORM_GENERATED_ROOTS` to process-backed run units.

Build authority rule: `build.kn` is now the preferred blade/workspace authority surface, not just an optional task sidecar. `crates/kain-blades` synthesizes an effective manifest by overlaying script-authored `workspace_defaults()`, `package("...")`, `blade("...")`, `build_defaults()`, `run_defaults()`, and `build_task("...")` data onto compatibility `KAIN.toml`. Script-only blades and workspaces may omit `KAIN.toml` for root metadata, blade discovery, build defaults, run defaults, and explicit build tasks, while `KAIN.toml` remains a supported compatibility/default lane and still carries surfaces such as current C FFI metadata that have not been promoted into the script vocabulary yet.

Blade dependency rule: a library blade may own `[c_ffi]` bridge requirements. `crates/kain-blades` resolves C FFI libraries transitively through blade dependencies, while the final `kain-run` executable unit owns materializing the inherited headers, sources, shared libraries, include paths, and `KAIN_TRANSITIVE_C_FFI_*` provenance. New blade module trees should not require hand-listing every nested folder: explicit `source_roots` remain the source roots, but module roots are inferred from nested `.kn` / `.god` files and generated platform modules under `.kain/platform`.

Converge backend rule: `converge` remains the semantic selector/validator, not a hardcoded SIMD switch. Native LLVM now carries multiple fast lanes, static lanes collapse directly, and CPU-gated lanes route through the native CPU capability plus converge selector/cache services. Real AVX2/AVX-512 acceleration should enter first as FFI-backed or runtime C kernels, then be proved equivalent to the spec lane and benchmarked through the existing harness. The native SIMD kernels live at `runtime/native/src/core/simd.c` with root stdlib wrappers in `stdlib/runtime.kn`; the current lane includes direct dot kernels plus the proof-backed affine fill/repeated-dot accumulator used by `benchmark/cases/simd_lane_mix`.

Important language-wide rule: native/Rust/C code provides capabilities, ABI substrate, validation, diagnostics, and target integration. Kain source authors behavior, engines, scenes, UI structures, primitives, simulation loops, and workflows. Do not add host-side catalogs, templates, default scenes, built-in primitives, or product layouts when a generic capability surface would let Kain author the system directly.

The repo currently spans five connected layers:

1. `crates/kain-core`
Language semantics, parsing, typing, effects, comptime, interpreter lanes, runtime-contract emission, shader metadata, and other compiler-owned truth.

2. Materialization, import, build, and capsule orchestration
`crates/kain-driver`, `crates/kain-omni`, `crates/kain-selfhost`, `crates/kain-build`, `crates/kain-run`, `crates/kain-amalgamate`, and importer crates turn Kain semantic truth into emitted bundles, packaged artifacts, portable source capsules, immediate execution plans, imported surfaces, and multi-runtime workflows.

3. Runtime and host bridges
`runtime/native` is the canonical ABI floor and C runtime substrate. `crates/kain-host`, `crates/kain-sdk`, `crates/kain-reflect`, `crates/kain-foreign-abi`, `crates/kain-c-ffi`, `crates/kain-crate-ffi`, `crates/kain-python`, `crates/kain-node`, `crates/kain-codebase`, and `crates/kain-interop` provide host/runtime integration.

4. UI, native desktop, and 3D
`crates/kain-ui`, `crates/kain-ui-native`, and `crates/kain-3D` are the semantic UI and accelerated native presentation stack. The low-level single-file LLVM UI lane lives at the C ABI floor in `runtime/native/include/ui_system.h` and `runtime/native/src/ui/ui_system.c`: it exposes sessions, host frame presentation metadata, arbitrary authored node kinds, stable node keys, rects, styles, accessibility labels, text/font/resource handles, clipboard/IME/drag/menu/dialog state, events, hit testing, focus, dirty tracking, hot reload generations, and draw-command buffers without a host-owned component catalog. The compiled-bundle/runtime reload lane now sits beside it in `runtime/native/include/ui_runtime.h`, `runtime/native/include/ui_hot_reload.h`, `runtime/native/src/ui/ui_runtime.c`, and `runtime/native/src/ui/ui_hot_reload.c`: bundle reload/state transfer is cross-platform by API, file-watch live reload works everywhere the runtime can stat the bundle path, and the shared-memory control ring is hidden behind platform-specific backends instead of being baked into the UI architecture. The low-level native graphics lane lives at `runtime/native/include/graphics_system.h` and `runtime/native/src/core/graphics_system.c`: it exposes sessions, backend targets, SPIR-V module registration, authored buffers, meshes, pipelines, frame submission, draw-command inspection, and diagnostics without a host-owned scene or primitive catalog.

5. Target adapters
`crates/web`, `crates/gpu`, `crates/kain-gpu-runtime`, and the `crates/ue5*` family consume compiler-owned contracts for specific runtime environments.

## Non-Negotiable Ownership

- `crates/kain-core` owns language meaning, typed metadata, executable semantics, capability requirements, and shader/compute-plan semantics. The current keyword surface includes machine axioms (`axiom`), temporal beats (`pulse`), silicon-oriented layout intent (`shatter struct`), and destructive cross-world ownership handoff (`teleport`) beside the earlier compiler-owned intent keywords.
- `crates/kain-actor` owns reusable actor-system model types: actor IDs, addresses, message contracts, mailbox policy, lifecycle/supervision metadata, scheduler policy, behavior contracts, native ABI descriptors, registry snapshots, actor-system validation, and the typed actor contract consumed by `kain-core`.
- `crates/kain-ownership` owns the portable ownership-state memory model behind the first-class `collapse`, `observe`, and `decay` keywords: region kinds, legal state transitions, conservative world/entangle policy, imported-pointer borrow policy, lowering hints, and the `memory.ownership` capability name.
- `crates/kain-fs` owns portable filesystem semantics: path normalization, text/byte file operations, metadata and directory-entry shapes, temp paths, atomic writes, copy/move/remove behavior, sandbox/capability policy, `fs://` virtual roots, chunked/ranged IO, polling watchers, transactional filesystem journals, and typed filesystem errors shared by interpreter and native-facing stdlib code.
- `crates/kain-input` owns portable input semantics: source provenance, event shapes, data-driven action/axis bindings, frame reduction, text commits, agent intent events, deterministic traces, and replay. Host adapters translate raw OS/web/UE/UI/CLI events into this model; they do not define app-facing input policy.
- `crates/kain-net` owns portable networking contracts: TCP endpoints, HTTP request/response specs, HTTP protocol preferences, TLS client specs, capability-state descriptors, headers, route specs, listener/server/client handles, lifecycle state, and typed network errors. Native adapters implement these semantics; actor and entangle integrations layer above the transport instead of redefining sockets.
- `crates/kain-process` owns portable child-process and PTY contracts: process specs, stdio policy, cwd/env overrides, process/PTY handles, lifecycle state, and captured output shapes. Native adapters implement these semantics; they do not redefine process policy per host.
- `crates/kain-run` owns the operator-facing run pipeline: target inference, `[run]` manifest metadata, blade/workspace resolution, cached C/Cargo execution, dev/watch polling, process-backed reports, and `.kain/cache/run` / `.kain/reports/run` layout. CLI commands call this crate instead of implementing run behavior inline.
- `crates/kain-stdlib-map` owns the generated stdlib atlas consumed by agents, docs, and build checks. It must stay deterministic across Cargo and Bazel execution roots; parser fallback diagnostics are repo-relative for that reason.
- `crates/kain-amalgamate` owns the portable capsule lane: comment-safe `.kn` capsule formats, generated preview headers, `//!kain-capsule` metadata blocks, editable inline file sections, sealed archive payload validation, pack/inspect/unpack/materialize behavior, and digest-scoped `.kain/cache/amalgamate/<digest>/workspace` extraction. CLI `run`, `build`, and `check` should treat capsule inputs as a transparent bridge back into the normal blade/workspace pipelines.
- `crates/kain-foreign-abi` owns the shared foreign ABI type graph, scalar type table, pointer/callback/aggregate bridge classification, ownership-policy tags for imported raw pointers, and the durable proof pack for ABI arithmetic. Import lanes such as `kain-c-ffi` consume this crate instead of hardcoding incompatible scalar or pointer policy locally.
- `crates/kain-driver` owns emitted bundle truth and app/runtime materialization.
- `runtime/native` owns the stable ABI floor, startup, service contracts, and low-level host/runtime substrate. `runtime/blades` is the Kain-authored runtime-policy workspace above that floor: Kain source can own batching, scheduling posture, service composition, probes, and future runtime self-host lanes while C continues to expose raw OS/syscall/handle contact through coarse ABI boundaries. Native startup now lazily boots the pooled actor scheduler on first actor use instead of eagerly paying that cost for every pure compute executable. It also owns truthful CPU feature discovery and the process-local converge selector/cache/telemetry substrate used by native LLVM fast-lane dispatch.
- Native UI primitives are ABI substrate, not product UI. The runtime may expose generic handles and buffers, but buttons, panels, inspectors, viewports, shells, and catalogs must be authored in Kain source or `stdlib/`, not hardcoded as Rust/C host recipes.
- Native graphics primitives are ABI substrate, not a game engine or DCC catalog. The runtime may expose sessions, buffers, shader modules, pipeline handles, draw commands, and backend probes; scenes, meshes, primitive recipes, simulation loops, renderers, editors, and engine policy must be authored in Kain source or data.
- Host bridges and adapters extend Kain into external ecosystems; they do not downgrade Kain source into "configuration only."
- Accelerated Rust lanes may optimize execution, but they must consume the same compiler-owned bundles rather than inventing a second semantic model.
- Web, UE5, selfhost, and future lanes are adapters, not alternate definitions of what Kain source means.

## Actor Runtime Shape

- The native actor ABI lives at `runtime/native/include/actor.h` with implementation in `runtime/native/src/core/actor.c`. ABI v3 separates compatibility blocking actors from LLVM-native microcell actors: generated actors use `KAIN_ACTOR_ENTRY_KIND_MICROCELL_TURN`, `KainActorTurnFn(actor_id, mailbox, user_data, budget)`, and a default 64-message turn budget.
- LLVM actor lowering must emit nonblocking turn handlers that poll `kain_actor_try_receive` and return `KAIN_ACTOR_TURN_IDLE` or `KAIN_ACTOR_TURN_YIELDED`; it must not put generated actors back on blocking `kain_actor_receive` loops inside pooled scheduler workers.
- `kain_actor_send` appends to the mailbox under the mailbox lock and calls `kain_scheduler_ready_actor`. `kain_actor_ask_send_ref` is the ask-only exact-ref fast path: when the target is a local `MICROCELL` turn actor with an empty mailbox and no scheduler ownership bits set, it claims `in_scheduler_turn` inline on the caller thread instead of queueing a worker wakeup; otherwise it falls back to the normal mailbox path. Keep this mailbox->scheduler lock order intact so sends racing with a running turn cannot strand live mailbox messages.
- `in_scheduler_turn` now means a scheduler-owned microcell turn is in flight, whether that owner is a pooled worker or the local ask inline handoff. `kain_actor_execute_microcell_turn` and `kain_scheduler_finish_turn` are the shared turn/cleanup path; finish-turn clears the ownership bit and requeues iff mailbox work remains.
- TLS reply ports are synthetic actor refs backed by a reusable actor-table slot. Successful asks rearm the slot by bumping generation in place, so late stale replies fail the exact ref match while hot roundtrips avoid table remove/insert churn.
- Reply waits use a bounded spin window before falling through to the OS event/condition wait. Timeout-zero waits remain poll-like; nonzero waits may catch same-core actor replies without a kernel transition.
- Mailboxes cache only message nodes, not payload buffers, and the cache is capped per mailbox. Do not add payload caches unless a benchmark beats the generic malloc/copy path on the real actor row; the last attempted payload cache regressed.
- Legacy bootstrap actors intentionally run on direct compatibility threads now, and runtime shutdown joins those direct threads before actor-table cleanup. The pooled scheduler is for bounded microcell turns, and blocking legacy code must not be allowed to consume the worker pool used by native ask/reply traffic.

## Main Folders

- [README.md](/M:/Code/Kain/README.md): repo-level operating brief
- [repomap.md](/M:/Code/Kain/repomap.md): top-level folder map
- [MEMORY.md](/M:/Code/Kain/MEMORY.md): durable architectural task memory
- [scripts](/M:/Code/Kain/scripts): directory-only operational tree. `scripts/docs/` holds the indexes and guide docs; `scripts/kain/` holds executable KAIN filesystem automation built on real runtime helpers like `read_dir`, `path_*`, `create_dir_all`, `copy_file`, `remove_file`, `read_file`, `write_file`, `file_exists`, and `env`; `scripts/kain/actor/` adds real actor-system demos for supervisor/worker fan-out and extension bucketing; `scripts/linux/`, `scripts/windows/`, `scripts/python/`, `scripts/rust/`, and `scripts/tests/` hold the other executable helpers and fixtures.
- [guides](/M:/Code/Kain/guides): canonical long-form guide tree for the live language, runtime, CLI, and example lanes
- [guides/reference/legacy-crosswalk.md](/M:/Code/Kain/guides/reference/legacy-crosswalk.md): bridge from stale prose to the current canonical docs
- [docs/kainplan/ui_slate_x100](/M:/Code/Kain/docs/kainplan/ui_slate_x100): legacy UI overhaul docs, acceptance criteria, regression notes, and Gamma operator guidance
- [docs/kainplan/08_COMPILER_OWNED_INTENT_QUARTET.md](/M:/Code/Kain/docs/kainplan/08_COMPILER_OWNED_INTENT_QUARTET.md): legacy syntax, lowering, bundle contracts, and validation notes for the compiler-owned intent suite: `law`, `patch`, `converge`, `world`, `entangle`, and `orchestrate`
- [crates](/M:/Code/Kain/crates): workspace crates
- [crates/kain-amalgamate](crates/kain-amalgamate): portable capsule pack/inspect/unpack/materialize crate. The live format stays comment-safe inside a `.kn` file by combining a generated preview header, a `//!kain-capsule` metadata block, and either inline `//!kain-file` sections for editable capsules or a `//!kain-capsule-payload` blob for `--archive`, while preserving whole blades/workspaces verbatim.
- [crates/kain-core/z3](crates/kain-core/z3): durable Z3 proof pack for compiler/frontend invariants in `kain-core`. It currently covers low-level memory layout arithmetic, signed-literal conversion bounds, diagnostics span/line-end math, parser slice/index guards, compiler-owned keyword invariants for `law`/`patch`/`converge`/`orchestrate` plus `axiom`/`pulse`/`shatter`/`teleport`, and lanes `memory`, `diagnostics`, `literals`, `parser`, `keywords`, `full`, plus workspace `smoke`.
- [crates/kain-ownership/z3](crates/kain-ownership/z3): durable Z3 proof pack for the ownership-state kernel behind first-class `collapse`, `observe`, and `decay`. It proves observe/collapse exclusivity, scoped observe/collapse balance, decay preconditions, terminal decayed-state behavior, imported-pointer lifetime policy, and conservative snapshot policy for world/entangle-backed regions.
- [crates/kain-foreign-abi/z3](crates/kain-foreign-abi/z3): durable Z3 proof pack for the shared foreign ABI model. It currently proves array-to-pointer decay stays inside the u8 pointer-depth budget used by bridge classification.
- [crates/gpu/z3](crates/gpu/z3): durable Z3 proof pack for the live GPU codegen backends in `crates/gpu/src/codegen_spirv.rs` and `crates/gpu/src/codegen_ptx.rs`. It currently covers Vulkan/std430 wrapper layout invariants, vector-constructor component-index bounds, hoisted-local slot removal safety, compute local-size slot mapping, PTX builtin/thread-index math, PTX parameter alignment, and PTX storage-buffer byte bounds through `kain.gpu.proofs`, with focused `layout`, `constructors`, `control`, `ptx`, `full`, and workspace `smoke` lanes.
- [crates/kain-sys-codegen/z3](crates/kain-sys-codegen/z3): durable Z3 proof pack for the LLVM backend in `crates/kain-sys-codegen/src/codegen_llvm/mod.rs`. It currently proves ABI alignment/layout arithmetic, string-literal byte-count headroom, match CFG label distinctness, guard-fail branch targets, SSA register monotonicity, integer/bool cast semantics, runtime memory-bridge span preconditions, and pointer-teleport bitcast roundtrip identity through `kain.sys.codegen.llvm.proofs`, with focused `layout`, `control`, `casts`, `memory`, `llvm`, `full`, and workspace `smoke` lanes.
- [runtime](/M:/Code/Kain/runtime): native runtime substrate, conformance, fixtures, and companion lanes
- [runtime/native/src/core/z3](runtime/native/src/core/z3): durable Z3 proof pack for the native C core runtime. It now proves actor mailbox/scheduler/supervision arithmetic, graphics file-read growth and per-session capacity counters, low-level memory allocation/header/pointer-addition arithmetic, ownership observer/decay/realloc registry invariants, net HTTP body-span/allocation guards, process buffer/spec arithmetic, entangle registry/text-copy bounds, realtime graphics binding-array bounds, services copy bounds, native map capacity arithmetic, machine-stone capability/shatter/pulse/teleport arithmetic, and native-stdlib fs-builder/patch-journal/path-copy arithmetic through `kain.native.core.proofs`, with focused `actor`, `graphics`, `memory`, `ownership`, `net`, `process`, `entangle`, `realtime`, `services`, `stdlib`, `machine`, `native`, `full`, and workspace `smoke` lanes. Reference-only SMT experiments for the branchless native map lookup, converge selector/cache/ring bounds, benchmark-release stage elision, machine capability token signatures, attrition flight-recorder ring-copy bounds, and net/async/process handle-index probe bounds live under `runtime/native/src/core/z3/proofs-experimental`.
- [runtime/native/src/ui/z3](runtime/native/src/ui/z3): durable Z3 proof pack for the raw native UI ABI. It currently carries the proved `abi_ui_decode_hex` allocation bound, a durable de Bruijn uniqueness proof for the occupancy-bit free-slot fast path, and the evolving UI proof surface for host presentation and lookup arithmetic. Exploratory SMT for UI-side magic constants should live under `runtime/native/src/ui/z3/proofs-experimental` until the invariant is settled.
- [smoketest](/M:/Code/Kain/smoketest): capability proof matrix for bridges, UI, 3D, and mixed runtimes
- [smoketest/native-ui](/M:/Code/Kain/smoketest/native-ui): focused raw native UI proof lane. `pilot/` is the first isolated LLVM smoke for Kain-authored UI systems over `stdlib/ui.kn`; it now proves the passive runtime-owned `software` path while live presenters are expected to come from blade-owned packages such as `blades/opengl`. Do not route this lane through the older smoketest delegate pipelines.
- [smoketest/compiler_owned_intent](/M:/Code/Kain/smoketest/compiler_owned_intent): compiler-owned intent suite smoke covering `kain run` plus LLVM runtime-contract / realtime-bundle staging
- [smoketest/UI](/M:/Code/Kain/smoketest/UI): UI proof surface for authored shells, dense operator layouts, shader-canvas proofs, and packaged native launches
- [smoketest/allinone](/M:/Code/Kain/smoketest/allinone): broad regression harness that replays importers, standalone FFI bridges, GPU artifacts, Omni, Fabric, and UE5 codegen into per-lane output folders
- [docs](/M:/Code/Kain/docs): legacy doctrine, plans, pipeline notes, validation notes, and research
- [docs/examples](/M:/Code/Kain/docs/examples): runnable example ladder built from real `.kn` files, with `examples_manifest.json` and `validate_examples.py` as the durable validation surface for future agents
- [mcp/kain-agent-mcp](mcp/kain-agent-mcp): successor repo-MCP workspace under permanent `/mcp` ownership rather than the experimental `blades/` proving ground. The first scaffold is intentionally transport-only: `KAIN.toml` plus `src/main.kn`, `src/server/http_probe_server.kn`, `src/protocol/http_probe_protocol.kn`, and `src/tools/health/health_tool.kn` prove a native LLVM localhost HTTP self-test with route-to-actor registration, parsed request metadata, JSON-ish health response, TLS request construction, HTTP/2 request construction, cleanup, and a staged `kain-agent-mcp.exe` proof artifact. Validate from `mcp/kain-agent-mcp` with direct Bazel `kain.exe` invocations: `check src/main.kn --target llvm`, `build --target llvm`, then `run . --target llvm --keep-artifacts --json`.
- [blades/kain-mcp](blades/kain-mcp): legacy Kain-authored MCP blade. Keep it as donor/reference material while the permanent `/mcp/kain-agent-mcp` successor grows; do not expand the old blade for new repo-MCP architecture unless explicitly debugging the historical launch path. Root `mcp.json` and `codex.config.toml` may still point here until the `/mcp` successor implements the actual MCP handshake/tool protocol.
- [blades/kain-example](blades/kain-example): the canonical native LLVM proving ground blade. `src/main.kn` remains the one-file semantic stress test for low-level memory helpers, `Option`/`Result`/`Future`, compiler-owned intents, worlds/entangle, actors, filesystem, input, networking, process, native stdlib UI, native graphics, shader declarations, heap-health checkpoints, and live native LLVM lowering for enum `match`, numeric `for` over `range`, `vec!`, `format!`, and `println`. `src/ui.kn` is now the authored runtime workbench shell over the raw UI ABI; it should teach session/state/draw semantics, not a runtime-owned OpenGL presenter contract.
- [blades/machine-stones](blades/machine-stones): compact native LLVM dogfood blade for the machine-stones quartet. `src/main.kn` demonstrates runtime-backed `axiom`, `pulse`, `shatter struct`, and `teleport` with native UI/viewport world surfaces; `machine-stones.exe` is left in the blade root and `.kain/out/` holds the LLVM IR plus runtime-contract/realtime sidecars.
- [blades/converge-autotune-probe](blades/converge-autotune-probe): compact native LLVM proof blade for the CPU capability plus converge selector/autotune substrate. `src/main.kn` imports `std.runtime`, uses scalar and `cpu.x86.avx2` converge fast lanes, calls the runtime selector/telemetry wrappers directly, and leaves `converge-autotune-probe.exe` in the blade root while `.kain/out/` holds emitted LLVM/native artifacts.
- [blades/kain-labs](blades/kain-labs): native lab workspace for reference-driven experiments. Its first app, KQuantum, recreates `reference/KQuantum.tsx` as a Kain-authored native GPU particle/fluid lab with `src/main.kn`, dense native UI modules, `src/kernels.kn` SPIR-V compute kernels, data-driven mode config, a blade-root `kain-labs.exe` proof artifact, and a real Win32 Vulkan particle window bridged through `[c_ffi]` in `native/kquantum_vulkan_bridge.c`. Validate with `blades/kain-labs/run.ps1`, `spirv-val` on both Kain kernels and Vulkan window shaders, Z3 bounds proofs for particle/fluid/Vulkan fixed-array math, direct C harnesses under `.kain/native/`, and screenshot/report artifacts under `blades/kain-labs/.kain/`.
- [blades/vulkain](blades/vulkain): minimal reusable raw Vulkan blade package. `src/vulkain.kn` intentionally exports only bare probe/counter/run/report helpers over the blade-local C bridge in `native/vulkain_bridge.c`, so future blades can import Vulkan window capability without inheriting app policy. `build-vulkain.ps1` keeps SPIR-V and bridge outputs under `.kain/gpu/basic_window/`, `.kain/native/`, and `.kain/run/`; `run.ps1` compiles `vulkain.exe` into the blade root, stages `vulkain_bridge.dll` beside it for direct testing, moves runtime sidecars under `.kain/out/vulkain/`, and can reuse an already-built Bazel `kain.exe` through `-KainBin` or `$env:KAIN_BIN` when unrelated workspace failures would otherwise block local blade validation. Validate with `blades/vulkain/run.ps1`, `spirv-val`, `native/z3/vulkain_bridge_bounds.smt2`, and `.kain/run/vulkain_report.txt`.
- [blades/opengl](blades/opengl): raw reusable Win32/WGL compatibility blade package. Its live bridge now lives in `native/opengl_bridge.c`, while the deleted runtime-owned OpenGL lane is preserved only as salvage material under `reference/runtime_legacy/`. Validate with `blades/opengl/run.ps1`, `.kain/native/opengl_bridge.obj`, `.kain/run/opengl_report.txt`, and `.kain/run/opengl.bmp`.
- [blades/kaintana](blades/kaintana): blade-owned Kain UI framework core. The old monolithic `src/kaintana.kn` is now a tiny prelude; the real framework is split into `src/api/kaintana_ui.kn` builder specs, `src/api/widgets.kn`, `src/core/{types,reconciliation,layout,theme,input,render_commands,widget_events}.kn`, and `src/platform/{desktop,vulkan,winit}/*.kn`. `widget_events.kn` is the Kain-owned interaction seam: it pumps `std::ui` events at frame begin, tracks pointer capture/hover/drag through generic UI state cells, and lets builder widgets return real `activated` / dragged slider values instead of static placeholders. `platform/vulkan/vulkan_adapter.kn` is the base stdlib-graphics/SPIR-V capability seam; the opt-in foreign presenter remains `blades/kaintana-vulkan`. `platform/winit/winit_adapter.kn` is the Kain-side window/event-loop adapter contract over the passive `std::ui` host state, ready for a future Rust/winit bridge without making the base blade import it. `examples/*.kn` is the one-exe tour suite: to-do list, tabbed pane, modal popup, data grid, keypad, resizable panel, file explorer, and mega button stress all compile into the normal `kaintana.exe` through `examples/example_tour_suite.kn`. `src/main.kn` now also runs a headless two-frame widget probe before the desktop tour so button activation and slider dragging stay proved without depending on a live presenter. The examples exe still enters `kaintana_desktop_host_run_window(spec)` after composition; `KAINTANA_EXAMPLES_FRAME_BUDGET` or `run.ps1 -FrameBudget N` controls how long it stays live. The core consumes root stdlib surfaces directly: `std::collections::SlotMap` for renderer-neutral node handles, `std::alloc` arena state for frame widget cells, `std::text::StringView` for labels/keys, `std::math` for layout, `std::input` for action/axis wrappers, and `std::ui`/`std::graphics` for passive draw/resource recording. `native/kaintana_desktop_bridge.c` remains the Win32/GDI compatibility host, but it is still mostly draw-only; live pointer interactivity now belongs in Kain as soon as the active presenter feeds `pointer.*` events into the session. Validate with `blades/kaintana/run.ps1 -NoRun`, `run.ps1 -FrameBudget 3`, manifest `kain run . --target llvm --keep-artifacts --json` after bridge staging, `.kain/run/kaintana_host_report.txt`, `.kain/run/kaintana_host.bmp`, and the Kaintana Z3 reports under `z3/reports/*kaintana*.json`.
- [blades/kaintana-vulkan](blades/kaintana-vulkan): optional Vulkan adapter blade for Kaintana. `src/kaintana_vulkan.kn` exposes probe/run/report wrappers over `blades/vulkain` so foreign-presenter routing stays opt-in instead of contaminating the base framework or desktop acceptance executable.
- [blades/kaintana-test](blades/kaintana-test): desktop acceptance blade for the Kaintana framework. `src/main.kn` is now the oxide DCC-style control deck proof: top bar, viewport, charts, sliders, keypad, action maps, host services, snapshot text, and input traces in one authored shell. `run.ps1` builds only `kaintana-test.exe`, and `.kain/run/` holds the durable desktop artifacts: `kaintana_test_desktop.bmp`, `kaintana_test_desktop_frame.txt`, `kaintana_test_desktop_host.txt`, `kaintana_test_desktop_snapshot.txt`, and `kaintana_test_desktop_input_trace.txt`. The latest direct desktop pressure proof reached `frames=1000` with `last_error=ok`. This blade should stay free of `vulkain_bridge.dll` imports.
- [blades/kaintana-vulkan-test](blades/kaintana-vulkan-test): dedicated foreign-presenter acceptance blade for Kaintana's Vulkan adapter. `src/main.kn` proves the same framework vocabulary can drive the `vulkain` lane without sharing the desktop manifest/output path. Its durable artifacts live under `.kain/run/` as `kaintana_vulkan_test_frame.txt` and `kaintana_vulkan_test_host.txt`.
- [blades/network-domains](blades/network-domains): compact sovereign-networking proof blade for the new built-in `std.net`, `std.http`, `std.tls`, and `std.http2` surfaces. `src/main.kn` now proves the public import shape directly: raw TCP loopback plus local HTTP server handling, actor route registration, request/response protocol metadata, TLS capability queries, and HTTP/2 request intent from one native LLVM blade.
- [blades/stdlib-domains](blades/stdlib-domains): compact stdlib import-shape proof blade for the normalized public root domains. It imports `std.runtime`, `std.actor`, `std.collections`, `std.diagnostics`, `std.result`, `std.time`, `std.intent`, `std.fs`, `std.input`, `std.net`, `std.http`, `std.tls`, `std.http2`, `std.process`, `std.graphics`, and `std.ui`, then calls representative clean names under native LLVM so future alias rewrites do not silently strand a domain.
- [blades/hash-domains](blades/hash-domains): compact root-stdlib hash proof blade. It imports `std.hash` and exercises deterministic 32-bit masking, rotation, Wang-style word mixing, ordered/unordered combinators, power-of-two/mod buckets, byte-fed FNV-1a, CRC32 byte folding, and salted fingerprints through `kain run` plus LLVM checking.
- [blades/pong](blades/pong): quick state-lattice proof blade for `world`, `entangle`, `collapse`, `observe`, actors, and blade-owned live presentation. `src/main.kn` keeps the Kain-authored simulation, entangled authority/mirror worlds, passive native-UI graph, and proof/report surface; `native/pong_window_bridge.c` is the actual Win32/WGL presenter that paints the same frame state into a real window and writes `pong.bmp`; `build-pong-window.ps1` compiles the bridge into `.kain/native/pong_window_bridge.obj`; `config/pong_demo.json` is the data-driven tuning surface; and `z3/proofs-experimental/` carries the focused bounce/paddle/swarm SMT checks for the demo's board math. Validate with `blades/pong/run.ps1`, `.kain/run/pong_report.txt`, `.kain/run/pong_window_report.txt`, `.kain/run/pong.bmp`, and the root `z3/reports/*pong-*.json` artifacts.
- [benchmark](benchmark): multi-language benchmark lane for Kain LLVM, Rust LLVM, C++ on Clang, Zig `ReleaseFast`, Erlang/OTP, Go `gc`, and optional JavaScript/Node plus Python/CPython rows when a case declares them. `benchmarks.json` owns the case manifest, maturity labels, per-language source paths, language notes, fairness notes, and case telemetry metrics; `cases/<case>/main.kn`, `main.rs`, `main.cpp`, `main.zig`, `main.js`, and `main.py` are dependency-free case sources for normal rows, while manifest-declared language subsets, per-case Cargo manifests, Erlang OTP modules, and runner-built support artifacts cover focused Tokio/Rayon, actor mailbox, and shared-library FFI stress comparisons. `run.py` is the stable core: it builds/checks selected languages, compiles Erlang via `erlc`, builds Zig through direct `zig build-exe -O ReleaseFast`, stages support DLLs beside the executables when needed, times executable/interpreter samples, derives manifest-declared domain metrics such as requests/s or bytes/s from those timings, writes the latest/timestamped report files, and removes stale HTML output. The suite-extension layer now lives under `benchmark/wrappers/*.json` and launches through `benchmark/run_wrapper.py`, with `run_fast.py` and `run_sim.py` as compatibility shims; future fire-and-forget suite categories should land there instead of growing more one-off control flow inside `run.py`. The benchmark runner now also owns a foreign-baseline cache under `benchmark/out/baselines/`: `--baseline-mode auto` is the default dev loop, which reruns Kain fresh and reuses matching non-Kain baselines when Kain is part of the selected language set, while `--baseline-mode refresh-foreign` is the explicit full cross-language refresh path. Cache keys include machine fingerprint, tool binary identity, workload/source fingerprint, build flags, and run-count settings, and the reports surface hit/refresh counts so fast-path runs are visible rather than implicit. The runner prefers a release-built `kain.exe`, defaults the C++ lane to the repo-bundled `toolchain/llvm/bin/clang++.exe`, defaults the Zig lane to `zig` from PATH unless `--zig` or `ZIG` overrides it, resolves Erlang from the official Windows OTP `bin` directory before PATH shims, pins Kain benchmark links to `runtime/native_core_runtime.toml`, injects `KAIN_NATIVE_PROFILE=benchmark-release` with `opt-level=3`, `target-cpu=native`, and no debug info into Kain native builds, and records that tuning provenance in the report. Kain rows that rely on a case-local `KAIN.toml` plus `use c::...` must compile from the case directory so nearest-manifest resolution works; `run.py` now does that automatically for the shared-library stress lane. Wrapper-owned suites can now keep separate roots such as `benchmark/latest_fast.md` or `benchmark/latest_sim.md` plus matching `benchmark/out/reports/latest_fast.llm.md` or `latest_sim.llm.md` via `--latest-stem`, and manifest composition now supports `include_manifest` plus `case_ids` for suite-specific catalogs without copy-pasting the whole case inventory. `benchmark/ffi_boundary/` is the dedicated ABI-tax/native-neighborhood probe for pure Kain LLVM, direct-object/shared-library C FFI, interpreter/live-bridge comparisons, and Zig `ReleaseFast` twins; it writes `ffi_boundary_latest.llm.md` plus JSON siblings under `benchmark/out/reports/` and should be used when the question is boundary overhead or whether Kain LLVM is in Zig/C territory rather than language-to-language fairness. `benchmark/blades/kain-benchmark` is the compact native UI console for running quick/full benchmark passes and previewing the latest LLM report; the built operator executable lives at `benchmark/kain-benchmark.exe`. `benchmark/assesments/` is the durable markdown research surface for full-suite summaries and root-cause analysis, while `benchmark/out/reports/` stays the generated report surface. The current cases now cover contention/collapse, entangle mirroring, converge dispatch, direct ownership memory, branch dispatch, call chains, memory streaming, allocation churn, top-level consts, recursion, string ops, array scanning, aggregate fields, tagged Option/Result values, ready-future async overhead, native TCP loopback versus Tokio, Rayon parallel reduction, SIMD-lane pressure, native map lookup, manual JSON roundtrip, filesystem streaming, process/stdout loops, local HTTP request handling, actor ask/reply fanout versus Erlang, Unicode-heavy substring search, large-object allocator churn, raw graphics submission, shared-library FFI call stress, `semantic_singularity` Kain-only fused keyword pressure, and `semantic_singularity_crucible` as the single-file native LLVM torture lane for ordinary language constructs plus Kain-only semantic systems. The sharp live suite findings now are that Kain still has a real synchronous-local-HTTP gap against Tokio even after the request-slot leak fix, while the actor ask/reply row still needs one unmeasured warmup ask per worker to measure steady-state behavior cleanly. Repo-wide release blocking now lives beside this suite in `release/readiness_policy.json` plus `scripts/python/release_readiness_gate.py`: the `quick` profile reruns the honest Kain-only release subset, rejects caveated benchmark notes such as float-array parity workarounds, and pairs those rows with attrition and runtime-conformance evidence instead of treating one megacase as proof.
- [attrition](attrition): benchmark-shaped runtime-certification lane for long-horizon stress, deterministic replay, sabotage, and teardown closure. `attritions.json` owns the case manifest, determinism tiers, scales, sabotage knobs, profiles, and runtime configuration; `invariants.json` is the explicit invariant catalog with owner subsystem, exact formula, units, idle floor, allowed permanent floor entries, sabotage mappings, and isolate/mixed coverage mapping; `run.py` compiles the native C lanes against `runtime/native_attrition_runtime.toml`, records runtime manifest provenance, emits replay commands plus minimized deterministic repro counts, and writes `latest.md`, `out/reports/latest.llm.md`, `out/reports/latest.json`, and timestamped siblings. The current implemented foundation lanes are saturated RC hot-object churn, virtual-time async/timer cleanup, actor reply-port recycle, process slot recycle, the mixed runtime boss fight, and Kain-authored certification lanes such as `kain_actor_ask_roundtrip`, `kain_stdlib_domains`, `kain_float_array_literal_indexing`, and `kain_semantic_singularity_crucible_attrition`. Expected-fail sabotage runs are first-class and should be used to prove the harness can actually catch leaks or stale-handle bugs rather than assumed.
- [release](release): machine-readable release-blocking policy surface. `release/readiness_policy.json` is the source of truth for the repo-level readiness matrix: hook commands to rerun benchmark/attrition/conformance lanes, import-shape rules for root stdlib benchmark cases, and feature coverage entries that map concrete capabilities such as float-array literal indexing, graphics stdlib importability, root stdlib domains, actor roundtrips, and runtime conformance categories to durable evidence ids. `scripts/python/release_readiness_gate.py` executes or reuses those hooks, inspects the generated JSON reports, and fails explicitly on proxy/caveated release rows, missing `use std::<domain>` imports, or uncovered feature classes. Use `--profile quick --run` for the focused pre-release matrix and `--profile full --run` when actor/async/diagnostics/ABI/reflection/host-bridge/hot-reload/platform conformance must also block release.
- `benchmark/cases/semantic_singularity_*`: Kain-only fused-semantics attribution matrix. The full `semantic_singularity` boss fight keeps all weird semantics live, while `no_actor`, `no_entangle`, `no_patch`, `shatter_only`, `actor_only`, and `converge_only` isolate the major subsystem costs in one comma-selectable benchmark report. The matrix proof lives under `benchmark/cases/semantic_singularity/z3`.
- `benchmark/cases/semantic_singularity_crucible`: Kain-only native LLVM crucible. It keeps the semantic singularity hot loop intact but adds enum `match`, trait/impl syntax, top-level const/type alias, `comptime`, shader declarations, `vec!`/`format!`/string indexing, Option/Result/Future/`await`, bitwise packet math, and raw `realloc_mem` plus `collapse`/`observe`/`decay` preflight checks in one checksum-guarded file. Its local SMT files prove the preflight checksum, raw memory offsets, shatter/hot-loop index bounds, and bit-vector packet mix.
- `benchmark/cases/quantumerlang`: Kain/Erlang semantic-flex row, not the fair mailbox latency row. Erlang runs 64 stateful processes and 300,000 synchronous request/reply mailbox folds; Kain computes the same checksum with `shatter struct` lane metadata, `collapse`/`observe`/`decay` helper memory, `converge`, and a boot-time `teleport` plus entangled world patch. The bounds proof lives under `benchmark/cases/quantumerlang/z3`.
- [blades/kain-fmt](blades/kain-fmt), [blades/kain-log](blades/kain-log), [blades/kain-fsx](blades/kain-fsx), [blades/kain-config](blades/kain-config), [blades/kain-process-kit](blades/kain-process-kit), [blades/kain-http](blades/kain-http), [blades/kain-actor-kit](blades/kain-actor-kit), [blades/kain-interop-kit](blades/kain-interop-kit), and [blades/kain-json](blades/kain-json): the essential Kain-library blade layer. Future runnable blades should compose these first instead of re-implementing config parsing, formatting, logging, filesystem helpers, process launch, JSON shaping, or lightweight native capability wrappers locally.
- [guides](/M:/Code/Kain/guides): canonical long-form language, runtime, CLI, and reference guides
- [apps](/M:/Code/Kain/apps): first-class applications and prototypes
- [website](/M:/Code/Kain/website): the official KAIN public site, data-driven launch surface, and browser playground; this is now the canonical website dogfood for the public-site archetype
- [tools/kain-flight-control](tools/kain-flight-control): older repo-native Go MCP sidecar and config corpus. Keep it for adjacent experiments and reference data, but the canonical repo MCP launch path is now the Kain-authored `blades/kain-mcp` blade.
- [tools/foreign_abi](tools/foreign_abi): local mining tools for C/C++ header ABI shapes. `mine_c_abi_shapes.py` scans headers for callback typedefs, function-pointer parameters, multi-level pointers, raw scalar pointers, byte-buffer returns, arrays, and by-value named types so FFI work can be driven by observed API pressure instead of guesses.
- [apps/kain-fabric-modeler](/M:/Code/Kain/apps/kain-fabric-modeler): Fabric-first native 3D modeling app scaffold that converges Python, Kain, C ABI, Rust crate, GPU compute, Node, and native-ui packaging
- [apps/kain-fabric-dcc-suite](/M:/Code/Kain/apps/kain-fabric-dcc-suite): broader flagship Fabric-first DCC suite scaffold with scene, ingest, sculpt, material, rig, animation, sim, render, compositor, publish, automation, and tensor planning lanes
- [apps/kain-canvas-forge](/M:/Code/Kain/apps/kain-canvas-forge): Node-first desktop-ready painting and Three.js composition studio prototype that proves a browser and `.exe` app lane can live under `apps/`
- [docs/reference/dcc-parity-matrix.md](/M:/Code/Kain/docs/reference/dcc-parity-matrix.md): flagship KSculpt and KPainter parity inventory, baseline rules, and validation entrypoint
- [scripts/python/validate_dcc_parity_matrix.py](/M:/Code/Kain/scripts/python/validate_dcc_parity_matrix.py): strict validator for the machine-readable parity inventory owned by `apps/kain-fabric-dcc-suite/config/dcc_parity_matrix.json`
- [scripts/python/run_dcc_parity_harness.py](/M:/Code/Kain/scripts/python/run_dcc_parity_harness.py): executable scenario harness for the highest-priority shared, sculpt, and painter parity hooks
- [stdlib](/M:/Code/Kain/stdlib): runtime support and standard library data. The root profile is the canonical public and native-authored surface for actor, entangle, filesystem, hash/data-integrity helpers, input, math, net, process, runtime, diagnostics, time, intent, raw native UI, and raw native graphics helpers; `stdlib/c` is the direct C bridge overlay; `stdlib/stdlib.map.json` and `stdlib/STDLIB_MAP.llm.md` are generated by `kain stdlib-map`; and the root `gen_server.kn` helper layers `gen_server_start`, `gen_server_call`, `gen_server_cast`, and `gen_server_info` on top of raw actor primitives. `start_link` is currently naming-only until real link semantics land
- [testing](/M:/Code/Kain/testing): test infrastructure and fixtures
- [src](src): owned selfhost root; keep only `src/core`, source docs, `src/.legacy`, and `src/.rustimport` at the top level
- [src/core](src/core): canonical owned Kain core surface; this is the active hand-authored selfhost language tree
- [src/.rustimport](src/.rustimport): reference-only donor and phase2 mirror root; `reference/` holds the moved Rust import corpus plus earlier mirrored crate-layout examples, `phase2/` is the canonical live selfhost mirror root, and `kain import crates --blades` is the operator path for recreating that per-crate `.kn` layout from a live Rust workspace
- [src/.legacy](src/.legacy): archival donor tree kept for historical reference only
- [ouroboros](ouroboros): selfhost control-plane manifests, repair tooling, inventories, reports, and pipeline automation

## Docs And Example Ownership

- `guides/README.md` is the top-level reader map for the canonical guide tree.
- `guides/quickstart.md` should stay short and point outward to the deeper pages, not become a second manual.
- `docs/examples/README.md` is the operator-facing entrypoint for the runnable example ladder, and `docs/examples/examples_manifest.json` is the machine-readable source of truth for validation commands and coverage tags.
- `docs/examples/validate_examples.py` is the canonical way to prove the local example ladder. Keep per-example validation metadata in the manifest rather than scattering it across prose pages.
- `docs/reference/dcc-parity-matrix.md` is the canonical operator page for flagship KSculpt and KPainter parity, while `apps/kain-fabric-dcc-suite/config/dcc_parity_matrix.json` is the machine-readable source of truth and `scripts/python/validate_dcc_parity_matrix.py` is the validator.
- `scripts/python/run_dcc_parity_harness.py` is the executable scenario layer for the DCC parity program. Keep structural/shared scenario checks there instead of inventing ad-hoc one-off validation snippets.
- `guides/pipelines/*` and `guides/ue5/*` are conceptual deep-dive pages for orchestration and Unreal-facing authoring. Keep the CLI pages focused on command syntax and keep the conceptual pages focused on data models, validation, and outputs.
- `guides/syntax-and-semantics/functions-traits-and-impls.md` is the canonical chapter for function signatures, traits, and impl blocks.
- `guides/syntax-and-semantics/low-level-memory.md` is the canonical memory/provenance chapter, and `guides/runtime/compiler-owned-intents.md` is the canonical runtime-intent chapter for `patch`, `law`, `converge`, `world`, `entangle`, and `orchestrate`.
- `guides/reference/troubleshooting.md` is for recurring operator failures and should point readers to the live code, the current CLI, and the legacy crosswalk when old terminology shows up.
- `guides/reference/legacy-crosswalk.md` is the bridge from stale prose to current docs; update it whenever old terminology or historical topics surface again.

## Key Crates

- [kain-core](/M:/Code/Kain/crates/kain-core): parser, AST, executable-body semantic typechecker, shared filesystem module resolution (`module_resolution.rs`), first-class `fs_*` interpreter globals, compiler-owned source formatter, `comptime`, interpreter/runtime execution for real Kain logic, runtime contract emission, realtime bundle metadata, the compiler-owned intent suite (`law`, `patch`, `converge`, `world`, `entangle`, `orchestrate`), and the repo-local `z3/` proof pack for memory layout arithmetic, diagnostics span math, parser indexing, and signed literal conversion bounds
- [kain-actor](/M:/Code/Kain/crates/kain-actor): actor-system foundation for reusable Rust/native model types. `lib.rs` is only the public index; extend focused files such as `id.rs`, `message.rs`, `definition.rs`, `mailbox.rs`, `lifecycle.rs`, `supervision.rs`, `scheduler.rs`, `behavior.rs`, `registry.rs`, `system.rs`, and `native.rs`. `kain-core` consumes this crate for validated typed actor contracts and runtime actor IDs/messages while keeping AST ownership local to the compiler frontend.
- [kain-fs](/M:/Code/Kain/crates/kain-fs): portable filesystem crate for Kain-owned file operations. It exposes path helpers, text/byte reads and writes, append, atomic write, metadata, deterministic directory entries/walks, temp file/dir creation, copy/move/remove operations, SHA-256 hashing, scoped capability/sandbox resolution, `fs://project` / `fs://temp` / custom virtual mounts, range/chunk streaming helpers, polling watchers, best-effort transactional journals, and typed `FsError` values for strict or result-returning caller flows.
- [kain-input](/M:/Code/Kain/crates/kain-input): portable input crate for source provenance, typed events, frame/action reduction, text commits, axis values, binding maps, first-class `agent.intent` events, and deterministic traces/replay. Interpreter builtins in `kain-core`, `stdlib/input.kn`, and the native C kernel consume this shape.
- [kain-net](/M:/Code/Kain/crates/kain-net): portable networking contract crate for TCP endpoints, HTTP request/response specs, headers, server route specs, connection/listener/server/request/response handles, lifecycle state, and typed errors. Native Kain builds consume this through `stdlib/net.kn` plus the `abi_net_*`, `abi_tcp_*`, and `abi_http_*` C ABI.
- [kain-process](/M:/Code/Kain/crates/kain-process): portable child-process and PTY contract crate for executable path/argv, cwd/env overrides, stdio policy, process/PTY handles, lifecycle state, and captured output metadata. Native Kain builds consume this through `stdlib/process.kn` plus the `abi_process_*` C ABI.
- [blade crate](/M:/Code/Kain/crates/kain-blades): crate-like local workspace discovery for Kain blades. The Rust package/import crate is `blade`, while the source folder remains `crates/kain-blades` and workspace folders remain plural (`blades/*`). It treats `blades/*`, `apps/*`, and `crates/*` as interchangeable blade roots, resolves explicit `KAIN.toml` blade metadata plus synthetic `Cargo.toml` Rust blades, and feeds `kain equip`, Fabric steps, Kain module lookup, Rust crate FFI, C ABI FFI, and the blade build graph.
- [kain-build](/M:/Code/Kain/crates/kain-build): workspace build orchestration for blades plus the older Cargo `build.rs` helper surface. `src/workspace.rs` plans and executes C shared libraries, Cargo crates, GPU artifacts, Kain file/project/native-ui builds, Fabric validation/runs, and explicit Node/Bun/custom tasks from one DAG with canonical artifacts under `.kain/out/<host>/<lane>/<target>/<unit>/<task>/...`, stamps under `.kain/cache/build`, and JSON/JSONL reports under `.kain/reports/build`. Its artifact, cache, report, clean, fingerprint, and discovery IO is routed through `kain-fs`.
- [kain-run](/M:/Code/Kain/crates/kain-run): unified immediate execution crate behind `kain run`, `kain run dev`, `kain run plan`, `kain watch`, `kain blades run`, and `blade run`. It plans a `RunUnit` from an input, blade, or workspace manifest, consumes `[run]` fields (`entry`, `blade`, `target`, `args`, `env`, `cwd`, `watch`), executes Kain/C/Cargo/Fabric/Node/Bun adapters, uses `kain-fs` for IO/cache/watch/report paths, and uses `kain-process` shapes for process-backed report metadata. The CLI now auto-routes direct `kain run dev <component-or-native-world>.kn` inputs into the stronger native-ui dev loop when no conflicting blade/json/dry-run/app-arg flags are present.
- [kain-stdlib-map](/M:/Code/Kain/crates/kain-stdlib-map): deterministic native stdlib atlas generator behind `kain stdlib-map`. It parses only top-level root stdlib Kain files, merges `kain-core` builtins and native runtime service manifests, and emits compact JSON plus capsule-preview-style LLM markdown for surgical agent use.
- [kain-entangle](/M:/Code/Kain/crates/kain-entangle): deterministic state-coupling primitives for compiler-owned `entangle` metadata and interpreter/runtime graph policy
- [kain-ownership](/M:/Code/Kain/crates/kain-ownership): portable ownership-state kernel for Kain memory regions. It provides the shared state machine and conservative region policy consumed by `collapse`, `observe`, and `decay` syntax, typechecking, interpreter guards, LLVM runtime lowering, and the native C ownership ABI, with a local Z3 pack proving the core exclusivity and lifetime invariants.
- [kain-driver](/M:/Code/Kain/crates/kain-driver): target orchestration, shader bundles, hybrid JS/WASM artifact emission, native app materialization, packaged launcher snapshots, compute residency sidecars, derived HLSL/PTX shader sidecars, and thin embeddable frontend helpers such as `format_source`. Native and Tauri app manifests/snapshots now carry `std::reload` participant schemas for worlds, actors, and planned GPU swap boundaries so reload classification is package-contract-driven instead of sidecar-name-driven.
- [kain-commands](/M:/Code/Kain/crates/kain-commands): command brain for `kain`, `kn`, and standalone `blade`. It owns the `commands/index.toml` pack index, top-level command pack manifests such as `core.toml`, `run.toml`, `blade.toml`, `import.toml`, and `unreal.toml`, typed Clap routers, shared argument structs, command registry metadata, dynamic Clap rendering, conflict detection, launcher selection helpers, and the first runtime `[[commands]]` contribution loader/fallback. It must not own command implementation logic.
- [cli](/M:/Code/Kain/crates/cli): host binary and execution shell for `kain`, `kn`, and `blade`. It parses through `kain-commands`, dispatches handlers, prints results, sets exit codes, and calls domain crates such as `kain-driver`, `kain-build`, `kain-check`, `kain-test`, and `kain-repair`.
- [kain-check](/M:/Code/Kain/crates/kain-check) and [kain-test](/M:/Code/Kain/crates/kain-test): reusable source checking and compiletest-style suite harnesses behind `kain check` and `kain test`, kept separate from the CLI so IDE, CI, and future agents can reuse structured reports without shelling through command output.
- [kain-sys-codegen](/M:/Code/Kain/crates/kain-sys-codegen): native backend emitters, now including LLVM, Rust, C++, and an experimental direct C backend under `src/codegen_c.rs`. The LLVM backend emits compiler-owned intents, native stdlib bridges, multi-lane converge wrappers, static fast-lane direct paths, CPU-capability dynamic lane dispatch, benchmark-release orchestrate telemetry elision, and native machine-stone ABI calls for axiom predicates, pulse snapshots, local shattered arrays, and pointer teleport handoff. Return-path cleanup preserves shattered-array metadata while lowering sibling return branches so each branch emits `kain_machine_shatter_free(...)` instead of misclassifying machine-stone handles as generic RC pointers. Non-void callable lowering now consumes final block expressions and emits a typed `ret`, so expression-bodied enum `match` functions do not fall through to the fallback `unreachable`/`int3` path in optimized native builds. The direct C backend emits extern declarations for `@extern`, lowers actor `spawn`/`send` to the native stdlib facade, and registers entangle metadata at generated `main` entry.
- [kain-repair](/M:/Code/Kain/crates/kain-repair): profile-driven deterministic source repair engine consumed by the doctor/CLI repair lane; now split into a declarative rule registry plus a per-rule execution engine so repair policy stays visible and mode-aware; includes header normalization for parser-hostile `enum_` / `struct_` / `trait_` / `impl_` declaration forms
- [kain-host](/M:/Code/Kain/crates/kain-host): Rust embedding and native function registration
- [kain-codebase](/M:/Code/Kain/crates/kain-codebase): trusted-local workspace control for discovering roots, reading/writing/deleting/scanning files, JSON/TOML round trips, structured command execution, and first-class Node/Cargo/Python/C/TypeScript runtime operators. Its workspace file operations delegate to `kain-fs` so the trusted local helper layer does not fork path, directory, hash, copy, or write semantics.
- [kain-reflect](/M:/Code/Kain/crates/kain-reflect): reflection schemas and type identity
- [kain-sdk](/M:/Code/Kain/crates/kain-sdk): high-level embedding facade
- [kain-interop](/M:/Code/Kain/crates/kain-interop): shared buffer/image payload contracts
- [kain-foreign-abi](/M:/Code/Kain/crates/kain-foreign-abi): shared foreign ABI type graph and bridge classifier for import lanes. It owns normalized C scalar metadata, pointer depth/classification policy, callback-pointer metadata, raw-pointer ownership tags, safety reports, and its local Z3 pack.
- [kain-c-ffi](/M:/Code/Kain/crates/kain-c-ffi): C ABI import lane behind `use c::<module>`. It resolves `[c_ffi]` / `[[c_ffi.libraries]]`, extracts headers through `kain-import` plus regex fallback, consumes `kain-foreign-abi` for v2 raw API shapes, emits `.kn` extern modules, binding reports/manifests, and host bridge crates, then optionally builds/loads bridge cdylibs for interpreter/test/runtime lanes.
- [gpu](/M:/Code/Kain/crates/gpu): shader/compute backend crate. SPIR-V remains the canonical shader artifact backend, while `codegen_ptx.rs` emits raw compute-only PTX for `CompileTarget::Cuda` / `ptx` / `nvptx` without invoking CUDA Toolkit tools.
- [kain-gpu-runtime](/M:/Code/Kain/crates/kain-gpu-runtime): GPU compute executors consuming emitted shader bundles and residency metadata. Vulkan remains the portable executor path; `nvidia_ptx.rs` dynamically loads the CUDA Driver API on Windows and JIT-loads derived PTX in memory via the installed NVIDIA driver.
- [kain-ui](/M:/Code/Kain/crates/kain-ui): semantic UI graph and patch-oriented UI meaning
- [kain-ui-native](/M:/Code/Kain/crates/kain-ui-native): authored native desktop host/runtime lane. Default non-egui flow is `app.rs` -> `session.rs` -> `qt_host.rs`: compile Kain source into `UiBuildOutput`, project authored surfaces/nodes into `KainUiNativeSessionManifest`, and launch a thin Qt shell that renders only authored projection data. Do not add Rust-side UI catalogs, placeholder pane lanes, renderer switchboards, sample dashboards, or default widget layouts here; dead polluted/legacy hosts should be deleted rather than archived inside this crate.
- [native UI system C ABI](/M:/Code/Kain/runtime/native/include/ui_system.h): raw single-file LLVM UI substrate exposed through [stdlib/ui.kn](/M:/Code/Kain/stdlib/ui.kn). It gives Kain source enough low-level power to build its own UI system: sessions, windows, host frame presentation, arbitrary node kind strings, stable node keys for reload, parent/rect/text/style/state/flag mutation, generic pointer-state flags, accessibility metadata, font/texture/canvas/shader resource handles, generic resource byte upload, text measurement, clipboard, IME, drag/drop, menus, dialogs, focus, hit testing, events, dirty counts, hot reload generations, and draw commands. Generic node state cells are intentionally untyped-by-policy slots for Kain-authored shape, hit, draw, resource, and app session data; the runtime stores values but does not interpret them as components. The session kernel now keeps power-of-two hash sidecars plus occupancy bitsets for nodes, stable keys, styles, state cells, resources, menus, and dialogs so hot lookups and free-slot scans do not fall back to full linear walks. `draw_text` now carries an explicit font handle, and the runtime-owned host lane is deliberately passive (`software` / `memory`) while live presenters belong in blade-owned packages such as `blades/opengl`. Keep it generic; do not add baked component kinds or a catalog to this layer.
- [native graphics system C ABI](/M:/Code/Kain/runtime/native/include/graphics_system.h): raw LLVM/direct-C graphics substrate exposed through [stdlib/graphics.kn](/M:/Code/Kain/stdlib/graphics.kn). It gives Kain source enough low-level power to build its own 3D engine path: sessions, backend target selection, truthful Vulkan/D3D12 availability probes, SPIR-V shader modules, authored buffers, meshes, pipelines, draw commands, frame submission, and diagnostics. Keep it generic; do not add baked scenes, primitives, or default engine policy to this layer.
- [native net system C ABI](/M:/Code/Kain/runtime/native/include/net_system.h): raw LLVM/direct-C networking substrate exposed through [stdlib/net.kn](/M:/Code/Kain/stdlib/net.kn) and mirrored at the public `std.net`, `std.http`, `std.tls`, and `std.http2` entrypoints. It gives Kain source a handle-driven TCP and protocol-aware HTTP model: TCP connect/listen/accept/read/write, HTTP request/response handles, per-request protocol selection, response protocol inspection, local HTTP server listen/pump, actor route registration, request inspection, response writes, capability-state queries, local URL helpers, and status diagnostics. The current implementation lives in `runtime/native/src/core/net_system.c`; HTTPS client requests are Windows-first through WinHTTP, HTTP/2 client intent currently negotiates only on the secure client path, and server TLS, WebSockets, and HTTP/3 are intentionally follow-up layers.
- [native process system C ABI](/M:/Code/Kain/runtime/native/include/process_system.h): raw LLVM/direct-C child-process substrate exposed through [stdlib/process.kn](/M:/Code/Kain/stdlib/process.kn). It gives Kain source a handle-driven process model: process specs, argv/cwd/env overrides, inherit/pipe/null stdio modes, child spawn, PTY spawn, poll/wait/exit status, stdin writes, stdout/stderr capture, PTY output capture, resize, and status/error diagnostics. The current real implementation is Windows-first and lives in `runtime/native/src/core/process_system.c`; non-Windows hosts report explicit unsupported status instead of pretending parity exists.
- [native CPU/converge runtime ABI](/M:/Code/Kain/runtime/native/include/converge.h): raw LLVM/direct-C silicon-selection substrate exposed through [stdlib/runtime.kn](/M:/Code/Kain/stdlib/runtime.kn) and mirrored at `std.runtime`. `cpu.h` discovers CPU feature bits and maps capability keys such as `cpu.x86.avx2` / `cpu.x86.avx512f`; `converge.h` owns lane selection, a bounded tuning cache, telemetry ring writes, winner commits, and selector counters for compiled converge wrappers.
- [native machine-stones runtime ABI](/M:/Code/Kain/runtime/native/include/machine_stones.h): raw LLVM/direct-C substrate for `axiom`, `pulse`, `shatter struct`, and `teleport`, included by the native stdlib facade and mirrored through `std.runtime` telemetry helpers. `kain_machine_axiom_accept` gates target/arch/capability predicates against host CPU/runtime features, `kain_machine_pulse_start` registers a perpetual timer-thread callback while `kain_machine_pulse_snapshot` records beat/miss state, `kain_machine_shatter_alloc` creates contiguous 8-byte SoA lane payloads with `kain_machine_shatter_lane_base` exposing compiler-cached hot lane bases for proven loop indexes, and `kain_machine_teleport_ptr` records a zero-copy destructive handoff while preserving the pointer address returned to the destination world.
- [kain-ui-tauri](/M:/Code/Kain/crates/kain-ui-tauri): Tauri 2 desktop adapter generator, reflective bridge manifest builder, capability/permission preset catalog, and generated host/frontend scaffold for the webview desktop lane
- [kain-3D](/M:/Code/Kain/crates/kain-3D): native 3D renderer and viewport runtime

## Primary Data Flows

### Semantic execution flow

`Kain source -> lexer/parser -> comptime -> executable-body typecheck -> kain-core runtime/interpreter executes authored Kain logic`

This repo does not only compile source outward into foreign runtimes. `kain-core` is already an execution engine for meaningful language behavior:

- direct evaluation of functions, blocks, loops, assignment, field/index mutation, closures, and pattern matching
- async/await and future/poll semantics in the in-language runtime lane
- actor state initialization, message handling, runtime-side actor behavior, and `kain-actor` typed actor contract construction/validation
- JSX/UI expression evaluation and signal-driven UI contract execution
- runtime execution of `law`, `patch`, `converge`, `entangle`, and `orchestrate`, including law calls, converge verification, entangled state propagation, and patch transaction recording

When `kain run` succeeds, Kain is not merely validating authored source before handing work to another backend. In many cases it is executing the language's own semantic model directly. Treat that lane as a first-class truth source for what Kain code means.

### Compile and runtime bundle flow

`Kain source -> kain-core semantic analysis -> runtime contract / realtime app bundle / shader bundle metadata -> kain-driver materialization -> runtime/native and accelerated lanes consume the same bundle family`

The semantic-analysis part of that pipeline now includes real executable-body checks in `kain-core`, not only declaration registration. The compiler validates return values, call arguments, `match` arm type agreement, duplicate boolean arms, and `await` / `async` future typing before downstream codegen and bundle emission consume the typed program. That typechecked program also feeds the runtime/interpreter lane; bundle/codegen flows are downstream consumers of the same semantic truth, not a replacement for it.

That same frontend lane now owns six compiler-owned intent declarations:

- `law` lowers to callable invariant metadata through explicit `laws[]` contract sections, and the LLVM/C backends now emit laws as callable native-target functions.
- `patch` lowers to transactional mutation metadata with inferred undo mode plus explicit `patches[]` contract sections.
- `converge` lowers to dispatcher-plus-lane metadata with deterministic selection and executable `verify random(n)` verification through `converges[]`. Native LLVM now emits every eligible fast lane up to the backend cap, collapses purely static lanes directly, and routes CPU-gated lane sets through the native CPU/converge selector service.
- `world` lowers to shared state/surface projection metadata through sparse `worlds[]` entries and compiler-owned active-world selection.
- `entangle` lowers to single-writer state-coupling metadata through `entanglements[]` and the `state.entangle` capability; v1 interpreter semantics propagate authority endpoint writes to mirrors and reject direct mirror writes. The LLVM lane emits `@__kain_register_entanglements` calls into the native C runtime registry, and the direct C backend now emits a static metadata table plus a generated `__kain_register_entanglements()` thunk called from `main`.
- `orchestrate` lowers to strict typed stage metadata through `orchestrations[]`.

The runtime-contract and realtime-bundle families now both carry these explicit sections, and downstream adapters should consume them directly instead of reverse-engineering equivalent intent from local conventions.

Native target codegen for compiler-owned intents and filesystem calls is intentionally explicit:
`crates/kain-sys-codegen/src/codegen_llvm/mod.rs` owns LLVM lowering for semantic `Option`, `Result`, `Future`, `async` / `await`, `?`, `patch`, `law`, `converge`, `world`, `entangle`, `orchestrate`, `axiom`, `pulse`, `shatter`, `teleport`, and native stdlib wrapper signatures; `crates/kain-sys-codegen/src/codegen_c.rs` owns the experimental direct C source lane including string equality lowering for stdlib helper checks; `runtime/native/include/entangle.h` plus `runtime/native/src/core/entangle.c` provide the low-level C ABI registry; `runtime/native/include/machine_stones.h` plus `runtime/native/src/core/machine_stones.c` provide the machine-stones ABI; and `runtime/native/include/stdlib_abi.h` plus `runtime/native/src/core/stdlib_abi.c` provide the stdlib-facing C ABI facade consumed by LLVM/direct-C native builds.

### Native Stdlib And Runtime Facade

Native Kain builds now load the canonical root stdlib directly:

- The root `stdlib/` profile is loaded for `CompileTarget::Llvm` and `CompileTarget::C`. It exposes runtime init/shutdown, actor, entangle, filesystem, input, net, http, tls, http2, process, diagnostics, time, collections, result/status, intent, raw native UI, and raw native graphics helpers as Kain-callable declarations/wrappers.
- `stdlib/c` is loaded after the root stdlib only for direct C output and keeps C bridge helpers as `@extern` declarations.
- `runtime/native_core_runtime.toml` is the canonical production runtime manifest for normal file builds. It links only the Kain-owned lean C core, diagnostics, actor, async, entangle, filesystem-capable native stdlib facade, raw UI/graphics ABI, and related platform helpers.
- `runtime/native_runtime.toml` is now a lean compatibility mirror of `runtime/native_core_runtime.toml`. Keep it vendor-free so older discovery paths resolve to the same runtime surface instead of reviving archived app/vendor lanes.
- `runtime/BUILD.bazel` mirrors that contract for Bazel: `//runtime:native_runtime` aliases the lean `native_core_runtime` lane by default, while `//runtime:native_full_runtime` is only a legacy-named compatibility mirror target over the same lean source set.
- `runtime/fixtures/native_option_result_future/main.kn` is the focused LLVM fixture for native tagged `Option`, `Result`, `Future`, `async`, `await`, `?`, and unwrap-style helpers.
- `runtime/fixtures/native_world_actor_intent/main.kn` is the current all-in-one fixture proving `world`, `entangle`, `actor`, `patch`, `law`, `converge`, `orchestrate`, and stdlib facade calls through both `-t llvm` and `-t c`.
- `runtime/fixtures/native_fs/main.kn` is the focused filesystem fixture proving text writes/appends/reads, ranged text and byte-hex reads, temp directories, path joins, existence/type checks, structured metadata text, directory path listings, streaming copies, SHA-256 hashing, recursive removal, and native stdlib status plumbing through both `-t llvm` and `-t c`.
- `runtime/fixtures/native_input_actions/main.kn` is the focused input fixture proving semantic action bindings, held/released keyboard actions, CLI text commits, pointer axis scaling, first-class `agent.intent` events, and trace export through both `-t llvm` and `-t c`.
- `runtime/fixtures/native_net_http/main.kn` is the focused LLVM fixture proving authored Kain TCP plus HTTP server flows, actor route registration, request inspection, request/response protocol metadata, request-slot reuse, response writes, and native net status/reset behavior.
- `runtime/fixtures/native_process_stdio/main.kn` is the focused LLVM fixture proving process specs, argv wiring, piped stdout capture, stdin writes into a real child, PTY spawn/capture, and the native process status surface.
- `runtime/fixtures/native_ui_runtime_systems/main.kn` is the focused LLVM fixture proving a single Kain file can define a compact UI system over the raw native UI ABI: stable keyed nodes, host presentation, font/texture/canvas/shader handles, generic texture byte upload, text measurement, accessibility metadata, clipboard/IME/drag/menu/dialog state, hot reload generation, and draw submission without runtime-authored widgets.
- `runtime/fixtures/native_ui_stdlib_layer/main.kn` is the focused LLVM fixture for the first Kain-authored stdlib UI layer above the ABI. It proves stable reconciliation, layout helpers, style metrics/inheritance, generic state cells for arbitrary shape/hit/draw/resource payloads, render helpers, and authored pointer-state handling without adding a runtime widget catalog.
- `runtime/fixtures/native_graphics_engine/main.kn` is the focused LLVM fixture proving Kain-authored graphics sessions, Vulkan/D3D12 target probes, SPIR-V module registration, authored vertex/index buffers, mesh handles, pipeline handles, draw command submission, and two distinct authored mesh submissions through the raw native graphics kernel.
- `runtime/conformance/native_stdlib_bridge/test_native_stdlib_bridge.c` is the direct C conformance entrypoint for the facade itself.
- `runtime/conformance/process_runtime/run_tests.sh` is the direct C conformance lane for `abi_process_*`, covering argv/cwd/env wiring, stdio pipes, exit/wait, PTY attach/capture, resize, and honest unsupported-platform behavior.
- `runtime/conformance/net_runtime/run_tests.sh` is the direct C conformance lane for `abi_net_*`, covering TCP loopback, capability/protocol queries, HTTP server request/response, actor route registration, request-slot reuse beyond the old capacity wall, and HTTP client roundtrip.

### Networking Flow

`Kain std.net/std.http/std.tls/std.http2 -> stdlib/net/http/tls/http2 wrappers -> kain-net portable contracts -> runtime/native C net ABI -> raw sockets or platform HTTPS backend -> LLVM/direct-C executables`

The root `std.*` networking modules intentionally mirror the native profile so authored Kain code can treat networking as a built-in domain instead of a package hunt. `std.net` carries the raw TCP and shared HTTP handle surface, `std.http` carries the higher-level request/server helpers, `std.tls` carries the secure-client capability and HTTPS entrypoints, and `std.http2` carries protocol-selection intent plus negotiated-response inspection.

Two current runtime rules matter:

- Incoming server request handles are auto-released after `http_respond_*`, which fixes the earlier repeated-local-POST capacity failure and means request-slot reuse is now part of the durable conformance story.
- HTTP/2 is still an early client-side lane. The request can express `http/2`, capability state is queryable, and the secure WinHTTP path can report the negotiated response protocol; do not assume a full portable HTTP/2 session/runtime yet.

The LLVM native lane represents `Option<T>`, `Result<Ok, Err>`, and `Future<T>` as native tagged `i8*` handles at the ABI boundary rather than erasing them to their payload type. The C facade exposes constructors, tag checks, payload-copy helpers, ready-future/await helpers, and intent counters. This is the compatibility layer until the backend grows fully typed aggregate payloads for every `T`; for now, generated payload extraction is strongest for scalar payloads and focused fixture coverage.

LLVM ownership semantics now split one important fast path: ordinary non-extern RC parameters still behave as owned locals that release on scope exit, but authored non-extern `String` parameters are lowered as borrowed aliases when the frontend knows the callee signature. `crates/kain-sys-codegen/src/codegen_llvm/mod.rs` caches `strlen` once per borrowed string param at function entry, skips caller-side retain for those direct calls, and marks the callee local as borrowed so scope exit does not release it. Keep caller/callee coordination together: if the borrowed-string parameter mask or borrowed-local mark drifts, the string hot path either regresses back into retain/release ping-pong or reintroduces heap corruption.

LLVM ownership lowering also has a new compiler-owned `EphemeralLocal` provenance lane for fresh single-cell helper allocations that never escape their local ownership trace. When `crates/kain-sys-codegen/src/codegen_llvm/mod.rs` can prove the cell is only used through a balanced local `collapse` / `observe` / `decay` sequence, it materializes stack-backed byte storage, erases `__kain_alloc` plus ownership runtime calls, and lowers `mem_load` / `mem_store` directly against that storage. A stricter follow-up proof may also delete the fresh zero-fill itself: if the first ownership touch is a full-width dominating store before any read, the backend skips the `zeroinitializer` store and lets the first write define the entire cell. Treat this as a strict proof-backed lane, not a heuristic: if the trace stops being fresh, single-cell, non-escaping, or write-before-read, the lowering must fall back to the helper-owned or imported runtime paths.

### Filesystem Flow

`Kain fs_* calls -> kain-core runtime globals -> kain-fs portable crate -> stdlib wrappers -> runtime/native C facade -> LLVM/direct-C executables`

Interpreter-side file APIs delegate to `crates/kain-fs`, so strict globals can raise typed `FsError` values and `fs_try_*` globals can return structured result values. FS v2 also adds a runtime `FsSandbox` in `crates/kain-core/src/runtime.rs` for capability checks, host-path gating, and `fs://` mount resolution. New scoped helpers such as ranged reads/writes, streaming chunks/copies, watchers, and transactions resolve through that sandbox before touching the host filesystem. Watcher and transaction handles are process-local runtime registries owned by `kain-core`; the reusable semantics live in `crates/kain-fs/src/watch.rs` and `crates/kain-fs/src/transaction.rs`.

Native LLVM/direct-C builds load `stdlib/fs.kn`, which wraps the C facade in `runtime/native/src/core/stdlib_abi.c`. The current native parity surface includes ranged text reads, byte helpers encoded as lowercase hex, metadata encoded as key-value text, newline-delimited directory/walk path listings, and streaming copy. Keep the Rust and C lanes behaviorally aligned: `fs_hash_file` is SHA-256 in both lanes, deterministic walk/list order should remain stable, and new native-callable `fs_*` globals need precise `StdLib::new` return types so LLVM call lowering does not infer the wrong ABI. The encoded native helpers are a compatibility ABI, not a replacement for typed Kain records once native arrays/records/results are stronger.

Build and workspace orchestration also consume `kain-fs` now. Blade discovery/build, Fabric manifest/report IO, `kain check`, `kain test`, Omni staging, C/Rust FFI generation, import file discovery, and the trusted-local `kain-codebase` workspace helpers should use `crates/kain-fs` for reads, writes, deterministic directory entries, atomic artifact/report writes, copying, moving, removing, canonicalization, and SHA-256 hashing. Raw `std::fs` is still acceptable inside `crates/kain-fs` itself, native/runtime ABI adapters that must own OS handles directly, UE/vendor/demo surfaces, or narrow low-level process/file-handle code; do not reintroduce lane-local filesystem wrappers in the core build/import/FFI/workspace stack.

### Input Flow

`Raw host events -> target adapter -> kain-input event/session model -> stdlib/input.kn -> Kain source queries frames/actions/axes/text/trace`

`crates/kain-input` is the semantic owner. It reduces typed source events into `InputFrame` snapshots with semantic action pressed/down/released sets, frame-local axes, text commits, source provenance, and replayable traces. Source kinds are stable strings such as `human.keyboard`, `human.pointer`, `cli.stdin`, `ui.runtime`, `agent.intent`, `test.synthetic`, and `native.platform` so agents and runtime adapters can describe input without hardcoding target APIs into app code.

Interpreter execution registers `kain_input_*` bridge builtins in `crates/kain-core/src/runtime.rs` and root `stdlib/input.kn` exposes the public `input_*` helpers. LLVM/direct-C native builds now use that same root `stdlib/input.kn`, which wraps `runtime/native/include/input_system.h` and `runtime/native/src/core/input_system.c`. Keep adapters thin: Win32, web DOM, UE5 Enhanced Input, UI runtime, CLI stdin, and agent tooling should translate into this model instead of defining separate app-facing input systems.

### LLVM Native Actor ABI

`crates/kain-sys-codegen` now lowers actors against the canonical native runtime actor header instead of the legacy `KAIN_spawn` / `mq_*` lane.

- LLVM actor entrypoints use the canonical `(actor_id, mailbox, user_data)` signature and talk to `kain_actor_spawn_config_init`, `kain_actor_spawn`, `kain_actor_send`, and `kain_actor_receive`.
- The LLVM actor message and spawn-config layouts are emitted as explicit ABI types, so any change to `runtime/native/include/actor.h` must be reflected in codegen, `crates/kain-actor/src/native.rs`, and fixture validation together. `crates/kain-sys-codegen` depends directly on `kain-actor` for actor ABI sizing where possible.
- Native LLVM ask/reply now has a dedicated reply-port fast path instead of routing the return leg through the generic mailbox queue. The canonical handle shape is no longer a raw actor id in LLVM actor state; it is a first-class `KainActorRef = { actor_id, generation, execution_class, locality_class }`. `ask` / `ask_timeout` allocate a TLS-cached synthetic reply-port ref, handlers sending `reply_to.Reply(...)` lower to `kain_actor_reply_port_send_ref(...)`, typed waits use `kain_actor_reply_port_wait(...)`, and scalar `i64` waits keep `kain_actor_reply_port_wait_i64(...)`. The reply-port runtime no longer spawns a useless waiting OS thread; each successful ask rebinds a fresh synthetic actor generation so stale late replies are rejected on the next roundtrip. If actor roundtrips regress, inspect the actor-ref / reply-port seam before chasing mailbox micro-optimizations.
- Actor state ownership in the LLVM lane follows the native runtime contract: compiler-owned actor state is passed as `user_data` with `retain_user_data = 1`, while host/C/C++ callers default to borrowed `user_data` with no retain/release. Do not reintroduce unconditional `rc_retain` on actor `user_data`.
- Native actor ABI compatibility is queryable at runtime through `KainActorAbiDescriptor`, `kain_actor_abi_descriptor`, and `kain_actor_abi_descriptor_is_compatible`.
- `runtime/conformance/actor_runtime/run_tests.sh` is the broad native actor C conformance runner. It includes `test_actor_abi_contract.c`, which catches ABI drift, mailbox payload-size retention, user-data ownership mistakes, monitor tags, supervision snapshots, links, registry behavior, and scheduler statistics.

### Actor Crate Flow

`crates/kain-actor` is the shared actor-system model crate. It is intentionally independent from `kain-core` AST types so future native schedulers, LLVM lowering, C ABI glue, IDE tooling, and stdlib helpers can reuse the same vocabulary without importing the whole compiler frontend.

Current flow:

`Kain actor AST in kain-core -> typechecker resolves state/handler/method types -> TypedActor.actor_contract: kain_actor::ActorDefinition -> runtime-contract reflection and interpreter/runtime actor IDs/messages consume kain-actor primitives`

Keep new actor-system capabilities in focused `kain-actor/src/*.rs` modules, not in `kain-actor/src/lib.rs` and not as more hidden structs in `kain-core/src/runtime.rs`. `kain-core` should continue to own parsing, AST, typechecking, and interpreter execution; `kain-actor` should own reusable actor-system contracts, validation, mailbox/scheduler/supervision policy, behavior contracts, registry/address model, and native ABI descriptors.

### Formatting flow

`Kain source -> kain-core lexer/parser -> compiler-owned AST printer -> kain-driver helper -> cli format command`

The formatter now lives in `crates/kain-core/src/formatter.rs` and is intentionally compiler-owned. The rule is the same as the rest of the toolchain: editors, CLIs, and future LLM workflows should reuse the compiler printer instead of growing lane-local pretty-printers that drift from the actual grammar.

### Checking and source test flow

`Kain source tree -> kain-check discovery/frontend validation -> kain-test directive runner/runtime test lane -> cli check/test summaries and JSON reports`

`crates/kain-check` is the reusable typecheck-only pipeline. It discovers `.kn` / `.ks` files, skips build/generated folders, validates through `kain-driver::DriverSession`, and reports typed item counts, typed test counts, required runtime capabilities, and frontend errors without emitting backend artifacts.

`crates/kain-test` is the Rust-inspired compiletest-style harness. It parses `//@` and `#@` directives such as `check-pass`, `check-fail`, `run-pass`, `run-fail`, `kain-test`, `target`, `error`, `ignore`, `skip`, and `known-bug`. It should own future snapshot, revision, bless/update, and target-conditional behavior rather than letting one-off scripts duplicate suite semantics.

The runtime test lane now recursively executes `test` items nested inside typed modules. `kain test` reports skipped cases separately from failed cases, and `--ignored` reruns ignored cases when the operator wants to burn down known-bug inventory.

### Host bridge flow

`.kn source -> compiler/runtime contracts -> host bridge crates (Python, Node, C ABI, Rust crate FFI, codebase control) -> shared payload contracts via kain-interop`

Bridges exist to expose external capabilities cleanly. They should not become the default place to hide logic that Kain can already express and execute itself. Prefer Kain-owned logic for domain behavior, control flow, state transitions, and semantic contracts; use bridge crates for foreign APIs, packaged dependencies, and target-native runtime services.

`kain-codebase` is the trusted-local workspace authority layer, not a read-only indexer. Its default mode is `trusted-local`, which is allowed to mutate files and run toolchains for private/dev work. Future public or user-authored plugin policies should be host-side reviewed/curated modes layered above this crate, not fake safety inside the core local bridge.

The CLI exposes the same layer through `kain codebase inspect <path> --json` and `kain codebase run <cwd> -- <command> ...`. The run command is a structured capture operator: the Kain process succeeds when it captured stdout/stderr/status correctly, and callers must inspect the child `success`/`status` fields.

Current native-ui packaging rule for C ABI imports:

- `kain-c-ffi` now routes C type classification through `kain-foreign-abi`. The v2 bridge can represent callback typedef parameters, inline callback pointers, multi-level pointers, raw scalar pointers, arrays-as-pointers, and byte-buffer pointer returns as explicit `Any`-surface handles instead of rejecting them during extraction. By-value aggregates are captured in the ABI graph but still gated from callable lowering until layout metadata is known.
- `kain-c-ffi` is no longer only an `Interpret`/`Test` lane concern. The Rust/native-ui packaging lane now emits packaged bridge manifests, copies bridge/shared-library sidecars into the app artifact set, and has the generated native app launcher load those packaged bridges before boot.
- This does not mean the current native UI host is a full general-purpose Kain interpreter. The lane is still bundle-driven; the packaging change makes foreign bridge dependencies explicit and shippable rather than hidden behind cache-local host-backed behavior.

### Blade workspace flow

`workspace root -> blade crate discovers blade roots -> kain-build plans a typed task DAG -> CLI, Fabric, filesystem modules, Rust crate FFI, C ABI FFI, Cargo, GPU, Node/Bun, and reports consume the same blade metadata`

The blade system is intentionally local-first and crate-like. A `KAIN.toml` can declare `[blade]` metadata and optional `rust`, `cffi`, `fabric`, and `gpu` sections, while a plain `Cargo.toml` under `crates/*`, `apps/*`, or `blades/*` is still exposed as a synthetic Rust blade. This makes a Kain blade able to live beside Rust crates, and a Rust crate able to be equipped from blade-aware workflows without duplicating path rules.

`kain equip <blade>` is the human-facing resolver. `kain blades list`, `kain blades graph`, and `kain blades check` are the inspection and validation surface. `kain blades build .` and the standalone `blade build .` command are the build surface; `kain blades run [blade]` and `blade run [blade]` are the immediate execution surface through `kain-run`. Runtime code should call the `blade` crate, `kain-build`, or `kain-run` instead of re-scanning folders directly; future remote update/install work should extend these crate boundaries rather than inventing a second registry path.

Blade module resolution now merges module roots from ancestor workspaces as it walks upward from the current directory. That means a runnable blade launched from `blades/<name>/src` can still import sibling blades declared by the repo-root workspace instead of only seeing its own local blade root.

Build artifacts are deliberately workspace-local and disposable:

- `.kain/out/<host>/<lane>/<target>/<unit>/<task>/...` owns canonical build products, including Kain file/project/native-ui outputs, C sidecars, Cargo target dirs, GPU bundles, and per-task `kain-artifacts.json` manifests.
- `.kain/cache/build/stamps/*.stamp` owns task fingerprints over inputs, adapter settings, outputs, profile, and target.
- `.kain/reports/build/session-*.json` plus `.jsonl` owns build reports and event streams.

`[build]` in root or blade `KAIN.toml` can override `artifact_root`, `cache_root`, and `profile`, while CLI `--lane bootstrap|dev|release|dist|selfhost` chooses the invalidation lane. `[[build.tasks]]` can add explicit tasks with `kind`, `entry`, `manifest`, `command`, `args`, `cwd`, `inputs`, `outputs`, and `depends_on`. Prefer manifest-declared tasks over lab-local build scripts.

### Selfhost lanes

The repo now has two explicit selfhost lanes under the same `kain selfhost` control plane:

- the Rust mirror/reference lane
- the hand-written owned bootstrap/native lane

The hand-written lane is the promotion target. The Rust mirror lane is reference and oracle infrastructure.

#### Rust mirror/reference lane

- The active owned language surface still lives under `src/core`, but the mirror lane materializes reference output under `src/.rustimport/phase2`.
- `src/.rustimport/reference` is the moved donor corpus from the earlier Rust import lane; do not hand-edit it.
- `crates/kain-import` imports Rust selfhost crates per source file/module and exposes per-file typed `Program` results.
- `crates/cli/src/selfhost.rs` consumes those file-level imports through a data-driven `SelfHostSourceProfile`.
- The default profile lives at `ouroboros/docs/selfhost/metadata/selfhost_source_profile.json`.
- The current mirror priority is still executable-first: the default phase2 profile keeps `cli` ahead of `kain-sys-codegen` so executable parity is proven before backend expansion.
- Phase output emits three aligned artifact families:
  - canonical Kain mirrors under `src/.rustimport/phase2/<crate>/...` or the profile-configured canonical root
  - output-local mirror copies under `<phase-output>/mirror/src/<crate>/...`
  - a `source_correspondence_manifest.json` that records exact Rust-to-Kain path correspondence per file
- Phase2 still writes aggregate `<crate>.kn` and `<crate>.roundtrip.rs` compatibility artifacts because the active frontend/codegen path is still single-source-string oriented.
- `kain selfhost phase1|phase2 --force` keeps emitting mirrors, manifests, aggregate bundles, and any later-crate artifacts even when earlier crates fail. The command still reports `hard_fail`, but it no longer discards the partial artifact graph.

#### Hand-written bootstrap/native lane

- `src/core` is the canonical owned compiler surface.
- `src/KAIN.toml` is the canonical hand-written selfhost contract.
- `runtime/native_core_runtime.toml` is the canonical lean runtime contract for ordinary native builds and Bazel validation.
- `runtime/native_runtime.toml` is only a compatibility mirror of the canonical lean runtime contract; the owned bootstrap lane should still resolve to the same vendor-free runtime surface by default.
- `kain selfhost bootstrap` is the owned lane entrypoint, and `src/build_selfhost.sh` is only a thin wrapper around that CLI path.
- `src/.selfhost/` is the canonical artifact/report root for the owned lane.
- The owned lane may temporarily assemble an aggregate bootstrap source from ordered `src/core` files, but that aggregate source is a compatibility bridge, not the end-state module system.
- Current state on 2026-04-15: `kain selfhost bootstrap --combine-only` succeeds and emits the combined owned source plus reports, but `--emit-llvm-only` still fails with parser errors in the current owned `src/core` source set, concentrated in `runtime.kn` and `types.kn`. That is the present blocker for a native self-build.
- The architectural boundary is strict:
  - Rust is allowed to own manifest loading, filesystem/path/env/process helpers, report emission, and runtime artifact discovery during bootstrap.
  - Rust must not remain the permanent owner of parser, typechecker, lowering, or backend/codegen logic on the real compile path.
  - The native C runtime is not optional in this lane. The produced native `kainc` is expected to link against the real runtime bundle defined by the runtime manifests, not a guessed `-lkain_runtime` string or a Rust-host substitute.

The important rule is: the mirror lane proves reference and repair behavior, while the owned lane proves real selfhost direction. Keep the Rust mirror lane available for comparison, but promote the manifest-first hand-written lane as the compiler that is supposed to survive once bootstrap is over.

### Compute pipeline flow

The current compute direction is:

- authored compute truth starts in shader `comptime` metadata in `kain-core`
- `kain-core` emits workgroup, dispatch, tensor, stream, and neural metadata into realtime/shader-facing bundle structures
- `kain-driver` materializes compute residency sidecars and native app packaging data
- `runtime/native` validates and surfaces `primary_compute`
- `kain-gpu-runtime` is the real dispatch bridge that consumes emitted shader/residency artifacts; Vulkan is the portable path and the NVIDIA PTX path uses derived bundle PTX with the CUDA Driver API instead of `nvcc`

The architecture rule here is important: runtimes may execute the compute plan, but they must not become the source of truth for what that plan is.

SPIR-V remains the canonical shader bundle payload. PTX is a derived compute-only peer output: `CompileTarget::Cuda`, `ptx`, and `nvptx` all map to raw PTX text, the bundle writer emits optional `.derived.ptx` sidecars for compute shaders, and the runtime path loads that PTX directly from memory when an NVIDIA driver is available. The PTX emitter deliberately targets conservative `.version 7.8` / `sm_50` for broad driver JIT compatibility even though NVIDIA's current CUDA 13.2 documentation describes PTX ISA 9.2; raise that default only when the emitted instruction set actually requires newer PTX.

### Shader Canvas Lane

The native shader-canvas UI lane now follows this contract:

- `kain-ui` owns authored semantic surfaces and shader-canvas intent on canvas-like nodes
- `kain-core` emits explicit `shader_canvases` entries in `RealtimeAppBundle` so hosts do not have to rediscover shader-canvas bindings by guessing from local UI props
- `kain-core` also emits first-class shader-canvas text resources per surface: font atlas descriptors, text runs, declared runtime resource bindings, and optional asset-backed font references through the shared realtime asset catalog
- `kain-driver` materializes shader bundles and native app sidecars that keep shader-canvas metadata, shader refs, native UI bundles, and packaged realtime font assets aligned, resolving relative realtime asset sources against the authored source root instead of the materialization working directory
- `kain-ui-native` resolves shader canvases from realtime bundle metadata first and only falls back to surface-local shader refs when metadata is missing
- `kain-ui-native` now turns the shader-canvas text contract into real GPU inputs by serializing atlas/text metadata into the surface storage buffer and synthesizing a host-provisioned packed atlas texture, preferring packaged realtime font assets first, then `ab_glyph` rasterization from data-driven system-font aliases, with bitmap fallback and cache reuse across repeated surfaces that share atlas content
- `smoketest/UI/spv_ui_surface_probe` is the canonical native proof for this lane: it authors a real `<canvas>` node, packages a relative font asset, emits SPIR-V, and shows a fragment shader sampling the runtime-provided packed atlas texture
- canonical native shader payload remains SPIR-V, while the current WGPU native host may consume derived WGSL or runtime-transpiled WGSL from the same bundle family

The architecture rule here is the same as the viewport and compute lanes: shader-canvas execution can optimize presentation, but it must stay subordinate to compiler-owned bundle truth rather than inventing a renderer-local UI shader dialect.

### UI Runtime Boundary

The native runtime no longer owns a compile-backed UI vendor slice. The active C lane is intentionally narrow:

- `runtime/native` owns only the passive UI/runtime substrate: `ui.bundle`, `ui.component`, `ui_system`, hot reload, and host attachment seams. It does not own a baked widget catalog, OpenGL presenter, or compile-backed UI vendor trees.
- Archived runtime-owned UI/OpenGL/vendor material now lives under `.zarchive/runtime_devendor_2026-05-15/` for salvage only. Future richer presenters should land as blades, packages, or Rust hosts above the ABI instead of re-entering the C runtime floor.
- `crates/kain-ui` now emits explicit backend-role truth in runtime metadata and per-surface preferences through `UiHostBackendKind`, `UiLayoutEngineKind`, and `UiRenderEngineKind`
- `crates/kain-ui-native` now defaults to a no-`egui` Qt Quick host. The old 9k-line host survives in `src/legacy_egui.rs`, but only behind the explicit Cargo feature `legacy-egui`; default builds keep the bundle/build API, materialize a Qt session manifest plus generated `Main.qml`, and launch the external `qml` / `qmlscene` runtime when it is available on the host
- The current non-`egui` host is still compatibility-first, but it no longer drops every packaged app into one fixed Qt workstation frame. Generic packaged apps now render the compiled authored layout through `UiNativeProjection`, with a fallback projection synthesized directly from `output.tree` when the bundle does not carry the sidecar explicitly.
- The generated Qt shell skin is a smoke/demo presentation, not the authored UI contract. Kain UI meaning still lives in `UiStyleSpec`, `UiThemeRegistry`, surface roles, and bundle metadata; the host should consume those inputs rather than become the source of a fixed look.
- The Qt host now supports deterministic smoke artifacts through `KAIN_UI_NATIVE_QT_ARTIFACT_DIR` and `KAIN_UI_NATIVE_QT_SCREENSHOT_PATH`; this lets repo-local smokes render the real generated shell offscreen, save `Main.qml` plus `session.json`, capture a PNG, and exit without operator interaction
- `smoketest/UI/qt_plasma_runtime_lounge` is the current proof surface for that contract: a curated runtime bundle with document, viewport, devtools, and browser panes that exercises the live Qt host and produces `outputs/qt_plasma_runtime_lounge.png`
- `crates/kain-ui` and `crates/kain-core` now also recognize `UiHostBackendKind::Tauri`, including authored `host_backend="tauri"` and `host_backend="webview"` aliases. Shell/document/devtools surfaces use browser/webview semantics while viewport and shader surfaces stay on the existing canvas/GPU path.
- `crates/kain-ui-tauri` is now the data-driven Tauri 2 adapter crate. It owns plugin/capability/permission preset enums, bridge-manifest construction, reflection merge logic, hybrid frontend bridge JS, and generated `src-tauri/*` scaffolding instead of pushing that policy into the CLI.

### Semantic Tab Workspace Lane

Semantic authored tabs now follow the same ownership rule:

- `kain-ui` and `kain-core` own tab intent through authored node metadata such as `tab_group_id`, `tab_label`, `tab_order`, `tab_default_active`, and `persistent_layout_id`
- `kain-ui-native` may render that intent as native clickable tab chrome, but it should resolve and persist the active selection through `output.systems.workspace_layout.active_tabs`
- host-side tab rendering is allowed to optimize presentation, but it must not invent a second tab schema or bypass the emitted UI/runtime bundle truth

`smoketest/UI/kinetic_ui_atlas` is now the durable repo-local proof for this lane: a fresh four-page native executable that uses semantic top tabs, docked shells, shader canvases, and a real viewport workspace without reusing the older smoketest compositions.

### Reload-Safe UI Contract Lane

UI reload and derived-value semantics now follow an explicit compiler-to-runtime contract:

- `kain-core` emits stable signal ids, computed lowering (`writes_signal`, runtime `expr`, invalidation targets, scheduler phase), and event-route metadata including route ids, command routes, and transaction labels
- `kain-ui` owns runtime execution of those contracts, including derived recompute, exact invalidation, hot-reload state transfer, reload patch reporting, and bounded reconciliation
- `realtime_app_bundle.ui_contracts` now exposes computed, event-route, reload, focus, selection, overlay, motion, and workspace payloads so downstream native hosts can inspect semantic truth without rediscovering it from local widget state

The architectural rule is the same as every other lane: reload behavior may be optimized by hosts, but the host must not become the source of truth for identity transfer, derived state, focus/selection state, or transaction semantics.

`UiRuntimeBundle.native_projection` is now a compatibility-only sidecar rather than a normal bundle contract surface. Canonical serialization keeps `output.tree` and `output.systems` authoritative and omits the projection when it is empty; legacy raw-native consumers should opt into the explicit projection helper when they still need the flat view.

### Native Packaging And Operator Loop

The native packaging lane is the operator-facing loop for UI iteration:

- `kain-driver` materializes native apps as a package set, writing the runtime bundle, runtime contract, realtime bundle, `app_manifest.json`, `runtime_snapshot.json`, and any required sidecars into the app artifact tree.
- Generated launchers resolve those packaged sidecars beside the executable so the app boots from authored bundle truth instead of a debug-host template.
- The runtime snapshot is the reload/control surface, not hidden launcher state. It carries explicit provider, session, workspace, command, and capability records, including the `runtime.reload` command already emitted by the packaging path.
- `stdlib/reload.kn` is the canonical author-facing reload surface for new Kain code. Today it wraps the existing native UI reload ABI; going forward it is where world, actor, and GPU participation becomes user-visible without baking app policy into the language core. The current public surface is intentionally getter-oriented for policy/lane text instead of returning one fat aggregate, because the LLVM path still needs a deeper fix for multi-`String` aggregate return trustworthiness.
- `crates/cli/src/native_ui_dev.rs` is the canonical desktop dev loop for this lane. `kain native-ui dev <file.kn>` materializes once, launches the child app, watches the input file's parent directory recursively, ignores generated/project artifact trees plus common temp files, and debounces save bursts before rebuilding.
- Native app manifests now emit `hot_reload.participants`, and runtime snapshots mirror that under `reload`. The participant contract inventories the `std::reload` package surface, migration defaults, world state schemas, actor state/message schemas, planned GPU hook boundaries, default restart mode, and the declared compatibility lanes so reload decisions compare semantics instead of only launcher identity.
- The native packaging lane is now host-selectable instead of Qt-only. `NativeUiHostKind` currently supports `qt` and `tauri`, with Qt remaining the default and Tauri selected through `--host tauri` or config.
- The Tauri host path is package-first, not a new compile target. `kain-driver` compiles the existing native/runtime-contract truth plus hybrid JS/TS/WASM artifacts, then materializes a node-free `src-tauri/` project, static frontend payload, bridge manifest, permissions, capabilities, `app_manifest.json`, and `runtime_snapshot.json` under one generated app root.
- Tauri dev launches from a Cargo manifest instead of a prebuilt executable. `native_ui_dev` now abstracts launch targets as either `Executable(...)` or `CargoManifest(...)`, which lets the same watcher/reload classifier drive both Qt and Tauri without forking the whole loop.
- Reload behavior is now explicit instead of host-local. The dev loop classifies each rebuild as `Noop`, `HotReloadInProcess`, or `RestartProcess` from emitted runtime compatibility metadata plus changed artifact roles such as runtime bundle, runtime contract, realtime bundle, shader sidecars, app manifest, and runtime snapshot.
- The emitted reload transition lattice is richer than the operator triad: packaging/runtime metadata now distinguishes `cold-start`, `noop`, `presentation-only`, `structural-migrate`, `quiesce-and-migrate`, `frame-boundary-gpu-swap`, and `restart-with-restore`, with explicit reasons/actions carried beside the class.
- `kain run dev <file.kn>` now promotes simple native UI Kain files into that same dev loop automatically instead of defaulting to a generic rerun watcher. The current auto-route is intentionally conservative: direct `.kn` inputs only, no extra app args, no blade selection, no `--json`, and no `--dry-run`.
- Compatible rebuilds preserve live UI/workspace/session state through the existing runtime reload contract. Incompatible rebuilds restart the packaged child and restore only from the persisted manifest/snapshot boundary.
- V1 honesty rule: actor/world compatibility is structural today. Default-value edits that keep the emitted participant schema stable stay hot-reload eligible; schema changes flip the lane to restart. Full GPU resource-graph swap remains a planned participant hook, not a delivered in-process reload promise yet.
- Attrition now has a reload-semantic certification lane beside the older teardown-closure gates. `attrition/cases/kain_std_reload_contract/main.kn` proves generation monotonicity, begin/commit sequencing, checkpoint/progress accounting, and sabotage detection for `std::reload`; closure/RC drift exposed by the string-heavy reload helpers is tracked as a separate LLVM/runtime follow-up instead of being silently normalized.
- Devtools and inspectors must stay opt-in and remain represented in packaged truth, not injected as default product chrome.
- When a packaged launch stops reflecting a change, check the materialized manifest and snapshot sidecars first. That is the stable operator boundary before assuming the host itself is wrong.
- target-aware world selection now resolves native desktop targets against `native_ui`, web targets against `web`, and UE5 targets against `ue5`; ambiguous multi-world cases must use an explicit `--root` selection.

### Viewport Contract Lane

Viewport startup intent now follows the same compiler-owned pattern:

- `kain-core` emits `render.scenes` bindings with authored scene ids plus optional camera and presentation metadata
- `kain-ui-native` and `runtime/native` consume those bundle defaults first and only fall back to local scene/profile defaults when the bundle leaves a field unspecified
- scene ids, shader refs, camera presets, and presentation presets should travel together through the realtime bundle instead of being re-guessed independently by each host
- the Win32 raw-native viewport now boots a `KainRuntimeRendererSession` from the runtime graphics bundle and the `KAIN_RUNTIME_RENDERER_BACKEND` env var, then surfaces requested backend, active backend, service key, vendor runtime/version, and compatibility-executor diagnostics directly in the viewport overlay
- renderer-session ownership currently means backend identity and diagnostics are real even when scene execution still routes through a compatibility executor; do not claim native direct vendor rendering until the host path actually hands viewport execution to the vendor bridge
- `kain-3D` now owns the reusable manipulator drag contract as well: screen drag, axis/plane constraints, snap application, and local-vs-world transform math live in `crates/kain-3D/src/interaction.rs`, while `kain-ui-native` should stay a host/input forwarder instead of carrying a second copy of viewport-edit math
- `kain-3D` owns primitive support as an authored mesh ingestion system, not as a Rust shape catalog. `crates/kain-3D/src/primitive.rs` validates Kain-authored primitive identities, metadata, registries, and explicit geometry; it must not grow built-in cube/sphere/capsule/torus builders or default primitive libraries.
- authored `.kn` code reaches that seam through the `zen3d` prelude and the generic runtime-native `__zen3d_triangle_geometry` binding. Kain source owns primitive recipes and vertex/index data; Rust owns validation, conversion into `Geometry`/`Mesh`, scene attachment, and runtime consumption.
- raw LLVM/direct-C Kain code reaches the lower-level engine-building surface through `stdlib/graphics.kn` and `graphics_system`. That layer is intentionally below `kain-3D`: it records authored buffers, SPIR-V modules, meshes, pipelines, backend targets, and draw commands, but does not manufacture a scene, geometry, material, camera, or engine loop.

## Important Folders By Intent

- [runtime/native](/D:/Kain-Lang/runtime/native): canonical vendor-free C runtime and ABI/service floor; keep this lane mechanism-only and free of hardcoded apps, vendored trees, and baked presenters
- [.zarchive/runtime_devendor_2026-05-15](/D:/Kain-Lang/.zarchive/runtime_devendor_2026-05-15): archived runtime vendor/app/OpenGL/asset material preserved for salvage and package extraction, but not part of the active build or manifest surfaces
- [runtime/conformance](/M:/Code/Kain/runtime/conformance): lane-level conformance harnesses
- [runtime/parallel](/M:/Code/Kain/runtime/parallel): Rust/Zig companion runtime work that must stay aligned with the native runtime doctrine
- [docs/kainplan](/M:/Code/Kain/docs/kainplan): legacy design and execution docs
- [docs/pipeline](/M:/Code/Kain/docs/pipeline): legacy pipeline notes and operational docs
- [labs](/M:/Code/Kain/labs): focused validation labs
- [labs/blades_workspace_smoke](/M:/Code/Kain/labs/blades_workspace_smoke): full local blades workspace smoke with root `KAIN.toml`, `apps/*`, `blades/*`, and `crates/*`, covering app, Kain library, C ABI, Rust crate, synthetic Cargo, GPU metadata, Fabric `blade = "..."`, `kain equip`, `blade build`, CPU Fabric execution, build-cache hits, GPU artifact generation, and the Cargo-built `blade_singularity_atlas` executable that renders a workspace/shader atlas from the generated artifacts
- [labs/playground/piano](/M:/Code/Kain/labs/playground/piano): Linux-native 2D piano lab that drives the semantic UI surface through a C audio bridge, note playback, and loop recording
- [labs/llvm_world_dogfood_lab](/M:/Code/Kain/labs/llvm_world_dogfood_lab): canonical LLVM dogfood lab that exercises world, patch, converge, orchestrate, actor mailbox traffic, and native UI + viewport rendering from one authored entrypoint
- [labs/llvmzone](/M:/Code/Kain/labs/llvmzone): five-app LLVM utility lane that keeps separate executables for enum/match, world/patch/orchestrate, actor mailbox, float/bitwise, and native UI/viewport coverage
- [labs/chronos_native](/M:/Code/Kain/labs/chronos_native): first native Chronos proof app for the `kain native-ui dev` loop, combining compiler-owned world state, native UI shells, viewport3d authoring, and packaged shader sidecars from one Kain source file
- [labs/threejs_node_ffi_space_lab](/M:/Code/Kain/labs/threejs_node_ffi_space_lab): Node-first localhost sculpt-suite proof that now dogfoods Kain hybrid codegen too, layering manifest-driven Three.js viewport modes, a Kain-authored JS + WASM motion bundle, and a Rust-built WASM brush core behind the `std::javascript::bridge` orchestration seam
- [generated](/M:/Code/Kain/generated): disposable generated outputs

## Common Commands

Prefer the live CLI and source over stale docs when they disagree.

Typical commands:

- `python3 install_kain.py`
- `py install_kain.py`
- `kain doctor`
- `kain doctor --repair <file>`
- `kain doctor --repair-tree <dir>`
- `kain build`
- `kain build native-ui <file.kn>`
- `kain build native-ui <file.kn> --host tauri`
- `kain native-ui dev <file.kn>`
- `kain native-ui dev <file.kn> --host tauri`
- `kain run <file.kn>`
- `kain run <file.c> --target c -- <args>` for hidden cached C compile and execute
- `kain run dev <input>` and `kain watch <input>` for the run watcher loop
- `kain run plan <input> --json` for target/blade/manifest run-plan inspection
- `kain format <file.kn>`
- `kain fmt --check <file.kn>`
- `kain commands list --bin kain`, `kain commands list --bin kain --runtime`, and `kain commands export --bin blade` for command registry inspection
- `kain gpu-artifacts <file.kn> --output <dir>`
- `kain selfhost phase1`
- `kain selfhost phase2` for the bounded self-host repair lane
- `kain selfhost phase2 --emit-roundtrip-rust false --assemble-stage2 false --build-stage2 false` for mirror-only validation without forcing the roundtrip/build lane
- `kain selfhost phase2 --force` to keep partial selfhost artifacts even when one crate trips the current phase2 blockers
- `kain selfhost phase2 --all-crates --emit-roundtrip-rust false --assemble-stage2 false --build-stage2 false --force` to mirror every discovered `crates/*/Cargo.toml` workspace crate into `src/.rustimport/phase2/` and preserve the full forced artifact graph
- `kain selfhost bootstrap --combine-only` to validate `src/KAIN.toml` resolution and ordered `src/core` source assembly
- `kain selfhost bootstrap --emit-llvm-only` to validate the owned lane through LLVM plus native sidecar staging without linking
- `kain selfhost bootstrap --backend c --emit-llvm-only` currently means "emit selected backend only" and exercises the parallel experimental C lane without native link or ouroboros verification
- `kain selfhost bootstrap --link-native` to drive the owned lane through native runtime resolution and final link
- `kain selfhost bootstrap --verify-ouroboros` to run the first native self-recompile/parity check
- `kain omni build`
- `kain check <file-or-dir>` and `kain test <file-or-dir>` for structured source checking and compiletest-style pass/fail suites
- `kain blades list`, `kain blades graph`, `kain blades check`, `kain blades build .`, `kain blades run [blade]`, and `kain equip <blade>` for blade workspace inspection, build orchestration, execution, and resolution
- `blade build . --json` for the standalone blade build command. Use `--clean` for safe cleanup of workspace-local `.kain` build/cache/report roots and `--include-vulkan` only when a machine has a working Vulkan compute runtime.
- `blade run [blade] --target auto -- <args>` for standalone blade execution through the same `kain-run` pipeline.
- `python labs\blades_workspace_smoke\scripts\run_blades_smoke.py` for the full blade workspace smoke. Set `KAIN_BIN` and `BLADE_BIN` when validating against freshly built non-default target dirs.
- `kain fabric init --template polyglot`
- `kain fabric validate`
- `kain fabric run`
- `python3 scripts/python/validate_dcc_parity_matrix.py`
- `kain import-c`, `kain import-rust`, `kain import-ts`, `kain import-asm`, `kain import-crate`
- `kain import crates [workspace-root]` for workspace-scale Rust bundling or `kain import crates [workspace-root] --blades` to mirror each Cargo crate into a blades-style `.kn` tree
- `python tools\typescript_import\extract_ambient_manifest.py` to regenerate the embedded TypeScript ambient manifest from `reference/TypeScript-main/src/lib` plus `tools/typescript_import/typescript_ambient_overrides.json`
- `kain --strict import-ts <input>` to fail on degraded generated Kain output while still writing the structured import report JSON
- `kain runtime build`
- `kain runtime validate`
- `python tools/bazel/sync_rust_builds.py`
- `python tools/bazel/sync_rust_builds.py --check`
- `bazel build //:kain --config=dev`
- `bazel build //:kn --config=dev`
- `bazel build //:blade --config=dev`
- `bazel test //crates/kain-build:unit_test --config=dev`
- `bazel test //:developer_smoke_tests --config=dev`
- `bazel test //:workspace_diagnostic_tests --config=dev`
- `py -3 tools/bazel/sync_native_runtime_builds.py`
- `py -3 tools/bazel/sync_native_runtime_builds.py --check`
- `bazel build //runtime:all`
- `bazel test //runtime:native_runtime_tests`
- `kain run blades/kain-mcp`
- `kain run plan blades/kain-mcp`
- `kain blades build blades/kain-mcp --json`
- `D:\Kain-Bazel\bin\kain.exe run D:\Kain-Lang\blades\kain-mcp`
- `./runtime/fixtures/validate_all.sh`
- `powershell -ExecutionPolicy Bypass -File runtime\fixtures\validate_all.ps1`
- `./runtime/conformance/run_all.sh`
- `powershell -ExecutionPolicy Bypass -File runtime\conformance\run_all.ps1`
- `./runtime/validate_native_runtime.sh`
- `powershell -ExecutionPolicy Bypass -File runtime\validate_native_runtime.ps1`
- `py -3 scripts/python/launch_kain_mcp.py` when you explicitly want the managed-sync wrapper or launcher trace breadcrumbs
- `powershell -ExecutionPolicy Bypass -File scripts/windows/sync-kain-source-of-truth.ps1`
- `cd tools/kain-flight-control && go test ./...`
- `powershell -ExecutionPolicy Bypass -File smoketest/allinone/run_all.ps1`

Runtime validation meaning:

- `cargo test -p kain-sys-codegen --test llvm_codegen_test -- --nocapture` is the backend IR/codegen proof lane.
- `PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1 cargo check -p kain-core -p kain-c-ffi -p kain-sys-codegen -p kain-driver -p cli` is the current fastest end-to-end validation path for new backend plumbing on this machine because local Python 3.14 is newer than the repo's pinned PyO3 support window.
- `cargo build -p cli` builds the Rust compiler host only. It does not precompile the native C runtime bundle. The runtime bundle compiles on demand during `kain build ... -t llvm` / `-t c`, or explicitly through `runtime/compile_native_runtime.sh` and its PowerShell wrapper.
- `kain runtime build` is the first-class operator entrypoint for standalone native runtime bundle compilation. It forwards to the existing bash/PowerShell wrappers after resolving the repo root from `KAIN_REPO_ROOT`, the current working tree, or the repo-built binary location.
- `./runtime/fixtures/validate_all.sh` is the generated LLVM/native executable proof lane. It now compiles, links, and executes dedicated heap, actor, and world fixtures.
- `./runtime/conformance/run_all.sh` is the runtime-native harness lane. Its `--backend llvm` flag is not a substitute for executable LLVM proof.
- `kain runtime validate` is the first-class aggregate operator entrypoint. It forwards to the existing validation wrapper and supports `--skip-cli-build`, `--skip-runtime-build`, `--skip-fixtures`, and `--skip-conformance`.
- `./runtime/validate_native_runtime.sh` and `powershell -ExecutionPolicy Bypass -File runtime\validate_native_runtime.ps1` are the aggregate commands that run the CLI build, native runtime build, fixture suite, and conformance suite together.

If the debug CLI is missing:

- `cargo build -p cli`
- `target/debug/kain --help`

## Architectural Guardrails

- Do not split semantic truth across runtime lanes.
- Do not describe Kain as "only orchestration" when `kain-core` already executes real authored logic.
- Do not let hosts re-parse source as the normal execution path.
- Do not push Kain-expressible business/domain logic into host bridges by default. Bridge when a capability is genuinely external, not because the language/runtime lane was ignored.
- Do not invent lane-specific shader, UI, or compute metadata when compiler-owned bundles already exist.
- Prefer data-driven capabilities, manifests, registries, and bundle metadata over scattered string checks and host-local assumptions.
- Keep blade lookup centralized in `crates/kain-blades`. Do not add new `blades/*`, `apps/*`, or `crates/*` scans inside Fabric, FFI, or module-resolution call sites when a typed resolver already exists.
- Keep the interpreter/runtime lane and emitted bundle/codegen lanes semantically aligned. A packaged target may optimize or lower behavior, but it should not silently define different language meaning than `kain-core`.
- Preserve the distinction between authored language semantics, importer behavior, and backend/runtime support.
- TypeScript import ambient globals must stay data-driven: generated data lives at `crates/kain-import/src/typescript/data/typescript_ambient_manifest.json`, Kain-specific aliases/helpers live in `tools/typescript_import/typescript_ambient_overrides.json`, and `crates/cli/src/import_typescript.rs` should consume that manifest instead of reintroducing hardcoded DOM/JS prelude arrays.
- Vendor runtimes may strengthen `runtime/native`, but they must land behind Kain-owned service families, startup contracts, diagnostics, and scheduler policy instead of becoming the public contract themselves.
- UI vendors follow that same rule more aggressively: Qt, ImGui, Yoga, RmlUi, Slint, CEF, Skia, and future backends are adapters or substrates behind Kain-owned semantic trees, layout intent, runtime metadata, and service contracts. Do not let a host UI stack become the source of authored truth.
- Do not confuse the Qt smoke shell skin with the Kain UI contract. The smoke host may use a fixed look to prove the lane quickly, but app-authored UI should continue to flow through theme data, style tokens, and Kain-owned surface metadata rather than hardcoded host visuals.
- Platform- or console-specific render-command experiments should start as isolated adapter lanes under `smoketest/` or another dedicated adapter crate before any shared `kain-3D` contract is widened. The new `smoketest/3D/sm64_fast3d_smoke` is the pattern: it owns its own manifest, segment registry, display-list interpreter, and combiner logic instead of baking N64-specific assumptions into the common scene/material API too early.
- The SM64 import refresh workflow for that lane is now profile-driven and lives beside the smoke under `smoketest/3D/sm64_fast3d_smoke`. Use `refresh_sm64_import.bat` and `sm64_import_profile.render_us.json` instead of reconstructing long one-off `import-c` commands from memory.
- The same smoke now has a title-face extraction lane. `extract_sm64_title_face.bat`, `launch_title_face_visual_exe.bat`, and `capture_title_face_snapshot.bat` are the quickest path to a compiled proof that uses real extracted Mario face geometry while keeping N64-specific semantics inside the adapter.
- The adapter is no longer only a smoke-local runtime. The reusable host surface now lives in `crates/kain-fast3d-runtime`, while the smoke folder acts as a consumer that provides manifests, scripts, and validation assets.
- Keep the Fast3D lane data-driven. Its host startup now flows through crate-owned config files and env hooks (`KAIN_FAST3D_CONFIG`, `KAIN_FAST3D_MANIFEST`, `KAIN_FAST3D_SM64_ROOT`) rather than widening Kain language semantics or shared runtime contracts for one experimental console adapter.
- Native app launcher materialization now also supports generic host-sidecar packaging in `kain-driver`: generated launchers can copy arbitrary sidecars into the artifact/executable set, optionally export them as env vars, and switch between the default `run_bundled_app_json(...)` launcher path and a crate-owned no-arg entrypoint like `kain-fast3d-runtime::run_fast3d_cli()`. Preserve that mechanism as a generic host adapter capability, not a Fast3D-specific special case.
- The Bob-omb Battlefield proof now uses the same host-sidecar path with three data files: the extracted scene manifest, a gameplay animation document, and a display-list shader-override document. Keep live actor binding and material experiments in these sidecars instead of adding SM64-specific semantics to `kain-core` or the shared runtime.

## Common Errors

- The root `README.md` is useful, but live source and the built CLI are the real source of truth.
- On this Windows workstation, `kain` and `kn` should resolve through Bazel-backed launcher shims, not copied Cargo binaries. The shared shim lives at `D:/Kain-Bazel/bin`, shadow shims also replace `%USERPROFILE%/.cargo/bin/kain.exe` and `kn.exe`, and `kain doctor` should report `Binary Kind: bazel-output` plus wrapper-aware PATH status.
- The successor repo MCP lane is `mcp/kain-agent-mcp`; `blades/kain-mcp` is legacy/donor material until configs are migrated. For the current scaffold, run from `mcp/kain-agent-mcp` with the direct Bazel-built `kain.exe`; the PowerShell launcher can still mis-handle `-o`, and direct file LLVM compile can miss imported module definitions, while manifest-root `check`, `build`, and `run . --target llvm --keep-artifacts --json` are the validated path.
- KAIN capsules are comment-safe text containers, not opaque blobs. Keep the generated comment prelude and `//!kain-capsule` block intact, then preserve either the inline `//!kain-file` sections or the `//!kain-capsule-payload` archive block so normal `.kn` parsing can stay ignorant until the capsule bridge materializes the workspace.
- `kain run`, `kain build`, and `kain check` materialize capsule inputs under `.kain/cache/amalgamate/<digest>/workspace`. If an operator needs the real extracted tree or wants to inspect a manifest/project capsule after delegation, start there.
- `docs/examples/09_ue5_authoring_gallery.kn` is intentionally validated on the Rust backend in this checkout. Direct `kain build -t ue5 ...` still fails during `stdlib/ue5` loading because `max` does not resolve there yet.
- Filesystem imports now share lookup through `crates/kain-core/src/module_resolution.rs`. `use module::item` can fall back from `module/item.kn` to `module.kn` or `src/module.kn` and register the requested top-level item, while `use module::*` exposes top-level module items to best-effort typechecking. Lookup is still rooted in the process current directory, so launch nested scripts from the intended project/runtime root until source-file-relative module roots are added.
- Filesystem stdlib globals are ABI-sensitive now. When adding new native-callable `fs_*` helpers, update `crates/kain-core/src/stdlib.rs` with precise return types, keep `stdlib/fs.kn` wrapper signatures explicit, and ensure LLVM skips declaring stdlib functions that the target stdlib defines in Kain source.
- Bare `kain check <file.kn>` and `kain run <file.kn>` are not the authoritative validation path for low-level native fixtures that depend on the native stdlib profile. If a one-file LLVM proof suddenly claims `native_runtime_init` or other native globals are unknown, compile it directly with `target\debug\kain.exe <file.kn> -t llvm -o <output.exe>` before assuming the language or runtime regressed.
- Tree-wide `kain check <blade-root>` can still see `.kn` files under local `.kain` folders. If a blade keeps unpacked capsule probes or other generated Kain source under `.kain/unpacked`, those stale copies can become accidental check inputs.
- FS v2 scoped interpreter helpers resolve through `FsSandbox`, but older v1 interpreter helpers still take host paths directly unless they are explicitly routed through the newer scoped path helpers. If a virtual `fs://` path works in `fs_read_text_range` but not an older helper, check the runtime wrapper before blaming `kain-fs`.
- Direct C fixture assertions should avoid generic `len(string)` until the C backend lowers that shape consistently in all native fixture contexts. Prefer direct string comparisons or explicit native helpers in `runtime/fixtures/native_fs/main.kn`.
- Fresh Linux and macOS clones should start with the root `install_kain.py` bootstrapper. It is now the cross-platform entrypoint that resolves or installs LLVM, repopulates `toolchain/llvm/bin`, builds `kain`, installs `kain` and `kn`, and emits shell activation scripts under `generated/`.
- Fresh clones may not include a populated `toolchain/llvm/bin/clang.exe` even though older docs and helper scripts reference it. When that happens, install LLVM separately and point `KAIN_CLANG_PATH` at the external `clang.exe`; `scripts/windows/sync-kain-source-of-truth.ps1` now falls back to PATH and `C:\Program Files\LLVM\bin\clang.exe` before assuming the vendored drop exists.
- The `cli` suite no longer depends on the external self-hosting fixture under `M:\Code\Other\kainselfhosting\...`; the repo-local import-c fixture under `crates/cli/tests/fixtures/import_c` is the durable regression source now.
- The repair lane is profile-driven. If a file is being "fixed" in a way that changes meaning, that is a bug in the caller or profile selection, not a feature.
- Large Windows test binaries can hit linker OOM pressure.
- The workspace still pins `pyo3 0.20.x`, so a machine-default Python 3.13+ or 3.14 can break builds. Prefer Python 3.12, set `PYO3_PYTHON` explicitly when needed, and keep the Python 3.12 install directory on PATH so the built `kain.exe` can resolve `python312.dll` at runtime.
- `generated/`, `target/`, `.kain`, runtime sidecars, and compiled smoke outputs are disposable unless explicitly archived in a repo-owned archive path and cross-linked from `guides/reference/troubleshooting.md`.
- Some Windows environments in this checkout can fail the final `.git/index.lock -> .git/index` swap with `fatal: unable to write new index file` even after the command already staged content. When that happens, use [scripts/windows/git-safe-index.ps1](D:/Kain-Lang/scripts/windows/git-safe-index.ps1) to run the mutating git command through a temporary index and stream-copy the result back into the live index.
- The root `mcp.json` and repo `codex.config.toml` templates must stay repo-relative for this checkout. They may still launch `blades/kain-mcp` until `mcp/kain-agent-mcp` implements full MCP handshake/tool protocol; when migrating, prefer `command = "kain"`, `args = ["run", "mcp/kain-agent-mcp"]`, and `cwd = "."`. Codex in this lane treats `${KAIN_REPO_ROOT}` literally instead of interpolating it, so placeholder-based repo configs can silently override a good global MCP block with a broken local one.
- The live SM64 decomp root currently sits at `M:\Code\Other\Research\sm64-master\sm64-master`, not the outer `sm64-master` folder. The older stale import reports pointed at the outer folder, which hid a real pathing mistake.
- Linux now validates the core raw-native lane end-to-end: `cargo build -p cli`, `kain build -t llvm`, `./runtime/fixtures/validate_all.sh`, `./runtime/conformance/run_all.sh`, and `./runtime/validate_native_runtime.sh` all pass on a Linux host. The Win32 app-host, input, and viewport host services are still Windows-specific until a non-Win32 native host lands.
- Runtime conformance harnesses that compile `services.c` or `contract.c` in isolation must also compile `runtime/native/src/core/core.c`, `runtime/native/src/core/actor.c`, `runtime/native/src/core/net_system.c`, and `runtime/native/src/core/process_system.c`; the live catalog now binds the owned net/process function tables directly.
- The native runtime now has three related metadata surfaces: `runtime/native_core_runtime.toml` is the canonical manifest/build truth, `runtime/native_runtime.toml` is a lean compatibility mirror for older discovery paths, and `runtime/native_runtime_metadata.json` is the tooling-facing reflection of that same lean contract. Keep the mirrors in sync with the core manifest.
- Bazel now has a manifest-synced native runtime lane. Regenerate `runtime/runtime_manifest_data.bzl` with `py -3 tools/bazel/sync_native_runtime_builds.py`, treat `//runtime:native_runtime` as the lean default lane, and treat `//runtime:native_full_runtime` as a legacy-named compatibility mirror target rather than a broader vendor lane.
- The validated Windows/MSVC Bazel proof lane is `bazel build //runtime:all` plus `bazel test //runtime:native_runtime_tests`. Both runtime bundle targets now resolve to the same lean source set, so any doc or script that still describes a Bazel vendor lane is stale.
- Bazel also has a generated Rust workspace lane. `Cargo.toml`/`Cargo.lock` remain the source of truth; regenerate crate `BUILD.bazel` files with `python tools/bazel/sync_rust_builds.py` and verify drift with `python tools/bazel/sync_rust_builds.py --check`.
- Bazel caches are intentionally D-drive-local on this Windows machine: `.bazelrc` sets `--output_user_root=D:/Kain-Bazel/output-user-root`, `--repository_cache=D:/Kain-Bazel/repository-cache`, repo/action/test temp env to `D:/Kain-Bazel/tmp`, and the shared disk cache to `D:/Kain-Bazel/disk-cache`.
- The Rust Bazel lane uses a single generated target per workspace crate, so package binaries/tests must explicitly depend on their sibling library target when Cargo would make it visible implicitly. Keep that behavior in `tools/bazel/sync_rust_builds.py`, not hand-edited generated `BUILD.bazel` files.
- The default Bazel profile is intentionally throttled for interactive Windows work on this machine: about 62.5% of logical CPUs (`--jobs=HOST_CPUS*.625`, `--local_resources=cpu=HOST_CPUS*.625`), 75% of host RAM (`--local_resources=memory=HOST_RAM*.75`), 50% loading threads, and 25% concurrent local tests. Use `--config=maxperf` only when you explicitly want the box pegged harder.
- If a Bazel-backed `kain` or `kn` launcher suddenly fails during analysis with a `crate_universe` digest mismatch after Cargo manifest edits, repin the Rust extension state with `$env:CARGO_BAZEL_REPIN='1'; bazel fetch //:kain --config=dev` before blaming the wrapper itself. That command refreshes `MODULE.bazel.lock` to the live Cargo workspace state.
- Windows Bazel Rust tests need `PATH` and `PATHEXT` inherited in the test environment here so subprocess-driven tests can find tools like `rustc`; keep those `test --test_env=...` lines in `.bazelrc`.
- The Windows Bazel lane now depends on a two-step `rules_swift` override in [MODULE.bazel](/M:/Code/Kain/MODULE.bazel): one patch guards the missing-`SDKROOT` path and a second patch fixes the generated Windows toolchain stanza so it no longer emits `target_compatible_with = APPLE_PLATFORMS_CONSTRAINTS[arch]`. If `name 'arch' is not defined` reappears during analysis, treat it as override drift and re-check the patch stack instead of accepting it as harmless noise.
- Keep throwaway or local-only test apps out of the root Cargo workspace unless they are intentionally promoted into the core Bazel lane. A stale workspace member such as `apps/<experiment>/controller` will break `cargo metadata`, crate-universe repins, and any Bazel lane that needs fresh Cargo workspace state.
- `bazel test //:developer_smoke_tests --config=dev` is the current green Rust lane on this host: `kain-actor`, `kain-blades`, `kain-build`, `kain-check`, `kain-codebase`, `kain-commands`, `kain-entangle`, `kain-fs`, `kain-input`, `kain-net`, `kain-process`, `kain-run`, and `kain-test` all pass there under Bazel.
- `bazel test //:workspace_diagnostic_tests --config=dev` is intentionally not green yet. Current Bazel-visible source/test failures include `kain-core` (`language_features::tests::default_profile_keeps_struct_literals_disabled`, two `realtime_app_bundle` tests), `cli` (`import_c::tests::test_import_with_target`, `selfhost::tests::indent_repaired_block_matches_nested_selfhost_layout`), and `runtime:native_test_actor_monitor_link`.
- On Windows, blade-local `[c_ffi]` shared libraries should link through the sibling import library when one exists. `crates/cli/src/main.rs::resolve_c_ffi_shared_libraries_for_linking` now prefers `foo.lib` beside `foo.dll`, which is required for blades like `blades/vulkain` that compile bridge DLLs under `.kain/native/`. If a freshly built blade-root exe still exits with `0xC0000135`, stage the bridge DLL beside the exe or add the blade's `.kain/native/` directory to `PATH` before assuming the Kain source is wrong.
- PowerShell blade scripts run under `Set-StrictMode -Version Latest`, so do not rely on convenience variables such as `$IsWindows` or `$IsMacOS` unless you know the host defines them. Use explicit `RuntimeInformation::IsOSPlatform(...)` checks in portable blade scripts like `blades/vulkain/build-vulkain.ps1`.
- If a blade needs the Bazel-built compiler but `bazel build //:kain` is red because of unrelated workspace breakage, it is acceptable to reuse an already-built Bazel `kain.exe` through `-KainBin` or `$env:KAIN_BIN` rather than regressing the blade to repo-root artifacts or the broad runtime lane. `blades/vulkain/run.ps1` is the reference pattern.
- The MCP window enumerator may not surface short-lived Vulkan proof windows even when the executable is healthy. For `blades/vulkain`, treat `.kain/run/vulkain_report.txt` frame counters plus `last_error=ok` as the durable proof until the capture tooling can see this path.
- The current native UI host adapter is passive-only for `auto`, `software`, `memory`, and `headless`. A blade that needs real pixels, real screenshots, or close events must bring a blade-owned presenter through `[c_ffi]` instead of assuming `ui_host_session_create(..., "software")` opens a live window. `blades/pong` is the reference pattern: keep the authored UI/session state if it is useful for proof or reporting, but drive the visible window and screenshot path through a blade-local presenter such as `native/pong_window_bridge.c`.
- Kaintana's remaining durable UI-native gotchas are boundary-related: `[c_ffi]` bindings are still local to the consuming blade manifest, so if a library blade such as `blades/kaintana` wraps a native bridge, consumer blades such as `blades/kaintana-test` or `blades/kaintana-vulkan-test` must still declare matching `[[c_ffi.libraries]]` entries locally for `use c::...` imports to resolve. Inside `blades/kaintana`, keep `use c::kaintana_desktop_bridge` in the entrypoint that owns linking, not in `platform/desktop/desktop_adapter.kn`, or the generated LLVM can duplicate C bridge definitions. Kaintana builders intentionally pass `KaintanaContext` only to `*_render(ctx, builder)` because imported `impl Self_` method lowering and repeated full-context builder copies still have native LLVM edges. `SlotMapInsert.map.free_head` currently returns stale in the nested Kaintana path, so reconciliation normalizes append-only node maps from `count` after inserts. Because backend manifests are local, do not mix desktop and Vulkan acceptance paths inside one consuming blade unless per-entry manifests become real; a shared manifest can silently reintroduce backend contamination. The earlier imported-`world` / `entangle` double-staging failure was fixed in `crates/kain-driver`: realtime/native UI staging must build UI from target-prepared source, not the flattened frontend import bundle, or compiler-owned intent items will be replayed twice. Also remember that `kaintana_split_right(rect, fraction, gap)` interprets `fraction` as the left-hand share, not the right-hand share: for a 25% right inspector rail, pass about `0.75`, not `0.25`, or the right panel will quietly consume most of the shell.
- Native crash forensics now has a repo-owned entrypoint at `tools/crash-forensics/analyze_native_crash.ps1`. Feed it a Windows minidump plus the matching executable, and optionally the emitted `.ll`, frame report, and host report. It will batch LLDB backtrace capture, `llvm-objdump` disassembly around the crash PC, non-entry-`alloca` scanning for the emitted LLVM IR, and the last completed frame evidence from app/host reports into one durable report file under `.kain/forensics/`.
- The native core runtime proof pack now has a focused `memory` lane for low-level allocation/header/pointer-address arithmetic. Use `uv run --project C:\\Dev\\polytools\\z3-mcp --no-sync z3-mcp-batch --pack-path D:\\Kain-Lang\\runtime\\native\\src\\core --lane memory` after touching `runtime/native/src/core/memory.c`, and rerun `--lane full` before closing broader native-runtime math work.
- The native core runtime now has a durable Z3 pack at `runtime/native/src/core/z3`. Use `uv run --project C:\Dev\polytools\z3-mcp --no-sync z3-mcp-batch --pack-path D:\Kain-Lang\runtime\native\src\core --lane smoke` after touching actor mailbox/scheduler/supervision arithmetic or low-level net size arithmetic.
- The proof-pack workflow is broader now: use `uv run --project C:\\Dev\\polytools\\z3-mcp --no-sync z3-mcp-batch --pack-path D:\\Kain-Lang\\runtime\\native\\src\\core --lane actor|net|process|entangle|native|full` for focused reruns, and `uv run --project C:\\Dev\\polytools\\z3-mcp --no-sync z3-mcp-batch --workspace --project-root D:\\Kain-Lang --lane smoke` to prove pack discovery plus workspace orchestration.
- `crates/kain-core` now has its own durable Z3 pack at `crates/kain-core/z3`. Use `uv run --project C:\Dev\polytools\z3-mcp --no-sync z3-mcp-batch --pack-path D:\Kain-Lang\crates\kain-core --lane memory|diagnostics|literals|parser|full` after touching `src/low_level_memory.rs`, `src/diagnostics.rs`, parser slice/index helpers, or signed `usize -> i64` literal conversions. When a proof only fails on impossible values outside the ABI domain, constrain the model first: `usize` proofs should stay within `SIZE_MAX`, and `size_literal_i64` success paths should stay within `i64::MAX`.
- `crates/gpu` now has its own durable Z3 pack at `crates/gpu/z3`. Use `uv run --project C:\\Dev\\polytools\\z3-mcp --no-sync z3-mcp-batch --pack-path D:\\Kain-Lang\\crates\\gpu --lane layout|constructors|control|full` after touching `crates/gpu/src/codegen_spirv.rs`, especially storage-buffer stride/layout logic, vector constructor flattening, or hoisted local-slot plumbing. Pair it with `cargo test -p gpu --test spirv_layout --target-dir target\\codex-spirv-proof-layout -- --nocapture` so the emitted module is also checked by `spirv-val --target-env vulkan1.3`.
- `runtime/native/src/core/z3/reports/` and root `z3/reports/` are generated validation output. Keep the durable proof surface in `proofs/`, `templates/`, `fixtures/`, and manifest files; do not commit report JSON unless a future workflow intentionally promotes it.
- `crates/kain-sys-codegen` now has its own durable LLVM Z3 pack at `crates/kain-sys-codegen/z3`. Use `uv run --project C:\\Dev\\polytools\\z3-mcp --no-sync z3-mcp-batch --pack-path D:\\Kain-Lang\\crates\\kain-sys-codegen --lane layout|control|casts|memory|full` after touching `crates/kain-sys-codegen/src/codegen_llvm/mod.rs`, especially ABI layout math, match CFG lowering, `next_reg`/`next_label`, or integer/bool coercions.
- Native LLVM long-running UI loops are sensitive to textual `alloca` placement. Backend stack slots must route through `emit_entry_alloca`, `next_reg()` must stay on named locals such as `%rN` rather than LLVM's ordered unnamed `%0` form, and generated `.ll` should have zero non-entry allocas in `@main`. A deterministic crash around one exact frame count, such as the 711-frame Win32/GL workbench failure, is a strong signal for loop-local stack growth. Validate with `blades/kain-example/run-ui.ps1`, `llvm-as`, `mcp__z3_local__.run_proof_pack(path="D:\\Kain-Lang\\crates\\kain-sys-codegen", lane="full")`, and a high frame budget run using `KAIN_NATIVE_UI_WIN32_GL_AUTO_EXIT_AFTER_FRAMES`.
- `blades/kain-example/src/main.kn` is the canonical one-file native LLVM proving ground. The durable validation loop is `kain check blades/kain-example/src/main.kn --target llvm`, `kain blades\\kain-example\\src\\main.kn -t llvm -o target\\kain-example\\kain_example.ll`, then execute `target\\kain-example\\kain_example.exe`. If this file regresses, treat it as a real native-lane compatibility break, not just an example drift.
- The first verified machine-level native UI crash family on this host is older runtime-owned Win32/GL menu handling, not the new blade-owned presenters. `kain_example_workbench.exe` crash dumps under `%LOCALAPPDATA%\\CrashDumps` consistently stop with `0xc00000fd` in GDI text rendering (`ExtTextOut*`) after re-entering `the archived Win32/GL menu-processing function` around the modal `TrackPopupMenuEx(...)` path from archived source commit `291bf3211a30d0bb2e56b32763080300d703d7da`. If a native app still crashes while using that archived GL host, suspect modal-menu reentrancy and stack exhaustion before blaming Kaintana or Pong.
- If a native LLVM regression only appears when calling root stdlib wrappers or authored string helpers from Kain, inspect `crates/kain-sys-codegen/src/codegen_llvm/mod.rs` before blaming the C runtime. The current string fast path depends on three linked pieces: the direct-call borrowed-string parameter mask, callee borrowed-local marking, and `string_length_values` entry caching. Breaking that coordination either brings back retain/release churn in `string_ops`-style loops or reintroduces ownership bugs.
- If LLVM float coercions regress, inspect `cast_numeric_value` and `coerce_to_i64_storage` before assuming the frontend is wrong. The current backend semantics include two proven caveats: `double -> i1` treats `NaN` as `false` because it uses `fcmp one`, and `double -> i64/i32/i8` only stays defined when the source value is finite and within the destination integer domain.
- If native LLVM starts inventing extern calls such as `@Float(...)` or emitting direct-call arg type mismatches between `double`, `i64`, `i32`, `i8`, and `i1`, inspect `compile_direct_call` in `crates/kain-sys-codegen/src/codegen_llvm/mod.rs`. The backend now treats scalar constructor names like `Float(...)`, `Int(...)`, `Bool(...)`, and friends as numeric coercions, and it also coerces direct-call numeric arguments to the declared primitive parameter type before emission. The durable regressions are `llvm_coerces_numeric_call_arguments_to_declared_param_types` and `llvm_lowers_float_constructor_calls_as_numeric_casts` in `crates/kain-sys-codegen/tests/llvm_codegen_test.rs`.
- The stronger `blades/kain-example` validation loop now also includes `toolchain\\\\llvm\\\\bin\\\\llvm-as.exe target\\\\kain-example\\\\kain_example.ll -o target\\\\kain-example\\\\kain_example.bc` and `cmd /c "start /wait "" target\\\\kain-example\\\\kain_example.exe & echo EXITCODE:%ERRORLEVEL%"`. `llvm-as` catches textual-IR regressions that string-based codegen tests miss, and `cmd /c start /wait` is the reliable way to observe the real exit code of generated native executables on this Windows host.
- If native LLVM `match` code starts failing only at link or verifier time, inspect `Expr::Match` lowering in `crates/kain-sys-codegen/src/codegen_llvm/mod.rs` before suspecting the parser or typechecker. The merge block must stay separate from every arm-condition block, guard-fail paths need explicit scope cleanup, and string-returning fallbacks must contribute typed values such as `null` instead of integer `0`.
- The owned selfhost lane has the same split-contract rule: `src/KAIN.toml` is the hand-written compiler manifest, `runtime/native_core_runtime.toml` is the canonical native runtime manifest, and `runtime/native_runtime.toml` is only the compatibility mirror. Do not hide compiler source ordering, artifact paths, or runtime linkage behind hardcoded CLI assumptions once the manifests exist.
- The owned bootstrap lane should never report success just because an earlier artifact still exists on disk. If `kain selfhost bootstrap` emits compile, runtime, or link errors and the expected artifact set was not freshly produced, that is a hard failure even if stale `.ll`, `.json`, or native binary files remain under `src/.selfhost/`.
- The aggregate bootstrap source under `src/.selfhost/phase0/combined/` is an explicit temporary bridge. Future work should widen `src/KAIN.toml` and the frontend toward a real multi-file module graph instead of treating the combined source file as the permanent compiler shape.
- `blades/kain-mcp/config/runtime_policy.json` and `blades/kain-mcp/config/tools.json` are the deterministic registry for the canonical repo MCP lane. Add env keys, binary resolution order, limits, tool schemas, and handler ids there first; keep the Kain server generic and avoid smuggling repo paths into code.
- `crates/cli/src/main.rs` now suppresses the human CLI banner whenever stdout is non-terminal and also honors `KAIN_NO_BANNER` / `KAIN_ENGINE_NO_BANNER`. Keep machine-facing stdout clean so MCP, JSON, and other pipe-driven consumers can launch `kain` directly without a banner-stripping shim.
- `scripts/python/launch_kain_mcp.py` now runs a policy-driven stale check before server boot (`repo_sha` + runtime stamp + binary stamp), uses a global lock under `~/.kain/locks/sync.lock`, respects a cooldown window, and falls back to the current binary when another agent holds the lock. Keep this behavior data-driven through `blades/kain-mcp/config/runtime_policy.json` rather than hardcoding paths in the launcher.
- Codex MCP config changes are session-scoped in practice on this machine. If `~/.codex/config.toml` changes after a Codex session starts, restart that session before blaming `kain-mcp`. The current Codex default should be `command = "kain"` with `cwd = "D:\Kain-Lang"` so it resolves through the Bazel-backed shim; inspect `~/.kain/state/kain_mcp_launcher_trace.jsonl` only when you are explicitly debugging the Python managed-sync fallback.
- `scripts/windows/sync-kain-source-of-truth.ps1` now owns the Windows Bazel launcher install. It builds the Bazel CLI targets, compiles `scripts/windows/kain_bazel_cli_launcher.rs` into a native shim, installs that shim to `D:/Kain-Bazel/bin`, shadows `%USERPROFILE%/.cargo/bin/kain.exe` and `kn.exe`, writes compatibility `.cmd` wrappers in the shared launcher dir, and refreshes the sync stamp consumed by launcher + doctor.
- The managed sync script is launched through Windows `powershell` on this machine, not `pwsh`. Keep its JSON parsing compatible with Windows PowerShell 5; relying on `ConvertFrom-Json -AsHashtable` silently breaks runtime-policy reads and can leave managed build numbers stuck at `1`.
- Platform-specific runtime support should be expressed explicitly in the manifest rather than implied by global defines. The active lean runtime manifest carries shared `defines` plus per-platform `windows_defines`, `linux_defines`, and `macos_defines`; prefer those over leaking POSIX-only flags into Windows builds.
- The active runtime bundle is vendor-free by policy. If an experiment still needs archived OpenGL/vendor/app code, salvage it from `.zarchive/runtime_devendor_2026-05-15/` into a blade or package instead of reactivating it inside `runtime/native`.
- `kain build ... -t llvm` now reuses native runtime objects incrementally under `generated/native_runtime/cache/<host>/<runtime-name>/objects` using per-object depfiles and compile fingerprints. That cache now covers only the Kain-owned lean runtime sources; archived vendor trees are intentionally outside the active cache/build path. If a warm LLVM/native build starts recompiling the whole runtime again, suspect a changed compile fingerprint, missing depfiles/fingerprints, a different cache root, or a newer source/header dependency before blaming LLVM itself.
- On Windows, benchmark and native-build warm-cache failures can still masquerade as compiler regressions when clang cannot rename a temporary object into `generated/native_runtime/cache/.../objects/<slot>.obj` and reports `permission denied`. `crates/cli/src/main.rs` now clears stale slot artifacts and retries that rename path, so a one-off object-slot failure is usually cache-state fallout, not broken Kain codegen. If a manual experiment still gets stuck on the same slot, clear that slot's `.obj` / depfile / fingerprint / `.tmp` files before assuming a semantic bug.
- The raw-native lane is no longer LLVM-only. `CompileTarget::C` now shares the runtime contract, realtime bundle staging, and native link path with LLVM, but the actual C emitter is still an experimental subset and will currently stop on unsupported stdlib/selfhost constructs with explicit backend errors instead of silently falling back.
- Graphics runtime doctrine is now mechanism-only. `gfx.raw-native`, `gfx.shader.spirv`, `gfx.compute`, `gfx.backend.vulkan`, and `gfx.backend.d3d12` are the active raw seams; concrete presenters, renderers, and richer asset loaders must live in blades, packages, or higher hosts. Older `bgfx` and similar vendor-renderer doctrine is archived, not active runtime truth.
- The first durable smoke for this renderer seam now lives under `smoketest/3D/generic_scene_smoke`. The launcher embeds `smoke.kn` directly, the top bar and runtime-owner messaging are authored in Kain source, and the smoke treats `geometry_fixture` as authored fixture data rather than a runtime-baked demo.
- The smoke compiles through the LLVM/native executable lane into `smoketest/3D/generic_scene_smoke/llvm-native/generic-scene-smoke`, but Linux still uses the Qt presentation shell. Treat the standalone binary as the native compile proof, not as the full presenter yet.
- The Qt launcher packages `generic_scene_visual_reference.png` as an optional compatibility sidecar and forwards it through `KAIN_UI_NATIVE_QT_VIEWPORT_IMAGE_PATH`; authored Kain source remains the runtime truth.
- The Windows native viewport now exposes one neutral `default_viewport` fallback profile. Scene identity, mesh data, camera intent, and presentation policy should come from Kain source, realtime bundles, or assets instead of hardcoded Win32 scene branches.
- LLVM/native actor proof now uses the canonical native actor runtime and the stdlib facade. If actor `spawn`/`send` regresses, check `runtime/native/include/actor.h`, `runtime/native/src/core/actor.c`, `runtime/native/src/core/stdlib_abi.c`, and the lowering in `crates/kain-sys-codegen` together; the legacy `KAIN_spawn` / `mq_*` path is not the active native ABI.
- If a native LLVM program with a local `shatter struct` array crashes on some return branches, inspect the emitted `.ll` before blaming the C runtime. Every return branch for a shatter handle must call `kain_machine_shatter_free(...)`; any `rc_release(i8* %shatter_handle)` means `emit_all_scopes_cleanup` lost shatter metadata while lowering sibling branches.
- The compute pipeline is mid-transition from heuristic metadata to compiler-owned truth. When touching it, prefer extending bundle contracts over adding new runtime-only inference.
- Multiple authored `world` roots are now treated as an explicit-selection problem, not a guessing problem. If build/run flows see more than one world, require a caller-provided selection instead of silently picking one.
- Frontend bridge registration must be target-scoped. Host/runtime extensions that are valid for `Interpret` or `Test` must not leak into shader artifact compilation or other non-host targets, or Fabric and direct driver paths will diverge.
- The formatter is AST-based in v1, and the lexer still drops comments. `kain format` is canonical for code structure today, but it will currently discard authored comments until trivia becomes part of the frontend contract.
- The formatter deliberately errors on a few parser-hostile or non-round-trippable shapes instead of emitting lossy source. Current notable cases are empty executable blocks, `shader surface` emission, and standalone block/comptime expressions that do not map cleanly back to authored statement form.
- The native shader-canvas lane is SPIR-V-canonical at the bundle level, but the current WGPU host still resolves WGSL for execution. Do not mistake that compatibility bridge for permission to move shader-canvas truth out of the emitted bundles.
- The native packaging loop is file-backed. If hot reload or packaged state looks stale, verify the generated `app_manifest.json`, `runtime_snapshot.json`, and launcher env vars before blaming the runtime.
- Native compiled-UI hot reload is now cross-platform by surface, not Windows-hardcoded by design. File-watch reload comes from `KAIN_NATIVE_UI_BUNDLE`, while the optional low-latency control plane is negotiated through `KAIN_NATIVE_UI_HOT_RELOAD_CHANNEL`; if a live bundle swap works through file edits but not through the control channel, inspect the platform shared-memory backend before changing the runtime reload logic itself.
- If different native-ui apps still appear to share the same Qt presentation, inspect the generated `session.json` or bundled `native_projection` first. A healthy generic Qt launch should now carry authored projection nodes and render them; identical chrome without projection data usually means the host fell back to the older compatibility summary path.
- `kain native-ui dev` can be healthy even when the launched child immediately dies. In this checkout the Chronos proof materializes, launches, and keeps the watch loop alive, but the child currently aborts through `/usr/local/bin/qmlscene` with exit status `134` in this environment. Treat that as a Qt/GUI-session runtime-host failure before assuming the dev loop or reload classifier is broken.
- Native desktop launchers that do not inherit a GUI session can still boot if the wrapper resolves the live compositor socket. `labs/playground/piano/run.sh` now auto-detects the current Wayland/X11 runtime and exports the minimum env needed to attach on Linux; if a native app starts but no window appears, check `WAYLAND_DISPLAY`, `DISPLAY`, `XDG_RUNTIME_DIR`, and `XAUTHORITY` before blaming Kain UI.
- The LLVM dogfood lab in `labs/llvm_world_dogfood_lab` uses named payloads for both `spawn` and `send`. If an actor message stops parsing, check `Pulse(amount = ...)` / `spawn Actor(...)` syntax before assuming the LLVM backend regressed.
- The native Chronos proof under `labs/chronos_native` currently packages realtime and compute sidecars successfully, but the native-ui packaging/typecheck lane is still stricter than the direct GPU artifact lane for at least some compute expressions. If a shader proof works under `kain gpu-artifacts` but not under `kain build native-ui`, inspect dispatch-index access and bundle-lane differences before widening the language surface.
- Node-backed browser labs such as `labs/threejs_node_ffi_space_lab` depend on a local package install inside the lab root. If bundling fails, check the lab-local `node_modules` state before assuming the browser app regressed; if the Rust sculpt lane fails, verify `rustup target add wasm32-unknown-unknown` in the local environment before blaming the app code; if Kain execution itself fails with unknown `js_import` / `js_bridge_import` identifiers, treat that as host-backed bridge registration drift in the current checkout.
- `kain build -t hybrid` is now expected to materialize four aligned artifacts at once: a `.hybrid` descriptor plus `.js`, `.ts`, and `.wasm` sidecars. If a browser proof emits only JS text or the generated JS starts fetching its sidecar relative to the page root instead of the script URL, treat that as a CLI/driver regression rather than app-local config drift.
- General named-argument `TypeName(field = value)` construction is not live KAIN syntax. Use a struct literal like `TypeName { field: value }` or construct the value empty and assign fields explicitly.
- The blade workspace smoke now delegates sidecar/artifact work to `blade build . --json`; a fresh checkout will not have `blades/native_filter/native/blade_filter.dll` or the platform equivalent until the build graph runs. If `target\debug\kain.exe` or `blade.exe` predates the build command, build the CLI into an isolated target dir and set `KAIN_BIN` / `BLADE_BIN`.
- Windows `std::fs::canonicalize` can produce verbatim `\\?\` paths. Rust can usually consume those, but Node's CLI can fail when launched with them. Keep process-bound path normalization in bridge crates such as `kain-node` instead of pushing workaround paths into manifests.
- Blade-library import fixes in this repo only take effect in a freshly built CLI. If `C:\Users\Admin\.cargo\bin\kain.exe run blades/<blade>` still reports missing sibling modules after a blade-system patch, validate with `cargo run -p cli -- ...` or a rebuilt `target\debug\kain.exe` before assuming the workspace code is still wrong.
- C ABI functions imported into interpreted Kain should return signed, bounded integer values when Kain code treats them as `Int`; a raw `uint64_t` checksum can overflow the bridge at runtime.
- Function-valued actor state is not callable through `self.field(...)` because that parses as a method call. Load the state field into a local first, then call the local closure value.
- Fabric Python execution should stay behind `kain-python` helpers. Do not make `kain-host` reach directly into `pyo3` imports or `PythonScopeState` internals when the Python lane can expose a narrower execution API.
- Fabric runtime ownership is now split cleanly: `kain-omni` owns `KAIN.fabric.toml` schema/validation/report types, while `kain-host` owns local execution, dependency plumbing, and runtime adapter behavior.
- Fabric step inputs now flow through raw `fabric_inputs` for every runtime adapter. Kain/C/Rust glue consumes canonical host objects directly, while the Python and Node bridge crates project shared buffer/image payloads into language-native contract objects with `bytearray` and `Uint8Array` bytes views.
- Fabric Python and Node steps now support mixed named outputs when they return a dict/object whose fields match the manifest's declared output names. Shared outputs round-trip through the canonical host-owned interop contract family instead of falling back to string-only placeholders.
- Missing declared Python/Node output fields now fail with structured Fabric errors keyed as `missing_output_field`, with `output_name` recorded in failure details. Preserve that contract surface when touching adapter execution or bridge helpers.
- The durable end-to-end Fabric proof lives under `smoketest/fabric/polyglot_local`. It is the quickest repo-local example of Python -> Kain -> C ABI -> Rust crate -> Node execution with typed shared image/shared buffer flow.
- The durable GPU Fabric proof lives under `smoketest/fabric/gpu_compute_convergence`. If a Fabric GPU step succeeds through `kain gpu-artifacts` but fails through `kain fabric run`, compare `kain-driver` target registration/augmentation behavior first, then inspect Fabric residency metadata and shared-buffer shape/access inference before blaming Vulkan.
- `kain fabric init --template polyglot` now emits a runnable local smoke-grade scaffold, including its local Rust crate manifest and native C fixture, instead of a validation-only placeholder.
- Fabric host smoketests now use deterministic roots under `target/fab-init` and `target/fab-smoke`, preserving `.kain/cache` across separate `cargo test` invocations. If a rerun is still slow, the remaining wall time is usually Cargo recompilation rather than bridge cache misses inside the test body.
- The generated polyglot scaffold also writes `FABRIC.README.md`; treat that file as the first-stop quickstart for the smoke-grade local pipeline shape.
- `smoketest/allinone` is the broad regression umbrella for major CLI and bridge surfaces. Its runner is manifest-driven and clears each lane's generated outputs before rerun, so stale artifacts should not be treated as proof that a current codegen path still works.
- For SM64/Fast3D research, Fabric should stay an optional post-extraction simulation lane that feeds buffers or textures into the adapter. Do not make display-list extraction or the base render loop depend on Fabric before the geometry and segment path is stable.

## Template Packs

`templates/Web` now serves as a reusable web starter/reference pack, but the canonical public-facing example is `website/`.

Key rules for this lane:

- Kain owns the page semantics, the compiled preview subtree, and the site content model.
- Browser and Node glue should stay thin: bundling, local serving, and browser compiler bootstrapping only.
- themes, content, sections, and examples should be registry-driven data, not scattered starter literals.
- Avoid reintroducing a helper-owned site runtime or fake enterprise metadata shell around the website surface.

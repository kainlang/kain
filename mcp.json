{
  "mcpServers": {
    "kain-mcp": {
      "command": "py",
      "args": [
        "-3",
        "${KAIN_REPO_ROOT}/scripts/python/launch_kain_mcp.py"
      ],
      "cwd": "${KAIN_REPO_ROOT}",
      "env": {
        "KAIN_REPO_ROOT": "${KAIN_REPO_ROOT}"
      }
    }
  }
}

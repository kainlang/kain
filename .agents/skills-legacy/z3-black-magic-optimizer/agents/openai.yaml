interface:
  display_name: "Z3 Black Magic Optimizer"
  short_description: "Find solver-backed alien math and replacement pathways"
  default_prompt: "Use $z3-black-magic-optimizer to decide whether this code path just needs ordinary optimization or whether the abstraction itself is wrong and should be replaced with a proof-backed alien mechanism."

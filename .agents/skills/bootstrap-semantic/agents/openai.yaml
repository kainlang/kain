interface:
  display_name: "Bootstrap Semantic"
  short_description: "Semantic oracle, CUDA-forged packs, and proof loop"
  default_prompt: "Use $bootstrap-semantic to work on the Kain semantic oracle pipeline, its CUDA kernels, CUDA-forged sidecar packs, and the real forge/search/compiler-diagnostic proof loop."

# 2026-04-14

- Confirmed `src/core` is the active owned selfhost lane.
- Confirmed `src/.rustimport/reference` and `src/.rustimport/phase2` are reference-only donor/mirror surfaces.
- Read the live source scaffold in `src/README.md`, `src/TASK.md`, `src/core/README.md`, `src/core/kainc.kn`, and `src/core/language_features.kn`.
- Read the donor corpus README in `src/.rustimport/README.md`.
- Installed Python 3.12 because PyO3 0.20.3 rejects Python 3.14.
- Updated `generated/kain-env.ps1` and `generated/kain-env.sh` so `PYO3_PYTHON` points at Python 3.12 and the Python 3.12 install dir is on PATH.
- Recorded that `crates/kain-core/src/formatter.rs` was patched earlier to handle `Item::Law`.
- Added AGENTS/memory notes reminding future passes to write down toolchain and setup discoveries immediately, not only at the end.
- Reran `cargo build --release -p cli` with the corrected interpreter and MinGW toolchain env.
- Copied `kain.exe` and `kn.exe` into `C:\Users\ephemara\.cargo\bin` so the launchers resolve on PATH.
- Ran `kain doctor` and `kn doctor` successfully inside the generated environment.

Next:

- Let the release build finish, then verify the resulting `kain`/`kn` binaries and run `kain doctor`.
- After setup is stable, continue the selfhost rewrite in `src/core` using the donor/reference trees only as references.

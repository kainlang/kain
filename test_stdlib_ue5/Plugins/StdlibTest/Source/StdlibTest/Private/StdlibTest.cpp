// Generated by KAIN Compiler - UE5 C++ Codegen
// Do not edit - regenerate from .kn source

#include "StdlibTest.h"
#include "StdlibTestBlueprintLibrary.h"

FVector GetActorLocation()
{
}

void SetActorLocation(const FVector location)
{
}

FRotator GetActorRotation()
{
}

void SetActorRotation(const FRotator rotation)
{
}

FVector GetActorScale()
{
}

void SetActorScale(const FVector scale)
{
}

FVector GetActorForwardVector()
{
}

FVector GetActorRightVector()
{
}

FVector GetActorUpVector()
{
}

void DestroyActor()
{
}

float GetDistanceTo(AActor* other)
{
}

void SetActorHiddenInGame(const bool hidden)
{
}

void SetActorEnableCollision(const bool enable)
{
}

FVector GetActorBounds()
{
}

bool IsActorBeingDestroyed()
{
}

void AddActorWorldOffset(const FVector offset)
{
}

void AddActorWorldRotation(const FRotator rotation)
{
}

void SetLifeSpan(const float seconds)
{
}

float GetLifeSpan()
{
}

void AttachToActor(AActor* parent)
{
}

void DetachFromActor()
{
}

TArray<AActor*> GetAttachedActors()
{
}

AActor* GetAttachParentActor()
{
}

void SetScalarParameter(const UMaterial material, const FString param_name, const float value)
{
}

float GetScalarParameter(const UMaterial material, const FString param_name)
{
}

void SetVectorParameter(const UMaterial material, const FString param_name, const FVector value)
{
}

FVector GetVectorParameter(const UMaterial material, const FString param_name)
{
}

void SetTextureParameter(const UMaterial material, const FString param_name, const UTexture2D texture)
{
}

UMaterial CreateDynamicMaterialInstance(const UMaterial material)
{
}

void SetMaterial(const MeshComponent mesh, const int64 slot, const UMaterial material)
{
}

UMaterial GetMaterial(const MeshComponent mesh, const int64 slot)
{
}

float max(const float a, const float b)
{
}

float min(const float a, const float b)
{
}

float abs(const float x)
{
}

float sqrt(const float x)
{
}

float pow(const float base, const float exp)
{
}

float floor(const float x)
{
}

float ceil(const float x)
{
}

float round(const float x)
{
}

float clamp(const float value, const float min_val, const float max_val)
{
}

float sin(const float x)
{
}

float cos(const float x)
{
}

float tan(const float x)
{
}

float asin(const float x)
{
}

float acos(const float x)
{
}

float atan(const float x)
{
}

float atan2(const float y, const float x)
{
}

float random()
{
}

float Vector_Length(const FVector v)
{
}

FVector Vector_Normalize(const FVector v)
{
}

float Vector_Dot(const FVector a, const FVector b)
{
}

FVector Vector_Cross(const FVector a, const FVector b)
{
}

float Vector_Distance(const FVector a, const FVector b)
{
}

FVector Rotator_GetForwardVector(const FRotator r)
{
}

FVector Rotator_GetRightVector(const FRotator r)
{
}

FVector Rotator_GetUpVector(const FRotator r)
{
}

float Lerp(const float a, const float b, const float alpha)
{
}

FVector VLerp(const FVector a, const FVector b, const float alpha)
{
}

FRotator RLerp(const FRotator a, const FRotator b, const float alpha, const bool shortest_path)
{
}

float FInterpTo(const float current, const float target, const float delta_time, const float speed)
{
}

FVector VInterpTo(const FVector current, const FVector target, const float delta_time, const float speed)
{
}

AActor* SpawnEmitterAtLocation(const FString system_name, const FVector location)
{
}

AActor* SpawnEmitterAttached(const FString system_name, const USceneComponent attach_to, const FString socket_name)
{
}

void ActivateSystem(AActor* niagara)
{
}

void DeactivateSystem(AActor* niagara)
{
}

void SetFloatParameter(AActor* niagara, const FString param_name, const float value)
{
}

void SetVectorParameter(AActor* niagara, const FString param_name, const FVector value)
{
}

void SetColorParameter(AActor* niagara, const FString param_name, const FVector4 color)
{
}

void SetActorParameter(AActor* niagara, const FString param_name, AActor* target)
{
}

void SetBoolParameter(AActor* niagara, const FString param_name, const bool value)
{
}

void PlayAnimation(const USkeletalMesh mesh, const FString anim_name)
{
}

void StopAnimation(const USkeletalMesh mesh)
{
}

void SetAnimationSpeed(const USkeletalMesh mesh, const float speed)
{
}

bool IsAnimationPlaying(const USkeletalMesh mesh)
{
}

FVector GetBoneLocation(const USkeletalMesh mesh, const FString bone_name)
{
}

FRotator GetBoneRotation(const USkeletalMesh mesh, const FString bone_name)
{
}

void SetBoneTransform(const USkeletalMesh mesh, const FString bone_name, const FVector location, const FRotator rotation)
{
}

void AttachToSocket(const USkeletalMesh mesh, const FString socket_name, AActor* target)
{
}

FVector GetSocketLocation(const USkeletalMesh mesh, const FString socket_name)
{
}

FRotator GetSocketRotation(const USkeletalMesh mesh, const FString socket_name)
{
}

void EnableRagdoll(const USkeletalMesh mesh)
{
}

void DisableRagdoll(const USkeletalMesh mesh)
{
}

void AddImpulseToBone(const USkeletalMesh mesh, const FString bone_name, const FVector impulse)
{
}

void SetMorphTarget(const USkeletalMesh mesh, const FString morph_name, const float value)
{
}

float GetMorphTarget(const USkeletalMesh mesh, const FString morph_name)
{
}

float GetWorldTimeSeconds()
{
}

float GetWorldDeltaSeconds()
{
}

bool IsServer()
{
}

bool IsClient()
{
}

bool IsStandalone()
{
}

AActor* SpawnActor(const FString class_name, const FVector location, const FRotator rotation)
{
}

AActor* SpawnActorDeferred(const FString class_name, const FVector location, const FRotator rotation)
{
}

void FinishSpawningActor(AActor* spawned)
{
}

void PrintToScreen(const FString message)
{
}

void PrintToLog(const FString message)
{
}

AActor* GetGameMode()
{
}

AActor* GetGameState()
{
}

AActor* GetPlayerController()
{
}

AActor* GetPlayerPawn()
{
}

bool LineTraceSingle(const FVector start, const FVector end, const int64 channel)
{
}

TArray<AActor*> LineTraceMulti(const FVector start, const FVector end, const int64 channel)
{
}

bool SphereTraceSingle(const FVector start, const FVector end, const float radius, const int64 channel)
{
}

bool BoxTraceSingle(const FVector start, const FVector end, const FVector half_size, const int64 channel)
{
}

AStdlibTestActor::AStdlibTestActor()
{
	PrimaryActorTick.bCanEverTick = true;
}

void AStdlibTestActor::BeginPlay()
{
	Super::BeginPlay();
	const auto current_loc = GetActorLocation();
	SetActorLocation(FVector(100.000000f, 200.000000f, 300.000000f));
	const auto time = GetWorld()->GetTimeSeconds();
	const auto delta = GetWorld()->GetDeltaSeconds();
	const auto remapped = UStdlibTestFunctionLibrary::remap(0.500000f, 0.000000f, 1.000000f, 10.000000f, 20.000000f);
	UE_LOG(LogTemp, Warning, TEXT("Stdlib test complete"));
}

void AStdlibTestActor::Tick(float DeltaTime)
{
	Super::Tick(DeltaTime);
}

			float UStdlibTestFunctionLibrary::apply_damage(const float current_health, const float max_health, const float damage, const float armor)
			{
				const auto mitigated_damage = (damage * ((1.000000f - (armor / 100.000000f))));
				const auto new_health = (current_health - mitigated_damage);
				return FMath::Max(new_health, 0.000000f);
			}

			float UStdlibTestFunctionLibrary::apply_healing(const float current_health, const float max_health, const float heal_amount)
			{
				const auto new_health = (current_health + heal_amount);
				return FMath::Min(new_health, max_health);
			}

			float UStdlibTestFunctionLibrary::get_health_percentage(const float current_health, const float max_health)
			{
				if ((max_health <= 0.000000f))
				{
					return 0.000000f;
				}
				return (((current_health / max_health)) * 100.000000f);
			}

			bool UStdlibTestFunctionLibrary::is_low_health(const float current_health, const float max_health, const float threshold)
			{
				return (UStdlibTestFunctionLibrary::get_health_percentage(current_health, max_health) < threshold);
			}

			float UStdlibTestFunctionLibrary::calculate_crit_damage(const float base_damage, const float crit_multiplier)
			{
				return (base_damage * crit_multiplier);
			}

			bool UStdlibTestFunctionLibrary::should_crit(const float crit_chance)
			{
				return (FMath::FRand() < ((crit_chance / 100.000000f)));
			}

			float UStdlibTestFunctionLibrary::calculate_armor_mitigation(const float damage, const float armor)
			{
				return (damage * ((1.000000f - (armor / ((armor + 100.000000f))))));
			}

			int64 UStdlibTestFunctionLibrary::calculate_experience_for_level(const int64 level)
			{
				return ((level * level) * 100);
			}

			int64 UStdlibTestFunctionLibrary::add_experience(const int64 current_xp, const int64 xp_to_add, const int64 current_level)
			{
				const auto new_xp = (current_xp + xp_to_add);
				const auto xp_for_next_level = UStdlibTestFunctionLibrary::calculate_experience_for_level((current_level + 1));
				if ((new_xp >= xp_for_next_level))
				{
					return (new_xp - xp_for_next_level);
				}
				return new_xp;
			}

			float UStdlibTestFunctionLibrary::get_xp_progress(const int64 current_xp, const int64 current_level)
			{
				const auto xp_for_current = UStdlibTestFunctionLibrary::calculate_experience_for_level(current_level);
				const auto xp_for_next = UStdlibTestFunctionLibrary::calculate_experience_for_level((current_level + 1));
				const auto xp_needed = (xp_for_next - xp_for_current);
				const auto xp_progress = (current_xp - xp_for_current);
				return (((xp_progress / xp_needed)) * 100.000000f);
			}

			bool UStdlibTestFunctionLibrary::can_add_item_to_inventory(const int64 current_count, const int64 max_capacity, const int64 item_stack_size)
			{
				return ((current_count + item_stack_size) <= max_capacity);
			}

			float UStdlibTestFunctionLibrary::calculate_inventory_weight(const TArray<FInventorySlot> items, const TArray<float> item_weights)
			{
				const auto total_weight = 0.000000f;
				const auto i = 0;
				while ((i < items.Num()))
				{
					const auto slot = items[i];
					if (((slot.item_id >= 0) && (slot.item_id < item_weights.Num())))
					{
						total_weight = (total_weight + (item_weights[slot.item_id] * slot.quantity));
					}
					i = (i + 1);
				}
				return total_weight;
			}

			bool UStdlibTestFunctionLibrary::is_cooldown_ready(const float last_use_time, const float cooldown_duration, const float current_time)
			{
				return ((current_time - last_use_time) >= cooldown_duration);
			}

			float UStdlibTestFunctionLibrary::get_cooldown_remaining(const float last_use_time, const float cooldown_duration, const float current_time)
			{
				const auto elapsed = (current_time - last_use_time);
				const auto remaining = (cooldown_duration - elapsed);
				return FMath::Max(remaining, 0.000000f);
			}

			float UStdlibTestFunctionLibrary::get_cooldown_percentage(const float last_use_time, const float cooldown_duration, const float current_time)
			{
				const auto elapsed = (current_time - last_use_time);
				return FMath::Min((((elapsed / cooldown_duration)) * 100.000000f), 100.000000f);
			}

			float UStdlibTestFunctionLibrary::apply_buff(const float base_value, const float buff_magnitude, const EBuffType buff_type)
			{
				return ((buff_type == EBuffType::Damage) ? (base_value * ((1.000000f + (buff_magnitude / 100.000000f)))) : (buff_type == EBuffType::Defense) ? (base_value * ((1.000000f + (buff_magnitude / 100.000000f)))) : (buff_type == EBuffType::Speed) ? (base_value * ((1.000000f + (buff_magnitude / 100.000000f)))) : base_value);
			}

			float UStdlibTestFunctionLibrary::update_buff_duration(const float remaining_time, const float delta_time)
			{
				return FMath::Max((remaining_time - delta_time), 0.000000f);
			}

			bool UStdlibTestFunctionLibrary::is_buff_active(const float remaining_time)
			{
				return (remaining_time > 0.000000f);
			}

			bool UStdlibTestFunctionLibrary::roll_loot_drop(const float drop_chance)
			{
				return (FMath::FRand() < ((drop_chance / 100.000000f)));
			}

			ELootRarity UStdlibTestFunctionLibrary::determine_loot_rarity(const float luck_stat)
			{
				const auto roll = ((FMath::FRand() * 100.000000f) + luck_stat);
				if ((roll >= 95.000000f))
				{
					return ELootRarity::Mythic;
				}
				if ((roll >= 85.000000f))
				{
					return ELootRarity::Legendary;
				}
				if ((roll >= 70.000000f))
				{
					return ELootRarity::Epic;
				}
				if ((roll >= 50.000000f))
				{
					return ELootRarity::Rare;
				}
				if ((roll >= 25.000000f))
				{
					return ELootRarity::Uncommon;
				}
				return ELootRarity::Common;
			}

			int64 UStdlibTestFunctionLibrary::update_quest_objective(const int64 current_progress, const int64 increment, const int64 required_progress)
			{
				const auto new_progress = (current_progress + increment);
				return FMath::Min(new_progress, required_progress);
			}

			bool UStdlibTestFunctionLibrary::is_quest_objective_complete(const int64 current_progress, const int64 required_progress)
			{
				return (current_progress >= required_progress);
			}

			float UStdlibTestFunctionLibrary::get_quest_progress_percentage(const int64 current_progress, const int64 required_progress)
			{
				if ((required_progress <= 0))
				{
					return 0.000000f;
				}
				return (((current_progress / required_progress)) * 100.000000f);
			}

			FVector UStdlibTestFunctionLibrary::fresnel_schlick(const float cos_theta, const FVector f0)
			{
				const auto one_minus_cos = (1.000000f - cos_theta);
				const auto factor = FMath::Pow(one_minus_cos, 5.000000f);
				const auto one_vec = FVector(1.000000f, 1.000000f, 1.000000f);
				return (f0 + (((one_vec - f0)) * factor));
			}

			float UStdlibTestFunctionLibrary::distribution_ggx(const FVector n, const FVector h, const float roughness)
			{
				const auto a = (roughness * roughness);
				const auto a2 = (a * a);
				const auto n_dot_h = FMath::Max(FVector::DotProduct(n, h), 0.000000f);
				const auto n_dot_h2 = (n_dot_h * n_dot_h);
				const auto denom = ((n_dot_h2 * ((a2 - 1.000000f))) + 1.000000f);
				const auto pi = 3.141593f;
				return (a2 / (((pi * denom) * denom)));
			}

			float UStdlibTestFunctionLibrary::geometry_schlick_ggx(const float n_dot_v, const float roughness)
			{
				const auto r = (roughness + 1.000000f);
				const auto k = (((r * r)) / 8.000000f);
				const auto denom = ((n_dot_v * ((1.000000f - k))) + k);
				return (n_dot_v / denom);
			}

			float UStdlibTestFunctionLibrary::hash(const FVector2D p)
			{
				const auto dot_val = FVector::DotProduct(p, FVector2D(127.100000f, 311.700000f));
				const auto sin_val = FMath::Sin(dot_val);
				return FMath::Frac((sin_val * 43758.545312f));
			}

			float UStdlibTestFunctionLibrary::perlin_noise(const FVector2D uv)
			{
				const auto i = FMath::FloorToFloat(uv);
				const auto f = FMath::Frac(uv);
				const auto a = UStdlibTestFunctionLibrary::hash(i);
				const auto b = UStdlibTestFunctionLibrary::hash((i + FVector2D(1.000000f, 0.000000f)));
				const auto c = UStdlibTestFunctionLibrary::hash((i + FVector2D(0.000000f, 1.000000f)));
				const auto d = UStdlibTestFunctionLibrary::hash((i + FVector2D(1.000000f, 1.000000f)));
				const auto u = ((f * f) * ((3.000000f - (2.000000f * f))));
				const auto ab = (a + (((b - a)) * u.X));
				const auto cd = (c + (((d - c)) * u.X));
				return (ab + (((cd - ab)) * u.Y));
			}

			float UStdlibTestFunctionLibrary::fbm(const FVector2D uv, const int64 octaves)
			{
				const auto value = 0.000000f;
				const auto amplitude = 0.500000f;
				const auto frequency = 1.000000f;
				const auto i = 0;
				while ((i < octaves))
				{
					const auto scaled_uv = FVector2D((uv.X * frequency), (uv.Y * frequency));
					value = (value + (amplitude * UStdlibTestFunctionLibrary::perlin_noise(scaled_uv)));
					frequency = (frequency * 2.000000f);
					amplitude = (amplitude * 0.500000f);
					i = (i + 1);
				}
				return value;
			}

			FVector UStdlibTestFunctionLibrary::apply_contrast(const FVector color, const float contrast)
			{
				const auto adjusted_r = ((((color.X - 0.500000f)) * contrast) + 0.500000f);
				const auto adjusted_g = ((((color.Y - 0.500000f)) * contrast) + 0.500000f);
				const auto adjusted_b = ((((color.Z - 0.500000f)) * contrast) + 0.500000f);
				return FVector(adjusted_r, adjusted_g, adjusted_b);
			}

			FVector UStdlibTestFunctionLibrary::apply_saturation(const FVector color, const float saturation)
			{
				const auto gray = (((0.299000f * color.X) + (0.587000f * color.Y)) + (0.114000f * color.Z));
				const auto r = (gray + (((color.X - gray)) * saturation));
				const auto g = (gray + (((color.Y - gray)) * saturation));
				const auto b = (gray + (((color.Z - gray)) * saturation));
				return FVector(r, g, b);
			}

			FVector UStdlibTestFunctionLibrary::tonemap_aces(const FVector color)
			{
				const auto a = 2.510000f;
				const auto b = 0.030000f;
				const auto c = 2.430000f;
				const auto d = 0.590000f;
				const auto e = 0.140000f;
				const auto numerator_r = (color.X * (((a * color.X) + b)));
				const auto numerator_g = (color.Y * (((a * color.Y) + b)));
				const auto numerator_b = (color.Z * (((a * color.Z) + b)));
				const auto denom_r = ((color.X * (((c * color.X) + d))) + e);
				const auto denom_g = ((color.Y * (((c * color.Y) + d))) + e);
				const auto denom_b = ((color.Z * (((c * color.Z) + d))) + e);
				return FVector((numerator_r / denom_r), (numerator_g / denom_g), (numerator_b / denom_b));
			}

			FVector UStdlibTestFunctionLibrary::tonemap_reinhard(const FVector color)
			{
				const auto r = (color.X / ((1.000000f + color.X)));
				const auto g = (color.Y / ((1.000000f + color.Y)));
				const auto b = (color.Z / ((1.000000f + color.Z)));
				return FVector(r, g, b);
			}

			FVector2D UStdlibTestFunctionLibrary::rotate_uv(const FVector2D uv, const float angle)
			{
				const auto cos_angle = FMath::Cos(angle);
				const auto sin_angle = FMath::Sin(angle);
				const auto centered = FVector2D((uv.X - 0.500000f), (uv.Y - 0.500000f));
				const auto rotated_x = ((centered.X * cos_angle) - (centered.Y * sin_angle));
				const auto rotated_y = ((centered.X * sin_angle) + (centered.Y * cos_angle));
				return FVector2D((rotated_x + 0.500000f), (rotated_y + 0.500000f));
			}

			FVector2D UStdlibTestFunctionLibrary::polar_coordinates(const FVector2D uv)
			{
				const auto centered = FVector2D((uv.X - 0.500000f), (uv.Y - 0.500000f));
				const auto radius = FMath::Sqrt(((centered.X * centered.X) + (centered.Y * centered.Y)));
				const auto angle = FMath::Atan2(centered.Y, centered.X);
				return FVector2D(radius, angle);
			}

			float UStdlibTestFunctionLibrary::vignette(const FVector2D uv, const float intensity)
			{
				const auto centered = FVector2D((uv.X - 0.500000f), (uv.Y - 0.500000f));
				const auto dist = FMath::Sqrt(((centered.X * centered.X) + (centered.Y * centered.Y)));
				const auto vignette_val = (1.000000f - (dist * intensity));
				return FMath::Clamp(vignette_val, 0.000000f, 1.000000f);
			}

			FVector2D UStdlibTestFunctionLibrary::chromatic_aberration(const FVector2D uv, const float offset)
			{
				const auto centered = FVector2D((uv.X - 0.500000f), (uv.Y - 0.500000f));
				const auto dist = FMath::Sqrt(((centered.X * centered.X) + (centered.Y * centered.Y)));
				const auto direction = FVector2D((centered.X / dist), (centered.Y / dist));
				return FVector2D((direction.X * offset), (direction.Y * offset));
			}

			float UStdlibTestFunctionLibrary::remap(const float value, const float in_min, const float in_max, const float out_min, const float out_max)
			{
				const auto normalized = (((value - in_min)) / ((in_max - in_min)));
				return (out_min + (normalized * ((out_max - out_min))));
			}

			float UStdlibTestFunctionLibrary::smooth_step(const float edge0, const float edge1, const float x)
			{
				const auto t = FMath::Clamp((((x - edge0)) / ((edge1 - edge0))), 0.000000f, 1.000000f);
				return ((t * t) * ((3.000000f - (2.000000f * t))));
			}

			FVector UStdlibTestFunctionLibrary::lerp_color(const FVector a, const FVector b, const float alpha)
			{
				const auto r = (a.X + (((b.X - a.X)) * alpha));
				const auto g = (a.Y + (((b.Y - a.Y)) * alpha));
				const auto b_val = (a.Z + (((b.Z - a.Z)) * alpha));
				return FVector(r, g, b_val);
			}

			float UStdlibTestFunctionLibrary::distance_2d(const FVector2D a, const FVector2D b)
			{
				const auto dx = (b.X - a.X);
				const auto dy = (b.Y - a.Y);
				return FMath::Sqrt(((dx * dx) + (dy * dy)));
			}

			float UStdlibTestFunctionLibrary::random_range(const float min, const float max)
			{
				const auto rand_val = FMath::FRand();
				return (min + (rand_val * ((max - min))));
			}

			int64 UStdlibTestFunctionLibrary::random_int_range(const int64 min, const int64 max)
			{
				const auto range = ((max - min) + 1);
				const auto rand_val = FMath::FRand();
				return (min + FMath::FloorToFloat((rand_val * range)));
			}

			bool UStdlibTestFunctionLibrary::random_bool(const float probability)
			{
				return (FMath::FRand() < probability);
			}

			int64 UStdlibTestFunctionLibrary::weighted_random(const TArray<float> weights)
			{
				const auto total_weight = 0.000000f;
				const auto i = 0;
				while ((i < weights.Num()))
				{
					total_weight = (total_weight + weights[i]);
					i = (i + 1);
				}
				const auto rand_val = (FMath::FRand() * total_weight);
				const auto cumulative = 0.000000f;
				const auto j = 0;
				while ((j < weights.Num()))
				{
					cumulative = (cumulative + weights[j]);
					if ((rand_val <= cumulative))
					{
						return j;
					}
					j = (j + 1);
				}
				return (weights.Num() - 1);
			}

			float UStdlibTestFunctionLibrary::clamp_01(const float value)
			{
				if ((value < 0.000000f))
				{
					return 0.000000f;
				}
				if ((value > 1.000000f))
				{
					return 1.000000f;
				}
				return value;
			}

			float UStdlibTestFunctionLibrary::normalize_angle(const float angle)
			{
				const auto normalized = angle;
				while ((normalized > 180.000000f))
				{
					normalized = (normalized - 360.000000f);
				}
				while ((normalized < (-180.000000f)))
				{
					normalized = (normalized + 360.000000f);
				}
				return normalized;
			}

			FString UStdlibTestFunctionLibrary::format_time(const float seconds)
			{
				const auto total_seconds = FMath::FloorToFloat(seconds);
				const auto minutes = FMath::FloorToFloat((total_seconds / 60.000000f));
				const auto remaining_seconds = (total_seconds - ((minutes * 60.000000f)));
				if ((minutes > 0.000000f))
				{
					return TEXT("{minutes}m {remaining_seconds}s");
				}
				return TEXT("{remaining_seconds}s");
			}

			FString UStdlibTestFunctionLibrary::format_number(const float value, const int64 decimals)
			{
				const auto multiplier = FMath::Pow(10.000000f, decimals);
				const auto rounded = (FMath::RoundToFloat((value * multiplier)) / multiplier);
				return TEXT("{rounded}");
			}


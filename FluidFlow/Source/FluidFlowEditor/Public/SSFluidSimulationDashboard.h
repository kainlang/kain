// Generated by KAIN - UE5 Editor Tools
#pragma once
#include "CoreMinimal.h"
#include "FluidFlowEditorTypes.h"
#include "Widgets/SCompoundWidget.h"
#include "Widgets/DeclarativeSyntaxSupport.h"
#include "Widgets/Views/SListView.h"
#include "Widgets/Input/SButton.h"
#include "Widgets/Input/SCheckBox.h"
#include "Widgets/Input/SComboBox.h"
#include "Widgets/Input/SEditableTextBox.h"
#include "Widgets/Input/SSlider.h"
#include "Widgets/Input/SSpinBox.h"
#include "Widgets/Text/STextBlock.h"
#include "Widgets/Images/SImage.h"
#include "Widgets/Layout/SScrollBox.h"
#include "Widgets/Layout/SSplitter.h"
#include "Widgets/Layout/SBorder.h"
#include "Widgets/Colors/SColorBlock.h"

class SSFluidSimulationDashboard : public SCompoundWidget
{
public:
	// Layout Optimization Report
	// Static Properties: 9 (no Tick overhead)
	// Reactive Properties: 0 (requires Tick)
	// Event Handlers: 0
	// Estimated Tick Reduction: 100.0%
	//
	// SLATE_ARGUMENT title - Explicit @argument attribute
	// SLATE_ARGUMENT simulation_running - Default (no clear reactive usage)
	// SLATE_ARGUMENT current_fps - Default (no clear reactive usage)
	// SLATE_ARGUMENT particle_count - Default (no clear reactive usage)
	// SLATE_ARGUMENT grid_resolution - Default (no clear reactive usage)
	// SLATE_ARGUMENT on_start_simulation - Default (no clear reactive usage)
	// SLATE_ARGUMENT on_stop_simulation - Default (no clear reactive usage)
	// SLATE_ARGUMENT on_reset_simulation - Default (no clear reactive usage)
	// SLATE_ARGUMENT on_quality_changed - Default (no clear reactive usage)
	SLATE_BEGIN_ARGS(SSFluidSimulationDashboard)
		: _Content()
		, _title(FText::GetEmpty())
		, _simulation_running(false)
		, _current_fps(0.0f)
		, _particle_count(0)
		, _grid_resolution(FVector::ZeroVector)
		, _on_start_simulation(FOnButtonClicked())
		, _on_stop_simulation(FOnButtonClicked())
		, _on_reset_simulation(FOnButtonClicked())
		, _on_quality_changed(FOnSelectionChanged())
		{}
		
		SLATE_DEFAULT_SLOT(FArguments, Content)
		SLATE_ARGUMENT(FText, title)
		SLATE_ARGUMENT(bool, simulation_running)
		SLATE_ARGUMENT(float, current_fps)
		SLATE_ARGUMENT(int32, particle_count)
		SLATE_ARGUMENT(FVector, grid_resolution)
		SLATE_ARGUMENT(FOnButtonClicked, on_start_simulation)
		SLATE_ARGUMENT(FOnButtonClicked, on_stop_simulation)
		SLATE_ARGUMENT(FOnButtonClicked, on_reset_simulation)
		SLATE_ARGUMENT(FOnSelectionChanged, on_quality_changed)
	SLATE_END_ARGS()
	
	void Construct(const FArguments& InArgs);
	
	FReply Handleon_start_simulation();
	
	FReply Handleon_stop_simulation();
	
	FReply Handleon_reset_simulation();
	
	FReply Handleon_quality_changed();
};


// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "SmoothedParticleHydrodynamics.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FSmoothedParticleHydrodynamicsShader, "/Plugin/FluidFlow/SmoothedParticleHydrodynamics.usf", "SmoothedParticleHydrodynamicsCS", SF_Compute);

// Helper function to add pass to render graph
void AddPass_SmoothedParticleHydrodynamics(
    FRDGBuilder& GraphBuilder,
    PhysicalPropertiesComponent physics,
    TurbulenceComponent turbulence,
    FRDGTextureRef OutputTexture,
    FIntVector GroupCount
)
{
    FSmoothedParticleHydrodynamicsShader::FParameters* Params = GraphBuilder.AllocParameters<FSmoothedParticleHydrodynamicsShader::FParameters>();
    // Bind Scalars
    Params->physics = physics;
    Params->turbulence = turbulence;
    // Bind UAVs
    Params->OutputTexture = GraphBuilder.CreateUAV(OutputTexture);

    FSmoothedParticleHydrodynamicsShader::Exec(GraphBuilder, *Params, GroupCount);
}

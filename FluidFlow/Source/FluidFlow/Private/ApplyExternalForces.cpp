// Generated by KAIN Compiler - UE5 Shader Implementation
// Shader registration and dispatch helpers

#include "ApplyExternalForces.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderParameterUtils.h"
#include "GlobalShader.h"

// Register shader with UE5
IMPLEMENT_GLOBAL_SHADER(FApplyExternalForcesShader, "/Plugin/FluidFlow/ApplyExternalForces.usf", "ApplyExternalForcesCS", SF_Compute);

// Helper function to add pass to render graph
void AddPass_ApplyExternalForces(
    FRDGBuilder& GraphBuilder,
    FPhysicalPropertiesComponentData physics,
    FThermalComponentData thermal,
    FTimeIntegrationComponentData time,
    FRDGTextureRef velocity_texture,
    FRDGTextureRef density_texture,
    FRDGTextureRef temperature_texture,
    FRDGTextureRef OutputTexture,
    FIntVector GroupCount
)
{
    FApplyExternalForcesShader::FParameters* Params = GraphBuilder.AllocParameters<FApplyExternalForcesShader::FParameters>();
    // Bind Scalars
    Params->physics = physics;
    Params->thermal = thermal;
    Params->time = time;
    // Bind Textures
    Params->velocity_texture = velocity_texture;
    Params->velocity_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    Params->density_texture = density_texture;
    Params->density_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    Params->temperature_texture = temperature_texture;
    Params->temperature_textureSampler = TStaticSamplerState<SF_Bilinear, AM_Clamp, AM_Clamp, AM_Clamp>::GetRHI();
    // Bind UAVs
    Params->OutputTexture = GraphBuilder.CreateUAV(OutputTexture);

    FApplyExternalForcesShader::Exec(GraphBuilder, *Params, GroupCount);
}

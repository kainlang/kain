// Generated by KAIN Compiler - UE5 Shader Parameter Reflection
// This file provides C++ bindings for shader parameters

#pragma once

#include "CoreMinimal.h"
#include "GlobalShader.h"
#include "ShaderParameters.h"
#include "ShaderParameterStruct.h"
#include "ShaderParameterUtils.h"
#include "RenderGraphResources.h"
#include "RenderGraphBuilder.h"
#include "RenderGraphUtils.h"
#include "ShaderCompilerCore.h"
#include "RHIStaticStates.h"

#include "FluidFlowShaderTypes.h"

class FJacobiPressureShader : public FGlobalShader
{
    DECLARE_GLOBAL_SHADER(FJacobiPressureShader);
    SHADER_USE_PARAMETER_STRUCT(FJacobiPressureShader, FGlobalShader);

    BEGIN_SHADER_PARAMETER_STRUCT(FParameters, )
        SHADER_PARAMETER_STRUCT(FPhysicalPropertiesComponentData, physics)
        SHADER_PARAMETER_RDG_TEXTURE(Texture3D, pressure_texture)
        SHADER_PARAMETER_SAMPLER(SamplerState, pressure_textureSampler)
        SHADER_PARAMETER_RDG_TEXTURE(Texture3D, divergence_texture)
        SHADER_PARAMETER_SAMPLER(SamplerState, divergence_textureSampler)
        SHADER_PARAMETER_RDG_TEXTURE_UAV(RWTexture2D<float4>, OutputTexture)
    END_SHADER_PARAMETER_STRUCT()

    // Automatically generates RDG compute pass
    static void Exec(FRDGBuilder& GraphBuilder, const FParameters& Parameters, FIntVector GroupCount)
    {
        TShaderMapRef<FJacobiPressureShader> ComputeShader(GetGlobalShaderMap(GMaxRHIFeatureLevel));

        FParameters* PassParameters = GraphBuilder.AllocParameters<FParameters>();
        *PassParameters = Parameters;

        FComputeShaderUtils::AddPass(
            GraphBuilder,
            RDG_EVENT_NAME("JacobiPressure Dispatch"),
            ComputeShader,
            PassParameters,
            GroupCount
        );
    }
};

// In your .cpp file, add:
// IMPLEMENT_GLOBAL_SHADER(FJacobiPressureShader, "/Plugin/FluidFlow/JacobiPressure.usf", "JacobiPressureCS", SF_Compute);

// Helper function to add pass to render graph
void AddPass_JacobiPressure(
    FRDGBuilder& GraphBuilder,
    FPhysicalPropertiesComponentData physics,
    FRDGTextureRef pressure_texture,
    FRDGTextureRef divergence_texture,
    FRDGTextureRef OutputTexture,
    FIntVector GroupCount = FIntVector(32, 32, 1)
);

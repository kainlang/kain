// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// POD mirror for MultiphaseComponent
struct FMultiphaseComponentData {
    int phases;
    float phase_field_mobility;
    float interface_thickness;
    float surface_tension_coupling;
    float contact_angle;
    float bubble_spawn_rate;
    float droplet_spawn_rate;
};

// POD mirror for ThermalComponent
struct FThermalComponentData {
    float temperature;
    float thermal_diffusivity;
    float buoyancy_alpha;
    float buoyancy_beta;
    float radiation_gain;
    int radiation_model;
};

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
FMultiphaseComponentData multiphase;
FThermalComponentData thermal;

// UAV Outputs
RWTexture2D<float4> OutputTexture : register(u0);

[numthreads(32, 32, 1)]
void PhaseChange_CondensationCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float cond = ((((300.000000 - thermal.temperature)) * multiphase.phase_field_mobility) * 0.001000);
    OutputTexture[ThreadId.xy] = float4(cond, cond, cond, 1.000000);
    return;
}

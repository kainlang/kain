// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
PhysicalPropertiesComponent physics;
TurbulenceComponent turbulence;

// UAV Outputs
RWTexture2D<float4> OutputTexture : register(u0);

[numthreads(32, 32, 1)]
void LatticeBoltzmannCollisionCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float x = ThreadId.x;
    float y = ThreadId.y;
    float z = ThreadId.z;
    float energy = (float(((x + y) + z)) * turbulence.intensity);
    OutputTexture[ThreadId.xy] = float4(energy, energy, energy, 1.000000);
    return;
}

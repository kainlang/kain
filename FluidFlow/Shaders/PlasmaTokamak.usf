// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// POD mirror for ElectroMagneticComponent
struct FElectroMagneticComponentData {
    float charge_density;
    float3 electric_field;
    float3 magnetic_field;
    float lorentz_force_gain;
    float resistivity;
    float hall_parameter;
    float ambipolar_diffusion;
};

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
FElectroMagneticComponentData em;

// UAV Outputs
RWTexture2D<float4> OutputTexture : register(u0);

[numthreads(32, 32, 1)]
void PlasmaTokamakCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float confinement = (em.hall_parameter * 2.000000);
    OutputTexture[ThreadId.xy] = float4(confinement, confinement, confinement, 1.000000);
    return;
}

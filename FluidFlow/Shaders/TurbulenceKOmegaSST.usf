// Generated by KAIN Compiler - Unreal Shader Format
// Compatible with UE5 SHADER_PARAMETER_STRUCT bindings

#include "/Engine/Public/Platform.ush"

// POD mirror for TurbulenceAdvancedComponent
struct FTurbulenceAdvancedComponentData {
    int les_model;
    int rans_model;
    int wall_function;
    float smagorinsky_constant;
    float wale_constant;
    float vreman_constant;
    float k_epsilon_c_mu;
    float k_epsilon_c1;
    float k_epsilon_c2;
    float k_omega_beta_star;
    float k_omega_sigma_k;
    float k_omega_sigma_omega;
    float y_plus_target;
    bool wall_damping;
    int transition_model;
    float intermittency;
    float transition_onset_reynolds;
    int synthetic_method;
    int eddy_count;
    float eddy_lifetime;
    int reynolds_stress_model;
};

// Scalar Parameters - Bind via SHADER_PARAMETER_STRUCT
FTurbulenceAdvancedComponentData turbulence_advanced;

// UAV Outputs
RWTexture2D<float4> OutputTexture : register(u0);

[numthreads(32, 32, 1)]
void TurbulenceKOmegaSSTCS(uint3 ThreadId : SV_DispatchThreadID)
{
    // Safe Texture Load Pattern
    int3 TexCoord = int3(ThreadId.xy, 0);
    float omega = (turbulence_advanced.k_omega_beta_star * 0.500000);
    OutputTexture[ThreadId.xy] = float4(omega, omega, omega, 1.000000);
    return;
}

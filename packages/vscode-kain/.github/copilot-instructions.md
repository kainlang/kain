

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v7.30.0 -->
<!-- Updated: 2026-06-26T10:47:28.663Z -->
<!-- sigmap: version=7.30.0 -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures
## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| To rank files by topic | `sigmap --query "<topic>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

Always run `sigmap ask` (or `sigmap --query`) before searching for files relevant to a task.

## test

### test\syntax_test.kn
```
fn test_fn(x: Int, y: Int) -> Int with Pure:
fn score(_self: Self_) -> Int:
fn score(_self: Self_) -> Int:
pub fn clamp(v: Int, lo: Int, hi: Int) -> Int:
fn pure_fn(x: Int) -> Int with Pure:
fn io_fn(msg: String) -> Int with IO:
fn gpu_fn() -> Int with GPU:
fn react_fn() -> Int with Reactive:
fn unsafe_fn(p: ptr<Int>) -> Int with Unsafe:
fn demo_teleport() -> Int:
let s = Shard { bias: 1, phase: 2, tick: 3, checksum: 4, alive: true }
let moved = teleport s from AppState to Mirror via bus
let result = (val * self.bias + self.ticks) % 1000000007
fn raw_demo() -> Int with Unsafe:
fn contention_demo(counter: ptr<Int>, iterations: Int) -> Int with Unsafe:
```

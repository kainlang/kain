# THE SCOOP // NIGHT CITY PIPELINE WIRE

**Edition Time (ET):** 2026-03-11 19:24
**Delta Window:** Since last automation run at **2026-03-11T20:20:35.130Z** (2026-03-11 16:20:35 ET)
**Scan Scope:** `M:/K_OS`, `M:/dev3D/PBR`, `M:/dev3D/ZenMocap`

## Executive Signal
No new git commits landed in-window, but `M:/K_OS` received major working-tree feature expansion and architecture wiring. `PBR` had no in-window code updates (one docs file touch), and standalone `ZenMocap` repo had no in-window changes.

## K_OS // Feature Broadcast

### 1) Native Renderer + GPU upload pipeline hardened
- Added staged partial-buffer upload path in `k-os-gpu-pipeline`:
  - `GpuMeshPartialUploadPlan` and byte-range diffing (`changed_range`) now uploads only modified ranges.
  - New typed growth policy: `GpuMeshBufferGrowthPolicy` + exported capacity helpers.
- `k-os-renderer` bridge now caches previous upload plans and reports uploaded-byte deltas from partial updates instead of full-buffer assumptions.
- Renderer service now distinguishes **soft-continue** vs **hard-fail** mesh sync errors and logs them with viewport/mesh context.
- Added regression test proving redraw continues even when evaluated mesh source temporarily fails.

### 2) Game runtime architecture expanded to packaging lanes
- Workspace members expanded with new crates:
  - `k-os-game-framework`, `k-os-game-play`, `k-os-game-ai`, `k-os-game-camera`, `k-os-game-input`
- `k-os-game-runtime` now includes:
  - `runtime_policy` strict readiness contract
  - `stage_schemas` enforcement
  - new stage kinds: `cook`, `build`, `export`
  - validation + artifact adapters for cook/build/export packaging paths
- `src-game/config/pipeline.toml` now declares:
  - required runtime registry kinds including `ai_agents`
  - stage schema contracts with explicit `schema_version`
  - runtime packaging stage chain (`cook_windows_content` -> `build_windows_player` -> `export_windows_bundle`)

### 3) Kain runtime registry growth (new builder surfaces)
- Added runtime app manifests for:
  - `material_builder`, `fluid_builder`, `procedural_builder`
- Extended Kain domain coverage:
  - `Brush`, `Shader`, `Procedural` added to `KainDomain`
  - source/runtime lookup tests expanded accordingly
- Tauri source mapping updated for new domains (`fluid`, `brush`, `shader`).

### 4) Photogrammetry stack moved from placeholder math to production contracts
- `feature_matching.rs`:
  - replaced placeholder fundamental-matrix path with normalized 8-point estimation
  - added Sampson epipolar distance
  - added essential-matrix decomposition and cheirality candidate selection
  - added deterministic RANSAC seed for reproducibility
- `mesh_generation.rs`:
  - added data-driven `MeshGenerationConfig` with explicit strategy/fallback types
  - sparse-cloud behavior now policy-driven (`CompatibilityQuad` vs `EmptyMesh`)
- `pbr_extraction.rs`:
  - added `PBRMapValidationContract` with per-map channel/non-uniform rules
  - extractor now validates map dimensions/channels/payload lengths before returning

### 5) Zen MOCAP inside K_OS got deeper runtime diagnostics + ownership controls
- New backend commands added and registered:
  - set active take ownership
  - fetch diagnostics policy
  - fetch diagnostics snapshot
  - commit to active take
- Timeline runtime state now tracks `active_take_path` and emits diagnostics-change events.
- Frontend ZenMocap UI now:
  - subscribes to diagnostics event stream
  - periodically refreshes diagnostics by policy
  - surfaces queue/ledger drift warnings in LiveLink + GPU Doctor panels
  - uses policy-driven commit behavior (`timelinePersistencePolicy.ts`)

### 6) Compose export utility bug fix
- Removed `@ts-nocheck` in `exportUtility.ts` and added explicit PNG bit-depth downcast logic (`32 -> 16`) for encoder compatibility.

## ZEN Painter Suite // In-Window Update
- In `M:/dev3D/PBR`, no in-window source edits were detected after the last run cutoff.
- One docs refresh landed at `docs/new/TRANSFORMATION_STATUS.md` (operational status rewrite).
- Existing modified ZenPainter files remain in working tree, but their file write times predate this run window.

## ZEN MOCAP Standalone Repo // In-Window Update
- `M:/dev3D/ZenMocap` had no in-window commits or changed-file writes.

## Bugs Fixed / Risks Reduced (Verified)
- Renderer redraw no longer hard-stops on transient evaluated-mesh source failures (soft-continue path added + tested).
- GPU upload waste reduced through partial-range staging uploads.
- Photogrammetry pipeline now rejects degenerate/invalid geometry and malformed PBR outputs earlier through hard validation contracts.
- Timeline runtime persistence moved to explicit active-take ownership model, reducing ambiguous commit targets.

## Verification Matrix (Executed This Run)
- `cargo test -p k-os-renderer --quiet` -> pass (7 passed, 1 ignored)
- `cargo test -p k-os-photogrammetry --quiet` -> pass (31 passed)
- `cargo test -p zen-mocap-engine --quiet` -> pass (all suites green; warnings only)
- `npm run test -- src-mocap/features/Sequencer/timelineBridge.test.ts` -> pass (3/3)
- `cargo test -p k-os-game-runtime --quiet` -> pass (22/22)
- `cargo check -p k-os-bevy --quiet` -> pass

## Risk Radar
- Most changes are still uncommitted working-tree state; integration durability depends on commit discipline and CI replay.
- `zen-mocap-engine` tests emit non-fatal warnings (unused imports/useless comparison) that should be cleaned to keep signal quality high.
- PBR repo has broad modified state, but no fresh in-window code movement; next cycle should verify whether pending ZenPainter branches actually advance or stall.

## Bottom Line
K_OS carried the night: renderer resilience, packaging-pipeline architecture, photogrammetry math/contracts, and ZenMocap diagnostics all moved materially forward and validated green in targeted tests. ZenPainter and standalone ZenMocap stayed mostly quiet in this specific time slice.
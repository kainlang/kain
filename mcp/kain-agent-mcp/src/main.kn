use std::runtime
use agent_mcp_server::run_agent_mcp

fn main() -> Int:
    let boot = runtime_init()
    if boot != 0:
        return 100 + boot

    let status = run_agent_mcp()

    let shutdown = runtime_shutdown()
    if shutdown != 0:
        return 200 + shutdown
    return status

use c::ffi_boundary_object

fn main() -> Int:
    let iterations: Int = 10000000
    let expected: Int = 658273918
    var acc: Int = 1
    var i: Int = 0
    while i < iterations:
        acc = ffi_boundary_mix(acc + i, i)
        i = i + 1

    if acc != expected:
        return 1
    return 0

# Benchmark Docs

- `assessments/`: historical benchmark tuning notes and post-run assessments.

Operational docs for current benchmark workflow are in the benchmark root README and catalog metadata (`benchmark/catalog`).

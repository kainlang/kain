[package]
name = "native-metrics"
version = "0.1.0"
description = "Rust crate blade with Kain Fabric glue"

[blade]
name = "native-metrics"
kind = "rust_crate"
entry = "src/rust_step.kn"
source_roots = ["src"]
module_roots = ["src"]
cargo_manifest = "Cargo.toml"

[blade.rust]
cargo_manifest = "Cargo.toml"
crate_name = "blade_smoke_native_metrics"

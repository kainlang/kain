[package]
name = "signal-math"
version = "0.1.0"
description = "Small Kain utility blade used by the workspace graph"

[blade]
name = "signal-math"
kind = "kain_library"
entry = "src/main.kn"
source_roots = ["src"]
module_roots = ["src"]
build_targets = ["run"]

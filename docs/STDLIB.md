# Kain Standard Library — Complete Reference

> **Scope:** Root LLVM/native profile. 67 modules, ~3,250 public symbols, ~4,166 total symbols, 240 Rust builtins, 48 native runtime services.
> **Source:** `X:\stdlib\*.kn`
> **Map:** `X:\stdlib\STDLIB_MAP.llm.md` (2625-line LLM-optimized symbol index, auto-generated by `kain stdlib-map`)

---

## Table of Contents

1. [Overview](#overview)
2. [Import Patterns](#import-patterns)
3. [Module Index](#module-index)
4. [Core Runtime](#core-runtime)
5. [Compiler-Owned Semantics (Intent)](#compiler-owned-semantics-intent)
6. [Actors & Concurrency](#actors--concurrency)
7. [GPU & Graphics](#gpu--graphics)
8. [Data Structures & Collections](#data-structures--collections)
9. [File I/O & OS](#file-io--os)
10. [Networking](#networking)
11. [Math & Geometry](#math--geometry)
12. [Text, JSON & Formatting](#text-json--formatting)
13. [Interop (Python, JS, C)](#interop-python-js-c)
14. [Systems & Machine](#systems--machine)
15. [Build, Test, Proof & Certification](#build-test-proof--certification)
16. [Tooling & Reflection](#tooling--reflection)
17. [Remaining Modules](#remaining-modules)
18. [Rust Builtins](#rust-builtins)
19. [Native Runtime Services](#native-runtime-services)
20. [Example: Full .kn File](#example-full-kn-file)

---

## Overview

The Kain standard library (`std`) is the canonical surface for all Kain application and systems authoring. It is organized into 67 top-level modules, each corresponding to a single `.kn` file in `stdlib/`. Every public symbol in the stdlib is backed by one of three mechanisms:

- **Pure Kain** — implemented entirely in Kain, no native runtime dependency
- **`@extern` ABI** — declared in Kain, implemented in native C code in the Kain runtime (`runtime/native/`)
- **Rust builtin** — built into the `kain` compiler binary itself, with no stdlib `.kn` source

The stdlib covers everything from `std::actor` (actor lifecycle, supervision, scheduling) to `std::z3` (SMT solver integration). All modules live under the `std::` namespace.

---

## Import Patterns

```kain
// Single module
use std::math
use std::fs
use std::json

// Sub-module (graphics/shared)
use std::graphics::shared

// Multiple symbols if needed (not typical — import the module)
use std::math::vec3
use std::math::mat4_identity
```

Convention: import the module, not individual symbols. Then prefix with the module name:

```kain
use std::math
use std::json
use std::fs

fn example() with Pure:
    let data = fs_read_text("config.json")
    let parsed = json_parse_text(data)
    let center = vec3(0.0, 0.0, 0.0)
```

---

## Module Index

| Module | Import | Category | Public Symbols |
|---|---|---|---|
| `actor` | `use std::actor` | Concurrency | 74 |
| `alloc` | `use std::alloc` | Memory | 33 |
| `ascii` | `use std::ascii` | Text | 53 |
| `atomic` | `use std::atomic` | Concurrency | 44 |
| `attrition` | `use std::attrition` | Quality | 3 |
| `base64` | `use std::base64` | Encoding | 14 |
| `bench` | `use std::bench` | Quality | 4 |
| `bits` | `use std::bits` | Numeric | 17 |
| `build` | `use std::build` | Tooling | 51 |
| `bytes` | `use std::bytes` | Data | 33 |
| `certify` | `use std::certify` | Quality | 3 |
| `collections` | `use std::collections` | Data | 110 |
| `compress` | `use std::compress` | Data | 9 |
| `crypto` | `use std::crypto` | Security | 9 |
| `cuda` | `use std::cuda` | GPU | 93 |
| `diagnostics` | `use std::diagnostics` | Tooling | 25 |
| `elf` | `use std::elf` | Binary | 11 |
| `fmt` | `use std::fmt` | Text | 76 |
| `fs` | `use std::fs` | IO | 148 |
| `gen_server` | `use std::gen_server` | Concurrency | 7 |
| `gpu` | `use std::gpu` | GPU | 123 |
| `graphics` | `use std::graphics` | GPU | 84 |
| `graphics::shared` | `use std::graphics::shared` | GPU | 32 |
| `hash` | `use std::hash` | Data | 47 |
| `http` | `use std::http` | Networking | 44 |
| `http2` | `use std::http2` | Networking | 10 |
| `input` | `use std::input` | UI | 45 |
| `intent` | `use std::intent` | Semantics | 90 |
| `interop` | `use std::interop` | Interop | 10 |
| `io` | `use std::io` | Data | 29 |
| `js` | `use std::js` | Interop | 69 |
| `json` | `use std::json` | Text | 133 |
| `kain` | `use std::kain` | Tooling | 31 |
| `machine` | `use std::machine` | Systems | 38 |
| `math` | `use std::math` | Numeric | 250 |
| `mcp` | `use std::mcp` | Interop | 73 |
| `memory` | `use std::memory` | Systems | 19 |
| `mmio` | `use std::mmio` | Systems | 20 |
| `net` | `use std::net` | Networking | 58 |
| `os` | `use std::os` | IO | 127 |
| `os_path` | `use std::os_path` | IO | 32 |
| `path` | `use std::path` | IO | 11 |
| `platform` | `use std::platform` | Systems | 24 |
| `process` | `use std::process` | IO | 63 |
| `proof` | `use std::proof` | Quality | 49 |
| `python` | `use std::python` | Interop | 78 |
| `random` | `use std::random` | Numeric | 21 |
| `reflect` | `use std::reflect` | Tooling | 5 |
| `reload` | `use std::reload` | Systems | 43 |
| `result` | `use std::result` | Tooling | 20 |
| `runtime` | `use std::runtime` | Core | 56 |
| `semver` | `use std::semver` | Data | 28 |
| `simd` | `use std::simd` | Numeric | 16 |
| `sync` | `use std::sync` | Concurrency | 62 |
| `tar` | `use std::tar` | Data | 9 |
| `target` | `use std::target` | Tooling | 6 |
| `test` | `use std::test` | Quality | 27 |
| `text` | `use std::text` | Text | 59 |
| `thread` | `use std::thread` | Concurrency | 18 |
| `time` | `use std::time` | Core | 33 |
| `tls` | `use std::tls` | Networking | 11 |
| `ui` | `use std::ui` | UI | 340 |
| `unicode` | `use std::unicode` | Text | 13 |
| `uri` | `use std::uri` | Networking | 10 |
| `wasm` | `use std::wasm` | Binary | 18 |
| `z3` | `use std::z3` | Tooling | 40 |
| `zip` | `use std::zip` | Data | 9 |

---

## Core Runtime

### `std::runtime`

**Import:** `use std::runtime`
**Purpose:** Runtime lifecycle, CPU feature detection, SIMD domain kernels, converge telemetry, machine stone counters.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `runtime_init` | `fn runtime_init() -> Int` |
| `runtime_shutdown` | `fn runtime_shutdown() -> Int` |
| `runtime_heap_validate` | `fn runtime_heap_validate() -> Int` |
| `runtime_cpu_feature_mask` | `fn runtime_cpu_feature_mask() -> Int` |
| `runtime_cpu_feature_fingerprint` | `fn runtime_cpu_feature_fingerprint() -> Int` |
| `runtime_cpu_has_capability` | `fn runtime_cpu_has_capability(capability_key: String) -> Int` |
| `runtime_converge_telemetry_count` | `fn runtime_converge_telemetry_count() -> Int` |
| `runtime_machine_teleport_count` | `fn runtime_machine_teleport_count() -> Int` |
| `runtime_machine_pulse_total_fire_count` | `fn runtime_machine_pulse_total_fire_count() -> Int` |

**SIMD Domain Kernels:**
- `runtime_simd_i32_domain_dot_scalar_mod`, `runtime_simd_i32_domain_dot_avx2_mod`, `runtime_simd_i32_domain_dot_avx512_mod` — dot product at different SIMD widths
- `runtime_simd_i32_domain_affine_accumulate_scalar_mod`, `runtime_simd_i32_domain_affine_accumulate_avx2_mod`, `runtime_simd_i32_domain_affine_accumulate_avx512_mod` — affine accumulation
- `runtime_simd_i32_domain_affine_pow2_fill_pair_accumulate_mod` — power-of-two fill pair

**Converge Telemetry:**
- `runtime_converge_select_lane` — select best lane for shape
- `runtime_converge_commit_winner` — commit winning lane
- `runtime_converge_record_telemetry` — record timing/mismatch data
- `runtime_converge_telemetry_count`, `runtime_converge_cache_probe_count`, `runtime_converge_cache_hit_count`

**Usage:**
```kain
use std::runtime

fn main() with Pure:
    runtime_init()
    let mask = runtime_cpu_feature_mask()
    if runtime_cpu_has_capability("cpu.x86.avx2") != 0:
        // fast path
    runtime_shutdown()
```

### `std::time`

**Import:** `use std::time`
**Purpose:** Wall-clock time, durations, deadlines, and tickers.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `now_millis` | `fn now_millis() -> Int` |
| `sleep_millis` | `fn sleep_millis(milliseconds: Int) -> Int` |
| `duration_from_millis` | `fn duration_from_millis(ms: Int) -> Duration` |
| `duration_from_secs` | `fn duration_from_secs(secs: Int) -> Duration` |
| `instant_now` | `fn instant_now() -> Instant` |
| `instant_elapsed` | `fn instant_elapsed(self: Instant) -> Duration` |
| `deadline_from_duration` | `fn deadline_from_duration(dur: Duration) -> Deadline` |
| `deadline_is_elapsed` | `fn deadline_is_elapsed(self: Deadline) -> Bool` |
| `ticker_new` | `fn ticker_new(interval: Duration) -> Ticker` |
| `ticker_next` | `fn ticker_next(t: Ticker) -> Ticker` |
| `datetime_from_epoch_millis` | `fn datetime_from_epoch_millis(epoch_ms: Int) -> DateTime` |

**Usage:**
```kain
use std::time

fn measure() with Pure:
    let start = instant_now()
    sleep_millis(100)
    let elapsed = instant_elapsed(start)
    let ms = duration_to_millis(elapsed)
    // ms ≈ 100
```

### `std::result`

**Import:** `use std::result`
**Purpose:** Sentinels for operation outcomes — ok, cancelled, invalid argument, not found, capacity exceeded, runtime unavailable.

**Key Symbols:**
- `result_ok() -> Int`
- `result_cancelled() -> Int`
- `result_invalid_argument() -> Int`
- `result_not_found() -> Int`
- `result_capacity_exceeded() -> Int`
- `result_runtime_unavailable() -> Int`
- `result_is_ok(status: Int) -> Bool`
- `result_is_error(status: Int) -> Bool`
- `result_combine(left: Int, right: Int) -> Int`

---

## Compiler-Owned Semantics (Intent)

### `std::intent`

**Import:** `use std::intent`
**Purpose:** Runtime telemetry and debugging for compiler-owned semantic constructs: `entangle`, `patch`, `law`, `resonate`, `converge`, `orchestrate`. These are query-only functions — the actual constructs are language keywords.

**Key Symbols — Entangle:**
| Symbol | Signature |
|---|---|
| `entangle_reset` | `fn entangle_reset() -> Int` |
| `entangle_registered_count` | `fn entangle_registered_count() -> Int` |
| `entangle_register` | `fn entangle_register(authority: String, mirror: String, policy: String, type_name: String) -> Int` |
| `entangle_has_bindings` | `fn entangle_has_bindings() -> Bool` |
| `entangle_propagation_count` | `fn entangle_propagation_count() -> Int` |

**Key Symbols — Patch:**
| Symbol | Signature |
|---|---|
| `patch_journal_count` | `fn patch_journal_count() -> Int` |
| `patch_last_path` | `fn patch_last_path() -> String` |
| `patch_status` | `fn patch_status(changed: Bool) -> Int` |
| `patch_is_noop` | `fn patch_is_noop(status: Int) -> Bool` |
| `patch_applied` | `fn patch_applied(status: Int) -> Bool` |

**Key Symbols — Law:**
| Symbol | Signature |
|---|---|
| `law_status` | `fn law_status(valid: Bool) -> Int` |
| `law_is_valid_status` | `fn law_is_valid_status(status: Int) -> Bool` |

**Key Symbols — Resonate:**
| Symbol | Signature |
|---|---|
| `resonate_mutation_count` | `fn resonate_mutation_count() -> Int` |
| `resonate_fire_count` | `fn resonate_fire_count() -> Int` |
| `resonate_absorb_count` | `fn resonate_absorb_count() -> Int` |
| `resonate_last_target` | `fn resonate_last_target() -> String` |
| `resonate_last_old_i64` | `fn resonate_last_old_i64() -> Int` |
| `resonate_last_new_i64` | `fn resonate_last_new_i64() -> Int` |
| `resonate_last_dampen_ns` | `fn resonate_last_dampen_ns() -> Int` |

**Key Symbols — Converge:**
| Symbol | Signature |
|---|---|
| `converge_mismatch_count` | `fn converge_mismatch_count() -> Int` |
| `converge_choose_int` | `fn converge_choose_int(spec_value: Int, fast_value: Int) -> Int` |
| `converge_choose_bool` | `fn converge_choose_bool(spec_value: Bool, fast_value: Bool) -> Bool` |
| `converge_status` | `fn converge_status(spec_value: Int, fast_value: Int) -> Int` |

**Key Symbols — Orchestrate:**
| Symbol | Signature |
|---|---|
| `orchestrate_stage_count` | `fn orchestrate_stage_count() -> Int` |
| `orchestrate_transfer_count` | `fn orchestrate_transfer_count() -> Int` |
| `orchestrate_fallback_count` | `fn orchestrate_fallback_count() -> Int` |
| `orchestrate_last_runtime` | `fn orchestrate_last_runtime() -> String` |
| `orchestrate_stage_status` | `fn orchestrate_stage_status(status: Int) -> Int` |
| `orchestrate_merge_status` | `fn orchestrate_merge_status(left: Int, right: Int) -> Int` |

**Usage:**
```kain
use std::intent

fn check_entangle_state() with Pure:
    let count = entangle_registered_count()
    let prop_count = entangle_propagation_count()
    if entangle_has_bindings():
        for i in range(0, count):
            let auth = entangle_get_authority(i)
            let mir = entangle_get_mirror(i)
```

---

## Actors & Concurrency

### `std::actor`

**Import:** `use std::actor`
**Purpose:** Actor lifecycle, mailbox configuration, supervision, scheduling, registry, linking/monitoring.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `actor_spawn` | `fn actor_spawn(actor_name: String, init_payload: String) -> Int` |
| `actor_send` | `fn actor_send(actor_id: Int, message_name: String, data_payload: String) -> Int` |
| `actor_id_is_valid` | `fn actor_id_is_valid(actor_id: Int) -> Bool` |
| `actor_is_running` | `fn actor_is_running(actor_id: Int) -> Bool` |
| `actor_is_terminal` | `fn actor_is_terminal(actor_id: Int) -> Bool` |
| `actor_shutdown` | `fn actor_shutdown(actor_id: Int) -> Int` |
| `actor_kill` | `fn actor_kill(actor_id: Int) -> Int` |
| `actor_get_state` | `fn actor_get_state(actor_id: Int) -> Int` |
| `actor_registry_lookup` | `fn actor_registry_lookup(name: String) -> Int` |
| `actor_registry_register` | `fn actor_registry_register(name: String, actor_id: Int) -> Int` |
| `actor_registry_has` | `fn actor_registry_has(name: String) -> Bool` |
| `actor_monitor` | `fn actor_monitor(monitor_id: Int, monitored_id: Int) -> Int` |
| `actor_link` | `fn actor_link(actor_a: Int, actor_b: Int) -> Int` |
| `actor_unlink` | `fn actor_unlink(actor_a: Int, actor_b: Int) -> Int` |

**Configuration Constants:**
- `actor_default_mailbox_capacity() -> Int`
- `actor_unbounded_mailbox_capacity() -> Int`
- `actor_default_ask_timeout_ms() -> Int`
- `actor_default_shutdown_grace_ms() -> Int`
- `actor_supervision_max_restarts() -> Int`
- `actor_supervision_restart_window_millis() -> Int`
- `actor_invalid_id() -> Int`

**Supervision:**
- `actor_supervision_observed_child_exit_count(actor_id: Int) -> Int`
- `actor_supervision_restart_attempt_count(actor_id: Int) -> Int`
- `actor_supervision_escalation_count(actor_id: Int) -> Int`
- `actor_supervision_limit_hit(actor_id: Int) -> Bool`

**Scheduler:**
- `actor_scheduler_queue_depth() -> Int`
- `actor_scheduler_max_queue_depth() -> Int`
- `actor_scheduler_total_enqueued() -> Int`
- `actor_scheduler_total_dequeued() -> Int`
- `actor_scheduler_worker_count() -> Int`
- `actor_scheduler_active_workers() -> Int`
- `actor_scheduler_busy_workers() -> Int`
- `actor_scheduler_overflow_thread_spawns() -> Int`

**Usage:**
```kain
use std::actor

fn manage_actor() with Pure:
    let id = actor_spawn("worker", "{}")
    if actor_id_is_valid(id):
        actor_send(id, "process", "{\"task\":\"compute\"}")
        let state = actor_get_state(id)
        actor_shutdown(id)
```

### `std::gen_server`

**Import:** `use std::gen_server`
**Purpose:** Generic server actor pattern (Erlang/OTP-style `gen_server`). Wraps an actor with typed call/cast/info handlers.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `gen_server_start` | `fn gen_server_start(state: S, handle_call, handle_cast, handle_info) -> GenServer` |
| `gen_server_start_link` | `fn gen_server_start_link(state: S, ...) -> GenServer` |
| `gen_server_call` | `fn gen_server_call(server: GenServer, request: M) -> R` |
| `gen_server_cast` | `fn gen_server_cast(server: GenServer, request: M) -> Unit` |
| `gen_server_info` | `fn gen_server_info(server: GenServer, message: M) -> Unit` |
| `gen_server_call_result` | `fn gen_server_call_result(reply: R, state: S) -> GenServerCallOutcome` |

**Usage:**
```kain
use std::gen_server

// Start a counter gen_server
let counter = gen_server_start(
    0,                                              // initial state
    fn(msg, state): gen_server_call_result(state, state + 1),  // handle_call
    fn(msg, state): state + 1,                      // handle_cast
    fn(msg, state): state                           // handle_info
)
```

### `std::sync`

**Import:** `use std::sync`
**Purpose:** Low-level synchronization primitives: MCS locks, teleport channels, once, wait groups, RW locks, semaphores, condition variables.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `mcs_mutex_new` | `fn mcs_mutex_new() -> McsMutex` |
| `mcs_mutex_lock` | `fn mcs_mutex_lock(lock: McsMutex, node: ptr<Int>) -> Int` |
| `mcs_mutex_unlock` | `fn mcs_mutex_unlock(lock: McsMutex, node: ptr<Int>) -> Int` |
| `teleport_channel_new` | `fn teleport_channel_new(capacity: Int) -> TeleportChannel` |
| `teleport_channel_send` | `fn teleport_channel_send(chan: TeleportChannel, item_ptr: Int) -> Bool` |
| `teleport_channel_recv` | `fn teleport_channel_recv(chan: TeleportChannel) -> Int` |
| `once_new` | `fn once_new() -> Once` |
| `once_do` | `fn once_do(o: Once) -> Int` |
| `once_complete` | `fn once_complete(o: Once) -> Int` |
| `wait_group_new` | `fn wait_group_new() -> WaitGroup` |
| `wait_group_add` | `fn wait_group_add(wg: WaitGroup, delta: Int) -> Int` |
| `wait_group_done` | `fn wait_group_done(wg: WaitGroup) -> Int` |
| `wait_group_wait` | `fn wait_group_wait(wg: WaitGroup) -> Int` |
| `rwlock_new` | `fn rwlock_new() -> RwLock` |
| `rwlock_read_lock` | `fn rwlock_read_lock(lock: RwLock) -> Int` |
| `rwlock_write_lock` | `fn rwlock_write_lock(lock: RwLock) -> Int` |
| `semaphore_new` | `fn semaphore_new(initial: Int) -> Semaphore` |
| `semaphore_acquire` | `fn semaphore_acquire(sema: Semaphore) -> Int` |
| `semaphore_release` | `fn semaphore_release(sema: Semaphore, permits: Int) -> Int` |
| `condvar_new` | `fn condvar_new() -> CondVar` |
| `condvar_wait_timeout` | `fn condvar_wait_timeout(cv: CondVar, ...) -> Int` |
| `condvar_notify_one` | `fn condvar_notify_one(cv: CondVar) -> Int` |
| `condvar_notify_all` | `fn condvar_notify_all(cv: CondVar) -> Int` |

**Usage:**
```kain
use std::sync

fn parallel_work() with Pure:
    let wg = wait_group_new()
    wait_group_add(wg, 3)
    // spawn 3 workers, each calls wait_group_done(wg)
    wait_group_wait(wg)
    wait_group_destroy(wg)
```

### `std::thread`

**Import:** `use std::thread`
**Purpose:** Native OS thread spawn, join, affinity, yield.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `thread_spawn` | `fn thread_spawn(func: ptr<Int>, arg: ptr<Int>) -> Thread` |
| `thread_spawn_entry` | `fn thread_spawn_entry(entry: ThreadEntry, arg: ptr<Int>) -> Thread` |
| `thread_join` | `fn thread_join(thread: Thread) -> Int` |
| `thread_yield` | `fn thread_yield() -> Int` |
| `thread_current_id` | `fn thread_current_id() -> Int` |
| `thread_set_name` | `fn thread_set_name(name: String) -> Int` |
| `thread_logical_count` | `fn thread_logical_count() -> Int` |
| `thread_core_count` | `fn thread_core_count() -> Int` |
| `thread_is_done` | `fn thread_is_done(thread: Thread) -> Bool` |

---

## GPU & Graphics

### `std::gpu`

**Import:** `use std::gpu`
**Purpose:** GPU resource policies, memory policies, binding plans, buffer/image descriptors, layout conversions (std140/std430/cbuffer), element sizing.

**Constants — Stage Flags:**
- `GPU_STAGE_VERTEX` (1), `GPU_STAGE_FRAGMENT` (2), `GPU_STAGE_COMPUTE` (4)
- `GPU_STAGE_TASK` (8), `GPU_STAGE_MESH` (16)
- `GPU_STAGE_RAYGEN` (32), `GPU_STAGE_ANY_HIT` (64), `GPU_STAGE_CLOSEST_HIT` (128)
- `GPU_STAGE_ALL_GRAPHICS`, `GPU_STAGE_ALL_RAYTRACING`, `GPU_STAGE_ALL`

**Constants — Queue & Access:**
- `GPU_QUEUE_GRAPHICS` (1), `GPU_QUEUE_COMPUTE` (2), `GPU_QUEUE_TRANSFER` (4), `GPU_QUEUE_PRESENT` (8)
- `GPU_ACCESS_READ` (1), `GPU_ACCESS_WRITE` (2), `GPU_ACCESS_ATOMIC` (4), `GPU_ACCESS_READ_WRITE` (3)

**Constants — Residency:**
- `GPU_RESIDENCY_HOST_VISIBLE`, `GPU_RESIDENCY_HOST_COHERENT`, `GPU_RESIDENCY_DEVICE_LOCAL`
- `GPU_RESIDENCY_SHARED`, `GPU_RESIDENCY_IMPORTED`, `GPU_RESIDENCY_READBACK`, `GPU_RESIDENCY_UPLOAD`

**Constants — Buffer/Image Usage:**
- `GPU_BUFFER_USAGE_STORAGE`, `GPU_BUFFER_USAGE_UNIFORM`, `GPU_BUFFER_USAGE_VERTEX`, `GPU_BUFFER_USAGE_INDEX`
- `GPU_IMAGE_USAGE_SAMPLED`, `GPU_IMAGE_USAGE_STORAGE`, `GPU_IMAGE_USAGE_COLOR_ATTACHMENT`

**Constants — Descriptors & Layouts:**
- `GPU_DESCRIPTOR_STORAGE_BUFFER`, `GPU_DESCRIPTOR_UNIFORM_BUFFER`, `GPU_DESCRIPTOR_SAMPLED_IMAGE`
- `GPU_LAYOUT_TIGHT`, `GPU_LAYOUT_PACKED`, `GPU_LAYOUT_STD140`, `GPU_LAYOUT_STD430`, `GPU_LAYOUT_CBUFFER`

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `gpu_memory_policy` | `fn gpu_memory_policy(residency_flags, access_flags, queue_flags, layout_kind, descriptor_kind) -> GpuMemoryPolicy` |
| `gpu_shared_memory_policy` | `fn gpu_shared_memory_policy(...) -> GpuMemoryPolicy` |
| `gpu_device_local_memory_policy` | `fn gpu_device_local_memory_policy(...) -> GpuMemoryPolicy` |
| `gpu_upload_memory_policy` | `fn gpu_upload_memory_policy(...) -> GpuMemoryPolicy` |
| `gpu_readback_memory_policy` | `fn gpu_readback_memory_policy(...) -> GpuMemoryPolicy` |
| `gpu_resource_policy` | `fn gpu_resource_policy(memory: GpuMemoryPolicy, usage_flags, debug_name) -> GpuResourcePolicy` |
| `gpu_binding_plan` | `fn gpu_binding_plan(binding, descriptor_kind, stage_flags, access_flags, queue_flags) -> GpuBindingPlan` |
| `gpu_storage_buffer_binding` | `fn gpu_storage_buffer_binding(binding, stage_flags, access_flags, queue_flags) -> GpuBindingPlan` |
| `gpu_uniform_buffer_binding` | `fn gpu_uniform_buffer_binding(binding, stage_flags, queue_flags) -> GpuBindingPlan` |
| `gpu_element_size_bytes` | `fn gpu_element_size_bytes(element_type: String) -> Int` |
| `gpu_element_count` | `fn gpu_element_count(shape: Array<Int>) -> Int` |
| `gpu_required_byte_length` | `fn gpu_required_byte_length(element_type, shape) -> Int` |
| `gpu_shared_buffer_from_bytes` | `fn gpu_shared_buffer_from_bytes(bytes, element_type, shape, format, mime_type, policy) -> GpuBuffer` |
| `gpu_shared_buffer_zeroed` | `fn gpu_shared_buffer_zeroed(element_type, shape, format, mime_type, policy) -> GpuBuffer` |
| `gpu_shared_image_from_bytes` | `fn gpu_shared_image_from_bytes(bytes, width, height, channels, layout, pixel_format, mime_type, policy) -> GpuImage` |
| `gpu_shared_image_zeroed` | `fn gpu_shared_image_zeroed(width, height, channels, layout, pixel_format, mime_type, policy) -> GpuImage` |
| `gpu_backend_state` | `fn gpu_backend_state(backend_id: String) -> GpuBackendState` |
| `gpu_backend_preferred` | `fn gpu_backend_preferred() -> GpuBackendState` |
| `gpu_std140_vec3` | `fn gpu_std140_vec3(value: Vec3) -> Std140<Std140Vec3>` |
| `gpu_std140_vec4` | `fn gpu_std140_vec4(value: Vec4) -> Std140<Vec4>` |
| `gpu_std140_mat4` | `fn gpu_std140_mat4(value: Mat4) -> Std140<Mat4>` |
| `gpu_std430_vec3a` | `fn gpu_std430_vec3a(value: Vec3A) -> Std430<Vec3A>` |
| `gpu_cbuffer_mat4` | `fn gpu_cbuffer_mat4(value: Mat4) -> CBuffer<Mat4>` |

**Usage:**
```kain
use std::gpu

fn create_storage_buffer() with Pure:
    let policy = gpu_memory_policy(
        GPU_RESIDENCY_DEVICE_LOCAL,
        GPU_ACCESS_READ_WRITE,
        GPU_QUEUE_COMPUTE,
        GPU_LAYOUT_STD430,
        GPU_DESCRIPTOR_STORAGE_BUFFER
    )
    let resource = gpu_resource_policy(policy, GPU_BUFFER_USAGE_STORAGE, "particles")
    let buf = gpu_shared_buffer_zeroed("f32", [1024], "r32f", "application/octet-stream", resource)
```

### `std::graphics`

**Import:** `use std::graphics`
**Purpose:** Native graphics sessions, backends, frame management, buffer/shader/mesh/pipeline creation, draw commands.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `graphics_session_create` | `fn graphics_session_create(app_name: String, width: Int, height: Int) -> Int` |
| `graphics_session_destroy` | `fn graphics_session_destroy(session_id: Int) -> Int` |
| `graphics_backend_supported` | `fn graphics_backend_supported(backend_id: String) -> Int` |
| `graphics_backend_select` | `fn graphics_backend_select(session_id: Int, backend_id: String) -> Int` |
| `graphics_active_backend` | `fn graphics_active_backend(session_id: Int) -> String` |
| `graphics_begin_frame` | `fn graphics_begin_frame(session_id: Int, delta_ms: Float) -> Int` |
| `graphics_end_frame` | `fn graphics_end_frame(session_id: Int) -> Int` |
| `graphics_present` | `fn graphics_present(session_id: Int) -> Int` |
| `graphics_buffer_create` | `fn graphics_buffer_create(session_id, kind, label, byte_length, element_stride) -> Int` |
| `graphics_shader_spirv_from_hex` | `fn graphics_shader_spirv_from_hex(session, key, stage, entry_point, bytes_hex) -> Int` |
| `graphics_mesh_create` | `fn graphics_mesh_create(session, label, vertex_buffer_id, index_buffer_id, vertex_count, index_count) -> Int` |
| `graphics_pipeline_create` | `fn graphics_pipeline_create(session, label, vertex_shader_id, fragment_shader_id, backend) -> Int` |
| `graphics_draw_mesh` | `fn graphics_draw_mesh(session, pipeline_id, mesh_id, instance_count) -> Int` |

**Usage:**
```kain
use std::graphics

fn render_frame(session_id: Int) with Pure:
    graphics_begin_frame(session_id, 16.0)
    // ... create buffers, shaders, mesh, pipeline
    graphics_draw_mesh(session_id, pipeline, mesh, 1)
    graphics_end_frame(session_id)
    graphics_present(session_id)
```

### `std::graphics::shared`

**Import:** `use std::graphics::shared`
**Purpose:** Shared resource views for graphics pipelines — vertex buffers, index buffers, uniform/storage bindings, sampled/storage images, color/depth attachments.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `graphics_shared_vertex_policy` | `fn graphics_shared_vertex_policy(debug_name: String) -> GpuResourcePolicy` |
| `graphics_shared_index_policy` | `fn graphics_shared_index_policy(debug_name: String) -> GpuResourcePolicy` |
| `graphics_shared_uniform_policy` | `fn graphics_shared_uniform_policy(debug_name: String) -> GpuResourcePolicy` |
| `graphics_shared_storage_policy` | `fn graphics_shared_storage_policy(debug_name: String) -> GpuResourcePolicy` |
| `graphics_shared_vertex_buffer` | `fn graphics_shared_vertex_buffer(resource: GpuBuffer, element_stride: Int) -> GraphicsSharedBuffer` |
| `graphics_shared_index_buffer` | `fn graphics_shared_index_buffer(resource: GpuBuffer, element_stride: Int) -> GraphicsSharedBuffer` |
| `graphics_shared_uniform_binding` | `fn graphics_shared_uniform_binding(resource: GpuBuffer, binding_slot, stage_flags) -> GraphicsSharedBuffer` |
| `graphics_shared_storage_binding` | `fn graphics_shared_storage_binding(resource: GpuBuffer, binding_slot, stage_flags, access_flags) -> GraphicsSharedBuffer` |
| `graphics_shared_sampled_image` | `fn graphics_shared_sampled_image(resource: GpuImage, binding_slot, stage_flags) -> GraphicsSharedImage` |
| `graphics_shared_storage_image` | `fn graphics_shared_storage_image(resource: GpuImage, binding_slot, stage_flags, access_flags) -> GraphicsSharedImage` |
| `graphics_shared_color_attachment` | `fn graphics_shared_color_attachment(resource: GpuImage) -> GraphicsSharedImage` |
| `graphics_shared_depth_attachment` | `fn graphics_shared_depth_attachment(resource: GpuImage) -> GraphicsSharedImage` |

**Constants — Resource Kinds:**
- `GRAPHICS_SHARED_KIND_VERTEX_BUFFER`, `GRAPHICS_SHARED_KIND_INDEX_BUFFER`
- `GRAPHICS_SHARED_KIND_UNIFORM_BUFFER`, `GRAPHICS_SHARED_KIND_STORAGE_BUFFER`
- `GRAPHICS_SHARED_KIND_SAMPLED_IMAGE`, `GRAPHICS_SHARED_KIND_STORAGE_IMAGE`
- `GRAPHICS_SHARED_KIND_COLOR_ATTACHMENT`, `GRAPHICS_SHARED_KIND_DEPTH_ATTACHMENT`

### `std::cuda`

**Import:** `use std::cuda`
**Purpose:** CUDA compute dispatch: runtime state, shader bundle management, binding locators, payload read/write, byte packing/unpacking, manifest introspection.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `cuda_paths` | `fn cuda_paths() -> CudaPaths` |
| `cuda_runtime_state` | `fn cuda_runtime_state() -> CudaRuntimeState` |
| `cuda_driver_available` | `fn cuda_driver_available() -> Bool` |
| `cuda_runtime_ready` | `fn cuda_runtime_ready() -> Bool` |
| `cuda_compute_manifest` | `fn cuda_compute_manifest() -> JsonObject` |
| `cuda_compute_keys` | `fn cuda_compute_keys() -> Array<String>` |
| `cuda_has_compute_key` | `fn cuda_has_compute_key(compute_key: String) -> Bool` |
| `cuda_binding_locator` | `fn cuda_binding_locator(compute_key, binding_key) -> CudaBindingLocator` |
| `cuda_binding_payload_bytes` | `fn cuda_binding_payload_bytes(compute_key, binding_key) -> Array<Int>` |
| `cuda_write_binding_payload_bytes` | `fn cuda_write_binding_payload_bytes(compute_key, binding_key, bytes) -> Bool` |
| `cuda_dispatch_primary_compute` | `fn cuda_dispatch_primary_compute(compute_key: String) -> CudaDispatchStats` |
| `cuda_dispatch` | `fn cuda_dispatch(shader_bundle_path, compute_residency_path, compute_key) -> CudaDispatchStats` |
| `cuda_pack_u32_le` | `fn cuda_pack_u32_le(value: Int) -> Array<Int>` |
| `cuda_unpack_u32_le` | `fn cuda_unpack_u32_le(bytes: Array<Int>, offset: Int) -> Int` |

---

## Data Structures & Collections

### `std::collections`

**Import:** `use std::collections`
**Purpose:** Core data structures — string-int maps, queues, deques, priority queues, slot maps, intrusive hash maps, array lists, hash maps.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `int_min` / `int_max` / `int_clamp` | `fn int_min(left: Int, right: Int) -> Int` |
| `int_saturating_sub` | `fn int_saturating_sub(left: Int, right: Int) -> Int` |
| `string_int_map_new` | `fn string_int_map_new() -> StringIntMap` |
| `string_int_map_set` | `fn string_int_map_set(map: StringIntMap, key: String, value: Int) -> StringIntMap` |
| `string_int_map_get` | `fn string_int_map_get(map: StringIntMap, key: String) -> Int` |
| `queue_create` | `fn queue_create(capacity: Int) -> IntQueue` |
| `queue_push` | `fn queue_push(queue: IntQueue, value: Int) -> IntQueue` |
| `queue_pop` | `fn queue_pop(queue: IntQueue) -> IntQueue` |
| `deque_create` | `fn deque_create(capacity: Int) -> IntDeque` |
| `deque_push_back` / `deque_push_front` | push to either end |
| `deque_pop_front` / `deque_pop_back` | pop from either end |
| `priority_queue_create` | `fn priority_queue_create(capacity: Int) -> IntPriorityQueue` |
| `priority_queue_push` | `fn priority_queue_push(queue, value: Int, priority: Int) -> IntPriorityQueue` |
| `priority_queue_pop` | `fn priority_queue_pop(queue) -> IntPriorityQueue` |
| `slot_map_create` | `fn slot_map_create(capacity: Int) -> SlotMap` |
| `slot_map_insert` | `fn slot_map_insert(map: SlotMap, value: Int) -> SlotMapInsert` |
| `slot_map_remove` | `fn slot_map_remove(map: SlotMap, key: SlotMapKey) -> SlotMapRemove` |
| `array_list_create` | `fn array_list_create(capacity: Int) -> ArrayList` |
| `array_list_get` / `array_list_set` | indexed access |
| `array_list_append` / `array_list_pop` | dynamic append/pop |
| `hash_map_create` | `fn hash_map_create(capacity: Int) -> HashMap` |
| `hash_map_put` | `fn hash_map_put(map: ptr<HashMap>, key: Int, value: Int) -> Int` |
| `hash_map_get_or` | `fn hash_map_get_or(map: HashMap, key: Int, fallback: Int) -> Int` |
| `intrusive_hash_map_create` | `fn intrusive_hash_map_create(bucket_count: Int) -> IntrusiveHashMap` |

**Usage:**
```kain
use std::collections

fn use_queue() with Pure:
    let q = queue_create(100)
    q = queue_push(q, 42)
    q = queue_push(q, 100)
    let front = queue_peek(q)  // 42
    q = queue_pop(q)
    queue_destroy(q)
```

### `std::hash`

**Import:** `use std::hash`
**Purpose:** Hash functions and incremental fingerprints — Wang hash, FNV-1a, CRC32, fingerprint32 for deterministic hashing.

**Key Constants:**
- `HASH_FNV1A32_OFFSET`, `HASH_FNV1A32_PRIME`, `HASH_CRC32_POLY`, `HASH_GOLDEN32`, `HASH_EMPTY32`

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `hash_wang32` | `fn hash_wang32(value: Int) -> Int` |
| `hash_u32` | `fn hash_u32(value: Int) -> Int` |
| `hash_mix64` | `fn hash_mix64(value: Int) -> Int` |
| `hash_fnv1a32_init` | `fn hash_fnv1a32_init() -> Int` |
| `hash_fnv1a32_update_byte` | `fn hash_fnv1a32_update_byte(hash: Int, byte: Int) -> Int` |
| `hash_crc32_init` | `fn hash_crc32_init() -> Int` |
| `hash_crc32_update_byte` | `fn hash_crc32_update_byte(crc: Int, byte: Int) -> Int` |
| `hash_pair32` | `fn hash_pair32(left: Int, right: Int) -> Int` |
| `hash_bucket_power_of_two` | `fn hash_bucket_power_of_two(hash: Int, capacity: Int) -> Int` |
| `fingerprint32_begin` | `fn fingerprint32_begin(salt: Int) -> Fingerprint32` |
| `fingerprint32_add_word` | `fn fingerprint32_add_word(fp: Fingerprint32, word: Int) -> Fingerprint32` |
| `fingerprint32_finish` | `fn fingerprint32_finish(fp: Fingerprint32) -> Int` |

### `std::io`

**Import:** `use std::io`
**Purpose:** Low-level I/O primitives — ring buffers, string builders, buffered readers/writers.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `ring_buffer_new` | `fn ring_buffer_new(capacity: Int) -> RingBuffer` |
| `ring_buffer_write` | `fn ring_buffer_write(rb: ptr<RingBuffer>, src: ptr<Int>, count: Int) -> Int` |
| `ring_buffer_read` | `fn ring_buffer_read(rb: ptr<RingBuffer>, dest: ptr<Int>, count: Int) -> Int` |
| `string_builder_new` | `fn string_builder_new(initial_capacity: Int) -> StringBuilder` |
| `string_builder_append_char` | `fn string_builder_append_char(sb: ptr<StringBuilder>, c: Int) -> Int` |
| `string_builder_append_string` | `fn string_builder_append_string(sb: ptr<StringBuilder>, str: String) -> Int` |
| `string_builder_to_string` | `fn string_builder_to_string(sb: StringBuilder) -> String` |
| `buffered_reader_new` | `fn buffered_reader_new(capacity: Int) -> BufferedReader` |
| `buffered_reader_fill` | `fn buffered_reader_fill(br: ptr<BufferedReader>, src: ptr<Int>, src_len: Int) -> Int` |
| `buffered_reader_read` | `fn buffered_reader_read(br: ptr<BufferedReader>, dest: ptr<Int>, count: Int) -> Int` |
| `buffered_writer_new` | `fn buffered_writer_new(capacity: Int) -> BufferedWriter` |
| `buffered_writer_write` | `fn buffered_writer_write(bw: ptr<BufferedWriter>, src: ptr<Int>, count: Int, flush_target: ptr<Int>) -> Int` |
| `buffered_writer_flush` | `fn buffered_writer_flush(bw: ptr<BufferedWriter>, dest: ptr<Int>) -> Int` |

### `std::bytes`

**Import:** `use std::bytes`
**Purpose:** Byte-level data views, builders, hex encoding.

**Key Symbols:**
- `bytes_slice(source: String, start: Int, length: Int) -> ByteSlice`
- `bytes_from(source: String) -> ByteSlice`
- `bytes_len(view: ByteSlice) -> Int`
- `bytes_byte_at(view: ByteSlice, index: Int) -> Int`
- `bytes_find(view: ByteSlice, needle: String) -> Int`
- `bytes_materialize(view: ByteSlice) -> String`
- `bytes_builder_new() -> BytesBuilder`
- `bytes_builder_push_byte(builder, value: Int) -> BytesBuilder`
- `bytes_builder_push_string(builder, value: String) -> BytesBuilder`
- `bytes_builder_build(builder: BytesBuilder) -> String`
- `bytes_hex(value: String) -> String`
- `bytes_from_hex(text: String) -> BytesDecodeResult`

### `std::bits`

**Import:** `use std::bits`
**Purpose:** Integer bit-width conversion and wrapping arithmetic.

**Key Symbols:**
- `u8(value: Int) -> Int`, `u16(value: Int) -> Int`, `u32(value: Int) -> Int`
- `i8(value: Int) -> Int`, `i16(value: Int) -> Int`, `i32(value: Int) -> Int`
- `wrapping_add_u32(left: Int, right: Int) -> Int`
- `wrapping_sub_u32`, `wrapping_mul_u32`
- `popcount32(value: Int) -> Int`
- `clz32(value: Int) -> Int` (count leading zeros)
- `ctz32(value: Int) -> Int` (count trailing zeros)
- `bswap32(value: Int) -> Int` (byte swap)
- `rotl32(value: Int, bits: Int) -> Int`, `rotr32(value: Int, bits: Int) -> Int`

### `std::simd`

**Import:** `use std::simd`
**Purpose:** Explicit SIMD vector type — `I64x4` with 4×64-bit lanes.

**Key Symbols:**
- `i64x4(a: Int, b: Int, c: Int, d: Int) -> I64x4`
- `i64x4_splat(value: Int) -> I64x4`
- `i64x4_lane(value: I64x4, lane: Int) -> Int`
- `i64x4_add`, `i64x4_sub`, `i64x4_mul`, `i64x4_and`, `i64x4_or`, `i64x4_xor`
- `i64x4_dot(left: I64x4, right: I64x4) -> Int`
- `i64x4_horizontal_sum(value: I64x4) -> Int`
- `i64x4_gather(base: ptr<Int>, indexes: I64x4) -> I64x4`
- `i64x4_scatter(base: ptr<Int>, indexes: I64x4, values: I64x4) -> Int`

---

## File I/O & OS

### `std::fs`

**Import:** `use std::fs`
**Purpose:** Full filesystem API — read/write text and binary, metadata, directory traversal, file watching, journaled transactions, temp files, streaming chunks.

**Key Types:**
- `FsError` — structured error with kind, operation, path, message, raw_code
- `FsOpResult` — `{ ok, status, error }`
- `FsTextResult` — `{ ok, value, status, error }`
- `FsBytesResult` — `{ ok, value: Array<Int>, status, error }`
- `FsMetadata` — `{ file_type, len, readonly, created_millis, modified_millis, accessed_millis }`
- `FsDirEntry` — `{ path, file_name, file_type, metadata }`
- `FsChunk` — streaming data chunk with `{ index, offset, len, bytes }`
- `FsWatchEvent` — `{ kind, path, before_len, after_len }`
- `FsJournalEntry` — transactional log entry

**Key Symbols — Reading:**

| Symbol | Signature |
|---|---|
| `fs_read_text` | `fn fs_read_text(path: String) -> String` |
| `fs_read_bytes` | `fn fs_read_bytes(path: String) -> Array<Int>` |
| `fs_read_text_range` | `fn fs_read_text_range(path, offset, length) -> String` |
| `fs_try_read_text` | `fn fs_try_read_text(path: String) -> FsTextResult` |
| `fs_try_read_bytes` | `fn fs_try_read_bytes(path: String) -> FsBytesResult` |

**Key Symbols — Writing:**

| Symbol | Signature |
|---|---|
| `fs_write_text` | `fn fs_write_text(path: String, content: String) -> Unit` |
| `fs_append_text` | `fn fs_append_text(path: String, content: String) -> Unit` |
| `fs_atomic_write_text` | `fn fs_atomic_write_text(path: String, content: String) -> Unit` |
| `fs_write_bytes` | `fn fs_write_bytes(path: String, bytes: Array<Int>) -> Unit` |
| `fs_write_bytes_at` | `fn fs_write_bytes_at(path: String, offset: Int, bytes: Array<Int>) -> Unit` |

**Key Symbols — Directory & Metadata:**

| Symbol | Signature |
|---|---|
| `fs_exists` | `fn fs_exists(path: String) -> Bool` |
| `fs_is_file` | `fn fs_is_file(path: String) -> Bool` |
| `fs_is_dir` | `fn fs_is_dir(path: String) -> Bool` |
| `fs_metadata` | `fn fs_metadata(path: String) -> FsMetadata` |
| `fs_read_dir` | `fn fs_read_dir(path: String) -> Array<FsDirEntry>` |
| `fs_walk` | `fn fs_walk(path: String) -> Array<FsDirEntry>` |
| `fs_create_dir_all` | `fn fs_create_dir_all(path: String) -> Unit` |
| `fs_copy_file` | `fn fs_copy_file(src: String, dest: String) -> Unit` |
| `fs_move_path` | `fn fs_move_path(src: String, dest: String) -> Unit` |
| `fs_remove_file` | `fn fs_remove_file(path: String) -> Unit` |
| `fs_remove_dir_all` | `fn fs_remove_dir_all(path: String) -> Unit` |
| `fs_temp_file` | `fn fs_temp_file(prefix: String) -> String` |
| `fs_temp_dir` | `fn fs_temp_dir(prefix: String) -> String` |

**Key Symbols — File Descriptor API:**

| Symbol | Signature |
|---|---|
| `fs_open` | `fn fs_open(path: String, mode: String) -> File` |
| `fs_close` | `fn fs_close(file: File) -> Int` |
| `fs_read` | `fn fs_read(file: File, buffer: ptr<Int>, byte_count: Int) -> Int` |
| `fs_write` | `fn fs_write(file: File, buffer: ptr<Int>, byte_count: Int) -> Int` |
| `fs_seek` | `fn fs_seek(file: File, offset: Int, origin: Int) -> Int` |
| `fs_tell` | `fn fs_tell(file: File) -> Int` |
| `fs_flush` | `fn fs_flush(file: File) -> Int` |

**Key Symbols — Streaming & Watching:**

| Symbol | Signature |
|---|---|
| `fs_stream_chunks` | `fn fs_stream_chunks(path: String, chunk_size: Int) -> Array<FsChunk>` |
| `fs_copy_file_streaming` | `fn fs_copy_file_streaming(src, dest, chunk_size) -> Int` |
| `fs_hash_file` | `fn fs_hash_file(path: String) -> String` |

**Path Helpers:**
- `fs_path_join(base: String, child: String) -> String`
- `fs_path_parent(path: String) -> String`
- `fs_path_file_name(path: String) -> String`
- `fs_path_extension(path: String) -> String`
- `fs_path_stem(path: String) -> String`

**Error Queries:**
- `fs_last_status() -> Int`
- `fs_last_error_kind() -> String`
- `fs_last_error_message() -> String`

**Usage:**
```kain
use std::fs

fn read_config() with Pure:
    let path = fs_path_join("data", "config.json")
    if fs_exists(path):
        let text = fs_read_text(path)
        // process...
    else:
        let result = fs_try_read_text(path)
        if result.ok:
            // use result.value
```

### `std::os`

**Import:** `use std::os`
**Purpose:** Operating system interface — environment, process info, filesystem, mmap, syscalls, CPU topology, timers.

**Key Symbols — Platform Info:**

| Symbol | Signature |
|---|---|
| `os_name` | `fn os_name() -> String` |
| `os_is_windows` | `fn os_is_windows() -> Bool` |
| `os_is_linux` | `fn os_is_linux() -> Bool` |
| `os_is_macos` | `fn os_is_macos() -> Bool` |
| `os_arch_name` | `fn os_arch_name() -> String` |
| `os_cpu_count` | `fn os_cpu_count() -> Int` |
| `os_uname` | `fn os_uname() -> OsUname` |

**Key Symbols — Environment:**
- `os_getenv(key: String) -> String`
- `os_getenv_default(key, default) -> String`
- `os_setenv(key, value) -> Bool`
- `os_getpid() -> Int`, `os_getppid() -> Int`
- `os_getcwd() -> String`, `os_chdir(path) -> Bool`

**Key Symbols — Filesystem:**
- `os_listdir(path) -> Array<String>`
- `os_scandir(path) -> Array<OsDirEntry>`
- `os_mkdir`, `os_makedirs`, `os_remove`, `os_rmdir`, `os_rename`
- `os_stat(path) -> OsStatResult`
- `os_exists`, `os_isfile`, `os_isdir`
- `os_read_text`, `os_write_text`, `os_append_text`, `os_atomic_write_text`

**Key Symbols — Low-Level:**
- `os_syscall(nr, a1..a6) -> Int` — raw syscall interface
- `os_mmap_anon(byte_count) -> Int`
- `os_mmap_file(path) -> (Int, Int)` — mmap file for reading
- `os_munmap(addr, byte_count) -> Bool`
- `os_mprotect(addr, byte_count, prot) -> Bool`
- `os_make_rwx(addr, byte_count) -> Bool`
- `os_madvise(addr, byte_count, advice) -> Bool`
- `os_fork() -> Int`
- `os_execve(path, argv, envp) -> Int`
- `os_waitpid(pid, options) -> Int`

**Constants:**
- `OS_SEP`, `OS_ALT_SEP`, `OS_LINESEP`, `OS_PATHSEP`, `OS_DEVNULL`
- `MMAP_PROT_NONE/READ/WRITE/EXEC/RW/RX/RWX`
- `MMAP_SHARED/PRIVATE/FIXED/HUGETLB`
- `MADV_NORMAL/RANDOM/SEQUENTIAL/WILLNEED/DONTNEED/FREE/HUGEPAGE`

**Usage:**
```kain
use std::os

fn get_system_info() with Pure:
    let os = os_name()
    let arch = os_arch_name()
    let cpus = os_cpu_count()
    let user = os_getenv("USER")
    let cwd = os_getcwd()
```

### `std::os_path`

**Import:** `use std::os_path`
**Purpose:** OS-native path manipulation — join, split, dirname, basename, normalize, expand.

**Key Symbols:**
- `os_path_join(a, b) -> String`
- `os_path_split(path) -> (String, String)`
- `os_path_dirname(path) -> String`
- `os_path_basename(path) -> String`
- `os_path_splitext(path) -> (String, String)`
- `os_path_normpath(path) -> String`
- `os_path_abspath(path) -> String`
- `os_path_relpath(path, start) -> String`
- `os_path_expanduser(path) -> String`
- `os_path_expandvars(path) -> String`
- `os_path_isabs`, `os_path_exists`, `os_path_isfile`, `os_path_isdir`

### `std::path`

**Import:** `use std::path`
**Purpose:** Cross-platform path utilities — join, parent, filename, extension, stem, normalize, canonicalize.

**Key Symbols:**
- `path_sep() -> String`
- `path_delimiter() -> String`
- `path_join(base, child) -> String`
- `path_parent(path) -> String`
- `path_file_name(path) -> String`
- `path_extension(path) -> String`
- `path_stem(path) -> String`
- `path_normalize(path) -> String`
- `path_canonicalize(path) -> String`
- `path_is_absolute(path) -> Bool`
- `path_split(path) -> Array<String>`

### `std::process`

**Import:** `use std::process`
**Purpose:** Child process spawning, I/O pipes, PTY, output capture, command-line args.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `process_args` | `fn process_args() -> Array<String>` |
| `process_arg_count` | `fn process_arg_count() -> Int` |
| `process_arg` | `fn process_arg(index: Int) -> String` |
| `process_current_working_directory` | `fn process_current_working_directory() -> String` |
| `process_spec_create` | `fn process_spec_create(executable: String) -> Int` |
| `process_spec_add_arg` | `fn process_spec_add_arg(spec_id: Int, argument: String) -> Int` |
| `process_spec_set_cwd` | `fn process_spec_set_cwd(spec_id: Int, cwd_path: String) -> Int` |
| `process_spec_set_env` | `fn process_spec_set_env(spec_id: Int, key: String, value: String) -> Int` |
| `process_spec_set_pipe_stdio` | `fn process_spec_set_pipe_stdio(spec_id: Int) -> Int` |
| `process_spawn` | `fn process_spawn(spec_id: Int) -> Int` |
| `process_spawn_pty` | `fn process_spawn_pty(spec_id, columns, rows) -> Int` |
| `process_wait` | `fn process_wait(process_id: Int, timeout_ms: Int) -> Int` |
| `process_is_running` | `fn process_is_running(process_id: Int) -> Int` |
| `process_exit_code` | `fn process_exit_code(process_id: Int) -> Int` |
| `process_terminate` | `fn process_terminate(process_id: Int) -> Int` |
| `process_kill` | `fn process_kill(process_id: Int) -> Int` |
| `process_stdout_read_text` | `fn process_stdout_read_text(process_id: Int) -> String` |
| `process_stderr_read_text` | `fn process_stderr_read_text(process_id: Int) -> String` |
| `process_stdout_capture_text` | `fn process_stdout_capture_text(process_id: Int) -> String` |
| `process_output_text` | `fn process_output_text(executable, arg0, arg1, arg2, timeout_ms) -> String` |

**Usage:**
```kain
use std::process

fn run_command() with Pure:
    let spec = process_spec_create("python")
    process_spec_add_arg(spec, "script.py")
    process_spec_set_pipe_stdio(spec)
    let pid = process_spawn(spec)
    let exit = process_wait(pid, 5000)
    let output = process_stdout_capture_text(pid)
```

---

## Networking

### `std::net`

**Import:** `use std::net`
**Purpose:** TCP networking, HTTP client/server, HTTP/1.1 request/response.

**Key Symbols — TCP:**
- `tcp_connect(host: String, port: Int, timeout_ms: Int) -> Int`
- `tcp_listen(host: String, port: Int) -> Int`
- `tcp_accept(listener_id: Int, timeout_ms: Int) -> Int`
- `tcp_read_text(connection_id: Int) -> String`
- `tcp_write_text(connection_id: Int, payload: String) -> Int`
- `tcp_close`, `tcp_listener_close`

**Key Symbols — HTTP Client:**
- `http_request_create(method: String, url: String) -> Int`
- `http_request_set_header(request_id, key, value) -> Int`
- `http_request_set_body_text(request_id, payload) -> Int`
- `http_client_send(request_id) -> Int`
- `http_response_status(response_id) -> Int`
- `http_response_body_text(response_id) -> String`
- `http_get_text(url: String) -> String`
- `http_post_text(url: String, payload: String) -> String`

**Key Symbols — HTTP Server:**
- `http_server_create(host: String, port: Int) -> Int`
- `http_server_listen(server_id) -> Int`
- `http_server_pump(server_id, timeout_ms) -> Int`
- `http_server_next_request(server_id) -> Int`
- `http_respond_text(incoming_request_id, status_code, payload) -> Int`
- `http_route_actor(server_id, method, path, actor_id, message_kind) -> Int`

**Usage:**
```kain
use std::net

fn fetch_data() with Pure:
    let body = http_get_text("https://api.example.com/data")
    // ...
```

### `std::http`

**Import:** `use std::http`
**Purpose:** Protocol-aware HTTP client/server (higher-level than `std::net`). Similar API surface.

**Key Additional Symbols:**
- `request_create_checked`, `request_set_timeout`, `request_set_protocol`
- `client_send`
- `server_create_localhost(port) -> Int`
- `server_pump_batch(server_id, timeout_ms, max_requests) -> Int`
- `respond_buffered_text`, `respond_hex`
- `local_url(port, path) -> String`

### `std::http2`

**Import:** `use std::http2`
**Purpose:** HTTP/2 protocol support — client state, upgrade, protocol negotiation.

**Key Symbols:**
- `http2_client_supported() -> Bool`
- `http2_client_available() -> Bool`
- `http2_request_upgrade(request_id) -> Int`
- `http2_request_create(method, url) -> Int`
- `http2_client_send(request_id) -> Int`
- `http2_response_negotiated(response_id) -> Bool`
- `http2_get_text(url: String) -> String`

### `std::tls`

**Import:** `use std::tls`
**Purpose:** TLS/HTTPS client — session management, secure requests.

**Key Symbols:**
- `tls_client_state() -> Int`
- `tls_client_supported() -> Bool`
- `tls_https_request_create(method, url) -> Int`
- `tls_https_request_set_header`, `tls_https_request_set_body_text`
- `tls_https_client_send(request_id) -> Int`
- `tls_https_get_text(url: String) -> String`

---

## Math & Geometry

### `std::math`

**Import:** `use std::math`
**Purpose:** Comprehensive math library — vectors (Vec2/3/4), Vec3A, quaternions, matrices (Mat3/4), affine transforms, SIMD bundles (Vec3x4, Vec4x8), GPU layout helpers, colors, tonemapping, noise, intersections, curves, packing.

**Key Constants:**
- `PI = 3.141592653589793`, `TAU = 6.283185307179586`, `HALF_PI = 1.5707963267948966`
- `DEG_TO_RAD = 0.017453292519943295`, `RAD_TO_DEG = 57.29577951308232`
- `EPSILON = 0.000001`, `HUGE_EPSILON = 0.0001`

**Key Types:**
- `Vec2`, `Vec3`, `Vec4` — core vector types (tuple-based)
- `Vec3A` — 16-byte aligned vec3 (with padding)
- `Quat` — `{ x, y, z, w }`
- `Mat3` — `{ row0, row1, row2 }` where each row is Vec3
- `Mat4` — `{ row0, row1, row2, row3 }` where each row is Vec4
- `Affine2`, `Affine3` — 2D and 3D affine transforms
- `Float8`, `Vec3x4`, `Vec4x8` — SIMD bundle types
- `Plane`, `Ray3`, `Aabb`, `Obb`, `BoundingSphere`, `Frustum`, `RayHit`
- `ColorRgb`, `ColorRgba`, `Hsv`, `Hsl`
- `GpuLayoutInfo`, `Std140<T>`, `Std430<T>`, `CBuffer<T>`, `Std140Vec3`, `Std140Mat3`

**Key Symbols — Vector Construction:**
| Symbol | Signature |
|---|---|
| `vec2` | `fn vec2(x: Float, y: Float) -> Vec2` |
| `vec3` | `fn vec3(x: Float, y: Float, z: Float) -> Vec3` |
| `vec4` | `fn vec4(x: Float, y: Float, z: Float, w: Float) -> Vec4` |
| `vec2_zero` / `vec2_one` / `vec2_splat` | identity constructors |
| `vec3_zero` / `vec3_one` / `vec3_up` / `vec3_right` / `vec3_forward` | axis vectors |
| `vec4_zero` / `vec4_one` / `vec4_splat` | identity constructors |

**Key Symbols — Vector Operations:**
| Symbol | Signature |
|---|---|
| `vec2_dot` | `fn vec2_dot(a: Vec2, b: Vec2) -> Float` |
| `vec3_dot` | `fn vec3_dot(a: Vec3, b: Vec3) -> Float` |
| `vec3_cross` | `fn vec3_cross(a: Vec3, b: Vec3) -> Vec3` |
| `vec2_length` | `fn vec2_length(value: Vec2) -> Float` |
| `vec3_length` | `fn vec3_length(value: Vec3) -> Float` |
| `vec2_add` / `vec2_sub` / `vec2_mul_scalar` / `vec2_div_scalar` | arithmetic |
| `vec3_add` / `vec3_sub` / `vec3_mul_scalar` / `vec3_div_scalar` / `vec3_neg` / `vec3_abs` | arithmetic |
| `vec4_add` / `vec4_sub` / `vec4_mul_scalar` / `vec4_div_scalar` | arithmetic |
| `vec2_normalize_or_zero` | normalize, return zero on zero-length |
| `vec3_normalize_or_zero` | normalize, return zero on zero-length |
| `vec2_lerp` | `fn vec2_lerp(a: Vec2, b: Vec2, t: Float) -> Vec2` |
| `vec3_lerp` | `fn vec3_lerp(a: Vec3, b: Vec3, t: Float) -> Vec3` |
| `vec2_min` / `vec2_max` | component-wise min/max |
| `vec3_min` / `vec3_max` / `vec3_clamp` | component-wise min/max/clamp |
| `vec3_project` / `vec3_reject` | project/reject onto vector |
| `vec3_reflect` / `vec3_refract` / `vec3_faceforward` | reflection operations |
| `vec2_perp` | perpendicular vector |
| `vec2_rotate` | rotate by angle |

**Key Symbols — Quaternions:**
| Symbol | Signature |
|---|---|
| `quat_identity` | `fn quat_identity() -> Quat` |
| `quat_conjugate` | `fn quat_conjugate(value: Quat) -> Quat` |
| `quat_dot` | `fn quat_dot(a: Quat, b: Quat) -> Float` |
| `quat_length` | `fn quat_length(value: Quat) -> Float` |
| `quat_normalize_or_identity` | normalize, return identity on zero |
| `quat_from_axis_angle` | `fn quat_from_axis_angle(axis: Vec3, angle: Float) -> Quat` |
| `quat_mul` | `fn quat_mul(a: Quat, b: Quat) -> Quat` |
| `quat_rotate_vec3` | `fn quat_rotate_vec3(rotation: Quat, value: Vec3) -> Vec3` |
| `quat_nlerp` / `quat_slerp` | quaternion interpolation |

**Key Symbols — Matrices:**
| Symbol | Signature |
|---|---|
| `mat3_identity` | `fn mat3_identity() -> Mat3` |
| `mat3_mul_vec3` | `fn mat3_mul_vec3(m: Mat3, v: Vec3) -> Vec3` |
| `mat3_transpose` | `fn mat3_transpose(m: Mat3) -> Mat3` |
| `mat4_identity` | `fn mat4_identity() -> Mat4` |
| `mat4_translation` | `fn mat4_translation(offset: Vec3) -> Mat4` |
| `mat4_scale` | `fn mat4_scale(scale: Vec3) -> Mat4` |
| `mat4_rotation_x/y/z` | rotation around axis |
| `mat4_from_quat` | `fn mat4_from_quat(value: Quat) -> Mat4` |
| `mat4_mul` | `fn mat4_mul(a: Mat4, b: Mat4) -> Mat4` |
| `mat4_transform_point` | `fn mat4_transform_point(m: Mat4, point: Vec3) -> Vec3` |
| `mat4_transform_vector` | `fn mat4_transform_vector(m: Mat4, vector: Vec3) -> Vec3` |
| `mat4_from_trs` | translation-rotation-scale decomposition |
| `mat4_perspective` | `fn mat4_perspective(fov_y, aspect, near, far) -> Mat4` |
| `mat4_orthographic` | `fn mat4_orthographic(left, right, bottom, top, near, far) -> Mat4` |
| `mat4_look_at` | `fn mat4_look_at(eye, target, up) -> Mat4` |

**Key Symbols — Affine Transforms:**
- `affine2_identity()`, `affine2_transform_point`
- `affine3_identity()`, `affine3_from_trs`, `affine3_transform_point`, `affine3_transform_vector`, `affine3_to_mat4`, `affine3_mul`

**Key Symbols — GPU Layout:**
- `align_up_int(value: Int, alignment: Int) -> Int`
- `gpu_layout_info(size_bytes, alignment_bytes) -> GpuLayoutInfo`
- `std140_vec3(value: Vec3) -> Std140<Std140Vec3>`
- `std140_vec4(value: Vec4) -> Std140<Vec4>`
- `std140_mat3(value: Mat3) -> Std140<Std140Mat3>`
- `std140_mat4(value: Mat4) -> Std140<Mat4>`
- `std430_vec3a(value: Vec3A) -> Std430<Vec3A>`
- `cbuffer_mat4(value: Mat4) -> CBuffer<Mat4>`

**Key Symbols — Color:**
| Symbol | Signature |
|---|---|
| `color_rgb` | `fn color_rgb(r: Float, g: Float, b: Float) -> ColorRgb` |
| `color_rgba` | `fn color_rgba(r, g, b, a) -> ColorRgba` |
| `srgb_to_linear_scalar` | `fn srgb_to_linear_scalar(value: Float) -> Float` |
| `linear_to_srgb_scalar` | `fn linear_to_srgb_scalar(value: Float) -> Float` |
| `srgb_to_linear` / `linear_to_srgb` | vec3 versions |
| `rgb_to_hsv` / `hsv_to_rgb` | HSV conversion |
| `rgb_to_hsl` / `hsl_to_rgb` | HSL conversion |
| `tonemap_aces` / `tonemap_reinhard` / `tonemap_uncharted2` | tonemapping operators |
| `pack_unorm8` / `pack_unorm4x8` / `pack_rgba_to_u32` | packing |
| `unpack_u32_to_rgba` | unpacking |

**Key Symbols — Geometry & Intersection:**
| Symbol | Signature |
|---|---|
| `plane_from_point_normal` | `fn plane_from_point_normal(point, normal) -> Plane` |
| `plane_signed_distance` | `fn plane_signed_distance(plane, point) -> Float` |
| `ray3` | `fn ray3(origin: Vec3, direction: Vec3) -> Ray3` |
| `aabb_center` / `aabb_half_extents` | bounding box |
| `aabb_contains_point` | point-in-AABB test |
| `ray_vs_aabb` | `fn ray_vs_aabb(ray: Ray3, bounds: Aabb) -> RayHit` |
| `ray_vs_triangle` | `fn ray_vs_triangle(ray, a, b, c) -> RayHit` |
| `frustum_contains_point` | point-in-frustum test |
| `frustum_vs_aabb` | frustum-AABB test |
| `sphere_contains_point` | sphere-point test |
| `closest_point_on_aabb` / `closest_point_on_obb` | closest point queries |
| `sphere_vs_obb` | sphere-OBB intersection |

**Key Symbols — Curves:**
- `bezier_quadratic_vec2`, `bezier_quadratic_vec3`
- `bezier_cubic_vec2`, `bezier_cubic_vec3`
- `catmull_rom_vec3`, `catmull_rom_tangent_vec3`
- `bspline_cubic_vec3`

**Key Symbols — Noise:**
- `hash11(x: Float) -> Float`, `hash22(p: Vec2) -> Vec2`, `hash33(p: Vec3) -> Vec3`
- `pcg32_step(state: Int) -> Int`
- `noise2(p: Vec2) -> Float`, `noise3(p: Vec3) -> Float`
- `fbm2(p: Vec2, octaves: Int) -> Float`, `fbm3(p: Vec3, octaves: Int) -> Float`
- `worley_noise(uv, scale, randomness, seed) -> Float`
- `blue_noise_dither_approx(pixel_x, pixel_y, frame) -> Float`

**Key Symbols — Scalar Math:**
- `math_min`, `math_max`, `math_clamp`, `math_int_clamp`
- `degrees_to_radians`, `radians_to_degrees`
- `saturate`, `sign_nonzero`, `inverse_lerp`, `lerp`
- `smoothstep`, `smootherstep`
- `nearly_equal(a, b, epsilon) -> Bool`
- `fast_sin`, `fast_cos`, `fast_atan2`, `fast_acos`
- `tan_scalar`, `fma_scalar`, `frac_scalar`

**Usage:**
```kain
use std::math

fn compute_transform() with Pure:
    let pos = vec3(1.0, 2.0, 3.0)
    let rot = quat_from_axis_angle(vec3(0.0, 1.0, 0.0), math.PI / 2.0)
    let scale = vec3(2.0, 2.0, 2.0)
    let view = mat4_look_at(pos, vec3_zero(), vec3_up())
    let proj = mat4_perspective(TAU / 4.0, 16.0/9.0, 0.1, 100.0)
```

### `std::random`

**Import:** `use std::random`
**Purpose:** Deterministic RNG with Xoshiro128, ambient random, shattered RNG buffer (parallel-friendly).

**Key Symbols:**
- `xoshiro128_new(seed: Int) -> Xoshiro128`
- `xoshiro128_next(rng: Xoshiro128) -> XoshiroNextResult`
- `random_int_in_range(rng, min, max) -> IntResult`
- `random_float(rng: Xoshiro128) -> FloatResult`
- `random_float_norm(rng: Xoshiro128) -> FloatNormResult`
- `shattered_rng_buffer_new(seed: Int, lanes: Int) -> ShatteredRngBuffer`
- `random_ambient_next() -> Int`
- `random_ambient_float() -> Float`
- `random_ambient_int_in_range(min, max) -> Int`

**Usage:**
```kain
use std::random

fn gen_random() with Pure:
    let rng = xoshiro128_new(42)
    let result = random_int_in_range(rng, 0, 100)
    if result.ok:
        let value = result.value
```

### `std::alloc`

**Import:** `use std::alloc`
**Purpose:** Custom allocators — bump allocator, arena allocator, pool allocator, growable buffer, span slicing.

**Key Symbols:**
- `bump_allocator_create(cells: Int) -> BumpAllocator`
- `bump_alloc_cells(allocator, cells) -> BumpAllocation`
- `bump_allocator_reset`, `bump_allocator_destroy`
- `arena_allocator_create(cells: Int) -> ArenaAllocator`
- `arena_alloc_cells(arena, cells) -> ArenaAllocation`
- `pool_allocator_create(block_count, block_cells) -> PoolAllocator`
- `pool_alloc_block(pool) -> PoolAllocation`
- `pool_free_block(pool, block_index) -> PoolAllocator`
- `alloc_grow_buffer(buffer, current_cap, needed_cap) -> ptr<Int>`
- `alloc_span_slice(span, start, length) -> AllocSpan`

---

## Text, JSON & Formatting

### `std::json`

**Import:** `use std::json`
**Purpose:** JSON parsing, rendering, object/array construction, field extraction, scanning.

**Key Constants:**
- `JSON_KIND_NULL`, `JSON_KIND_BOOL`, `JSON_KIND_INT`, `JSON_KIND_FLOAT`, `JSON_KIND_STRING`, `JSON_KIND_OBJECT`, `JSON_KIND_ARRAY`
- `JSON_STATUS_OK`, `JSON_STATUS_MISSING_KEY`, `JSON_STATUS_WRONG_KIND`, `JSON_STATUS_SCAN_UNKNOWN_TOKEN`, etc.

**Key Types:**
- `JsonValue = Any`, `JsonObject = Any`, `JsonArray = Any`
- `JsonToken`, `JsonStatus`, `JsonParseResult`, `JsonScanReport`
- Result types: `JsonStringResult`, `JsonIntResult`, `JsonFloatResult`, `JsonBoolResult`, `JsonObjectResult`, `JsonArrayResult`

**Key Symbols — Parse/Render:**
| Symbol | Signature |
|---|---|
| `json_parse_text` | `fn json_parse_text(text: String) -> JsonValue` |
| `json_parse_text_result` | `fn json_parse_text_result(text: String) -> JsonParseResult` |
| `json_parse_text_or` | `fn json_parse_text_or(text, fallback) -> JsonValue` |
| `json_stringify` | `fn json_stringify(value: JsonValue) -> String` |

**Key Symbols — Introspection:**
| Symbol | Signature |
|---|---|
| `json_value_kind` | `fn json_value_kind(value: JsonValue) -> String` |
| `json_is_null` / `json_is_string` / `json_is_int` / `json_is_float` / `json_is_bool` / `json_is_object` / `json_is_array` | kind checks |

**Key Symbols — Object Access:**
| Symbol | Signature |
|---|---|
| `json_has_key` | `fn json_has_key(object: JsonObject, key: String) -> Bool` |
| `json_get_value` | `fn json_get_value(object: JsonObject, key: String) -> JsonValue` |
| `json_string_required` | `fn json_string_required(object, key) -> String` |
| `json_int_required` | `fn json_int_required(object, key) -> Int` |
| `json_float_required` | `fn json_float_required(object, key) -> Float` |
| `json_bool_required` | `fn json_bool_required(object, key) -> Bool` |
| `json_string_or` | `fn json_string_or(object, key, default) -> String` |
| `json_int_or` | `fn json_int_or(object, key, default) -> Int` |
| `json_float_or` | `fn json_float_or(object, key, default) -> Float` |
| `json_bool_or` | `fn json_bool_or(object, key, default) -> Bool` |
| `json_string_field` / `json_int_field` / `json_float_field` / `json_bool_field` / `json_object_field` / `json_array_field` | safe field access with result types |

**Key Symbols — Construction:**
| Symbol | Signature |
|---|---|
| `json_object` | `fn json_object() -> JsonObject` |
| `json_object_set_value` / `json_object_set_string` / `json_object_set_int` / `json_object_set_float` / `json_object_set_bool` | set fields |
| `json_array` | `fn json_array() -> JsonArray` |
| `json_array_push_value` / `json_array_push_string` / `json_array_push_int` / etc. | push elements |
| `json_array_length` | `fn json_array_length(array: JsonArray) -> Int` |
| `json_array_value_at` | `fn json_array_value_at(array, index) -> JsonValue` |

**Key Symbols — Array Extraction:**
- `json_string_array(values: JsonArray) -> Array<String>`
- `json_int_array`, `json_float_array`, `json_bool_array`
- `json_string_array_field(object, key) -> Array<String>`
- `json_array_from_strings / from_ints / from_floats / from_bools`

**Key Symbols — Quick Construction:**
- `json_object_with_string(key, value) -> JsonObject`
- `json_object_with_int`, `json_object_with_float`, `json_object_with_bool`, `json_object_with_array`, `json_object_with_object`
- `json_object_set_string_array`, `json_object_set_int_array`, etc.

**Key Symbols — Scanning:**
- `json_scan(text: String) -> Array<JsonToken>`
- `json_scan_significant(text: String) -> Array<JsonToken>`
- `json_scan_report(text: String) -> JsonScanReport`

**Usage:**
```kain
use std::json

fn work_with_json() with Pure:
    let data = json_parse_text("{\"name\":\"Kain\",\"version\":1}")
    let name = json_string_required(data, "name")  // "Kain"
    let ver = json_int_or(data, "version", 0)       // 1
    let obj = json_object_with_string("key", "value")
    let text = json_stringify(obj)                   // "{\"key\":\"value\"}"
```

### `std::fmt`

**Import:** `use std::fmt`
**Purpose:** Structured formatting with spec-driven string, int, float, bool output. Builder-based.

**Key Types:**
- `FmtWriter` — accumulator with string-building
- `FmtSpec` — format specification (width, pad, align, prefix, base, case, bool style)

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `fmt_writer_new` | `fn fmt_writer_new() -> FmtWriter` |
| `fmt_spec_default` | `fn fmt_spec_default() -> FmtSpec` |
| `fmt_spec_width` | `fn fmt_spec_width(spec, width: Int) -> FmtSpec` |
| `fmt_spec_pad` | `fn fmt_spec_pad(spec, pad: String) -> FmtSpec` |
| `fmt_spec_align` | `fn fmt_spec_align(spec, align: String) -> FmtSpec` |
| `fmt_spec_base` | `fn fmt_spec_base(spec, base: Int) -> FmtSpec` — `FMT_BASE_DECIMAL/HEX/BINARY` |
| `fmt_spec_uppercase` | `fn fmt_spec_uppercase(spec, enabled: Bool) -> FmtSpec` |
| `fmt_spec_bool_style` | `fn fmt_spec_bool_style(spec, style) -> FmtSpec` — word, json, numeric |
| `fmt_writer_push_string_spec` | `fn fmt_writer_push_string_spec(writer, value, spec) -> FmtWriter` |
| `fmt_writer_push_int_spec` | `fn fmt_writer_push_int_spec(writer, value, spec) -> FmtWriter` |
| `fmt_writer_push_float_spec` | `fn fmt_writer_push_float_spec(writer, value, spec) -> FmtWriter` |
| `fmt_writer_push_bool_spec` | `fn fmt_writer_push_bool_spec(writer, value, spec) -> FmtWriter` |
| `fmt_writer_build` | `fn fmt_writer_build(writer: FmtWriter) -> String` |

**Convenience Functions:**
- `fmt_string(value: String) -> String`
- `fmt_int(value: Int) -> String`
- `fmt_float(value: Float) -> String`
- `fmt_bool_word(value: Bool) -> String`
- `fmt_bool_json(value: Bool) -> String`
- `fmt_any(value: Any) -> String`
- `fmt_join_strings(items: Array<String>, sep: String) -> String`
- `fmt_pad_left/right/center(value, width, pad) -> String`
- `fmt_hex_u32`, `fmt_hex_int`, `fmt_binary_u32`, `fmt_binary_int`
- `fmt_json_escape`, `fmt_json_string`

### `std::text`

**Import:** `use std::text`
**Purpose:** String/text views, builders, unicode-aware slicing, escaping, joining.

**Key Types:**
- `TextSlice` — non-owning view into a string
- `StringView` — alternate view type
- `TextUnescapeResult` — result of unescaping

**Key Symbols:**
- `text_slice(source: String, start: Int, length: Int) -> TextSlice`
- `text_from(source: String) -> TextSlice`
- `text_len(view) -> Int`, `text_byte_at(view, index) -> Int`
- `text_find`, `text_contains`, `text_starts_with`, `text_ends_with`
- `text_subslice`, `text_trim_left`, `text_trim_right`, `text_trim`
- `text_materialize(view) -> String`
- `text_lines(view) -> Array<String>`
- `text_escape_basic`, `text_unescape_basic`
- `text_upper`, `text_lower`
- `text_replace_string(source, from, to) -> String`
- `text_repeat(source, count) -> String`
- `text_split_string(source, separator) -> Array<String>`
- `text_join_strings(items, separator) -> String`
- `text_split_lines`, `text_tokenize_whitespace`
- `text_ord(source) -> Int`, `text_chr(codepoint) -> String`
- `text_to_string(value: Any) -> String`
- `text_builder_new() -> BytesBuilder` (alias for bytes builder)

### `std::ascii`

**Import:** `use std::ascii`
**Purpose:** ASCII classification and conversion — byte and string variants.

**Key Constants:**
- `ASCII_NUL`, `ASCII_HT`, `ASCII_LF`, `ASCII_CR`, `ASCII_SPACE`, `ASCII_DEL`
- `ASCII_DIGITS`, `ASCII_LOWERCASE`, `ASCII_UPPERCASE`, `ASCII_HEX_LOWERCASE`, `ASCII_HEX_UPPERCASE`

**Key Symbols:**
- `ascii_is_byte(value: Int) -> Bool`
- `ascii_is_whitespace_byte`, `ascii_is_digit_byte`, `ascii_is_alpha_byte`, `ascii_is_alnum_byte`, `ascii_is_hex_byte`
- `ascii_to_upper_byte`, `ascii_to_lower_byte`
- `ascii_is_upper(value: String) -> Bool` (and same for whitespace/digit/alpha/alnum)
- `ascii_to_upper(value: String) -> String`
- `ascii_to_lower(value: String) -> String`
- `ascii_equals_ignore_case(left, right) -> Bool`

### `std::unicode`

**Import:** `use std::unicode`
**Purpose:** UTF-8 encoding/decoding, codepoint iteration, normalization.

**Key Symbols:**
- `unicode_utf8_char_length(first_byte: Int) -> Int`
- `unicode_utf8_decode_at(value: String, start_index: Int) -> UnicodeDecodeResult`
- `unicode_utf8_encode(codepoint: Int) -> String`
- `unicode_utf8_is_valid(value: String) -> Bool`
- `unicode_utf8_codepoint_count(value: String) -> Int`
- `unicode_cursor_new(source: String) -> UnicodeCursor`
- `unicode_cursor_next(cursor) -> UnicodeCursorNextResult`
- `unicode_normalize(value, form: UnicodeNormalizationForm) -> String`

---

## Interop (Python, JS, C)

### `std::python`

**Import:** `use std::python`
**Purpose:** Python interpreter bridge — exec, eval, import, call with kwargs, async futures, buffer/shared/tensor/geometry interop, GPU bridge.

**Key Symbols — Basic:**
| Symbol | Signature |
|---|---|
| `python_exec` | `fn python_exec(code: String)` |
| `python_eval` | `fn python_eval(code: String) -> Any` |
| `python_import` | `fn python_import(name: String) -> Any` |
| `python_module_available` | `fn python_module_available(name: String) -> Bool` |
| `python_require_module` | `fn python_require_module(name: String) -> Any` |

**Key Symbols — Region (lifecycle-scoped interop):**
- `python_region_begin() -> Any`
- `python_region_end(region) -> Int`
- `python_region_import(region, name) -> Any`
- `python_region_getattr`, `python_region_bind_attr`
- `python_region_call_raw`, `python_region_call_attr_raw`

**Key Symbols — Direct Call:**
| Symbol | Signature |
|---|---|
| `python_call` | `fn python_call(target: Any, args: Any) -> Any` |
| `python_call_kwargs` | `fn python_call_kwargs(target, args, kwargs) -> Any` |
| `python_call_attr` | `fn python_call_attr(target, attr, args) -> Any` |
| `python_call_attr_kwargs` | `fn python_call_attr_kwargs(target, attr, args, kwargs) -> Any` |
| `python_getattr` | `fn python_getattr(target: Any, name: String) -> Any` |
| `python_setattr` | `fn python_setattr(target, name, value)` |
| `python_hasattr` | `fn python_hasattr(target, name) -> Bool` |

**Key Symbols — Async:**
- `python_call_async(target, args) -> Any`
- `python_future_from_awaitable(awaitable) -> Any`
- `python_future_state`, `python_future_done`, `python_future_await`, `python_future_cancel`

**Key Symbols — Actor Callbacks:**
- `python_actor_callback(actor_id, message_name) -> Any`
- `python_actor_callback_callable(callback) -> Any`

**Key Symbols — GPU Bridge:**
- `python_shared_buffer(target: Any) -> Any`
- `python_shared_image(target: Any) -> Any`
- `python_shared_buffer_gpu(target, policy) -> GpuBuffer`
- `python_gpu_storage_buffer(target, debug_name) -> GpuBuffer`
- `python_gpu_uniform_buffer(target, debug_name) -> GpuBuffer`
- `python_tensor(target) -> Any`
- `python_tensor_interop_info(target) -> KainSharedBufferInfo`
- `python_tensor_to(tensor, backend) -> Any`

**Usage:**
```kain
use std::python

fn use_numpy() with Pure:
    let np = python_import("numpy")
    let arr = python_call(np, ["array", [[1, 2, 3], [4, 5, 6]]])
    let shape = python_getattr(arr, "shape")
    let result = python_call_attr(arr, "sum", [])
```

### `std::js`

**Import:** `use std::js`
**Purpose:** JavaScript/Node.js bridge — execute, evaluate, import, require, call methods, attribute access, web/site API.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `js_bridge_exec` | `fn js_bridge_exec(code: String)` |
| `js_bridge_eval` | `fn js_bridge_eval(code: String) -> Any` |
| `js_bridge_import` | `fn js_bridge_import(specifier: String) -> Any` |
| `js_bridge_require` | `fn js_bridge_require(specifier: String) -> Any` |
| `node_import` | `fn node_import(specifier: String) -> Any` |
| `node_require` | `fn node_require(specifier: String) -> Any` |
| `js_bridge_call` | `fn js_bridge_call(target: Any, args: Any) -> Any` |
| `js_bridge_call_method` | `fn js_bridge_call_method(target, name, args) -> Any` |
| `js_bridge_getattr` | `fn js_bridge_getattr(target, name) -> Any` |
| `js_bridge_setattr` | `fn js_bridge_setattr(target, name, value)` |

**Web Environment:**
- `js_web_fs()`, `js_web_path()`, `js_web_json() -> Any`
- `js_web_read_text(path) -> String`
- `js_web_write_text(path, text)`
- `js_web_json_stringify(value) -> String`, `js_web_json_parse(text) -> Any`

**Site Builder:**
- `js_site_runtime() -> Any`
- `js_site_load_app(path) -> Any`
- `js_site_build(path) -> Any`
- `js_site_serve(path, experience_id) -> Any`
- `js_site_build_report(app_manifest_path, template_name, archetypes, outputs) -> String`

### `std::interop`

**Import:** `use std::interop`
**Purpose:** Generic shared-buffer and shared-image interop info — inspect foreign buffer/image objects.

**Key Symbols:**
- `interop_shared_buffer_info(target: Any) -> KainSharedBufferInfo`
- `interop_shared_buffer_bytes(target: Any) -> Array<Int>`
- `interop_shared_buffer_from_bytes(bytes, element_type, shape, format, mime_type) -> Any`
- `interop_shared_image_info(target: Any) -> KainSharedImageInfo`
- `interop_shared_image_bytes(target: Any) -> Array<Int>`
- `interop_shared_image_from_bytes(bytes, width, height, channels, layout, pixel_format, mime_type) -> Any`

---

## Systems & Machine

### `std::machine`

**Import:** `use std::machine`
**Purpose:** Low-level machine primitives — fences, prefetch, cache flush, spin loops, CPU topology, virtual memory, RD TSC, CPUID.

**Key Symbols — Fences & Barriers:**
| Symbol | Signature |
|---|---|
| `pause` | `fn pause() -> Int` |
| `load_fence` | `fn load_fence() -> Unit` |
| `store_fence` | `fn store_fence() -> Unit` |
| `full_fence` | `fn full_fence() -> Unit` |
| `cache_flush` | `fn cache_flush(address: ptr<Int>) -> Unit` |
| `spin_loop_hint` | `fn spin_loop_hint() -> Unit` |

**Key Symbols — Timers & CPUID:**
| Symbol | Signature |
|---|---|
| `rdtsc` | `fn rdtsc() -> Int` |
| `cpuid_eax` | `fn cpuid_eax(leaf: Int, subleaf: Int) -> Int` |
| `cpuid_ebx`, `cpuid_ecx`, `cpuid_edx` | CPUID register reads |
| `prefetch_read` | `fn prefetch_read(address: ptr<Int>, locality: Int) -> Unit` |
| `prefetch_write` | `fn prefetch_write(address: ptr<Int>, locality: Int) -> Unit` |

**Key Symbols — CPU Topology:**
| Symbol | Signature |
|---|---|
| `cpu_logical_count` | `fn cpu_logical_count() -> Int` |
| `cpu_core_count` | `fn cpu_core_count() -> Int` |
| `cpu_package_count` | `fn cpu_package_count() -> Int` |
| `cpu_cache_line_bytes` | `fn cpu_cache_line_bytes() -> Int` |
| `current_thread_id` | `fn current_thread_id() -> Int` |
| `set_current_thread_affinity` | `fn set_current_thread_affinity(core_index: Int) -> Int` |
| `numa_node_count` | `fn numa_node_count() -> Int` |
| `numa_current_node` | `fn numa_current_node() -> Int` |
| `numa_bind_current_thread` | `fn numa_bind_current_thread(node_index: Int) -> Int` |

**Key Symbols — Virtual Memory:**
| Symbol | Signature |
|---|---|
| `vm_page_size` | `fn vm_page_size() -> Int` |
| `vm_reserve` | `fn vm_reserve(byte_count: Int) -> ptr<Int>` |
| `vm_commit` | `fn vm_commit(address: ptr<Int>, byte_count: Int) -> Int` |
| `vm_decommit` | `fn vm_decommit(address: ptr<Int>, byte_count: Int) -> Int` |
| `vm_release` | `fn vm_release(address: ptr<Int>, byte_count: Int) -> Int` |
| `vm_lock` | `fn vm_lock(address: ptr<Int>, byte_count: Int) -> Int` |
| `vm_unlock` | `fn vm_unlock(address: ptr<Int>, byte_count: Int) -> Int` |
| `vm_map_huge` | `fn vm_map_huge(byte_count: Int) -> ptr<Int>` |
| `vm_map` | `fn vm_map(byte_count: Int) -> ptr<Int>` |
| `vm_unmap` | `fn vm_unmap(address: ptr<Int>, byte_count: Int) -> Int` |
| `vm_protect_none` | `fn vm_protect_none(address: ptr<Int>, byte_count: Int) -> Int` |
| `vm_protect_read` | `fn vm_protect_read(address: ptr<Int>, byte_count: Int) -> Int` |
| `vm_protect_read_write` | `fn vm_protect_read_write(address: ptr<Int>, byte_count: Int) -> Int` |
| `vm_protect_execute_read` | `fn vm_protect_execute_read(address: ptr<Int>, byte_count: Int) -> Int` |
| `vm_protect_execute_read_write` | `fn vm_protect_execute_read_write(address: ptr<Int>, byte_count: Int) -> Int` |

**Usage:**
```kain
use std::machine

fn low_level_work() with Pure:
    let tsc_before = rdtsc()
    full_fence()
    // critical work
    full_fence()
    let tsc_after = rdtsc()
    let elapsed_cycles = tsc_after - tsc_before
```

### `std::memory`

**Import:** `use std::memory`
**Purpose:** Low-level memory operations — volatile load/store, atomic load/store (relaxed/acquire/release/seqcst), atomic RMW, fences.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `volatile_load_int` | `fn volatile_load_int(address: ptr<Int>) -> Int` |
| `volatile_store_int` | `fn volatile_store_int(address: ptr<Int>, value: Int) -> Int` |
| `atomic_load_relaxed` | `fn atomic_load_relaxed(address: ptr<Int>) -> Int` |
| `atomic_load_acquire` | `fn atomic_load_acquire(address: ptr<Int>) -> Int` |
| `atomic_load_seqcst` | `fn atomic_load_seqcst(address: ptr<Int>) -> Int` |
| `atomic_store_relaxed` | `fn atomic_store_relaxed(address, value) -> Int` |
| `atomic_store_release` | `fn atomic_store_release(address, value) -> Int` |
| `atomic_store_seqcst` | `fn atomic_store_seqcst(address, value) -> Int` |
| `atomic_add_acqrel` | `fn atomic_add_acqrel(address: ptr<Int>, value: Int) -> Int` |
| `atomic_sub_acqrel` | `fn atomic_sub_acqrel(address, value) -> Int` |
| `atomic_and_acqrel`, `atomic_or_acqrel`, `atomic_xor_acqrel` | bitwise atomic RMW |
| `atomic_exchange_acqrel` | `fn atomic_exchange_acqrel(address, value) -> Int` |
| `atomic_compare_exchange_seqcst` | `fn atomic_compare_exchange_seqcst(address, expected, desired) -> Bool` |
| `atomic_fence_acquire`, `atomic_fence_release`, `atomic_fence_acqrel`, `atomic_fence_seqcst` | explicit fences |

### `std::atomic`

**Import:** `use std::atomic`
**Purpose:** Higher-level atomic primitives — `AtomicInt`, `AtomicBool`, `AtomicPtr` with explicit memory ordering, plus raw operations and futex-like wait/notify.

**Key Types:**
- `Ordering` enum — `Relaxed`, `Acquire`, `Release`, `AcqRel`, `SeqCst`
- `AtomicInt` — `{ handle: Int }`
- `AtomicBool` — `{ handle: Int }`
- `AtomicPtr` — `{ handle: Int }`

**Raw Operations:**
- `atomic_raw_load(address: ptr<Int>, order: Ordering) -> Int`
- `atomic_raw_store(address, value, order) -> Int`
- `atomic_raw_exchange`, `atomic_raw_fetch_add`, `atomic_raw_fetch_sub`
- `atomic_raw_compare_exchange(address, expected, desired, success_order, failure_order) -> Bool`
- `atomic_raw_fetch_and`, `atomic_raw_fetch_or`, `atomic_raw_fetch_xor`
- `atomic_wait(address, expected, timeout_ms) -> Int`
- `atomic_notify_one`, `atomic_notify_all`

**AtomicInt Methods:**
- `atomic_int_new(initial_value) -> AtomicInt`
- `.load(self, order) -> Int`
- `.store(self, value, order) -> Int`
- `.add(self, delta) -> Int` (fetch_add, relaxed)
- `.exchange`, `.compare_exchange`, `.compare_exchange_ordered`
- `.fetch_and`, `.fetch_or`, `.fetch_xor`
- `.wait(expected, timeout_ms)`, `.notify_one()`, `.notify_all()`

### `std::mmio`

**Import:** `use std::mmio`
**Purpose:** Memory-mapped I/O helpers — bit field extraction/setting, swap, read/write.

**Key Constants:**
- `MMIO_ACCESS_RO`, `MMIO_ACCESS_WO`, `MMIO_ACCESS_RW`, `MMIO_ACCESS_W1C`
- `MMIO_ENDIAN_NATIVE`, `MMIO_ENDIAN_LITTLE`, `MMIO_ENDIAN_BIG`

**Key Symbols:**
- `mmio_field_get(word, bit_offset, width) -> Int`
- `mmio_field_set(word, bit_offset, width, value) -> Int`
- `mmio_field_w1c(word, bit_offset, width, clear_bits) -> Int`
- `mmio_swap16`, `mmio_swap32`
- `mmio_read_int(address: ptr<Int>) -> Int`
- `mmio_write_int(address: ptr<Int>, value: Int) -> Int`
- `mmio_write_one_to_clear(address, bit_offset, width, clear_bits) -> Int`

### `std::platform`

**Import:** `use std::platform`
**Purpose:** Platform library loading (dynamic libraries, .dll/.so) and platform identification.

**Key Symbols:**
- `platform_current_kind() -> Int`
- `platform_current_name() -> String`
- `platform_library_open(path) -> Int`
- `platform_library_close(handle) -> Int`
- `platform_library_resolve(handle, symbol_name) -> Int`
- `platform_library_is_valid(handle) -> Bool`

### `std::reload`

**Import:** `use std::reload`
**Purpose:** Hot-reload infrastructure — session management, revision tracking, migration planning across noop/presentation/structural/actor/gpu lanes.

**Key Symbols:**
- `reload_begin(session_id, revision_key) -> Int`
- `reload_commit(session_id) -> Int`
- `reload_generation(session_id) -> Int`
- `reload_snapshot(session_id) -> ReloadGeneration`
- `reload_default_migration_plan(session_id) -> ReloadMigrationPlan`
- `reload_structural_migration_plan`, `reload_actor_migration_plan`, `reload_gpu_migration_plan`
- `reload_lane_noop`, `reload_lane_presentation`, `reload_lane_structural`, `reload_lane_actor`, `reload_lane_gpu`

---

## Build, Test, Proof & Certification

### `std::build`

**Import:** `use std::build`
**Purpose:** Build graph construction DSL — define packages, blades, build tasks, platform requirements, and source sets for the Kain build system.

**Key Types:**
- `BuildContext`, `PackageSpec`, `BladeSpec`, `ProjectSpec`, `BuildTaskSpec`, `BuildGraphSpec`
- `BuildTask = BuildTaskSpec`, `BuildGraph = BuildGraphSpec`

**Key Constants:**
- `BUILD_KIND_CHECK`, `BUILD_KIND_NATIVE_EXECUTABLE`, `BUILD_KIND_TEST`, `BUILD_KIND_PROOF`
- `BUILD_KIND_BENCHMARK`, `BUILD_KIND_ATTRITION`, `BUILD_KIND_CERTIFY`, `BUILD_KIND_EXEC`, `BUILD_KIND_AMALGAMATE`

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `build_graph` | `fn build_graph() -> BuildGraphSpec` |
| `package` | `fn package(name: String) -> PackageSpec` |
| `blade` | `fn blade(name: String) -> BladeSpec` |
| `build_defaults` | `fn build_defaults() -> BuildDefaultsSpec` |
| `project` | `fn project(name: String) -> ProjectSpec` |
| `source_set` | `fn source_set(name: String) -> SourceSetSpec` |
| `build_task` | `fn build_task(id: String) -> BuildTaskSpec` |
| `native_executable` | `fn native_executable(id: String) -> BuildTaskSpec` |
| `source_tests` | `fn source_tests(id: String) -> BuildTaskSpec` |
| `gpu_suite` | `fn gpu_suite(id: String) -> BuildTaskSpec` |
| `cuda_artifacts` | `fn cuda_artifacts(id: String) -> BuildTaskSpec` |
| `amalgamate_capsule` | `fn amalgamate_capsule(id: String) -> BuildTaskSpec` |
| `certify` | `fn certify(subject: String) -> BuildTaskSpec` |
| `platform_package` / `platform_requirement` / `requires_platform_package` | platform dependencies |

**Usage (see `build.kn`):**
```kain
use std::build

export fn build(): build_graph()
    |> package("my_app")
    |> blade("my_blade")
    |> native_executable("my_exe")
        |> source_set("main")
        |> source_tests("test")
```

### `std::test`

**Import:** `use std::test`
**Purpose:** Test outcomes and combinators — pass/fail/skip/proved/witness, boolean assertions, proof integration.

**Key Types:**
- `TestOutcome` — `{ status, label, detail, evidence }`
- `TEST_STATUS_PASS`, `TEST_STATUS_FAIL`, `TEST_STATUS_SKIP`, `TEST_STATUS_PROVED`, `TEST_STATUS_WITNESS`

**Key Symbols:**
- `test_pass(label) -> TestOutcome`
- `test_fail(label, detail) -> TestOutcome`
- `test_skip(label, reason) -> TestOutcome`
- `test_bool(label, condition) -> TestOutcome`
- `test_combine(left, right) -> TestOutcome`
- `test_expect_proved(outcome) -> TestOutcome`
- `test_expect_proof_assessment(assessment) -> TestOutcome`
- `test_expect_proof_suite(summary) -> TestOutcome`
- `test_assert_unsat(label, solver) -> TestOutcome`
- `test_assert_sat(label, solver) -> TestOutcome`
- `test_task(id) -> BuildTaskSpec`, `test_suite(id) -> BuildTaskSpec`

### `std::proof`

**Import:** `use std::proof`
**Purpose:** Proof case infrastructure — outcomes, expectations, assessment, Z3 integration, suite summarization.

**Key Types:**
- `ProofOutcome` — `{ status, label, detail, evidence, model }`
- `ProofExpectation` — expected status filter
- `ProofCase` — labeled proof specification
- `ProofAssessment` — evaluated outcome against expectation
- `ProofSuiteSummary` — aggregated assessment results

**Key Constants:**
- `PROOF_STATUS_SKIP`, `PROOF_STATUS_PROVED`, `PROOF_STATUS_WITNESS`, `PROOF_STATUS_UNKNOWN`
- `PROOF_EXPECT_ANY`, `PROOF_BACKEND_Z3`

**Key Symbols:**
- `proof_proved(label, evidence) -> ProofOutcome`
- `proof_witness(label, evidence, model) -> ProofOutcome`
- `proof_unknown(label, detail, evidence) -> ProofOutcome`
- `proof_case(label) -> ProofCase`
- `proof_case_check(spec, backend_subject) -> ProofAssessment`
- `proof_suite_summary(label, assessments) -> ProofSuiteSummary`
- `proof_suite_ok(summary) -> Bool`
- `proof_task(id) -> BuildTaskSpec`, `proof_obligation(id) -> BuildTaskSpec`, `proof_smt2(id, entry) -> BuildTaskSpec`
- `z3_proof(id) -> BuildTaskSpec`

### `std::bench`

**Import:** `use std::bench`
**Purpose:** Benchmark task definitions for the build system.

**Key Symbols:**
- `bench_task(id: String) -> BuildTaskSpec`
- `bench_case(id: String) -> BuildTaskSpec`
- `benchmark_task(id: String) -> BuildTaskSpec`
- `bench_case_named(id, case_name) -> BuildTaskSpec`

### `std::attrition`

**Import:** `use std::attrition`
**Purpose:** Attrition (runtime certification) task definitions for the build system.

**Key Symbols:**
- `attrition_task(id: String) -> BuildTaskSpec`
- `attrition_case(id: String) -> BuildTaskSpec`
- `attrition_case_named(id, case_name) -> BuildTaskSpec`

### `std::certify`

**Import:** `use std::certify`
**Purpose:** Certification gate task definitions for the build system.

**Key Symbols:**
- `certify_task(id: String) -> BuildTaskSpec`
- `certify_gate(id: String) -> BuildTaskSpec`
- `release_gate(id: String) -> BuildTaskSpec`

---

## Tooling & Reflection

### `std::kain`

**Import:** `use std::kain`
**Purpose:** Compiler/LSP bridge — open workspace, open/update/check documents, hover, go-to-definition, references, completions, semantic tokens, formatting.

**Key Types:**
- `Position`, `Range`, `Location`
- `SymbolKind` enum, `CompletionKind` enum, `CompileTarget` enum
- `Symbol`, `Completion`, `SemanticToken`, `Hover`
- `DiagnosticLabel`, `DiagnosticFixIt`, `Diagnostic`, `CheckResult`, `FormatResult`
- `Workspace`, `Document`

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `open_workspace` | `fn open_workspace(root, target) -> Option<Workspace>` |
| `close_workspace` | `fn close_workspace(ws) -> Bool` |
| `open_document` | `fn open_document(ws, path, source, version) -> Option<Document>` |
| `update_document` | `fn update_document(doc, source, version) -> Bool` |
| `check_document` | `fn check_document(doc) -> CheckResult` |
| `hover_at` | `fn hover_at(doc, line, col) -> Option<Hover>` |
| `definition_at` | `fn definition_at(doc, line, col) -> Array<Location>` |
| `references_at` | `fn references_at(doc, line, col) -> Array<Location>` |
| `completions_at` | `fn completions_at(doc, line, col) -> Array<Completion>` |
| `document_symbols` | `fn document_symbols(doc) -> Array<Symbol>` |
| `workspace_symbols` | `fn workspace_symbols(ws, query) -> Array<Symbol>` |
| `semantic_tokens` | `fn semantic_tokens(doc) -> Array<SemanticToken>` |
| `format_document` | `fn format_document(doc) -> FormatResult` |

### `std::reflect`

**Import:** `use std::reflect`
**Purpose:** Runtime type reflection — kind, name, descriptor.

**Key Symbols:**
- `reflect_type_kind(val: Any) -> TypeKind`
- `reflect_type_name(val: Any) -> String`
- `reflect_descriptor(val: Any) -> TypeDescriptor`

### `std::target`

**Import:** `use std::target`
**Purpose:** Compilation target introspection — architecture, OS, environment, feature detection.

**Key Types:**
- `Arch` enum, `OS` enum, `Env` enum, `Target` struct

**Key Symbols:**
- `target_current() -> Target`
- `target_has_feature(feature_key: String) -> Bool`

### `std::diagnostics`

**Import:** `use std::diagnostics`
**Purpose:** Status code helpers, logging, progress emission, debug memory dumping.

**Key Symbols:**
- `status_ok(status: Int) -> Bool`, `status_failed`, `status_error`, `status_warning`
- `status_or(status, fallback) -> Int`
- `bool_to_status(value: Bool) -> Int`
- `status_to_bool(status: Int) -> Bool`
- `log_info(subsystem: String, message: String) -> Unit`
- `log_warning(subsystem: String, message: String) -> Unit`
- `log_error(subsystem: String, message: String, code: Int) -> Unit`
- `progress_emit(task_name, percentage) -> Unit`
- `debug_dump_memory(label, address, count) -> Unit`

### `std::mcp`

**Import:** `use std::mcp`
**Purpose:** Model Context Protocol (MCP) server — build tools, resources, prompts, request handling, JSON-RPC communication over stdio.

**Key Constants:**
- `MCP_PROTOCOL_VERSION`, `MCP_JSONRPC_VERSION`
- `MCP_ERROR_PARSE`, `MCP_ERROR_INVALID_REQUEST`, `MCP_ERROR_METHOD_NOT_FOUND`
- `MCP_LOG_DEBUG/INFO/NOTICE/WARNING/ERROR/CRITICAL/ALERT/EMERGENCY`

**Key Types:**
- `McpServer`, `McpToolDef`, `McpCallResult`, `McpResourceDef`, `McpPromptDef`, `McpRequest`

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `mcp_server_new` | `fn mcp_server_new(name, version) -> McpServer` |
| `mcp_tool_def` | `fn mcp_tool_def(name, description, schema_json) -> McpToolDef` |
| `mcp_resource_def` | `fn mcp_resource_def(uri, name, description, mime_type) -> McpResourceDef` |
| `mcp_prompt_def` | `fn mcp_prompt_def(name, description) -> McpPromptDef` |
| `mcp_build_initialize_result` | `fn mcp_build_initialize_result(server, supports_tools, ...) -> JsonObject` |
| `mcp_run_stdio` | `fn mcp_run_stdio(server: McpServer, tools: Array<McpToolDef>) -> Int` |
| `mcp_read_request` | `fn mcp_read_request() -> McpRequest` |
| `mcp_send_response` | `fn mcp_send_response(req, result)` |
| `mcp_is_initialize` | `fn mcp_is_initialize(req) -> Bool` |
| `mcp_is_tools_call` | `fn mcp_is_tools_call(req) -> Bool` |
| `mcp_tool_call_name` | `fn mcp_tool_call_name(req) -> String` |
| `mcp_tool_call_args` | `fn mcp_tool_call_args(req) -> JsonObject` |
| `mcp_text_result` | `fn mcp_text_result(text: String) -> McpCallResult` |
| `mcp_image_result` | `fn mcp_image_result(base64_data, mime_type) -> McpCallResult` |
| `mcp_error_result` | `fn mcp_error_result(message) -> McpCallResult` |

### `std::z3`

**Import:** `use std::z3`
**Purpose:** Z3 SMT solver integration — solver creation, variable declarations, constraint building, satisfiability checking, model extraction.

**Key Symbols:**

| Symbol | Signature |
|---|---|
| `z3_available` | `fn z3_available() -> Bool` |
| `z3_require` | `fn z3_require() -> Any` |
| `z3_version` | `fn z3_version() -> String` |
| `z3_solver` | `fn z3_solver() -> Any` |
| `z3_optimize` | `fn z3_optimize() -> Any` |
| `z3_int` | `fn z3_int(name: String) -> Any` |
| `z3_real` | `fn z3_real(name: String) -> Any` |
| `z3_bool` | `fn z3_bool(name: String) -> Any` |
| `z3_bitvec` | `fn z3_bitvec(name: String, bits: Int) -> Any` |
| `z3_int_val` / `z3_bool_val` / `z3_bitvec_val` | literal values |
| `z3_expr_add`, `z3_expr_sub`, `z3_expr_mul` | arithmetic expressions |
| `z3_expr_eq`, `z3_expr_ne`, `z3_expr_lt`, `z3_expr_le`, `z3_expr_gt`, `z3_expr_ge` | comparison |
| `z3_and`, `z3_or`, `z3_not`, `z3_distinct` | logical connectives |
| `z3_solver_add` | add constraints to solver |
| `z3_solver_check` | check satisfiability |
| `z3_is_sat` / `z3_is_unsat` / `z3_is_unknown` | result checking |
| `z3_solver_model` | extract model |
| `z3_model_eval` | evaluate expression in model |
| `z3_as_long` / `z3_as_string` | value extraction |

**Usage:**
```kain
use std::z3

fn check_formula() with Pure:
    let solver = z3_solver()
    let x = z3_int("x")
    let y = z3_int("y")
    z3_solver_add(solver, z3_expr_eq(z3_expr_add(x, y), z3_int_val(10)))
    z3_solver_add(solver, z3_expr_gt(x, z3_int_val(3)))
    let result = z3_solver_check(solver)
    if z3_is_sat(result):
        let model = z3_solver_model(solver)
        let x_val = z3_model_eval(model, x)
        let x_i64 = z3_as_long(x_val)
```

### `std::semver`

**Import:** `use std::semver`
**Purpose:** Semantic versioning — parse, format, compare, range matching, satisfy.

**Key Types:**
- `SemVer`, `SemVerParseResult`, `SemVerComparator`, `SemVerRange`, `SemVerRangeParseResult`

**Key Symbols:**
- `semver_new(major, minor, patch) -> SemVer`
- `semver_parse(text) -> SemVerParseResult`
- `semver_try_parse(text) -> Option<SemVer>`
- `semver_format(version) -> String`
- `semver_compare(left, right) -> Int` — returns `SEMVER_ORDER_LT/EQ/GT`
- `semver_satisfies(version, range_text) -> Bool`
- `semver_range_parse(text) -> SemVerRangeParseResult`
- `semver_range_matches(range, version) -> Bool`

### `std::uri`

**Import:** `use std::uri`
**Purpose:** URI parsing, encoding/decoding, query parameter iteration.

**Key Symbols:**
- `uri_parse(url: String) -> Uri`
- `uri_encode(value: String) -> String`
- `uri_decode(value: String) -> String`
- `uri_query_param_iterator(uri) -> UriQueryParamIterator`
- `uri_query_param_next(it) -> UriQueryParamNext`

---

## Remaining Modules

### `std::base64`

**Purpose:** Base64 and hex encoding/decoding.
**Import:** `use std::base64`

**Key Symbols:**
- `base64_encode(text) -> String`
- `base64_encode_nopad(text) -> String`
- `base64_encode_url(text) -> String`
- `base64_decode(text) -> String`
- `hex_encode(text) -> String`
- `hex_decode(text) -> String`

### `std::compress`

**Purpose:** Run-length encoding (RLE) compression.
**Import:** `use std::compress`

**Key Symbols:**
- `rle_writer_new(dest: ptr<BufferedWriter>) -> RleCompressionWriter`
- `rle_writer_write_char(w, c, flush_target) -> Int`
- `rle_writer_flush(w, flush_target) -> Int`
- `rle_reader_new(src: ptr<BufferedReader>) -> RleCompressionReader`
- `rle_reader_read_char(r) -> Int`

### `std::crypto`

**Purpose:** Cryptographic hashing and random bytes.
**Import:** `use std::crypto`

**Key Symbols:**
- `crypto_random_bytes_hex(length) -> String`
- `sha256(text) -> String`
- `hmac_sha256(key, message) -> String`
- `blake3(text) -> String`
- `random_bytes(length) -> String`

### `std::elf`

**Purpose:** ELF binary header parsing.
**Import:** `use std::elf`

**Key Symbols:**
- `ELF_MAGIC`, `ELF_CLASS_64`, `ELF_DATA_LSB`, `ELF_TYPE_EXEC`, `ELF_MACHINE_X86_64`, `ELF_MACHINE_AARCH64`
- `elf_read_header(buffer: ptr<Int>) -> ElfHeader`

### `std::input`

**Purpose:** Input session management — keyboard, pointer, CLI, UI, agent, synthetic input sources, action/axis binding, event tracing and replay.
**Import:** `use std::input`

**Key Symbols:**
- `input_session_create(name: String) -> Int`
- `input_bind_action(session, source_kind, event_kind, code, action) -> Int`
- `input_bind_axis(session, source_kind, event_kind, code, axis, scale) -> Int`
- `input_push_event(session, source_kind, source_id, event_kind, code, value, text, confidence) -> Int`
- `input_begin_frame(session, delta_ms) -> Int`
- `input_action_pressed(session, action) -> Int`
- `input_action_down(session, action) -> Int`
- `input_action_released(session, action) -> Int`
- `input_axis_value(session, axis) -> Float`
- `input_trace_json(session) -> String`
- `input_replay_trace_json(session, trace_json) -> Int`

### `std::tar`

**Purpose:** TAR archive reading/writing.
**Import:** `use std::tar`

**Key Symbols:**
- `tar_writer_new(dest: ptr<BufferedWriter>) -> TarWriter`
- `tar_write_header(w, name, size_bytes, flush_target) -> Int`
- `tar_reader_new(src: ptr<BufferedReader>) -> TarReader`
- `tar_read_entry(r) -> TarEntry`
- `tar_skip_data(r, size_words) -> Int`

### `std::ui`

**Import:** `use std::ui`
**Purpose:** Native UI session management — node creation, layout, styling, events, rendering, fonts, textures, drag-drop, IME, accessibility, menus, dialogs, hot reload.

**Statistics:** 340 public symbols — the largest module in the stdlib.

**Key Symbols (selected):**

**Session Management:**
- `ui_session_create(app_name, width, height) -> Int`
- `ui_window_open(session, title, width, height) -> Int`
- `ui_begin_frame(session, delta_ms) -> Int`
- `ui_end_frame(session) -> Int`, `ui_present(session) -> Int`

**Node Tree:**
- `ui_node_create(session, kind) -> Int`
- `ui_node_set_parent(session, node, parent) -> Int`
- `ui_node_set_rect(session, node, x, y, width, height) -> Int`
- `ui_node_set_text(session, node, text) -> Int`
- `ui_node_set_flag(session, node, flag, enabled) -> Int`

**Styling:**
- `ui_node_set_style_i64`, `ui_node_set_style_f64`, `ui_node_set_style_string`
- `ui_node_style_i64`, `ui_node_style_f64`, `ui_node_style_string`
- `ui_style_color_rgba`, `ui_style_metric`, `ui_style_padding`, `ui_style_spacing`

**State:**
- `ui_state_set_i64`, `ui_state_set_f64`, `ui_state_set_string`
- `ui_state_i64`, `ui_state_f64`, `ui_state_string`, `ui_state_bool`
- `ui_state_toggle`, `ui_state_counter`

**Events:**
- `ui_push_event(session, kind, target, x, y, key_code, text) -> Int`
- `ui_poll_event(session) -> Int`
- `ui_event_kind`, `ui_event_target`, `ui_event_x`, `ui_event_y`, `ui_event_key_code`, `ui_event_text`

**Rendering:**
- `ui_render_box`, `ui_render_text`, `ui_render_text_value`, `ui_render_resource`
- `ui_draw_command_count`, `ui_draw_command_kind`, `ui_draw_command_node`

**Resources:**
- `ui_resource_create(session, resource_type, key, width, height, byte_length) -> Int`
- `ui_font_create`, `ui_texture_create`, `ui_canvas_create`

**High-Level Helpers:**
- `ui_host_session_create(app_name, window_title, width, height, backend) -> Int`
- `ui_reconcile_node(session, parent, kind, stable_key, x, y, width, height) -> Int`
- `ui_frame_begin`, `ui_frame_submit`

### `std::wasm`

**Purpose:** WebAssembly binary format parsing — headers, sections.
**Import:** `use std::wasm`

**Key Symbols:**
- `wasm_validate_header(buffer: ptr<Int>) -> Bool`
- `wasm_read_section_header(buffer, offset) -> WasmSection`

### `std::zip`

**Purpose:** ZIP archive format — local headers, central headers, end-of-central-directory.
**Import:** `use std::zip`

**Key Symbols:**
- `zip_write_local_header(buffer, header) -> Int`
- `zip_read_local_header(buffer) -> ZipLocalHeader`
- `zip_write_eocd(buffer, eocd) -> Int`

---

## Rust Builtins

The `kain` compiler exports ~240 Rust builtins that are available without any `use std::*` import. These are always in scope. Most are re-exported by the stdlib modules with `use std::*` wrappers, but the raw builtins are always available.

**Core Language Builtins:**
- `print(value: Any) -> Unit`, `println(value: Any) -> Unit`
- `panic(message: String) -> Never`
- `assert(condition: Bool, message: String) -> Unit`
- `dbg(value: Any) -> Any` — debug print and return
- `len(collection: Any) -> Int`
- `to_string(value: Any) -> String`, `to_int(value: Any) -> Int`, `to_float(value: Any) -> Float`

**File System:**
- `read_file(path: String) -> String`, `write_file(path, content) -> Unit`
- `remove_file(path) -> Unit`, `copy_file(src, dest) -> Unit`
- `file_exists(path) -> Bool`, `create_dir_all(path) -> Unit`
- `read_dir(path) -> Array<String>`
- `fs_mount`, `fs_unmount`, `fs_capability_grant`, `fs_capability_has`, `fs_capability_revoke`
- `fs_sandbox_allow_host_paths(allow: Bool) -> Unit`
- `fs_watch`, `fs_watch_close`, `fs_watch_poll`
- `fs_tx_begin`, `fs_tx_commit`, `fs_tx_rollback`, `fs_tx_write_text`

**JSON:**
- `json_parse(text: String) -> Any`
- `json_string(value: Any) -> String`
- `json_object_new() -> Any`, `json_array_new() -> Array<Any>`
- `json_get(object, key) -> Any`, `json_set(object, key, value) -> Unit`
- `json_has`, `json_get_string`, `json_get_int`, `json_get_bool`, `json_get_float`

**Math:**
- `sin(x: Float) -> Float`, `cos(x: Float) -> Float`, `tan(x: Float) -> Float`
- `sqrt(x: Float) -> Float`, `pow(base, exp) -> Float`
- `abs(x: Int) -> Int`, `min(a, b) -> Int`, `max(a, b) -> Int`
- `floor(x: Float) -> Int`, `ceil(x: Float) -> Int`, `round(x: Float) -> Int`
- `vec2(x, y) -> Vec2`, `vec3(x, y, z) -> Vec3`, `vec4(x, y, z, w) -> Vec4`
- `dot(a, b) -> Float`, `cross(a, b) -> Vec3`, `length(v) -> Float`, `normalize(v) -> Vec3`
- `distance(a, b) -> Float`, `mix(a, b, t) -> Float`, `smoothstep(e0, e1, x) -> Float`

**Strings:**
- `starts_with(s, prefix) -> Bool`, `ends_with(s, suffix) -> Bool`, `contains(s, sub) -> Bool`
- `substring(s, start, end) -> String`, `replace(s, from, to) -> String`
- `trim(s) -> String`, `split(s, sep) -> Array`, `join(arr, sep) -> String`
- `to_lower(s) -> String`, `to_upper(s) -> String`
- `ord(s) -> Int`, `chr(codepoint) -> String`
- `byte_at(s, index) -> Int`, `char_at(s, index) -> String`
- `find_substring_from(s, needle, start) -> Int`

**Collections:**
- `range(start, end) -> Array`, `push(array, value) -> Unit`, `pop(array) -> Any`
- `map(array, fn: Function) -> Array`, `filter(array, fn: Function) -> Array`
- `reduce(array, initial, fn: Function) -> Any`
- `map_new() -> Any`, `map_set(map, key, value) -> Unit`, `map_get(map, key) -> Any`

**OS & Process:**
- `cwd() -> String`, `env(name) -> String`, `args() -> Array<String>`
- `now() -> Float`, `sleep(seconds: Float) -> Unit`
- `socket_connect(host, port) -> Int`, `socket_send(sock, data) -> Unit`, `socket_recv(sock) -> String`
- `read_line() -> String`, `stdin_read_exact(length) -> String`
- `stdout_write(value) -> Unit`, `stderr_write(value) -> Unit`

**Actors:**
- `spawn(actor: Actor) -> ActorRef`
- `send(actor: ActorRef, message: Message) -> Unit`
- `ask(actor: ActorRef, message: String, request: Any) -> Any`
- `ask_timeout(actor, message, request, timeout_ms) -> Any`

---

## Native Runtime Services

The Kain runtime exposes 48 native runtime services with availability on declared platforms. These are categorized as **required** (always available) or **optional** (available on specific platforms).

**Required Services:**
- `base.diagnostics` — structured diagnostics and error reporting
- `base.memory` — core allocation, retain/release, memory management
- `contract` — runtime contract bundle loading and validation
- `memory.ownership` — native collapse/observe/decay guards
- `platform.app-host` (Windows) — raw Win32 app/window host substrate
- `platform.input` (Windows) — canonical Kain input sessions

**Platform Services:**
- `io.net` — TCP, HTTP client/server (Windows, Linux, macOS)
- `io.process` — child-process, pipe, PTY (Windows, Linux, macOS)
- `machine.topology` — CPU/thread/affinity (Windows, Linux, macOS)
- `machine.virtual-memory` — page-size, map/unmap, protection (Windows, Linux, macOS)
- `platform.library` — dynamic library open/resolve/close (Windows, Linux, macOS)
- `memory.atomic-seqcst`, `memory.atomic-v2` — atomic operations (Windows, Linux, macOS)
- `gfx.backend.d3d12` (degraded, Windows), `gfx.backend.vulkan` (degraded, Windows, Linux)
- `gfx.compute.cuda` (degraded, Windows, Linux)

**Optional Services (all platforms):**
- `actor.registry`, `actor.runtime`
- `async.runtime`, `async.timers`
- `control.converge.autotune`, `control.runtime-tiers`
- `cpu.capabilities`
- `data.json`
- `device.reflection`, `reflection`
- `gfx.compute`, `gfx.raw-native`, `gfx.shader.spirv`
- `host.bridge`
- `interop.shared-contracts`
- `machine.stones`
- `memory.fixup`, `memory.handles`, `memory.shared-fanout`, `memory.volatile`
- `runtime.inspection`, `runtime.profile`
- `scene.mutation`, `scene.query`, `scene.runtime`
- `ui.bundle`, `ui.component`
- `compatibility`

---

## Example: Full .kn File

```kain
// A complete Kain program using multiple stdlib modules
use std::fs
use std::json
use std::math
use std::time
use std::http
use std::text
use std::runtime

world AppState:
    state counter: Int = 0
    surface "app" => AppComponent

component AppComponent(props):
    state click_count: Int = 0
    fn handle_click(_self: Self_):
        _self.state.click_count = _self.state.click_count + 1
    render <panel>
        <text>"Clicks: {click_count}"</text>
        <button on_click={handle_click}>"Click me"</button>
    </panel>

fn process_data(path: String) with IO:
    let content = fs_read_text(path)
    let data = json_parse_text(content)
    let name = json_string_required(data, "name")
    let version = json_int_or(data, "version", 0)
    println("Loaded " + name + " v" + to_string(version))

fn bench_timing() with Pure:
    let start = instant_now()
    let sum = 0.0
    for i in range(0, 1000):
        sum = sum + fast_sin(pi() * to_float(i) / 500.0)
    let elapsed = instant_elapsed(start)
    let ms = duration_to_millis(elapsed)
    println("Computed sum=" + to_string(sum) + " in " + to_string(ms) + "ms")

fn main() with IO:
    runtime_init()
    process_data("config.json")
    bench_timing()
    runtime_shutdown()
```

---

> **Note about naming:** All stdlib functions use flat naming — `fs_read_text()` not `fs.read_text()`, `json_parse_text()` not `json.parse_text()`, `vec3()` not `math.vec3()`. The `use std::module` import brings names into the current scope without a namespace prefix.

> **Generated from:** `X:\stdlib\STDLIB_MAP.llm.md` (auto-generated by `kain stdlib-map`)
> **Stdlib location:** `X:\stdlib\*.kn` (67 source files)
> **Total public symbols:** ~3,250 (across 67 modules) + ~240 Rust builtins + ~48 native runtime services

# THE SCOOP // PIPELINE NEWSCAST

**Broadcast Time (America/New_York):** 2026-03-11
**Automation ID:** the-scoop
**Last Scoop Run (UTC):** 2026-03-11T20:20:35.130Z

## Neon Headlines
- No new committed changes landed after the last scoop checkpoint; the active story is a large staged/working-tree wave now sitting in the repo.
- ZenPainter and ZenSample received substantial integration and type-alignment work, plus a new deterministic UV stroke test command.
- K_OS-side capability payloads were added in bulk (Kain domains, material/mesh/IO/HDR/photogrammetry crates, and generated bindings) but are mostly untracked and not fully wired into the active workspace build graph yet.
- Zen MOCAP domain reports a full SPIR-V compile pass set (22/22 PASS) with documented wgpu dispatch contracts and data-driven UI schemas.

## Control Tower: Automation Pipeline State

### What changed in automation control plane
- Added/updated `automation/` control-plane assets including:
  - `automation/config/pipeline.config.json`
  - `automation/prompts/hourly-loop.prompt.md`
  - `automation/BACKLOG.md`
  - `automation/scripts/next-turn.mjs`
  - `automation/scripts/write-report.mjs`
  - reports `TURN-001` through `TURN-005`
- Rotation policy is explicit: `feature x3 -> testing x1 -> docs x1`.
- Validation policy still has drift: fallback Rust commands reference crates not currently workspace members (`k-os-material`, `k-os-mesh-processing`).

### Last 5-turn loop recap (from reports)
- TURN-002/003 feature: ZenPainter 3D UV-pointer stroke path unified and drag interpolation added.
- TURN-004 testing: UV stroke logic extracted to `uvStroke.ts` + deterministic tests (`npm run test:zenpainter-uv`).
- TURN-005 docs: `docs/new/TRANSFORMATION_STATUS.md` rewritten to reflect real command health and workspace member truth.

## ZEN Painter Suite: Current Developments

### New features and behavior shifts
- ZenPainter channel model migrated from `metallic/ao/height` to `metalness/emission` in active UI channel set.
- 3D viewport now paints directly from pointer UV intersections with stroke segment chaining during drag.
- Layer manager capabilities expanded with `updateLayer`, `reorderLayers`, and explicit `setActiveLayer`.
- Quick-menu command icons upgraded to component-based icons (Lucide) in ZenPainter/ZenSample command surfaces.

### Validation signal
- `npm run test:zenpainter-uv` executed this run: **PASS (3/3 tests)**.
- `cargo check --package app-backend` executed this run: **PASS** (with warning about missing `DIRECTORY.md`).

### Bugs fixed (confirmed in docs + diffs)
- Fixed KOS/App path mismatch (`app_paths` -> `kos_paths`) in engine/Tauri wiring.
- Added missing `app-proto` dependency to `src-tauri/Cargo.toml`.
- Removed duplicate Tauri command definitions from `src-tauri/src/main.rs`.
- Added missing `FloatingQuickMenu` required props in ZenPainter/ZenSample.
- Resolved ZenSample type mismatches (service-aligned `GpuPbrParams`, `GpuPbrResult` property mapping).
- Added docs arsenal files (`docs/CARGO_ARSENAL.md`, `docs/NPM_ARSENAL.md`) for build protocol checks.

### Critical regressions / risks detected
- **P1 regression risk:** Export flow in `useZenPainter` now retrieves channel bytes but does not write files; explicit TODO remains in code path.
- **P1 runtime shader risk:** `crates/k-os-engine/src/gpu/svt/pbr_engine.rs` WGSL binding declarations show a suspicious duplicate `page_table` binding and dropped `physical_albedo` declaration in diff; this may break runtime shader binding correctness even if Rust compile passes.
- **P2 hygiene risk:** duplicated `use commands::*;` in `src-tauri/src/main.rs` import block.

## K_OS Feature Wire: New Payload in Staging

### Newly staged capability crates (untracked)
- `crates/k-os-hdr`: HDR merge/tone-map/light-authoring stack docs claim full implementation with tests.
- `crates/k-os-io`: storage system scaffold (matrix index/CAS/cache/transactions) with data-driven registry files.
- `crates/k-os-material`: broad PBR material library/presets/serialization crate.
- `crates/k-os-mesh-processing`: decimation/smoothing/subdivision/repair/UV/optimization toolkit.
- `crates/k-os-mesh`: mesh core helpers.
- `crates/k-os-photogrammetry`: multi-image reconstruction + texture/PBR extraction pipeline.
- `crates/k-os-kain`: expanded domain trees and generated SPIR-V/runtime registries.

### Practical status call
- The payload depth is high, but much of it is currently untracked and not reflected as committed workspace evolution yet.
- Treat these as “capability landed in staging” rather than “fully productized in active app path.”

## ZEN MOCAP Desk: Current State

### Verified development state
- Supermotion domain build log reports **22 shaders discovered, 22 passed, 0 failed**.
- Documented pass chain for ZenMocap:
  1. `mocap_denoise`
  2. `mocap_skeleton`
  3. `mocap_physics`
  4. `mocap_supermotion_livelink`
  5. optional `mocap_livelink`
- Data-driven mode registry present for `mocap_supermotion_livelink`: **16 modes (ID 0-15)**.
- UI schema provided for dynamic control generation and lane mapping (`a/b/c` param lanes + globals).

### Readiness judgment
- Shader/domain maturity: **strong on compile artifacts and pipeline documentation**.
- Product integration maturity: **mid-stage** until runtime wiring into the primary app UX is confirmed end-to-end.

## Net Assessment
- Momentum is real and fast, especially on ZenPainter input-path stabilization and MOCAP shader throughput.
- Main operational hazard is mismatch between “big staged capability drop” and “what is actually committed + wired + validated in the shipping lane.”
- Immediate high-value action: close the ZenPainter export regression and verify SVT WGSL bindings at runtime before this wave is considered release-safe.

## Evidence Sources Used This Run
- `git status`, `git diff`, `git diff --stat`, `git ls-files --others --exclude-standard`
- `automation/config/pipeline.config.json`, `automation/README.md`, `automation/BACKLOG.md`, `automation/reports/TURN-001..005`
- ZenPainter/ZenSample diffs and UV stroke test files
- K_OS/Kain domain docs, especially `supermotion` pipeline and build logs
- Command validation:
  - `npm run test:zenpainter-uv` (PASS)
  - `cargo check --package app-backend` (PASS)

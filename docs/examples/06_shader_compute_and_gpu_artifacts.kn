shader fragment DocsAccentGradient(uv: Vec2) -> Vec4:
    uniform accent: Vec3 @0
    return vec4(accent.x, accent.y, accent.z, 1.0)

shader compute DocsBlendKernel() -> Void:
    uniform blend_factor: Float @0
    return

fn main() -> Int:
    return 0

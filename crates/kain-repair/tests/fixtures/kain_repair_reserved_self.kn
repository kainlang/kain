impl WidgetBuilder {
	fn Self_(value: Int) -> Self_ {
		let type = value
		Self_:build(type)
	}

	fn build_pair(left: Self_, right: Self_) -> Result<Self_, Self_> {
		return Result::ok(Self_(left, right))
	}
}

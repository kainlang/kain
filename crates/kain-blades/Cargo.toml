[package]
name = "kain-blades"
version = "0.1.0"
edition = "2021"
description = "Local-first Kain blade workspace discovery and build graph planning"
license = "MIT"

[dependencies]
kain-fs = { path = "../kain-fs" }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }

[dev-dependencies]
tempfile = "3.8"

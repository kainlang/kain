[package]
name = "kain-codebase"
version = "0.1.0"
edition = "2021"
description = "Trusted local workspace control layer for Kain"
license = "MIT"

[dependencies]
kain-core = { path = "../kain-core" }
kain-fs = { path = "../kain-fs" }
libloading = "0.8"
once_cell = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
sha2 = "0.10"

[dev-dependencies]
tempfile = "3.8"

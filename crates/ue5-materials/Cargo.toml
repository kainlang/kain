[package]
name = "ue5-materials"
version = "0.1.0"
edition = "2021"

[dependencies]
kain-core = { path = "../kain-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
indoc = "2.0"

version: 1
id: kain.sys.codegen.llvm.proofs.control_next_label_sequence_keeps_match_block_ids_distinct
kind: check_smt2
description: match lowering allocates end, no-match, condition, and arm labels via
  successive next_label calls; the resulting block ids stay pairwise distinct for
  every arm index.
tags:
- llvm
- control
- match
- labels
- cfg
source:
  path: D:\Kain-Lang\crates\sys-codegen\src\codegen_llvm\mod.rs
  symbol: compile_expr
  start_line: 5982
  reason: the repaired match CFG depends on label_end, label_no_match, condition_labels, and arm_labels never aliasing
case:
  smt2: "(set-logic QF_LIA)\n(declare-const base Int)\n(declare-const arm_count Int)\n(declare-const i Int)\n(declare-const j Int)\n(assert (>= base 0))\n(assert (>= arm_count 0))\n(assert (>= i 0))\n(assert (>= j 0))\n(assert (< i arm_count))\n(assert (< j arm_count))\n(assert (distinct i j))\n(define-fun label_end () Int base)\n(define-fun label_no_match () Int (+ base 1))\n(define-fun cond_i () Int (+ base 2 (* 2 i)))\n(define-fun cond_j () Int (+ base 2 (* 2 j)))\n(define-fun arm_i () Int (+ base 3 (* 2 i)))\n(define-fun arm_j () Int (+ base 3 (* 2 j)))\n(assert (or (= label_end label_no_match) (= label_end cond_i) (= label_end arm_i) (= label_no_match cond_i) (= label_no_match arm_i) (= cond_i arm_i) (= cond_i cond_j) (= arm_i arm_j) (= cond_i arm_j)))\n(check-sat)"
  include_model: false
  include_stats: false

version: 1
id: kain.sys.codegen.llvm.proofs.layout_struct_offset_step_keeps_alignment_and_i64_headroom
kind: check_smt2
description: abi_layout_for_ty aligns each field start before adding the field
  width, and under an explicit signed-i64 bridge headroom contract the next
  running size stays aligned and still fits the runtime bridge domain.
tags:
- llvm
- layout
- struct
- offset
- i64
source:
  path: D:\Kain-Lang\crates\sys-codegen\src\codegen_llvm\mod.rs
  symbol: abi_layout_for_ty
  start_line: 794
  reason: the struct layout loop aligns the running size, adds field_size, and later forwards the result through i64 runtime helpers
case:
  smt2: "(set-logic QF_NIA)\n(declare-const current_size Int)\n(declare-const field_align Int)\n(declare-const field_size Int)\n(define-fun i64_max () Int 9223372036854775807)\n(assert (>= current_size 0))\n(assert (or (= field_align 1) (= field_align 4) (= field_align 8)))\n(assert (or (= field_size 1) (= field_size 4) (= field_size 8)))\n(assert (<= current_size (- i64_max (- field_align 1))))\n(define-fun aligned_offset () Int (ite (<= field_align 1) current_size (* (div (+ current_size (- field_align 1)) field_align) field_align)))\n(assert (<= field_size (- i64_max aligned_offset)))\n(define-fun next_size () Int (+ aligned_offset field_size))\n(assert (or (not (= (mod aligned_offset field_align) 0)) (> next_size i64_max) (< next_size aligned_offset)))\n(check-sat)"
  include_model: false
  include_stats: false

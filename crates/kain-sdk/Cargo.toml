[package]
name = "kain-sdk"
version = "0.1.0"
edition = "2021"
description = "Shippable Rust SDK for embedding KAIN into engine applications"
license = "MIT"

[dependencies]
kain-build = { path = "../kain-build" }
kain-host = { path = "../kain-host" }
kain-reflect = { path = "../kain-reflect" }

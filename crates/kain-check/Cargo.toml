[package]
name = "kain-check"
version = "0.1.0"
edition = "2021"
description = "Frontend and target-aware checking pipeline for KAIN"
license = "MIT"

[dependencies]
kain-core = { path = "../kain-core" }
kain-driver = { path = "../kain-driver", default-features = false }
serde = { workspace = true }

[dev-dependencies]
tempfile = "3.8"

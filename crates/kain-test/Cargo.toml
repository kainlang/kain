[package]
name = "kain-test"
version = "0.1.0"
edition = "2021"
description = "Rust-inspired Kain source test harness and suite runner"
license = "MIT"

[dependencies]
kain-check = { path = "../kain-check" }
kain-core = { path = "../kain-core" }
kain-driver = { path = "../kain-driver", default-features = false }
kain-fs = { path = "../kain-fs" }
serde = { workspace = true }

[dev-dependencies]
tempfile = "3.8"

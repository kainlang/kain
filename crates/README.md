# Crates Map

> Rust crates in the compiler workspace
> Auto-generated by `tools/repo_map_gen.py`
> Generated: 2026-06-06 02:30:43

**67 entries**

| Item | Description |
| :--- | :--- |
| `3d/` | 3D scene authoring, interaction, and rendering engine for Kain. Provides a node-based scene graph with authored primitives (boxes, spheres, meshes, splines, fields, volumes), manipulators, picking, camera/lighting rigs, material system, scene animation, and both a software renderer and a wgpu GPU renderer. Includes the Kain3dSession host runtime bridge for Kain-authored 3D applications. |
| `actor/` | Kain actor system vocabulary crate. Owns actor identities, typed message contracts, mailbox capacity/overflow policy, supervision strategy (restart/supervision trees), lifecycle management, address/path resolution, registry allocation, and the native ABI lowering contract. Provides the shared model that the parser, typechecker, LLVM lowering, and future scheduler implementations consume for actor semantics. |
| `amalgamate/` | Workspace amalgamation engine. Packs an entire Kain blade or workspace into a single self-contained .kn capsule file using base64-encoded JSON archives, sentinel-delimited sections, and SHA-256 content hashing. Equivalent to SQLite's amalgamation model: instead of copying Kain files around, emit one portable file that unpacks and builds anywhere. |
| `asm/` | Assembly importer for Kain. Supports multiple CPU dialects (6502/Furby, LR35902/Game Boy, Z80/Spectrum/MSX) with recovery scoring, section analysis, and direct import into Kain IR. Provides a pluggable dialect registry so new ISA targets can be added without changing the import pipeline. |
| `blades/` | Blade workspace discovery and KAIN.toml manifest loader. Discovers blade roots, resolves workspace trees, parses package/workspace/build/run/c-ffi/rust-ffi sections from Kain manifests, and provides the PackageStore for cross-blade dependency resolution. The backbone for how kain build, kain run, and blade discovery work. |
| `browser/` | WebAssembly-compiled Kain compiler for in-browser use. Exposes wasm-bindgen functions to compile Kain source to JS, TS, Rust, HLSL, WGSL, SPIR-V, and WASM targets directly from a browser context. Enables browser-based Kain playgrounds and web editors. |
| `build/` | Engine module build system. Handles the Rust-side build.rs integration for Kain engine modules: configures export paths, import shims, and module file names from environment variables. Provides the EngineModuleBuildConfig and WorkspaceType for build script orchestration. |
| `c-ffi/` | C header importer and native FFI bridge generator. Uses libclang to extract functions, structs, enums, and typedefs from C headers (including angle-bracket system headers like windows.h and vulkan/vulkan.h), generates Kain bridge source, manages dynamic library loading at runtime, and handles platform package resolution. The core of kain import-c. |
| `check/` | Kain source checking library. Drives source discovery, frontend validation, typechecking, and structured diagnostic reports for kain check. Decoupled from the CLI so CI, IDE, and test harnesses can reuse checking logic without the binary. |
| `clean/` | Build artifact cleanup. Manages removal of .kain/out, .kain/cache, and .kain/reports trees for build, run, and amalgamate scopes. Supports fine-grained clean targets (artifacts, cache, reports) with blade-aware workspace resolution. |
| `cli/` | Kain CLI binary crate. Thin host over kain-driver that wires subcommands (build, run, check, test, import-c, import-rust, import-typescript, amalgamate, gpu-artifacts, fabric, codebase, bridge, clean, blade-launcher) into the clap command tree. Re-exports core compiler types for the binary. |
| `codebase/` | Workspace inspection and codebase intelligence. Scans a project tree for Kain source files, discovers root markers, builds workspace inspection metadata, and exposes codebase queries (file listing, structure analysis) to Kain stdlib via registered env extensions. Supports trusted-local and reviewed trust modes. |
| `commands/` | CLI command registry and routing. Defines the command pack system (builtin commands, dynamic clap generation, per-command implementations for blade, codebase, fabric, kain, omni, repair, runtime, selfhost, UI) and the CommandRegistry that maps user-facing verbs to their implementations. |
| `core/` | Kain core compiler -- the heart of the language. Owns the parser, AST, lexer, typechecker, diagnostics, effects, comptime evaluation, monomorphization, module resolution, runtime interpreter, runtime contracts, shader analysis, low-level ABI/memory metadata, stdlib registration, and UI metadata. Every other compiler crate depends on this. |
| `crate-ffi/` | Rust crate importer for Kain. Resolves Rust crates from Cargo registries, extracts binding bundles via dylib loading, generates Kain bridge source, and caches build artifacts. Handles the full pipeline from kain import-crate through resolution, extraction, code generation, and runtime bridge loading. |
| `driver/` | Embeddable compiler driver. Glue between kain-core, language-specific backends (GPU, LLVM, sys-codegen), and host applications. Owns the compilation pipeline: source to lexer to parser to typecheck to monomorphize to backend emission. Manages DriverSession, progress events, and artifact collection for CLI, library, and Tauri consumers. |
| `entangle/` | Deterministic world-to-world state coupling. Defines the entangle model: single_writer semantics, authority/mirror split, state synchronization rules, and the validation contract for entangle declarations. Shared between parser, typechecker, and runtime. |
| `error/` | Data-driven diagnostic engine with legacy drop-in facade. Provides the full diagnostic pipeline: error codes, severity levels, source spans, labels, chains, traces, JSON output, rendering, explanation text, and a registry of diagnostic specifications. Preserves historical kain_error paths for backward compatibility while offering newer scratch-native surfaces. |
| `error-semantic/` | Compatibility shim for the historical kain-error-semantic crate name. Re-exports everything from kain_semantic -- prefer importing kain_semantic directly for new code. |
| `fmt/` | Compiler-owned source formatter for Kain. Canonicalizes authored source through the same frontend AST the rest of the compiler uses, giving the language a single printer of record for humans, tooling, and LLM workflows. Configurable indent width and max line width. |
| `foreign-abi/` | Foreign ABI type model and schema. Defines the data model for C/foreign calling conventions, ownership policies (borrowed/owned/shared/pinned), pointer depth representation, direction (in/out/inout/return), and base kinds (scalar/typedef/struct/enum). Shared schema for C-FFI, crate-FFI, and cross-language boundary codegen. |
| `fs/` | First-class filesystem primitives for Kain. Typed errors, deterministic directory output, byte/text helpers, temp paths, atomic writes, globbing, hashing, recursive operations, streaming file access, journaling/transactions, filesystem watchers, and capability/sandbox/mount policies. The foundation for all Kain IO operations. |
| `gpu/` | GPU shader code generation backends. Generates HLSL, WGSL, SPIR-V, and PTX (CUDA) from Kain shader IR. PTX generation includes variant module support and configurable options. Each backend is a standalone generator consuming the shared Kain shader AST. |
| `gpu-runtime/` | GPU compute runtime and executor layer. Wraps Vulkan compute dispatch (VulkanComputeExecutor) and NVIDIA PTX execution (NvidiaPtxExecutor) behind a unified GpuComputeExecutor trait. Handles device creation, descriptor bindings, dispatch requests, and result collection. The runtime layer that authored Kain GPU kernels execute through. |
| `host/` | Native Rust host runtime for Kain. Lets Rust applications compile and register Kain code for interpretation, expose Rust native functions to Kain, call Kain functions from Rust, and move primitive values across the boundary. Provides HostSession, EngineModuleExport, type registration, and the HostType system. |
| `host-derive/` | Proc-macro derive crate for kain-host. Generates ToKainValue, FromKainValue, and KainReflect implementations for Rust structs and enums, enabling automatic Kain/Rust value marshaling with kain attribute support for renaming, transparency, and versioning. |
| `import/` | Universal import system for Kain. Imports C (via lang-c with full C11 preprocessor support), Rust (via syn), and TypeScript (via swc_ecma_parser) into Kain IR. Pluggable architecture with planned C++ and Python support. The backbone of kain import-c, kain import-rust, and kain import-ts. |
| `input/` | Typed, replayable input semantics for Kain. Platform-agnostic model for input frames, actions, axes, text commits, and traces. Supports action binding maps, multi-source input (keyboard, mouse, gamepad, touch), and deterministic replay for testing and recording. |
| `interop/` | Shared buffer and image interop bridge for Kain. Defines SharedBufferMetadata and SharedImageMetadata types, registers stdlib extensions for cross-runtime value exchange (GPU tensors, CPU buffers, images), and manages zero-copy shared memory contracts between Kain, Python, and GPU backends. |
| `lattice/` | UI theme and styling lattice for Kain's terminal/CLI surface. Defines semantic style roles (chrome, border, panel, text, diagnostics, syntax highlighting), color themes (plain, lattice, slate, graphite, arctic, sandstone), and Ratatui integration for TUI rendering. |
| `net/` | Network protocol model for Kain. Defines NetProtocol (TCP, TLS, HTTP/1.1, HTTPS, HTTP/2), TLS client specs with ALPN, capability state tracking (unavailable/degraded/available), and the data shapes for Kain's std::net networking primitives. |
| `node/` | Node.js runtime bridge for Kain. Manages a child Node.js process, injects a bridge script, handles bidirectional JSON-RPC communication, registers stdlib extensions for Node functions, and supports shared buffer/image interop. Enables Kain code to call Node.js APIs and npm packages. |
| `omni/` | Omni project manifest and Fabric build system. Handles kain.toml project manifests, discovers source files, orchestrates multi-target compilation (LLVM, Rust, C++, WASM, SPIR-V, PTX), manages Fabric sessions, and generates the unified build output. The backbone of kain omni init and kain omni build. |
| `orchestrate/` | Orchestration graph planner for Kain. Defines stage residency, selectors, transfers, fallbacks, and graph validation for multi-stage execution pipelines. Enables Kain code to declare dataflow graphs where stages can target different compute backends (CPU, GPU, remote) with deterministic scheduling. |
| `ownership/` | Portable ownership-state semantics for Kain memory regions. Defines collapse/observe/decay lifecycle, region kinds (local alloca, heap, RC, world state, entangled, imported), active observer counts, and the shared model consumed by parser, typechecker, runtime, and codegen for Kain's ownership system. |
| `process/` | Process spawn specification model for Kain. Defines ProcessSpec (executable, args, cwd, env, stdio modes), CurrentProcessInfo, and the data shapes used by Kain's std::process for process creation, monitoring, and inter-process communication. |
| `python/` | Python runtime bridge for Kain via PyO3. Manages a Python interpreter scope, registers stdlib extensions for Python calls (eval, exec, getattr, call), handles shared buffer/image/tensor interop between Kain and Python, and supports region-cached Python objects. The runtime backbone for std::python and std::interop. |
| `reflect/` | Type reflection model for Kain. Provides TypeRef, PrimitiveType, TypeSchema, TypeRegistry, and KainReflect trait for describing Rust types to the Kain runtime. Enables automatic type registration, attribute metadata, and Kain/Rust type mapping. |
| `repair/` | Source code repair engine for Kain. Rule-based text repair with configurable profiles (trailing whitespace, final newline, blank line collapsing, line ending normalization, block comment fixing, indentation normalization, reserved identifier rewriting, etc.). Supports safe-apply and preview modes for automated source cleanup. |
| `repl/` | Kain REPL (Read-Eval-Print Loop) system. Owns interactive source buffering, evaluation loop, syntax highlighting, session management, metadata display, terminal IO, and directive handling. Provides both terminal and programmatic REPL interfaces for the kain and kn launchers. |
| `resonate/` | State resonance semantics for Kain. Defines resonance targets (dotted paths like World.field), dampen windows (time-based decay units), and the validation contract for resonate declarations that synchronize state across worlds with configurable damping. |
| `run/` | Kain run pipeline. Discovers blade workspaces, resolves run configurations from KAIN.toml, compiles Kain source, manages run caches and reports, spawns native processes with configured stdio, handles Fabric integration, and tracks execution metrics. The backbone of kain run. |
| `script/` | Script codegen backends for Kain. Generates JavaScript, Kain Script (KS), and TypeScript from Kain typed programs. Each backend is a standalone emitter consuming the shared typed AST. |
| `sdk/` | Public Kain SDK facade. Re-exports kain-build, kain-host, and kain-reflect types behind a unified KainEngine struct that provides a clean API for embedding Kain in Rust applications: type registration, native function binding, source compilation, and function invocation. |
| `selfhost/` | Self-hosting pipeline for Kain. Manages the Phase 1/Phase 2 self-host workflow: artifact contracts, blocker buckets, preflight structural reports, lane routing (parser to typechecker to lowering), repair rules, and taxonomy classification. The backbone of kain selfhost phase1 and kain selfhost phase2. |
| `semantic/` | (HOT/ACTIVE) Semantic diagnostic coprocessor for the Kain compiler. Corpus-baked, data-driven error intelligence: a pure-Rust expert engine classifies failure modes (typos, missing imports, ownership violations, shader stage mismatches, actor message mismatches, parser delimiter damage, entangle violations, etc.) and ranks repair suggestions with confidence scores. Experimental CUDA-forged packs are built offline and loaded through the same deterministic CPU reader. No GPU or ML runtime on the hot path -- sub-millisecond. |
| `service-api/` | (HOT/ACTIVE) Editor and tooling service surface for Kain. Provides a Rust API (and flat C ABI) for workspace management, document lifecycle, diagnostics, formatting, completions, hover, go-to-definition, references, semantic tokens, and symbol indexing. Transport protocols like LSP sit above this crate. The backbone for IDE integration. |
| `service-bridge/` | (HOT/ACTIVE) Bridge between kain-service-api and the Kain stdlib runtime. Registers service functions (open_workspace, open_document, update_document, check, format, completions, hover, definition, references, semantic_tokens) as stdlib builtins so Kain-authored tooling can call the service API through standard Kain function calls. |
| `shader-text/` | (HOT/ACTIVE) Shared text-shader lowering helpers for Kain GPU backends. SPIR-V is the canonical native payload; this crate owns the data-driven pieces shared by derived text backends (HLSL, WGSL, USF): type mapping (Kain types to target scalar/vector/matrix/sampler types), resource classification, identifier policy, workgroup sizing, and the direct HLSL/WGSL emitters. |
| `stdlib-map/` | Stdlib map generator. Parses all Kain stdlib source files and native manifests to produce a machine-readable JSON map (stdlib.map.json) and an LLM-optimized markdown surface (STDLIB_MAP.llm.md) covering every public function, actor, struct, enum, trait, shader, and type alias. Used by tooling, agents, and the MCP stdlib server for symbol discovery. |
| `sys-codegen/` | (HOT/ACTIVE) System-level code generation for Kain. Generates LLVM IR, Rust, C, and C++ from Kain typed programs. Rust output includes GPU host wrappers, shader artifact bundles, and text artifacts. The core backend for kain build --target llvm/rust/cpp and the LLVM native compilation pipeline. |
| `test/` | (HOT/ACTIVE) Compiletest-style test harness for Kain. Owns test directives, pass/fail modes (check-pass, check-fail, run-pass, run-fail, kain-test, prove-pass, prove-sat), structured JSON reports, suite discovery, and ignore/known-bug handling. Intentionally separate from the CLI so Kain can grow a full test pipeline without bloating the binary. |
| `ue5/` | UE5 code generation hub. Top-level crate orchestrating Blueprint, state machine, async task, and network sync code generation from Kain source into Unreal Engine 5 C++. |
| `ue5-asset-utils/` | Shared utilities for UE5 .uasset generation. Common building blocks (engine target versioning, property types, property converters, import builders) used by ue5-blueprints, ue5-materials, and other asset writers. |
| `ue5-blueprints/` | Kain to UE5 Blueprint .uasset pipeline. Converts Kain source into BlueprintDef IR, then generates C++ factory code and (in progress) binary .uasset bytes for Unreal Engine Blueprints. |
| `ue5-config/` | UE5 configuration system codegen. Generates UDeveloperSettings subclasses, config .ini files, console variables with auto-registration, project settings UI, and Blueprint accessors from Kain @config structs. |
| `ue5-editor/` | UE5 editor-specific codegen. Generates Slate UI widgets, custom viewports, detail customizations, asset factories, and editor module registration from Kain source. |
| `ue5-gas/` | UE5 Gameplay Ability System codegen. Generates abilities, attribute sets, gameplay cues, gameplay effects, gameplay tags, target actors, and async tasks from Kain source into GAS C++. |
| `ue5-graphs/` | UE5 graph editor codegen. Generates UEdGraph/UEdGraphNode/UEdGraphSchema from Kain source via Graph IR, producing both binary .uasset and C++ factory code. |
| `ue5-materials/` | UE5 material graph pipeline. Converts Kain source into material graph IR, then serializes material nodes, connections, and properties into UE5 .uasset format with C++ factory fallback. |
| `ue5-shaders/` | UE5 shader codegen. Generates USF (Unreal Shader Format) files, C++ shader headers/implementations, type mapping, shader validation, and shared shader libraries from Kain GPU shaders for the UE5 rendering pipeline. |
| `ui/` | Kain native-first UI runtime and semantic interface graph. Retained tree model with semantic nodes, signal system, renderer capability tables, and patch streams instead of virtual DOM. Defines UiNodeId, UiSignalId, renderer kinds, layout engines, and the UI runtime bundle. |
| `ui-native/` | Native UI host adapter. Qt-based host session and app bridge for Kain's native UI runtime, connecting the semantic UI tree to platform-native windowing and rendering. |
| `ui-tauri/` | Tauri desktop bridge for Kain UI. Generates Tauri-compatible bridge manifests, JavaScript bridge code, HTML entry points, and command dispatch wiring. Manages hot-reload events and frontend descriptor files for hybrid desktop apps. |
| `unreal/` | UE5 .uasset binary parsing and manipulation workspace. Contains sub-crates (unreal_asset, unreal_asset_base, unreal_asset_exports, unreal_asset_kismet, unreal_asset_properties, unreal_asset_registry) for reading and writing Unreal Engine asset files programmatically. |
| `wasm/` | (HOT/ACTIVE) WebAssembly code generation for Kain. Generates WASM binary modules from Kain typed programs and provides C runtime shims for WASM/host interop. The core backend for kain build --target wasm. |
| `web/` | (HOT/ACTIVE) Hybrid web code generation for Kain. Combines WASM bytecode with JS/TS/KS glue to produce complete web applications. Provides generate_hybrid (WASM+JS bundle), generate_js, generate_ks, generate_ts, and generate_wasm -- a one-stop backend for kain build --target web. |

______________________________________________________________________

> Edit descriptions in `map.json` (same directory)
> Run `python tools/repo_map_gen.py` to regenerate this file.

version: 1
id: kain.gpu.proofs.constructors_vector_component_flattening_keeps_extract_indices_in_bounds
kind: check_smt2
description: emit_vector_constructor only extracts component i from a vector argument when
  0 <= i < dim for dim in {2,3,4}.
tags:
- gpu
- spirv
- constructors
- vectors
- bounds
source:
  path: D:\Kain-Lang\crates\gpu\src\codegen_spirv.rs
  symbol: emit_vector_constructor
  start_line: 943
  reason: the constructor loop emits composite_extract over 0..dim only
case:
  smt2: "(set-logic QF_LIA)\n(declare-const dim Int)\n(declare-const i Int)\n(assert (or (= dim 2) (= dim 3) (= dim 4)))\n(assert (>= i 0))\n(assert (< i dim))\n(assert (or (< i 0) (>= i dim)))\n(check-sat)"
  include_model: false
  include_stats: false

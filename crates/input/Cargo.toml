[package]
name = "kain-input"
version = "0.1.0"
edition = "2021"
description = "Typed, replayable input semantics for Kain"
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

version: 1
id: kain.ownership.proofs.ownership_policy_world_and_entangle_observe_through_snapshot
kind: check_smt2
description: World and entangle-backed regions do not claim direct readonly aliasing; observe lowers through snapshot-style policy.
tags:
- ownership
- policy
- observe
- world
- entangle
source:
  path: D:\Kain-Lang\crates\ownership\src\lib.rs
  symbol: OWNERSHIP_POLICY_TABLE
  start_line: 330
  reason: WorldState, EntangledAuthority, and EntangledMirror all use ObserveMode::Snapshot
case:
  smt2: |
    (set-logic ALL)
    ; region ids: 0=world, 1=entangled_authority, 2=entangled_mirror
    ; observe modes: 0=readonly_borrow, 1=snapshot, 2=epoch_read, 3=unsupported
    (declare-const region Int)
    (declare-const observe_mode Int)
    (assert (or (= region 0) (= region 1) (= region 2)))
    (assert (= observe_mode 1))
    (assert (not (= observe_mode 1)))
    (check-sat)
  include_model: false
  include_stats: false

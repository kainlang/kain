[package]
name = "kain-error-semantic"
version = "0.1.0"
edition = "2021"
description = "Compatibility shim for the renamed kain-semantic crate."

[dependencies]
kain-semantic = { path = "../semantic" }

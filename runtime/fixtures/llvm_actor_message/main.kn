// LLVM actor bootstrap fixture
// Purpose: prove actor spawn and message-send paths are emitted and executable.

actor Printer:
    state count: Int = 0

    on Print(value: Int):
        count = count + value

fn main() -> Int:
    let actor = spawn Printer()
    send actor.Print(value = 7)
    return 0

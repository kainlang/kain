// Minimal realtime bundle startup smoke fixture
// Purpose: Validate realtime bundle ingestion and scene metadata loading
// Requirements: 4.2, 8.1, 13.4, 13.5

// This program references a minimal scene to trigger realtime bundle emission.
// The scene has no actual geometry, materials, or shaders - just enough metadata
// to validate that the native runtime can load and parse a realtime bundle.

fn main():
    print("Realtime startup smoke: OK")
    // Scene reference triggers realtime bundle emission
    // The scene "minimal_scene" is declared in the realtime bundle

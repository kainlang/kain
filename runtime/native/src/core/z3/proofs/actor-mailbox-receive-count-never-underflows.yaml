version: 1
id: kain.native.core.proofs.actor_mailbox_receive_count_never_underflows
kind: range_check
description: A receive or try_receive path only decrements a non-empty mailbox, so
  the next count remains non-negative.
tags:
- actor
- mailbox
- receive
- invariant
source:
  path: D:\Kain-Lang\runtime\native\src\core\kain_runtime_actor.c
  symbol: kain_actor_receive
  start_line: 807
  reason: mailbox->count-- occurs after a non-empty head check
case:
  declarations:
  - name: count
    sort:
      kind: int
  constraints:
  - op: ge
    args:
    - count
    - 1
  expression:
    op: sub
    args:
    - count
    - 1
  lower: 0

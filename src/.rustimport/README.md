# `src/.rustimport`

This tree is reference-only.

- `reference/` holds the moved donor corpus from the older Rust import lane.
- `phase2/` holds the canonical live selfhost phase2 mirror outputs.
- Do not hand-edit files under `src/.rustimport/*`.
- `src/core` is the active owned source tree.

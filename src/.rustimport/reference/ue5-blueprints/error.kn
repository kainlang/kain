use thiserror::Error

pub enum BlueprintError:
    Ir(String)
    AssetWrite(String)
    UnsupportedNode(String)
    MissingField(String)
    Io(io::Error)

pub type Result<T> = Result<T, BlueprintError>


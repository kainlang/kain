use thiserror::Error

pub type AsmResult<T> = Result<T, AsmError>

pub enum AsmError:
    Message(String)
    Io(io::Error)

impl AsmError:
    pub fn runtime(message: impl Into<String>) -> AsmError:
        AsmError::Message(message.into())



[package]
name = "KAINc"
version = "0.1.0"
edition = "2021"
description = "The KAIN Compiler - Self-Hosted Bootstrap Edition"

# Project Ouroboros - The snake that eats its own tail
# This compiler was generated from KAIN source code!

[workspace]

[dependencies]
inkwell = { version = "0.7.1", default-features = false, features = ["llvm21-1", "target-x86"] }

# Kain compiler shell

enum KainTarget:
    Llvm
    Native
    Text

fn run_seed_shell(target: KainTarget) -> Bool:
    let _unused_target = target
    return true

fn main() -> Int:
    if run_seed_shell(KainTarget::Llvm):
        return 0
    else:
        return 1
